# Tasks: Advance to Circulation

**Feature**: 057-advance-to-circulation
**Concretization**: Full context (plan + spec + interface-cli + scenarios)
**Inputs**: plan.md, spec.md, interface-cli.md, features/proposal-write-flow/advance-to-circulation.feature

---

## Dependency Graph

Phase 1: The `proposal propose` command (2 tasks, single-phase build) [Shared]

2 tasks total | T001 startable once the base is in place; T002 depends on T001 | Builder: pipeline

> Plan-faithful: the plan defines a **single phase** ("the `proposal propose` command") with 057 as a **pure consumer** of sibling-owned machinery. This decomposes into the command + outcome interpretation (T001) and executable acceptance (T002) — one reviewable change each. There is **no render phase** (the advanced proposal renders through the sibling-owned singular `proposal` path — `ProposalView`/`ResourceProposal`/`proposal.{full,compact}` — added by 055/056, not here, plan ADR-2), **no `internal/glassfrog` phase** (the `200` decodes into the sibling-owned `Proposal`/`Document[Proposal]`; no model field is added), **no transport phase** (a bodyless `POST` that decodes a response is served by `apiclient.Request`/`Execute` unchanged — `Method` is a free string, `ContentType` left empty, `Execute` decodes `out`), and **no validator phase** (the leaf has no editable-field flags). The only net-new surface is the `propose` leaf and its pure `run` function with the no-status-interception interpretation (plan ADR-1/ADR-2/ADR-3).
>
> All three story scenarios are served by the one command, so every task is `[Shared]` — US1 (advance a draft by id), US2 (produce the advanced proposal as parseable data), and US3 (trust the server to authorize, surface refusals plainly) are realized together by `runProposalPropose`, not by separable code paths.
>
> **⚠️ Base-branch sequencing — UNLIKE the tension family, the proposal group/model/render are NOT yet on `main`.** 057 consumes three artifacts owned by **Proposal Reads (056, Analyzed)** and/or **Proposal Creation (055, concurrent)**: the `proposal` command group (`newProposalCommand(seam)` + the `proposalSeam`), the `glassfrog.Proposal` model + `Document[Proposal]` decode, and the **singular** `proposal` render path. Per the plan's first-to-land-creates / follower-reuses contract, 057 is expected to be a **follower** — cut its base after 055 or 056 has landed and reuse all three unchanged. If 057 must land first (unexpected), the implementer creates the **minimal subset** it needs (the group + seam, the model + `Document[Proposal]`, the **singular** render only — no plural `proposals` render, no `validateProposalStatus`); 055/056 then reuse/grow those. **Do not start T001 until that base is in place.**
>
> **057 adds no new `Outcome` category, `ExitCode` case, generic/`glassfrog` type, transport field, validator set, render key, or root flag.** It extends `newProposalCommand` to attach one leaf and adds the bodyless-`POST`-and-decode orchestration with its no-status-interception interpretation in the proposal command code.

---

## Branching Guidance

**Pipeline mode**: `spec/057-advance-to-circulation/base` → `spec/057-advance-to-circulation/task-1`, `…/task-2` (one task branch per T-id, merged back into the spec base).

**Base branch**: ⚠️ Cut the 057 base from a point where the `proposal` group, the `glassfrog.Proposal` model (`Document[Proposal]`), and the **singular** `proposal` render path are present — i.e. after **056 Proposal Reads** (or **055 Proposal Creation**) has landed on `main`. As of writing, neither has landed (056 is Analyzed), so the 057 base has a real upstream dependency the tension write leaves (042–045) did not. If a sibling has not landed when 057 is picked up, either rebase onto it once it lands or (fallback) create the minimal subset in T001 per the plan's leads-first path.

**Role-based awareness**: parallel Conductor workspaces carry the sibling proposal specs (055 Proposal Creation, 056 Proposal Reads, and later Withdraw / Response Recording). 057 attaches **one leaf** to the shared `proposal` group (extending `newProposalCommand`) and adds the propose orchestration in the proposal command code — it touches **no** `internal/glassfrog` model, **no** transport field, **no** render key, and does not modify the siblings' `list`/`get`/`create` leaves. The shared files (`newProposalCommand`, the singular `proposal` render) are reused, never duplicated (first-to-land-creates).

---

## Phase 1: The `proposal propose` command [Shared]

- [x] **T001** [Shared] Add the `proposal propose <prp-id>` command — `newProposalProposeCommand` + `runProposalPropose` — 16 unit tests (POST shape, decode-and-render, JSON raw, 404/422-as-failure, Premium-403-generic, no-token, bad-output, 429-not-retried, transport, id-escape, wiring); follower of landed 055 (reuses group/seam/model/singular render unchanged) (bodyless `POST` to the `/propose` sub-path, decode `Document[Proposal]`, render via the singular `proposal` path, no status interception) + wiring under the `proposal` group — RED-first unit tests for every branch
  - **Scope**: In the proposal command code (beside the sibling `runProposal*` functions), add `newProposalProposeCommand(seam proposalSeam) *cobra.Command`: a guard-registered leaf (`Use:"propose <prp-id>"`, `Args: cobra.ExactArgs(1)`, non-empty `Short`, `SilenceErrors`/`SilenceUsage`) declaring **no flags**; it reads the persistent `--base-url`/`--output` and their `Changed()` presence into a config struct, then delegates to pure `runProposalPropose`. **Reuse the sibling `proposalSeam`** (no new seam). `runProposalPropose(cfg) (Outcome, error)` follows the sibling orchestration shape (035-widened): (1) `resolveRenderTarget(cfg.seam, cfg.outputFlag, cfg.outputPresent, cfg.stderr)` FIRST (a bad `--output` is a fail-fast `UsageError(2)` with no request — there are no input-field checks); (2) `assemble` → `newClient` → `NewRetryExecutor`; (3) send **one** `Execute` to `POST /proposals/{id}/propose` with **no body, no `Content-Type`, and `out == &Document[Proposal]`** (`url.PathEscape` the id, passed through unvalidated — plan ADR-1); send **no prior `GET`** (no client pre-check of `available_transitions` — plan ADR-3) and **no `If-Match`**. Then **interpret the outcome** (plan ADR-2/ADR-3): if `err == nil` (a `200`), render the decoded/raw proposal on stdout exactly as `proposal get` does — machine format → `output.RenderSuccess` over the raw `{data: Proposal}` bytes; human/`-o template` → `writeHuman(cfg.stdout, cfg.stderr, rt.tmpl, render.ResourceProposal, rt.format, ProposalView(doc.Data))` (buffer-then-write; a render failure → `RuntimeError(1)`); return `Success`. **Otherwise route every error to `reportFailure(cfg.stdout, cfg.stderr, rt.format, err)` with NO status interception** — `404` and `422` are real failures (the exact inverse of discard's `404`-as-success), and the Premium `403` is a generic `PermissionError(4)` with no plan-gate message. Attach the leaf to the `proposal` group by extending `newProposalCommand` to register it, and widen the group `Short` to name `propose`. Never read `ctx.Cred.Token`; add no new `Outcome`/`ExitCode`, no model, no render key.
  - **Acceptance criteria**:
    - `glassfrog proposal propose prp_0123` issues one bodyless `POST /proposals/prp_0123/propose` (no `Content-Type`, no body, `out == &Document[Proposal]`) and **no prior `GET`**; on `200` it renders the advanced proposal (status `proposed_outside_meeting`) and exits 0
    - `-o json` emits the raw `{data: Proposal}` document verbatim; `full`/`compact`/`-o <template-ref>` render the human projection through the singular `proposal` render path (reused, not re-added)
    - A `422` (transition not allowed) and a `404` (unknown/invisible proposal) are **failures** — routed through `reportFailure` to `APIError(3)`, exit non-zero, naming the HTTP status; **no status is intercepted as success**
    - A Premium `403` → `PermissionError(4)` with **no** plan-specific "not available on your plan" message; `401` → `PermissionError(4)`; `429` → `RateLimited(5)` and the `POST` is **not** auto-retried; transport → `NetworkUnavailable(6)`
    - Missing `<prp-id>` or more than one positional → cobra `UsageError(2)`, **no request** (transport tripwire); a stray flag (`--status`/`--changes`/…) is a cobra unknown-flag `UsageError(2)`
    - A present-but-invalid `--output` → `UsageError(2)`, **no request** (transport tripwire)
    - `*AuthError{NoCredentials}` → UsageError(2); `*AuthError{CredentialError}` → RuntimeError(1) — all via `reportFailure`
    - No new `Outcome`/`ExitCode`/model/render key/root flag; the siblings' `list`/`get`/`create` leaves unchanged; no token in any output; all branches run offline; `go build`/`go vet` clean
  - **Dependencies**: ⚠️ The `proposal` group + `proposalSeam` + the singular `proposal` render path + `glassfrog.Proposal`/`Document[Proposal]` from **055/056** (first-to-land-creates — see Branching Guidance); plus the landed shared seams — `resolveRenderTarget`/`writeHuman`/`output.RenderSuccess` (020/035), `Execute`/`ResponseError`/`RetryExecutor` (010/017), `reportFailure`/`classifyClientError` and the frozen registry (011/015). **Not startable until a sibling proposal spec has landed the group/model/singular render** (or this task creates the minimal subset per the leads-first fallback).
  - **Plan reference**: Phase 1 (the command); ADR-1 (flagless `propose` leaf on the `proposal` group, bodyless `POST` to the `/propose` sub-path, id pass-through), ADR-2 (decode `Document[Proposal]` and render via the singular `proposal` path — no synthesis, no new model/render), ADR-3 (server-authorized — no pre-check; `404`/`422` real failures, no status interception; generic Premium `403`); Cross-cutting (no new outcomes; `POST` non-retryable §133; sibling coordination)
  - **Interface references**: interface-cli.md — `proposal propose` Surface (flagless leaf, decoded proposal output), Interactions (dispatch order, bodyless POST + decode, no prior GET, outcome interpretation), Error Communication (the table; `404`/`422` as failure rows, generic `403`)
  - **Scenario references**: advance-to-circulation.feature: "A draft proposal is advanced into circulation", "The advanced proposal is rendered as JSON", "The advanced proposal carries the deadline and implicit response", "A disallowed transition fails with the API status", "An unknown proposal id fails with the API status", "A missing token fails as a not-authenticated usage error", "A missing proposal id is rejected before any request", "A Premium-gated refusal surfaces plainly", "A transport failure surfaces network-unavailable", "An invalid output format is rejected before any advance request"
  - **Risk**: ⚠️ Intercept **NO** status — every non-2xx routes through `reportFailure`; do **not** copy discard's `errors.As`-for-`404`-as-success divert (plan ADR-3, the inverse of 045). ⚠️ The `POST` is bodyless but DOES decode — pass no body, leave `ContentType` empty, and set `out == &Document[Proposal]`. ⚠️ Send **no prior `GET`** to check `available_transitions` (a validation tripwire asserts exactly one request, no read). ⚠️ The Premium `403` gets no bespoke plan message — keep it a generic `PermissionError(4)`. ⚠️ Reuse the singular `proposal` render path and `glassfrog.Proposal` — add no second render key or model (first-to-land-creates; grow, never duplicate). ⚠️ Extend `newProposalCommand` — do not create a second `proposal` group; widen its `Short`. ⚠️ Resolve render target BEFORE assembly so the tripwire confirms no request on a bad `--output`. ⚠️ Buffer-then-write the stdout render; never read `ctx.Cred.Token`. ⚠️ Capture stdout/stderr in tests with a temp file, not `os.Pipe` (PR #10 LEARNINGS).

- [x] **T002** [Shared] Make the driving scenarios pass as executable acceptance — new `internal/cli` godog suite over `advance-to-circulation.feature`; un-`@wip` the behavioral scenarios, keep `@validation` held — 11 behavioral scenarios pass (62 steps), 4 `@validation` held @wip; grew the shared `proposal.full` template by one guarded `Deadline:` line to satisfy the behavioral deadline scenario + 057 interface contract (see LEARNINGS)
  - **Scope**: Add godog step definitions for `features/proposal-write-flow/advance-to-circulation.feature` in a **new** `internal/cli` godog suite (e.g. `TestProposalProposeFeatures`) whose `Paths` names **only** that feature file (LEARNINGS: a suite points at its own file, never the `features/` directory). Drive `proposal propose` through the sibling `proposalSeam` over a fake base `http.RoundTripper` returning canned `POST /proposals/{id}/propose` responses (`200` with the advanced `{data: Proposal}` now `proposed_outside_meeting`, `422` transition-not-allowed, `404` unknown, `403` Premium-not-enabled, `429` rate-limited), plus a transport tripwire for the no-request paths (missing id; bad `--output`; no token) and a transport-failure stub. Assert, per scenario: stdout carries the advanced proposal (status `proposed_outside_meeting`, the `response_deadline`, the implicit `no_objection` in the response summary); `-o json` yields the raw `{data: Proposal}`; the `404`/`422` paths produce **no** success result and exit non-zero (the inverse-of-discard assertion); the `403` carries no plan-specific message; the `429` is surfaced once and the `POST` is **not** auto-retried (a retry tripwire / request counter confirms a single send); the request count is exactly one with no prior `GET` (the server-authorized validation). Remove `@wip` from the spec-derived + architecture-informed **behavioral** scenarios; keep the `@validation` scenarios `@wip` (held for validate). Grep existing `sc.Step(` registrations and reuse shared connection-context / token / exit-code / stderr-substring / no-request-tripwire phrasings from the `proposal-reads` and `tension-*` suites before writing new bindings; step helpers return errors, never panic.
  - **Acceptance criteria**:
    - Every non-`@validation` advance-to-circulation scenario has an executable, passing path; `@wip` removed from them
    - The `@validation` scenarios keep `@wip` (held for validate)
    - The new suite's `Paths` names only `advance-to-circulation.feature`; all `internal/cli` godog suites run and report their own independent scenario counts
    - The `422` and `404` scenarios assert a non-zero exit and **no** success result; the `403` scenario asserts no plan-specific message; the `429` scenario asserts a single surfaced rate-limit with **no** auto-retry (request count == 1); the server-authorized scenario asserts exactly one request and no prior read; the no-request scenarios assert the tripwire saw nothing
    - No real network (fake base transport / loopback only) and no real home/filesystem are touched; `go build ./...`, `go vet ./...`, and the feature suites run clean
  - **Dependencies**: T001 (the `proposal propose` command — all behavioral scenarios must be implementable)
  - **Plan reference**: Phase 1 (the command); Cross-cutting (testing — seam injection, transport tripwire, BDD over the driving scenarios, the no-pre-check + inverse-of-discard assertions)
  - **Interface references**: interface-cli.md — Surface, Interactions, Error Communication
  - **Scenario references**: advance-to-circulation.feature: all behavioral Rule-block scenarios — incl. "A rate-limited advance is surfaced, not silently retried" (the `@validation` scenarios — "The advanced result is the server's proposal unembellished", "The advance output carries no raw API envelope under a human format", "The advance issues one request and reads nothing first", "A 404 and a 422 are surfaced as real failures" — stay held for validate)
  - **Risk**: ⚠️ Suite scoping — point the suite at `advance-to-circulation.feature` only (not the directory); verify it reports its own count. ⚠️ Cover the `200`/`422`/`404`/`403`/`429` and every no-request (tripwire) fake so the advance, transition-not-allowed, unknown-id, Premium-refusal, rate-limited, missing-id, no-token, and bad-`--output` scenarios genuinely exercise their paths. ⚠️ Assert the inverse-of-discard behavior explicitly (`404`/`422` are failures, exit non-zero), the no-auto-retry on `429` (request count == 1, the `isSafeMethod` gate), and the single-request/no-prior-GET behavior. ⚠️ Reuse shared step phrasings before writing new bindings; step helpers return errors, never panic (LEARNINGS). ⚠️ Capture stdout/stderr with a temp file, not `os.Pipe` (PR #10 LEARNINGS).
