# Tasks: Withdraw Proposal

**Feature**: 059-withdraw-proposal
**Concretization**: Full context (plan + spec + interface-cli + scenarios)
**Inputs**: plan.md, spec.md, interface-cli.md, features/proposal-write-flow/withdraw-proposal.feature

---

## Dependency Graph

Phase 1: The `proposal withdraw` command (2 tasks, single-phase build) [Shared]

2 tasks total | T001 startable immediately off `main`; T002 depends on T001 | Builder: pipeline

> Plan-faithful: the plan defines a **single phase** ("the `proposal withdraw` command") with 059 as a **pure consumer** of landed machinery and the **structural twin of the landed `propose` leaf** (`internal/cli/proposal_propose.go`). This decomposes into the command + outcome interpretation (T001) and executable acceptance (T002) — one reviewable change each. There is **no render phase** (the withdrawn proposal renders through the landed singular `proposal` path — `ProposalView`/`ResourceProposal`/`proposal.{full,compact}` — added by 055/056, plan ADR-1), **no `internal/glassfrog` phase** (the `200` decodes into the landed `Proposal`/`Document[Proposal]`; no model field is added), **no transport phase** (a bodyless `POST` that decodes a response is served by `apiclient.Request`/`Execute` unchanged — `Method` is a free string, `ContentType` left empty, `Execute` decodes `out`), and **no validator phase** (the leaf has no editable-field flags). The only net-new surface is the `withdraw` leaf and its pure `run` function with the no-status-interception interpretation (plan ADR-1) and the destructive-but-unconfirmed stance (plan ADR-2).
>
> All three story scenarios are served by the one command, so every task is `[Shared]` — US1 (return a circulating proposal to draft by id), US2 (produce the resulting draft proposal as parseable data), and US3 (trust the server to authorize, surface refusals plainly) are realized together by `runProposalWithdraw`, not by separable code paths.
>
> **Base-branch sequencing — UNLIKE 057, every dependency is already on `main`.** 055 (Proposal Creation), 056 (Proposal Reads), 057 (Advance to Circulation), and 058 (Response Recording) have all merged, so the `proposal` command group (`newProposalCommand(seam)` + the `proposalSeam`), the `glassfrog.Proposal` model + `Document[Proposal]` decode, the **singular** `proposal` render path, AND the near-identical `propose` leaf template are present and proven. There is **no first-to-land-creates contract**, no minimal-subset fallback, and no sibling to wait on — 059 cuts its base directly off `main` and reuses all of it unchanged. **T001 is startable immediately.**
>
> **059 adds no new `Outcome` category, `ExitCode` case, generic/`glassfrog` type, transport field, validator set, render key, or root flag.** It extends `newProposalCommand` to attach one leaf and adds the bodyless-`POST`-and-decode orchestration with its no-status-interception interpretation in a new `proposal_withdraw.go`, mirroring `proposal_propose.go`.

---

## Branching Guidance

**Pipeline mode**: `spec/059-withdraw-proposal/base` → `spec/059-withdraw-proposal/task-1`, `…/task-2` (one task branch per T-id, merged back into the spec base).

**Base branch**: Cut the 059 base directly from `main` — the `proposal` group, the `glassfrog.Proposal` model (`Document[Proposal]`), the **singular** `proposal` render path, and the `propose` leaf template are all landed there (055/056/057/058 merged). Unlike 057, there is **no** upstream sibling dependency to time the base against.

**Role-based awareness**: parallel Conductor workspaces may carry later proposal specs, but 059 attaches **one leaf** to the shared `proposal` group (extending `newProposalCommand`) and adds the withdraw orchestration in a new file — it touches **no** `internal/glassfrog` model, **no** transport field, **no** render key, and does not modify the siblings' `list`/`get`/`create`/`propose`/`respond` leaves. The shared files (`newProposalCommand`, the singular `proposal` render) are reused, never duplicated.

---

## Phase 1: The `proposal withdraw` command [Shared]

- [ ] **T001** [Shared] Add the `proposal withdraw <prp-id>` command — `newProposalWithdrawCommand` + `runProposalWithdraw` in a new `internal/cli/proposal_withdraw.go`, mirroring the landed `proposal_propose.go` onto the `/withdraw` sub-path (bodyless `POST` to `/proposals/{id}/withdraw`, decode `Document[Proposal]`, render via the singular `proposal` path, no status interception, no confirmation/`--force`) + wiring under the `proposal` group — RED-first unit tests for every branch
  - **Scope**: In a new `internal/cli/proposal_withdraw.go` (mirroring `proposal_propose.go`), add `newProposalWithdrawCommand(seam proposalSeam) *cobra.Command`: a guard-registered leaf (`Use:"withdraw <prp-id>"`, `Args: cobra.ExactArgs(1)`, non-empty `Short`, `SilenceErrors`/`SilenceUsage`) declaring **no flags** (no confirmation/`--force`/`--yes` — plan ADR-2); it reads the persistent `--base-url`/`--output` and their `Changed()` presence into a `proposalWithdrawConfig`, then delegates to pure `runProposalWithdraw`. **Reuse the landed `proposalSeam`** (no new seam). `runProposalWithdraw(cfg) (Outcome, error)` follows the `runProposalPropose` shape exactly: (1) `resolveRenderTarget(cfg.seam, cfg.outputFlag, cfg.outputPresent, cfg.stderr)` FIRST (a bad `--output` is a fail-fast `UsageError(2)` with no request — there are no input-field checks); (2) `assemble` → `newClient` → `NewRetryExecutor`; (3) send **one** `Execute` to `POST /proposals/{id}/withdraw` with **no body, no `Content-Type`, and `out == &Document[Proposal]`** (`url.PathEscape` the id, passed through unvalidated — plan ADR-1); send **no prior `GET`** (no client pre-check of `available_transitions` — plan ADR-1) and **no `If-Match`**. Then **interpret the outcome** (plan ADR-1): if `err == nil` (a `200`), render the decoded/raw proposal on stdout exactly as `proposal propose`/`get` do — machine format → `output.RenderSuccess` over the raw `{data: Proposal}` bytes; human/`-o template` → `writeHuman(cfg.stdout, cfg.stderr, rt.tmpl, render.ResourceProposal, rt.format, render.ProposalView{Proposal: doc.Data})` (buffer-then-write; a render failure → `RuntimeError(1)`); return `Success`. **Otherwise route every error to `reportFailure(cfg.stdout, cfg.stderr, rt.format, err)` with NO status interception** — `404` and `422` are real failures (the inverse of discard's `404`-as-success), and the Premium `403` is a generic `PermissionError(4)` with no plan-gate message. Add **no** side-effect narration and **no** stderr advisory about the deleted responses (plan ADR-2). Attach the leaf to the `proposal` group by extending `newProposalCommand` to register it (beside the five landed leaves), and widen the group `Short` to name `withdraw`. Never read `ctx.Cred.Token`; add no new `Outcome`/`ExitCode`, no model, no render key.
  - **Acceptance criteria**:
    - `glassfrog proposal withdraw prp_0123` issues one bodyless `POST /proposals/prp_0123/withdraw` (no `Content-Type`, no body, `out == &Document[Proposal]`) and **no prior `GET`**; on `200` it renders the returned proposal (status `draft`) and exits 0
    - `-o json` emits the raw `{data: Proposal}` document verbatim; `full`/`compact`/`-o <template-ref>` render the human projection through the singular `proposal` render path (reused, not re-added)
    - A `422` (transition not allowed — already `draft`, or `withdraw` not offered) and a `404` (unknown/invisible proposal) are **failures** — routed through `reportFailure` to `APIError(3)`, exit non-zero, naming the HTTP status; **no status is intercepted as success**
    - A Premium `403` → `PermissionError(4)` with **no** plan-specific "not available on your plan" message; `401` → `PermissionError(4)`; `429` → `RateLimited(5)` and the `POST` is **not** auto-retried; transport → `NetworkUnavailable(6)`
    - The leaf declares **no** flags — a stray `--force`/`--yes`/`--status`/`--changes` is a cobra unknown-flag `UsageError(2)`; missing `<prp-id>` or more than one positional → cobra `UsageError(2)`, **no request** (transport tripwire)
    - A present-but-invalid `--output` → `UsageError(2)`, **no request** (transport tripwire)
    - `*AuthError{NoCredentials}` → UsageError(2); `*AuthError{CredentialError}` → RuntimeError(1) — all via `reportFailure`
    - No new `Outcome`/`ExitCode`/model/render key/root flag; the siblings' `list`/`get`/`create`/`propose`/`respond` leaves unchanged; no token in any output; all branches run offline; `go build`/`go vet` clean
  - **Dependencies**: None beyond `main` — the `proposal` group + `proposalSeam` + the singular `proposal` render path + `glassfrog.Proposal`/`Document[Proposal]` (055/056) and the `propose` leaf template (057) are all landed; plus the landed shared seams — `resolveRenderTarget`/`writeHuman`/`output.RenderSuccess` (020/035), `Execute`/`ResponseError`/`RetryExecutor` (010/017), `reportFailure`/`classifyClientError` and the frozen registry (011/015). **Startable immediately.**
  - **Plan reference**: Phase 1 (the command); ADR-1 (twin of the landed `propose` leaf — flagless `withdraw` leaf on the `proposal` group, bodyless `POST` to the `/withdraw` sub-path, id pass-through, decode-and-render, server-authorized — no pre-check, `404`/`422` real failures, no status interception, generic Premium `403`), ADR-2 (destructive but unconfirmed — no side-effect narration, no stderr advisory, no confirmation/`--force`); Cross-cutting (no new outcomes; `POST` non-retryable §133; no sibling-coordination concern)
  - **Interface references**: interface-cli.md — `proposal withdraw` Surface (flagless leaf, decoded `draft` proposal output, no `--force`), Interactions (dispatch order, bodyless POST + decode, no prior GET, outcome interpretation, destructive-but-unconfirmed), Error Communication (the table; `404`/`422` as failure rows, generic `403`)
  - **Scenario references**: withdraw-proposal.feature: "A circulating proposal is withdrawn back to draft", "The withdrawn proposal is rendered as JSON", "The withdrawn proposal carries the cleared deadline and updated transitions", "A disallowed transition fails with the API status", "An unknown proposal id fails with the API status", "A missing token fails as a not-authenticated usage error", "A missing proposal id is rejected before any request", "A Premium-gated refusal surfaces plainly", "A transport failure surfaces network-unavailable", "A rate-limited withdraw is surfaced, not silently retried", "An invalid output format is rejected before any withdraw request"
  - **Risk**: ⚠️ Intercept **NO** status — every non-2xx routes through `reportFailure`; do **not** copy discard's `errors.As`-for-`404`-as-success divert — copy `proposal_propose.go`, not `tension.go`'s discard path (plan ADR-1, Risks). ⚠️ The `POST` is bodyless but DOES decode — pass no body, leave `ContentType` empty, and set `out == &Document[Proposal]`. ⚠️ Send **no prior `GET`** to check `available_transitions` (a validation tripwire asserts exactly one request, no read). ⚠️ Add **no** confirmation prompt, `--force`/`--yes` flag, or stderr advisory about deleted responses (plan ADR-2 — destructive but unconfirmed; a test asserts the leaf has no flags). ⚠️ The Premium `403` gets no bespoke plan message — keep it a generic `PermissionError(4)`. ⚠️ Reuse the singular `proposal` render path and `glassfrog.Proposal` — add no second render key or model. ⚠️ Extend `newProposalCommand` — do not create a second `proposal` group; widen its `Short`. ⚠️ Resolve render target BEFORE assembly so the tripwire confirms no request on a bad `--output`. ⚠️ Buffer-then-write the stdout render; never read `ctx.Cred.Token`. ⚠️ Capture stdout/stderr in tests with a temp file, not `os.Pipe` (PR #10 LEARNINGS).

- [ ] **T002** [Shared] Make the driving scenarios pass as executable acceptance — new `internal/cli` godog suite over `withdraw-proposal.feature`; un-`@wip` the behavioral scenarios, keep `@validation` held
  - **Scope**: Add godog step definitions for `features/proposal-write-flow/withdraw-proposal.feature` in a **new** `internal/cli` godog suite (e.g. `TestProposalWithdrawFeatures`) whose `Paths` names **only** that feature file (LEARNINGS: a suite points at its own file, never the `features/` directory). Drive `proposal withdraw` through the landed `proposalSeam` over a fake base `http.RoundTripper` returning canned `POST /proposals/{id}/withdraw` responses (`200` with the returned `{data: Proposal}` now `draft` — `proposed_at`/`response_deadline` cleared, `response_summary` zeroed, `available_transitions` offering `propose`; `422` transition-not-allowed; `404` unknown; `403` Premium-not-enabled; `429` rate-limited), plus a transport tripwire for the no-request paths (missing id; bad `--output`; no token) and a transport-failure stub. Assert, per scenario: stdout carries the returned proposal (status `draft`, deadline cleared); `-o json` yields the raw `{data: Proposal}`; the `404`/`422` paths produce **no** success result and exit non-zero (the inverse-of-discard assertion); the `403` carries no plan-specific message; the `429` is surfaced once and the `POST` is **not** auto-retried (a retry tripwire / request counter confirms a single send); the request count is exactly one with no prior `GET` (the server-authorized validation); the command narrates nothing about deleted responses. Remove `@wip` from the spec-derived + architecture-informed **behavioral** scenarios; keep the `@validation` scenarios `@wip` (held for validate). Grep existing `sc.Step(` registrations and reuse shared connection-context / token / exit-code / stderr-substring / no-request-tripwire phrasings from the `proposal-advance` / `proposal-reads` / `tension-*` suites before writing new bindings; step helpers return errors, never panic.
  - **Acceptance criteria**:
    - Every non-`@validation` withdraw-proposal scenario has an executable, passing path; `@wip` removed from them
    - The `@validation` scenarios keep `@wip` (held for validate)
    - The new suite's `Paths` names only `withdraw-proposal.feature`; all `internal/cli` godog suites run and report their own independent scenario counts
    - The `422` and `404` scenarios assert a non-zero exit and **no** success result; the `403` scenario asserts no plan-specific message; the `429` scenario asserts a single surfaced rate-limit with **no** auto-retry (request count == 1); the server-authorized scenario asserts exactly one request and no prior read; the no-request scenarios assert the tripwire saw nothing
    - No real network (fake base transport / loopback only) and no real home/filesystem are touched; `go build ./...`, `go vet ./...`, and the feature suites run clean
  - **Dependencies**: T001 (the `proposal withdraw` command — all behavioral scenarios must be implementable)
  - **Plan reference**: Phase 1 (the command); Cross-cutting (testing — seam injection, transport tripwire, BDD over the driving scenarios, the no-pre-check + inverse-of-discard assertions)
  - **Interface references**: interface-cli.md — Surface, Interactions, Error Communication
  - **Scenario references**: withdraw-proposal.feature: all behavioral Rule-block scenarios — incl. "A rate-limited withdraw is surfaced, not silently retried" (the `@validation` scenarios — "The withdrawn result is the server's proposal unembellished", "The withdraw output carries no raw API envelope under a human format", "The withdraw issues one request and reads nothing first", "A 404 and a 422 are surfaced as real failures" — stay held for validate)
  - **Risk**: ⚠️ Suite scoping — point the suite at `withdraw-proposal.feature` only (not the directory); verify it reports its own count. ⚠️ Cover the `200`/`422`/`404`/`403`/`429` and every no-request (tripwire) fake so the withdraw, transition-not-allowed, unknown-id, Premium-refusal, rate-limited, missing-id, no-token, and bad-`--output` scenarios genuinely exercise their paths. ⚠️ Assert the inverse-of-discard behavior explicitly (`404`/`422` are failures, exit non-zero), the no-auto-retry on `429` (request count == 1, the `isSafeMethod` gate), and the single-request/no-prior-GET behavior. ⚠️ Reuse shared step phrasings before writing new bindings; step helpers return errors, never panic (LEARNINGS). ⚠️ Capture stdout/stderr with a temp file, not `os.Pipe` (PR #10 LEARNINGS).
