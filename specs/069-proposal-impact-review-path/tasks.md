# Tasks: Proposal Impact Review Path

**Feature**: 069-proposal-impact-review-path
**Concretization**: Full context (plan + spec + interface + scenarios) — tasks carry scenario and interface references
**Inputs**: plan.md (single phase; ADR-1 both-form, ADR-2 new agent, ADR-3 review-in-subagent + operator decision and gated respond in the caller context, ADR-4 pure composition + footprint honesty + reads-inform-never-decide, ADR-5 drift guard + one-in-nine-out gate-membership invariant), spec.md (3 user scenarios), interface-spec.md, `features/unequipped-agent-operators/proposal-impact-review-path.feature` (17 scenarios)

---

## Dependency Graph

Phase 1: Proposal-impact-review path artifacts + drift guard (2 tasks, no phase dependencies; intra-phase: T002 depends on T001) [Shared]

2 tasks total | 0 phases parallelizable | Builder: pipeline (single active spec)

The plan is a single phase (ADR-1/2/3 land the two artifacts together; ADR-5's guard pins the leaves they name and the one-write gate-membership invariant). The two PR-sized units follow the plan's own suggested split: artifacts + registration first, drift guard second — the sibling-standard shape (065/066/067/068).

---

## Branching Guidance

**Pipeline mode**: `spec/069-proposal-impact-review-path/base` → `spec/069-proposal-impact-review-path/task-1`, `spec/069-proposal-impact-review-path/task-2`

T002 depends on T001 (the guard pins the command leaves the artifacts name and asserts `proposal respond`'s membership in 063's gated set and the nine reads' absence from it), so the two task branches land in sequence onto the spec base.

---

## Phase 1: Proposal-impact-review path artifacts + drift guard [Shared]

- [ ] **T001** [Shared] Author the proposal-impact-review skill and the pure-read proposal-impact-reviewer agent, single-source the composed-leaf list, and register the agent — skill + agent + commands.txt covering 16 of the feature's 17 BDD scenarios (T002 owns the 17th, the no-invented-surface drift check); auto-discovered from plugin/agents/, no plugin.json change; if a gate anchor is needed by the BDD guardrail validation scenario (068's shape), land it with the BDD helpers in production source (e.g. `internal/build/proposalimpactreview.go`, `ProposalImpactReviewGatedWrite = "proposal respond"`) so T002's guard cross-checks the same anchor
  - **Scope**: Add `plugin/skills/proposal-impact-review/SKILL.md` (the thin, discoverable entry point: when to reach for it, the single-sourced workflow, the delegation-to-the-agent instruction, and the decision-and-respond note) and `plugin/agents/proposal-impact-reviewer.md` (the review executor: identity/scope with the family's strictest fence — zero proposal writes, including this path's own `respond` — the workflow it runs *referencing* the skill's workflow rather than restating a divergent copy, the footprint-honesty rule, the composed reads, and the impact-picture output contract). Single-source the composed-leaf list in `plugin/agents/proposal-impact-review-commands.txt` (ten leaves: nine reads + the skill's gated `proposal respond`). The agent is discovered by directory convention from `plugin/agents/` — **do not** add an `agents` key to `plugin/.claude-plugin/plugin.json` (the 063–068 auto-discovery convention; the manifest stays unchanged). Hand-authored, committed content; adds no CLI code beyond any BDD-consumed anchor. **Do not touch** 068's `proposal-circulator.md`, 067's `proposal-drafter.md`, or their leaf lists — ADR-2 mints a new agent precisely so their validate-pinned no-respond fences stay intact.
  - **Acceptance criteria**:
    - `plugin/skills/proposal-impact-review/SKILL.md` exists with YAML frontmatter `name` (`proposal-impact-review`) + `description`; the `description` states *when* to reach for it (a circulating proposal awaits the operator's consent response; the operator wants to see what a proposal would change for their own roles, actions, and projects before answering; the operator is ready to record `no_objection` or `bring_to_meeting` — a gated write, always the operator's own choice), worded not to fire on "advance / monitor / withdraw my own proposal" (068), "assemble changes / create the draft" (067), "am I allowed to do X" (065), "capture/refine/retire a tension" (066), or "understand the governance around a concern" (064)
    - The skill body carries the when / workflow (`prp_` id in hand — or delegate the pending-list question first and let the operator pick — → delegate the review to the `proposal-impact-reviewer` subagent → present the returned impact picture (what changes; where it intersects the operator's roles/actions/projects; the current-vs-proposed read-backs; the footprint-coverage qualifier) → the operator decides: `no_objection`, `bring_to_meeting`, or not yet (a first-class exit — present the picture and stop) → on an explicit choice, narrate the proposal and the chosen value, run `glassfrog proposal respond <prp-id> --response <value>` through 063's confirmed flow in the caller's context, and present the recorded response: `prr_` id, value, the parent proposal's status at the time of response, `accepted` meaning the response closed the consent window) / delegation / decision-and-respond note, pointing at `glassfrog <cmd> --help` for per-command detail and at the orientation skill (062) for output/pagination/exit-code mechanics rather than restating them
    - The decision-and-respond note states that recording a consent response is a governance write gated by 063's confirmed write flow, issued **in the caller's context** — the reviewer agent cannot and must not run it (plan ADR-3); the one-token value rides inline (`--response no_objection` / `--response bring_to_meeting`) so the confirmation shows the complete payload; the responding person is the token's own identity; a declined confirmation means no response recorded; and the skill **never infers or defaults the value** from the review's content — "no objections found" is not an instruction to answer `no_objection`. The path's only write is this one — never `proposal create` (067) or `proposal propose`/`proposal withdraw` (068), which 063 gates regardless
    - The agent is registered so the host discovers it and the skill's delegation resolves; if the agent is absent/unregistered, the skill's workflow remains usable as guidance with no CLI command broken (documented degradation)
    - `plugin/agents/proposal-impact-reviewer.md` exists with frontmatter `name` (`proposal-impact-reviewer`), `description`, and a **read-posture `tools` grant** that includes `Bash` (to invoke the composed `glassfrog` reads) and excludes `Write`/`Edit` (no workspace mutation); the body carries Identity & scope (zero writes — never `proposal respond` (the skill's caller-context step, taken after the operator decides — asked to record, it refuses and names the handoff), never `create` (067), never `propose`/`withdraw` (068), never a tension write (066); no objection verdict computed, no answer recommended; never an authority ruling (065), never coaching; reads inform, never gate — `available_transitions` is narration, never a client-side precondition), Workflow (by reference to the skill: ground via `proposal get <prp-id>` — or situate via `proposal list`, paged through the **full** set, never a silent single-page cap — → footprint via `me`, `me roles`, `me actions`, `me projects` → affected-governance read-backs via `roles <id>`, `domains <id>`, `policies <id>` → synthesize), Footprint honesty (`me roles` does **not** paginate: read its incompleteness signal — stderr note in human formats, in-band pagination metadata in `json`/`yaml` — and carry it into the picture; a no-impact conclusion over an incomplete footprint reads "not in the roles visible to this read (list incomplete)", never an unqualified negative; when unsure whether a change touches the operator, show it), Composed reads (exactly the nine read leaves), and the impact-picture Output contract (proposal?/changes?/footprint?/footprint_coverage/intersections/pending?/notes; `footprint_coverage` tri-state `complete`/`incomplete`/`unknown`, never a silent `complete`; `intersections: []` is the honest qualified no-impact case; **no verdict field exists**; every element carries its id; the proposal and change set exactly as the server returned them)
    - The artifacts' only write is the skill's caller-context `proposal respond`; no other proposal-write phrase appears as a command the path runs; advancing/monitoring/withdrawing hand to 068, drafting to 067, authority questions to 065, deeper navigation to 064; the workflow steps are single-sourced in the skill and referenced by the agent — no second, divergent copy
    - The existing `plugin/skills/orientation/` (062), the sibling path skills and agents (064 governance-navigation/navigator, 065 constraint-discovery/constraint-navigator, 066 tension-processing/tension-processor + commands.txt, 067 proposal-drafting/proposal-drafter + commands.txt, 068 proposal-circulation/proposal-circulator + commands.txt), and `plugin/hooks/` (063, including `gated-commands.txt`) are untouched, and no `marketplace.json` is added (distribution is #70)
  - **Dependencies**: None
  - **Plan reference**: Phase 1; ADR-1 (thin skill + subagent), ADR-2 (new `proposal-impact-reviewer`, `plugin/agents/` reuse, no 067/068 reuse), ADR-3 (review in-subagent; operator decision + gated respond in the caller context, announced divergence from the 067/068 write locus; layered confirmation; operator-chosen value only), ADR-4 (compose ten shipped leaves, footprint honesty, reads inform never decide, no CLI code)
  - **Scenario references**: proposal-impact-review-path.feature — "A circulating proposal is reviewed against the operator's footprint", "A failed review read yields a partial picture", "The reviewer is reachable once the plugin registers it", "A missing reviewer degrades the path to guidance", "The result is a synthesized impact picture, not raw output", "A no-impact review is a load-bearing result", "A review stands on its own without a response", "An incomplete footprint qualifies the no-impact conclusion", "The picture carries no verdict and chooses no response", "The path reviews without judging authority or coaching", "A no-objection is recorded through the confirmed write flow", "A bring-to-meeting is recorded and blocks auto-acceptance", "An unconfirmed response leaves the record untouched", "A rejected response fabricates no state", "The reviewer hands the respond step back to the caller", "The path routes its one write through the guardrail"
  - **Interface references**: interface-spec.md — Surface (structural layout, `SKILL.md` / agent frontmatter, required sections, footprint honesty, impact-picture output shape, single-source leaf list), Interactions (skill→agent delegation flow, the caller-context respond step, reviews-inform-never-decide), Error Communication (declined confirmation, rejected respond, partial picture, incomplete footprint, no-impact case, review-only exit, agent-refuses-to-respond handoff, gated-write nuance, never-decide nuance)

- [ ] **T002** [Shared] Add the best-effort drift-guard test in `internal/build` — pins the composed leaves AND the one-write gate-membership invariant (extending the 067/068 gate-posture pattern to a one-in-nine-out path) — 1 validation scenario + standalone guard; the gated write is anchored explicitly (`proposal respond`, cross-checked against the composed list) so a read/write swap across the gate is caught rather than satisfied by count, while the registries stay source-derived — validation scenario passing (17/17 total); guard verified red-on-drift (e.g. dropping `proposal respond` from the gated set fails the build naming the leaf)
  - **Scope**: A new `internal/build` test (`proposal_impact_review_guard_test.go`) asserting every leaf the skill/agent compose still resolves in the CLI's command registry **and** the gate-membership posture holds: `proposal respond` **is a member of** 063's gated proposal-write set, and the nine reads (`proposal get`, `proposal list`, `me`, `me roles`, `me actions`, `me projects`, `roles`, `domains`, `policies`) are **not**. Command-path resolution must handle the registry's mixed shapes: two-token `proposal <sub>` / `me <sub>` paths, bare `me`, and the top-level reads (`domains`/`policies` are top-level commands, not `roles` subcommands). Best-effort and explicitly partial — pins the *existence* of the command leaves and their *gate-membership posture*, not their flags (deferred to `--help`), not the impact-picture prose, not the footprint-honesty or never-decide disciplines (prompt-level, validation-scenario-verified), and not parser robustness. If an anchor proves infeasible to assert, state the reduced coverage rather than dropping it silently.
  - **Acceptance criteria**:
    - Test asserts the ten leaves the artifacts compose each resolve to a real command in the CLI's registry, reading the composed set from the single-source leaf-list file (`plugin/agents/proposal-impact-review-commands.txt`, T001), not a hard-coded copy
    - Test asserts `proposal respond` is **present in** 063's gated set (source-derived from `plugin/hooks/gated-commands.txt`) — the confirmed-write-flow promise pinned structurally: the respond leaf leaving the gated registry fails the build — and that the nine composed reads are **absent from** it (the review must not start prompting)
    - The gated write is anchored as an explicit per-leaf contract-fact cross-checked against the composed list — a count-only membership check (e.g. "exactly one leaf is gated") that a read/write swap could satisfy does not pass for this criterion (drift-guard boundary nuance: derive the sets, anchor the specific member)
    - Test fails loudly and names the offending leaf when one no longer exists in the shipped CLI or violates the gate-membership invariant on either side
    - All sides source-derived — composed set from the leaf list, live surface from the command registry, gated set from `gated-commands.txt` — never a hard-coded second copy (drift-guard-must-not-hardcode-the-SoT)
    - Any leaf or fact deliberately left uncovered is documented in the test, not omitted silently (no silent caps)
    - Reuses the `internal/build` config-guard home/idiom established by 062–068 (068's `proposal_circulation_guard_test.go` + `proposal-circulation-commands.txt` single-source are the concrete models; this guard asserts one membership and nine absences where 068 asserted two and two)
  - **Dependencies**: T001
  - **Plan reference**: Phase 1; ADR-5 (best-effort drift guard + one-in-nine-out gate-membership invariant)
  - **Scenario references**: proposal-impact-review-path.feature — "The path names no command the CLI lacks"
  - **Interface references**: interface-spec.md — Surface (single-source leaf list), Error Communication (drift guard red on missing leaf or gate-membership violation on either side; reduced coverage stated)
