# Analyze: Actor Read

**Feature**: 049-actor-read
**Artifacts analyzed**: spec.md, plan.md, interface-cli.md, features/actors-disconnected-from-governance/actor-read.feature, tasks.md
**Checklist context**: checklist.md loaded (13/13 pass, 0 fail)
**Checks**: 16 (16 pass, 0 findings)
**Generated**: 2026-06-13

---

## Summary

All 16 cross-artifact checks pass. Consistency: 6/6 · Completeness: 6/6 · Coherence: 4/4.

P0: 0 · P1: 0 · P2: 0.

The artifact set tells one story: a single read-only `actors <id>` drill-in over `GET /actors/{id}`, grown onto 048's existing `actors` command (`cobra.NoArgs` → `cobra.MaximumNArgs(1)`, `RunE` branching on `len(args)`), validating `--include` locally against `{roles, assignments}`, passing the actor id through to a clean `404`, issuing exactly one request with no page walk, and rendering an `ActorDetail` (embedding the unchanged `Actor` + optional full `Role`/`Assignment` embeds) through a new singular `ResourceActor` render key. Spec → plan → interface → scenarios → tasks agree on scope, behavior, and surface.

---

## Consistency Checks (P0): 6/6 passed

- **C1 — spec Integration Boundaries ↔ plan System Architecture (pass)**: Every boundary the spec names (Glassfrog API `GET /actors/{id}`, Request Execution 010, Request Authentication 007, Output Format Selection 020, Exit-Code Convention 004 / API Error Extraction 015, Identity Read 011 as model sibling, Actor Directory 048 as command sibling, Actor Assignments 050 as embed-vs-standalone sibling) appears as a composed component or named relationship in the plan's System Architecture and data-flow. No boundary is missing or contradicted.
- **C2 — spec Behavioral Accord ↔ plan System Architecture (pass)**: The plan's architecture serves every behavior group — invocation (the `MaximumNArgs(1)` branch on `len(args)`, single positional id), related-resources (`--include` reject-unknown over `{roles, assignments}`, embedded inline), output (structured data handed to 020), and failure (the shared `classifyClientError` chain, `404`/transport distinction) each map to an accord clause. No behavior is contradicted by the design.
- **C3 — spec Non-Behaviors ↔ plan System Architecture (pass)**: The plan architects nothing the spec excludes — it explicitly does not list/search the directory (that is 048's 0-arg branch, preserved verbatim), does not provide a standalone assignments listing (050; `--include assignments` is the inline embed), does not route `agt_` through the gated `/agents/{id}` alias, follows no pagination, performs no write (`PATCH`/`DELETE` out of scope), defines no own format flag, and re-implements no seam. ADR-1–ADR-4 and "What This Plan Does Not Cover" mirror the spec's non-behaviors.
- **C4 — plan Architecture Decisions ↔ interface-cli Surface (pass)**: The interface reflects every plan choice — grow `actors` to `MaximumNArgs(1)` with the 0-arg/1-arg branch (ADR-1 → Surface + mode-separation table), `ActorDetail` embedding `Actor` + optional `Roles`/`Assignments` decoded from `{data: ActorDetail}` (ADR-2 → Consistency Notes), `--include` validated locally / id passed through (ADR-3 → `--include` row + `<id>` row), and the new singular `ResourceActor` key with `full`/`compact` templates (ADR-4 → Output section). No interface contract diverges from a plan decision.
- **C5 — plan System Architecture ↔ tasks Task Scope (pass)**: Every task builds a component the plan names — T001 (`ActorDetail` schema type), T002 (the singular `actor` render key + `ResourceActor`), T003 (growing the `actors` command + `--include` validator + mode-separation guards), T004 (executable acceptance, the plan's Cross-cutting/testing concern). No task introduces a component the plan doesn't describe; reused machinery (`Execute`, `classifyClientError`, the single-resource render dispatch, the landed `Actor`/`Role`/`Assignment` types) is correctly not re-built.
- **C6 — interface-cli Surface ↔ feature Given/When/Then (pass)**: Every scenario step references a surface the interface defines — the `actors <id>` command, `--include roles`/`assignments`, the `include` request query, the `/actors/{id}` path, the ungated-`/actors` vs gated-`/agents` distinction, the mode-separation rejections (filter-with-id, `--include`-no-id), the no-id directory list, and the exit-code classes (0/2/non-zero API-error). No step uses an endpoint or field absent from the interface.

## Completeness Checks (P1): 6/6 passed

- **K1 — spec Driving Scenarios → feature scenarios (pass)**: All 8 driving scenarios (3 happy: single read, agent read, footprint embed; 2 error: no credential, unknown-id 404; 3 edge: unsupported `--include`, ungated agent alias, assignments embed) have a Gherkin equivalent in `actor-read.feature`, and all 4 validation scenarios (gated-alias, structured-not-pre-rendered, no-page-walk, non-2xx-surfaced-not-classified) are present with `@validation @wip`. Each `# Source:` comment maps 1:1 to an actual spec scenario title (verified against spec.md Driving + Validation Scenarios). The feature file additionally carries 4 architecture-informed scenarios, clearly marked `# Proposed: interface CLI …` / `# Proposed: plan ADR-1 …` (mode-separation ×2 + the no-id-still-lists directory scenario) — additive coverage, not a gap.
- **K2 — spec Integration Boundaries → interface coverage (pass)**: The feature's only external surface is the CLI; `interface-cli.md` exists. The Glassfrog API boundary is consumed through the landed request seam (010), not a new interface — consistent with every sibling read spec (the spec's Integration Boundaries name 010/007/020/004/015 as composed seams, not new interfaces to author).
- **K3 — plan phases → tasks decomposition (pass)**: All three plan phases are decomposed — Phase 1 (Schema) → T001; Phase 2 (Render) → T002; Phase 3 (Command) → T003 — plus T004 (executable acceptance), which realizes the plan's Cross-cutting/testing requirement. No phase is left without tasks.
- **K4 — plan components → implementing tasks (pass)**: Every new component has an implementing task (`ActorDetail`→T001; `actor` render key / `ResourceActor`→T002; grown command + `--include` validator + mode-separation guards→T003); reused/landed components (the `Execute` seam, `classifyClientError`, the single-resource render dispatch, the `Actor`/`Role`/`Assignment`/`RoleDetail` types) correctly have no task.
- **K5 — interface surfaces → scenario coverage (pass)**: Every behavioral surface is exercised — single-id read, agent (`agt_`) read, `--include roles` footprint, `--include assignments` embed, unsupported-`--include` rejection, the two mode-separation rejections (filter-with-id, `--include`-no-id), single-request/no-walk, ungated-endpoint, the no-id directory list (048-preservation), the `404` unknown-id, the no-token usage error, and the structured-output validation. Each interface row has at least one scenario.
- **K6 — spec User Scenarios → interface coverage (pass)**: All three Connextra user scenarios have interface coverage — see what an actor does via the embedded governance footprint (`--include roles`), drill into a `per_`/`agt_` id with one command (the `actors <id>` single read), and tell "no such actor" apart from "the network failed" (the `404`-vs-transport split in the Error Communication table). Each maps to a documented surface on the `actors` command.

## Coherence Checks (P2): 4/4 passed

- **H1 — Terminology (pass)**: The key concepts — `actors`/actor, single-actor drill-in, `--include` / `roles` / `assignments`, governance footprint, `ActorDetail`, `ResourceActor` (singular) vs `ResourceActors` (048 plural), no-pagination/single-request — use consistent names across spec, plan, interface, scenarios, and tasks. The 048-vs-049 boundary (directory-list vs by-id drill-in) and the 050 boundary (embedded-on-actor vs standalone-paginated) are named identically wherever they appear. No concept is renamed without an explicit alias.
- **H2 — Detail symmetry (pass)**: spec↔plan and plan↔tasks are proportionate; no artifact carries 3x+ the detail of its neighbour on a shared topic. The plan elaborates the spec's drill-in/`--include`/no-pagination model into four ADRs; tasks elaborate the plan's three phases (plus acceptance) without over- or under-shooting (T003's command growth carries the most detail, matching the plan's richest ADR-1/ADR-3 treatment).
- **H3 — Scope alignment (spec + interface + tasks) (pass)**: All three describe the same capability set — one by-id single read grown onto the `actors` command, local `--include` validation over `{roles, assignments}`, id pass-through to a clean `404`, no pagination, and a new singular `actor`/`ResourceActor` render key. Nothing is silently added or dropped (the single-read-on-the-shared-command shape, the embed-vs-standalone 050 foreclosure, and the ungated-`/actors` agent reasoning hold across all three).
- **H4 — Phase coverage (plan + tasks) (pass)**: Tasks' phase structure mirrors the plan's three phases (Schema → Render → Command); tasks reference no phase absent from the plan, and every plan phase has corresponding tasks. The acceptance task T004 sits under the plan's Cross-cutting/testing concern, consistent with the plan. The linear T001→T002→T003→T004 chain matches the plan's stated dependencies (the `ActorDetail` schema gates the render view, which gates the command, which gates acceptance).

---

## Checklist Correlation

Checklist found 0 failing checks (13/13 pass), so there are no overlapping vertical findings to correlate. The strongest checklist passes (C-VI no silent truncation — confirmed by structural single-request inapplicability; C-VIII no fabricated data — explicit-absence guards on the embeds; C-V composition — bounded additive growth of 048's command) align with the horizontal C2/C3/H3 results: the no-pagination, anti-fabrication, and single-read-scope behaviors are consistently realized from spec through scenarios and tasks. The 048-command-growth coordination (checklist C-V.1 / plan ADR-1) is the same surface analyze C4/H3 confirm is described identically across plan, interface, and tasks.

## Governance Notes

- **One interface, one feature file**: scaled checks (C4/C6/K2/K5/K6) ran one evaluation each. No checks were skipped for missing artifacts — the full spec → tasks set is present (spec.md, plan.md, interface-cli.md, actor-read.feature, tasks.md, checklist.md).
- **Checklist context**: loaded — 13/13 checks pass, 0 failures. Correlation was possible; no overlaps to flag as shared-root-cause.
- **`--include` page-walk inapplicability** (cross-cut with checklist C-VI.1): unlike sibling list reads (048/025/038/041), Actor Read issues exactly one request with no cursor; the no-walk behavior is itself scenario-covered (a `@validation` held-out scenario asserts one request even with large embedded arrays), so there is no delegated-coverage gap to note (contrast 048's `--per-page` delegation to 016). Noted for transparency, not as a finding.
- **Godog step-text reuse (T004 build-time, not an artifact finding)**: the feature file's tripwire/ungated-alias steps (e.g. "a transport tripwire that records every request that is sent", "Given a token without the ai_integration feature") are phrased close to but not byte-identical with 048's sibling steps. This is a step-binding reuse concern for T004's implementation (godog matches by exact text), already flagged in tasks T004's Risk ("reuse shared step phrasings … before writing new bindings"); it is not a cross-artifact inconsistency among the 049 artifacts and produces no analyze finding.
