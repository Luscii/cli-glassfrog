# Tasks: Subroles Tension Roll-up

**Feature**: 046-subroles-tension-roll-up
**Concretization**: Full context (plan + spec + interface-cli + scenarios)
**Inputs**: plan.md, spec.md, interface-cli.md, features/tension-capture/subroles-tension-roll-up.feature

---

## Dependency Graph

Phase 1: The `tension subroles` roll-up leaf (1 task, no dependencies) [Shared]
Phase 2: Executable acceptance (1 task, depends on Phase 1) [Shared]

2 tasks total | T001 startable immediately | Builder: pipeline — single cohesive leaf

> Plan-faithful: the plan's two ordered steps map 1:1 — the command leaf (T001) and the BDD suite (T002). This is the **thinnest** member of the tension read family: where Tension Reads (043) had to *add* the plural `tensions` render path and a new `validateTensionStatus`, 046 **reuses every data-layer seam unchanged** — no render phase, no validator phase, no schema phase, no new `Outcome`/`ExitCode`/root flag. The only genuinely new code is one command leaf whose request differs from `tension list`'s only in the path segment (`/roles/{role_id}/subroles/tensions`).
>
> **✅ All cross-spec dependencies are landed on `main` and this task list is verified against the implementation.** 042 (#91) ships the `tension` group (`newTensionCommand(seam)`, parenting `create`/`list`/`get`/`update`), the `glassfrog.Tension` model + `Document[Tension]`, and the singular `tension` render; **043 (#97)** ships the plural `tensions` render key (`render.ResourceTensions` in `builtinResources` + `render.TensionsView` + `tensions.{full,compact}.tmpl`), the `validateTensionStatus`/`supportedTensionStatuses`/`supportedTensionStatusNames()` set (in the tension command file, **not** `status.go`), and the `tension list` runner shape (`newTensionListCommand`/`runTensionList`/`tensionsConfig` in `internal/cli/tension_reads.go`) that T001 clones with the path swapped; 044 (#107) ships `tension update`. 007/009/010/011/015/016/017/018/020/025/031/032/035 are Complete with their packages shipped — `Page[T]`/`paging.All`, the generic `Document[T]`, `RetryExecutor`, `classifyClientError`/`Diagnose`/`Outcome`/`ExitCode`, the persistent `--base-url`/`--output`/`-o`, the 035-widened `resolveRenderTarget`+`writeHuman`/`aggregateRawData`/`output.RenderSuccess` machinery, and 032's `reportFailure` chokepoint. 046 adds **no** new render key, model, validator, generic type, `Outcome`, `ExitCode`, or root flag.
>
> **The cross-role counterpart to `tension list` (043).** `tension list <role-id>` reads a role's own tensions; 046 reads `GET /roles/{role_id}/subroles/tensions` (`listSubrolesTensions`) — the one-level roll-up across the anchor's direct sub-roles — as a fifth verb leaf under the same `tension` group (plan ADR-1, define-session decision: a distinct `subroles` verb, not a `--subroles` flag on `list`). Story labels: the spec's four user scenarios — US1 (see what the circles inside are sensing), US2 (assemble the sub-roles' tensions in one roll-up), US3 (narrow by status), US4 (trust the roll-up is whole). The single leaf serves all four → `[Shared]`.

---

## Branching Guidance

**Pipeline mode**: `spec/046-subroles-tension-roll-up/base` → `spec/046-subroles-tension-roll-up/task-1`, `…/task-2` (one task branch per T-id, merged back into the spec base).

**Base branch**: 042/043/044 have all landed on `main`, so cut the 046 base from current `main` — the `tension` group, `Tension` model, the plural `tensions` render path, and `validateTensionStatus` are all present and reused as-is.

**Role-based awareness**: parallel Conductor workspaces may carry sibling tension specs (045 Tension Discard follows). 046 attaches **one** leaf to 042's `tension` group (extending `newTensionCommand`), makes **no** change to `internal/glassfrog` (042's `Tension` reused), `internal/render` (043's `tensions` key reused), or the validator set (043's `validateTensionStatus` reused) — the only new file content is the `subroles` leaf in `internal/cli` and its BDD suite.

---

## Phase 1: The `tension subroles` roll-up leaf [Shared]

- [x] **T001** [Shared] Add the `tension subroles <role-id>` command — parameterized `tensionsConfig.subroles` path swap (no second runner); 19 unit tests (path swap, leaf-404-vs-empty-200, status filter/validation, walk/first-page/mid-walk, errors, structured aggregate, command wiring); reuses 043's `tensions` render + `validateTensionStatus` unchanged (reusing 042's `tensionSeam`, 043's `tensions` render + `validateTensionStatus`) + `--status`/`--first-page`/`--per-page` + completeness + wiring under 042's `tension` group — RED-first unit tests for every branch
  - **Scope**: In `internal/cli/tension_reads.go` (beside `newTensionListCommand`), add `newTensionSubrolesCommand(seam tensionSeam) *cobra.Command`: a guard-registered leaf (`Use:"subroles <role-id>"`, `Args: cobra.ExactArgs(1)`, non-empty `Short`, `SilenceErrors`/`SilenceUsage`), declaring `--status`, `--first-page`, `--per-page`; reads the persistent `--base-url`/`--output`; delegates to a pure runner. **Reuse the landed `tension list` runner shape** — the recommended implementation (plan ADR-3) is to **parameterize the request path** on `runTensionList`/`tensionsConfig` (add a path field or a small `subrolesTensions` flag) so the status-validation, paging, completeness, render, and error logic is single-sourced; if a thin sibling `runTensionSubroles` is chosen instead, it must call the same helpers (no second copy of the page loop, render branch, or `errors.As` chain). The runner: call `resolveRenderTarget(cfg.seam, cfg.outputFlag, cfg.stderr)` → `rt` and **validate `--status` via the landed `validateTensionStatus`** (043) **before** assembly, so a bad template/format or bad status is a fail-fast `UsageError(2)` with no request; build the `status` query parameter only when `Changed()` and non-empty; default path walks **`GET /roles/{role_id}/subroles/tensions`** to completion using the landed two-track list pattern — machine format: walk `paging.All[json.RawMessage]` then `aggregateRawData(machineFmt, res.Records)` into the `{data:[…]}` document; human: walk `paging.All[glassfrog.Tension]` then `writeHuman(cfg.stdout, cfg.stderr, rt.tmpl, render.ResourceTensions, rt.format, view)` over a `render.TensionsView` (043's landed key — reused, NOT a new key). `--first-page` does one `Execute` into the corresponding `Page[…]` and writes a "more tensions exist" stderr note (exit 0) on `HasNextPage`; a mid-walk `Result.Stop` renders the partial set, writes an "incomplete — <cause>" stderr note, and exits non-zero via `classifyClientError(Stop)`; `--per-page` sets `WithPageSize` (presence, not value). The anchor `role_` id is `url.PathEscape`-d and passed through unvalidated (plan ADR-3) so a **leaf anchor's 404** (and an unknown id) surfaces the API's non-2xx via the shared classifier — **add no leaf-404 special case** (no "this role has no sub-roles" message; plan ADR-3). Attach the leaf to 042's `tension` group by extending `newTensionCommand` to `MustRegister` it. Never read `ctx.Cred.Token`.
  - **Acceptance criteria**:
    - `glassfrog tension subroles role_0123` walks every page of `GET /roles/{role_id}/subroles/tensions` (`paging.All`) and prints the projection of all the direct sub-roles' tensions; exits 0
    - Sub-roles that carry no tensions print the `tensions` empty line (`no tensions`) and exit 0
    - A **leaf anchor** (or unknown id) returns `404` → surfaced as APIError(3)/PermissionError(4) naming the HTTP status, non-zero exit, **with no special "no sub-roles" message** — distinct from the zero-exit empty-list success
    - `--status unprocessed` sends `status=unprocessed`; an omitted/empty `--status` sends nothing
    - `--status open` (unsupported) is a `UsageError(2)` naming the value + supported set (`archived`, `processed`, `unprocessed`), **no request sent** (transport tripwire)
    - `--first-page` against a multi-page roll-up prints one page, writes a "more tensions exist" stderr note, exits 0
    - A mid-walk failure prints the partial set, writes an "incomplete — <cause>" stderr note, exits non-zero (classified from `Stop`)
    - `*AuthError{NoCredentials}` → UsageError(2); `*TransportError` → NetworkUnavailable(6); base-URL/`--output` errors → UsageError(2); `-o json`/`yaml` emit the aggregated `{data:[…]}` document, `full`/`compact` the human projection, `-o <template-ref>` a 035 user template
    - No new `Outcome`/`ExitCode`/root flag/render key/validator; no token in any output; all branches run offline; `go build`/`go vet` clean
  - **Dependencies**: None at the spec level — 042's `tension` group + `Tension` model, 043's `tensions` render key + `validateTensionStatus` + `runTensionList` shape, and the shared seams are all landed on main. Startable immediately.
  - **Plan reference**: Phase 1 (Command); ADR-1 (a fifth verb leaf under 042's group, `ExactArgs(1)`), ADR-2 (reuse the `Tension` model, `tensions` render path, and `validateTensionStatus`), ADR-3 (reuse the `tension list` runner with the path swapped; leaf-404 surfaced verbatim)
  - **Interface references**: interface-cli.md — `tension subroles` Surface, list flags, Output, Interactions (validation order + completeness + one-level read), Error Communication (leaf-404 row)
  - **Scenario references**: subroles-tension-roll-up.feature: "A circle's direct sub-roles' tensions are rolled up", "A leaf anchor fails with the API status", "A missing token fails as a not-authenticated usage error", "Sub-roles with no tensions are a clean success", "The roll-up is narrowed by a supported status", "An unsupported status is rejected as a usage error", "The roll-up walks every page to completion", "The first-page opt-out stops at one page and signals more", "A mid-walk failure yields a partial set flagged incomplete"
  - **Risk**: ⚠️ Reuse 043's landed `tensions` render key, `validateTensionStatus`, and the `tension list` runner — add **no** new render key, validator, or second copy of the page loop / render branch / `errors.As` chain (parameterize the path, per plan ADR-3). ⚠️ Reuse 042's existing `tensionSeam` — do not define a parallel seam. ⚠️ **No leaf-404 special-casing** — surface the `404` through the shared classifier; keep it distinct from the empty-`200` success (both pinned by tests). ⚠️ The roll-up is **one level only** — issue exactly the one paginated read; never recurse into grand-children. ⚠️ Structured output is the aggregated `{data:[…]}` over `Page[json.RawMessage]`, NOT a decode-and-re-encode of `[]Tension`. ⚠️ `--status` sent only when `Changed()` and non-empty. ⚠️ Resolve the render target and validate `--status` BEFORE assembly so the tripwire confirms no request on rejection. ⚠️ Never silently truncate — every incomplete path writes an explicit stderr note (CONSTITUTION VI). ⚠️ Capture stdout/stderr in tests with a temp file, not `os.Pipe` (LEARNINGS). ⚠️ Attach to 042's group by extending `newTensionCommand` — don't create a second `tension` group. ⚠️ Never read `ctx.Cred.Token`.

## Phase 2: Executable acceptance [Shared]

- [x] **T002** [Shared] Make the driving scenarios pass as executable acceptance — new `TestSubrolesTensionRollUpFeatures` suite (Paths = subroles-tension-roll-up.feature only); 9 behavioral scenarios un-@wip'd & passing (51 steps), 4 @validation held @wip; reused tension-reads step phrasings, new bindings for subroles endpoint/leaf-404/multi-page/no-interpretation; leaf-404 (cannedTransport 404) vs empty-200 (tensionsPageEmpty) exercise distinct fakes — new `internal/cli` godog suite over `subroles-tension-roll-up.feature`; un-`@wip` the behavioral scenarios, keep `@validation` held
  - **Scope**: Add godog step definitions for `features/tension-capture/subroles-tension-roll-up.feature` in a **new** `internal/cli` godog suite (e.g. `TestSubrolesTensionRollUpFeatures`) whose `Paths` names **only** that feature file (LEARNINGS: a suite points at its own file, never the `features/` directory). Drive `tension subroles` through the shared seam over a fake base `http.RoundTripper` returning canned `GET /roles/{role_id}/subroles/tensions` responses (single-page, multi-page, mid-walk error, empty-`200`, status-filtered) and a **leaf-anchor `404`**, plus a transport tripwire for the no-request path (unsupported `--status`). Remove `@wip` from the spec-derived + architecture-informed behavioral scenarios; keep the 4 `@validation` scenarios (`The roll-up reads only the direct sub-roles`, `A leaf 404 is distinct from an empty roll-up`, `A rejected status issues no request`, `Default output carries no raw API envelope`) `@wip` (held for validate). **Reuse the sibling-suite step phrasings** (godog matches by text) from the `tension-capture`/`tension-reads`/`tension-update` suites before writing new bindings — the connection-context, no-credential, exit-code, stderr-substring, status-rejection, tripwire, first-page, and mid-walk steps already exist; add new bindings only for the subroles-specific steps (direct-sub-roles setup, subroles tensions endpoint, leaf-anchor 404, one-level read assertion). Step helpers return errors, never panic; never hard-code the validator's supported-set order (it is alphabetically sorted).
  - **Acceptance criteria**:
    - Every non-`@validation` subroles-roll-up scenario has an executable, passing path; `@wip` removed from them
    - The 4 `@validation` scenarios keep `@wip`
    - The new suite's `Paths` names only `subroles-tension-roll-up.feature`; all `internal/cli` godog suites run and report their own independent scenario counts
    - The leaf-`404` scenario and the empty-`200` scenario exercise genuinely different fakes and assert different outcomes (non-zero failure vs zero-exit empty)
    - No real network (fake base transport / loopback only) and no real home/filesystem are touched; `go build ./...`, `go vet ./...`, and the feature suites run clean
  - **Dependencies**: T001 (the `tension subroles` command) — all behavioral scenarios must be implementable.
  - **Plan reference**: Phase 2 (BDD); System Architecture (attach to 042's group); Cross-cutting (testing)
  - **Interface references**: interface-cli.md — Surface, Interactions, Error Communication
  - **Scenario references**: subroles-tension-roll-up.feature: all behavioral Rule-block scenarios (the 4 `@validation` scenarios stay held for validate)
  - **Risk**: ⚠️ Suite scoping — point the suite at `subroles-tension-roll-up.feature` only (not the directory); verify it reports its own count. ⚠️ Reuse shared step phrasings before writing new bindings; step helpers return errors, never panic (LEARNINGS). ⚠️ Cover the leaf-`404`, empty-`200`, multi-page, mid-walk-error, status-filtered, and no-request (tripwire) fakes so the failure-vs-empty, completeness, filter, and rejection scenarios genuinely exercise their paths. ⚠️ Never hard-code the alphabetically-sorted supported-set order in any step assertion.
