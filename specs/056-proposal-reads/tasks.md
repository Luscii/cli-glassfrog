# Tasks: Proposal Reads

**Feature**: 056-proposal-reads
**Concretization**: Full context (plan + spec + interface-cli + scenarios)
**Inputs**: plan.md, spec.md, interface-cli.md, features/proposal-write-flow/proposal-reads.feature

---

## Dependency Graph

Phase 1: `internal/glassfrog` `Proposal` model (1 task, no dependencies) [Shared]
Phase 2: `internal/render` `proposals` + `proposal` keys (1 task, depends on Phase 1) [Shared]
Phase 3: `validateProposalStatus` + proposal status set (1 task, no dependencies) [US3]
Phase 4: The `proposal` group + two read leaves (2 tasks: T004 group+list depends on Phases 1+2+3; T005 get depends on T004 scaffold + Phases 1+2) [US1/US3/US4 + US2]
Phase 5: Executable acceptance (1 task, depends on Phase 4) [Shared]

6 tasks total | T001 and T003 startable immediately (parallel) | Builder: pipeline (role-based awareness: 055 concurrent)

> Plan-faithful: the plan's five ordered steps map here as model (T001), render both keys (T002), validator (T003), commands group+list / get (T004 / T005), and BDD (T006). Unlike Tension Reads (043), **no proposal code exists** — 056 (as cut from current `main`) creates the `proposal` group, the `glassfrog.Proposal` model, and **both** render keys (plural `proposals` + singular `proposal`), all shared with the concurrently-specified Proposal Creation (055). The plan instantiates the landed generics `Page[Proposal]` (016) and `Document[Proposal]` (011/034) over the new model. Story labels follow the spec's four user scenarios — US1 (see which proposals are in flight / list), US2 (read one proposal's detail by id), US3 (track my own proposals — status/proposer/date filters), US4 (trust the list is whole). T004 serves US1/US3/US4 → `[Shared]`; T005 is cleanly `[US2]`; T003 is the status-filter machinery → `[US3]`.
>
> **⚠️ Concurrent sibling: Proposal Creation (055).** 055 and 056 both introduce the `proposal` group (`newProposalCommand`), the `glassfrog.Proposal` model, and the singular `proposal` render key. Design contract (plan ADR-1/ADR-2, Risks): **first-to-land creates, follower reuses**. As cut from current `main` (no proposal code present), 056 creates all three. **If 055 merges first**, rebase and: (a) attach 056's leaves to the existing group instead of creating it (T004); (b) reuse 055's `Proposal` model — **grow** it (never a second type) if 055's was thinner (T001); (c) reuse 055's singular `proposal` render key — **grow** it to surface `changes`/`response_summary`/`available_transitions` if 055's was thinner (T002). Structured `json`/`yaml` output is faithful regardless of model field coverage (`aggregateRawData`/`output.RenderSuccess` pass raw server bytes through), bounding any conflict to human render + cobra wiring.
>
> **All cross-spec seams are landed on main.** 007/009/010/011/015/016/017/018/020/025/031/035 are Complete with their packages shipped — `Page[T]`/`paging.All`, the generic `Document[T]`, `RetryExecutor`, `classifyClientError`/`Diagnose`/`Outcome`/`ExitCode` (011/015/031), the persistent `--base-url`/`--output`/`-o`, and the 035-widened render flow (`resolveRenderTarget` → `writeHuman` (human) / `aggregateRawData` (list) / `output.RenderSuccess` (single)). 056 adds **no** new `Outcome` category, `ExitCode` case, generic type, or root flag. **`validateProposalStatus` lives with the proposal command code (beside the `validateTensionStatus`/`validateMeetingType` precedent), NOT in the shared `status.go`.**
>
> **The proposal read surface — read half of the Governance Proposals write flow.** `proposal create` writes (055); 056 reads `GET /proposals` (global list) + `GET /proposals/{id}` (get) as verb leaves under a `proposal` group — the structural twin of Tension Reads (043), with the list global (`cobra.NoArgs`) rather than role-scoped (plan ADR-1). Proposal reads are **not Premium-gated** — no plan-gate `403` is expected or special-cased.

---

## Branching Guidance

**Pipeline mode**: `spec/056-proposal-reads/base` → `spec/056-proposal-reads/task-1`, `…/task-2`, … (one task branch per T-id, merged back into the spec base).

**Base branch**: no proposal code exists on `main`, so cut the 056 base from current `main`. The group, model, and both render keys are created here.

**Role-based awareness**: parallel Conductor workspaces carry sibling proposal specs — chiefly **Proposal Creation (055)**, which shares the `proposal` group, `Proposal` model, and singular `proposal` render key. Whichever of 055/056 lands first creates those three; the follower rebases and attaches/grows (never duplicates) — see the ⚠️ note in the dependency graph. 056 touches `internal/glassfrog` (new `proposal.go`), `internal/render` (two new keys), and `internal/cli` (new proposal command file + wiring); it makes no change to any existing model, render key, or the shared `status.go`.

---

## Phase 1: `internal/glassfrog` `Proposal` model [Shared]

- [x] **T001** [Shared] [P] Add the `glassfrog.Proposal` model + free-form `ProposalChange` + `ResponseSummary` — decode unit tests; `internal/glassfrog/proposal.go` — ~6 unit tests (full decode, nullable→empty, free-form change keys, `Document[Proposal]`, `Page[Proposal]`, anti-attribution shape) — **055 landed first and created the full model**; as follower added the two missing coverage tests (`Page[Proposal]` decode + reflect-based anti-attribution shape guard)
  - **Scope**: New `internal/glassfrog/proposal.go`. Add `Proposal` (forward-compatible decoding, the landed `Tension`/`Project` shape) with the v5 `Proposal` schema fields: `ID`, `Type`, `Status`, `TensionID`, `CircleID`, `ProposerID` (nullable → empty string), `ProposedAt`, `ResponseDeadline`, `AcceptedAt` (nullable timestamps), `Changes []ProposalChange`, `ResponseSummary`, `ExpectedResponseCount`, `ReceivedResponseCount`, `AvailableTransitions []string`, `CreatedAt`, `UpdatedAt`. Add `ProposalChange` keeping `ID` + `Type` and a **free-form remainder** for the `additionalProperties:true` per-type keys (the landed `actions.go` `Permissions map[string]any` precedent) — so changes decode without a per-type schema and are never interpreted (spec non-behavior). Add `ResponseSummary` with `Total`, `NoObjection`, `BringToMeeting` ints **only** — no per-person field exists, enforcing the anti-attribution non-behavior at the type level. The model is decoded via the existing generics (`Document[Proposal]`, `Page[Proposal]`) — add no new envelope. Depends on stdlib only; must not import `cli`/`apiclient`/`render`.
  - **Acceptance criteria**:
    - A full `Proposal` JSON body decodes every field; unknown/extra fields are tolerated (forward-compatible)
    - Nullable fields (`tension_id`/`circle_id`/`proposer_id`/`proposed_at`/`response_deadline`/`accepted_at`) decode to empty strings when `null` or absent
    - A `changes[]` entry with arbitrary per-type keys preserves `id`/`type` and retains the remaining keys (free-form remainder); a round-trip does not drop unknown change keys
    - `ResponseSummary` exposes only the three aggregate counts — no field carries per-person attribution
    - `Document[Proposal]` (single) and `Page[Proposal]` (list element) instantiate and decode; `go build`/`go vet` clean; no import of `cli`/`apiclient`/`render`
  - **Dependencies**: None — a new leaf model file. Startable immediately against current main.
  - **Plan reference**: Phase 1 (Model); ADR-2 (establish `Proposal` + free-form `ProposalChange` + aggregate-only `ResponseSummary`); Data Model Design
  - **Interface references**: interface-cli.md — Output (single/list field shapes), Consistency Notes (new model + both render keys)
  - **Scenario references**: proposal-reads.feature: "A single proposal is read with full detail", "A read proposal shows only aggregate response counts"
  - **Risk**: ⚠️ `ProposalChange` MUST keep a free-form remainder (`map[string]any`, the `actions.go` precedent) — a typed-only struct drops per-type keys (plan ADR-2; spec non-behavior: never interpret changes). ⚠️ `ResponseSummary` carries counts only — never add a per-person field (anti-attribution non-behavior). ⚠️ Nullable fields decode to empty, never panic. ⚠️ **055 coordination**: if 055 already added `proposal.go`, GROW the existing type, do not add a second `Proposal`/`ProposalChange`.

## Phase 2: `internal/render` `proposals` + `proposal` keys [Shared]

- [x] **T002** [Shared] Add both the plural `proposals` and singular `proposal` render keys + views + templates — golden + registry-guard tests; `internal/render` — ~8 golden tests (`proposals.{full,compact}`, `proposal.{full,compact}`, empty-set line, multi-change proposal, response summary) — **055 created the singular `proposal` key**; as follower GREW `proposal.full`/`.compact` to the 056 detail block (changes by type via a `changeProps` helper, proposed/deadline/accepted, expected/recv, response total) and ADDED the plural `proposals` key (`ProposalsView` + `ResourceProposals` + both templates in `builtinResources`); updated 055's render goldens
  - **Scope**: In `internal/render`, add **two** render keys (the landed `ResourceProjects`/`ResourceProject` plural/singular shape): the plural `proposals` (list, data `[]glassfrog.Proposal` via a `ProposalsView` mirroring `ProjectsView`) and the singular `proposal` (single, `ProposalView{Proposal}`). Add `ResourceProposals` and `ResourceProposal` to `builtinResources` so the registry-exhaustiveness guard covers both (PR #10 `len`+comma-ok shape). Add four `//go:embed` templates per the interface Output shapes: `proposals.full` (one block per proposal — `<prp_…>  [<status>]`, indented `proposer`, `changes: <N>`, one-line `responses:` summary) and `proposals.compact` (one line — `<prp_…>  [<status>]  <N changes>`), with the empty-set line `no proposals`; `proposal.full` (the full single-proposal detail block — tension/circle/proposer/timestamps via explicit-absence guards, `responses` aggregate counts, `expected/received`, `transitions`, then `changes (<N>)` each rendered **verbatim — never truncated or reflowed**, CONSTITUTION VI) and `proposal.compact` (`<prp_…>  [<status>]  <N changes>  <total> responses`). Use 019's absence-guard discipline for every nullable field. Must not import `cli`/`apiclient`.
  - **Acceptance criteria**:
    - The `proposals` key renders `full`/`compact`; an empty slice renders `no proposals` (not `<no value>`, not blank)
    - The `proposal` key renders `full`/`compact`; `full` shows status, the anchoring ids, lifecycle timestamps, the aggregate response counts, expected/received counts, available transitions, and each change by type
    - Nullable fields (`tension_id`/`circle_id`/`proposer_id`/timestamps) render explicit-absence markers; change bodies render verbatim (neither truncated nor reflowed)
    - Only aggregate response counts appear in any template — no per-person attribution
    - The registry-exhaustiveness guard passes with both new keys carrying both formats; golden tests pin each template; `internal/render` imports neither `cli` nor `apiclient`; `go build`/`go vet` clean
  - **Dependencies**: T001 (`glassfrog.Proposal` — the views reference it).
  - **Plan reference**: Phase 2 (Render); ADR-2 (add both render keys)
  - **Interface references**: interface-cli.md — Output (list `proposals`, single `proposal` shapes), Consistency Notes (new model + both render keys)
  - **Scenario references**: proposal-reads.feature: "The visible proposals are listed", "An empty visible set is a clean success", "A single proposal is read with full detail", "A read proposal shows only aggregate response counts"
  - **Risk**: ⚠️ Add BOTH keys — plural for the list, singular for the single read (unlike 043, which reused a landed singular). ⚠️ Never truncate/reflow change bodies (CONSTITUTION VI). ⚠️ Explicit-absence guards for every nullable; never invent a value. ⚠️ Add both `ResourceProposals` and `ResourceProposal` to the guarded `builtinResources` set so exhaustiveness still holds. ⚠️ Render only aggregate response counts — no per-person field. ⚠️ **055 coordination**: if 055 already added the singular `proposal` key, GROW its template to carry changes/response_summary/available_transitions; do not add a second singular key.

## Phase 3: `validateProposalStatus` + proposal status set [US3]

- [x] **T003** [US3] [P] Add `validateProposalStatus` + the proposal status set in the proposal command file — unit tests; a NEW set distinct from `validateStatus`/`validateTensionStatus` — in new proposal command file, ~3 unit tests — added to `internal/cli/proposal_reads.go` (with the proposal code, NOT shared `status.go`); single-sourced `supportedProposalStatuses` map incl. `draft_with_conflicts` + sorted `supportedProposalStatusNames()`; 3 unit tests (supported+empty pass, unsupported names value+set, `draft_with_conflicts` accepted)
  - **Scope**: In the proposal command file (`internal/cli/proposal.go` or `proposal_reads.go` — **proposal-domain validators live with the proposal code, NOT in the shared `status.go`**, per the 042/043 precedent), add a pure `validateProposalStatus(s string) error` mirroring the landed `validateTensionStatus`/`validateMeetingType` shape, over a **new** proposal status set (`draft`, `proposed_outside_meeting`, `escalated`, `accepted`, `draft_with_conflicts`) in a single-sourced `supportedProposalStatuses` map + a sorted `supportedProposalStatusNames()` helper. An empty string passes (no filter); an unsupported non-empty value returns an error naming the value and the sorted supported set. **Do not reuse `validateStatus` or `validateTensionStatus`** — their vocabularies are wrong for proposals (plan ADR-3). Pure, no I/O.
  - **Acceptance criteria**:
    - `validateProposalStatus` returns nil for each of `draft`, `proposed_outside_meeting`, `escalated`, `accepted`, `draft_with_conflicts`, and for `""`
    - `validateProposalStatus("open")` returns an error naming the unsupported value and listing the supported set (sorted)
    - `draft_with_conflicts` is in the set (the value the FEATURE-MODEL prose omits)
    - The set is single-sourced (one map/var); adding a status is a one-line change
    - The shared `status.go` and the tension validators are untouched; `go build`/`go vet` clean; unit tests pin supported + rejected cases incl. `draft_with_conflicts`
  - **Dependencies**: None — a pure new validator in the proposal command code. Startable immediately against current main.
  - **Plan reference**: Phase 3 (Validator); ADR-3 (validate the closed-enum `--status` locally via a new validator; pass the other filters through)
  - **Interface references**: interface-cli.md — `--status` flag (validated set), Consistency Notes (one local validator, a new set)
  - **Scenario references**: proposal-reads.feature: "An unsupported status value is rejected before any request", "A rejected status filter sends nothing over the wire"
  - **Risk**: ⚠️ A new set, NOT a reuse of `validateStatus`/`validateTensionStatus` — reusing either accepts invalid proposal statuses and rejects valid ones (plan ADR-3). ⚠️ Include `draft_with_conflicts` (easily missed against the FEATURE-MODEL prose). ⚠️ Empty string must pass (it means "no filter"). ⚠️ Keep the set single-sourced. ⚠️ Place it with the proposal code, NOT in the shared `status.go`. ⚠️ The four other filters (`--role-id`/`--proposer-id`/`--proposed-after`/`--accepted-after`) get NO local validation — they pass through (plan ADR-3).

## Phase 4: The `proposal` group + two read leaves [US1/US3/US4 + US2]

- [x] **T004** [Shared] Create the `proposal` group (`newProposalCommand`) + the `proposal list` command (global, five filters, completeness) + wiring — RED-first unit tests for every branch — runProposalList + newProposalCommand in proposal command file, wired via Assemble; ~18 unit tests (walk/empty/each-filter/first-page/per-page/mid-walk/classify/bad-output/structured/no-positional) — **the group already exists (055)**; attached the `list` leaf (`cobra.NoArgs`, global) to it via `newProposalCommand`; `runProposalList`/walk/first-page/incomplete-note/query-builder in `proposal_reads.go` follow `runTensionList` verbatim (035-widened); reused the existing `proposalSeam`; unit tests cover walk/empty/each-filter/unsupported-status-tripwire/bad-output/no-creds/structured/first-page/mid-walk/no-positional
  - **Scope**: New `internal/cli/proposal.go` (group + seam) and `proposal_reads.go` (list runner), or a single proposal command file. Add `newProposalCommand(seam proposalSeam) *cobra.Command`: a guard-registered, non-runnable `proposal` group (the `newTensionCommand` shape — carries no action, prints help), wired into `Assemble`/`main`. Define a `proposalSeam` (the `tensionSeam`/`projectsSeam` shape — `assemble`/`newClient`/`sleep`/`resolveSelection`/`readTemplateSource`; paging via `paging.All` in the runner, not a seam method); prod passes `productionSeam{}`, tests bind a fake. Add `newProposalListCommand(seam)`: a guard-registered leaf (`Use:"list"`, `Args: cobra.NoArgs` — **global, no positional**, plan ADR-1), declaring `--status`, `--role-id`, `--proposer-id`, `--proposed-after`, `--accepted-after`, `--first-page`, `--per-page`; reads the persistent `--base-url`/`--output`; delegates to pure `runProposalList`. `runProposalList(cfg)` follows `runProjectsList`/043's `runTensionList` verbatim (035-widened): `resolveRenderTarget(...)` → `rt` and **validate `--status` via `validateProposalStatus`** (T003) **before** assembly (fail-fast `UsageError(2)`, no request); build each query parameter (`status`/`role_id`/`proposer_id`/`proposed_after`/`accepted_after`) only when `Changed()` and non-empty (the four non-status filters pass through unvalidated — plan ADR-3); default path walks `GET /proposals` to completion using the two-track list pattern — machine: walk `paging.All[json.RawMessage]` then `aggregateRawData` into `{data:[…]}`; human: walk `paging.All[glassfrog.Proposal]` then `writeHuman(..., render.ResourceProposals, rt.format, ProposalsView)` (T002). `--first-page` does one `Execute` and writes a "more proposals exist" stderr note (exit 0) when `HasNextPage`; a mid-walk `Result.Stop` renders the partial set, writes an "incomplete — <cause>" stderr note, exits non-zero via `classifyClientError`; `--per-page` sets `WithPageSize`. Attach the leaf to the group via `MustRegister`. Never read `ctx.Cred.Token`.
  - **Acceptance criteria**:
    - `glassfrog proposal list` walks every page (`paging.All`) and prints the projection of all visible proposals; exits 0
    - No visible proposals prints `no proposals` and exits 0
    - A positional argument to `proposal list` is a `UsageError(2)` (cobra `NoArgs`), no request sent
    - Each of `--status`/`--role-id`/`--proposer-id`/`--proposed-after`/`--accepted-after` is sent as its query parameter only when supplied; supplied filters combine; omitted ones send nothing
    - `--status open` (unsupported) is a `UsageError(2)` naming the value + supported set, **no request sent** (transport tripwire); the four other filters are not validated locally
    - `--first-page` against a multi-page list prints one page, writes a "more proposals exist" stderr note, exits 0; a mid-walk failure prints the partial set, writes an "incomplete — <cause>" note, exits non-zero
    - `*AuthError{NoCredentials}` → UsageError(2); `*TransportError` → NetworkUnavailable(6); `*ResponseError` → APIError(3)/PermissionError(4)/RateLimited(5); base-URL/`--output` errors → UsageError(2); `-o json`/`yaml` emit the aggregated `{data:[…]}` document, `full`/`compact` the human projection
    - No new `Outcome`/`ExitCode`/root flag; no token in any output; all branches run offline; `go build`/`go vet` clean
  - **Dependencies**: T001 (`Proposal` model), T002 (`proposals` render key), T003 (`validateProposalStatus`).
  - **Plan reference**: Phase 4 (Commands); ADR-1 (create the group; `proposal list` global `NoArgs`; list-only flags), ADR-3 (`--status` validated locally, other filters + id passed through); Cross-cutting (completeness reuses 025 ADR-3)
  - **Interface references**: interface-cli.md — `proposal` group + `proposal list` Surface, list flags, Output, Interactions (validation order + completeness), Error Communication
  - **Scenario references**: proposal-reads.feature: "The visible proposals are listed", "A circle-and-status narrowed list is requested", "An empty visible set is a clean success", "A missing token fails as a not-authenticated usage error", "Default output carries no raw API envelope", "An invalid output format is rejected before any list request", "The list is narrowed to the caller's own proposals", "An unsupported status value is rejected before any request", "A rejected status filter sends nothing over the wire", "The first-page opt-out signals more proposals exist"
  - **Risk**: ⚠️ `proposal list` is `cobra.NoArgs` (global) — a positional is a usage error; the circle is the `--role-id` filter (plan ADR-1). ⚠️ Reuse `validateProposalStatus`(T003)/`classifyClientError`/`resolveRenderTarget`+`aggregateRawData`+`writeHuman`/`paging.All`/`RetryExecutor` — inline no second copy of the page loop, render branch, or `errors.As` chain (follow `runProjectsList`/043's `runTensionList` verbatim). ⚠️ Structured output is the aggregated `{data:[…]}` over `Page[json.RawMessage]`, NOT a decode-and-re-encode of `[]Proposal`. ⚠️ Each filter sent only when `Changed()` and non-empty; the four non-status filters pass through (no local validation). ⚠️ Resolve render target + validate `--status` BEFORE assembly so the tripwire confirms no request on rejection. ⚠️ Never silently truncate — every incomplete path writes an explicit stderr note (CONSTITUTION VI). ⚠️ Capture stdout/stderr in tests with a temp file, not `os.Pipe` (LEARNINGS). ⚠️ **055 coordination**: if the `proposal` group already exists (055 landed), attach the `list` leaf to it — do not create a second group. ⚠️ Never read `ctx.Cred.Token`.

- [x] **T005** [US2] Add the `proposal get <prp-id>` single-read command — `runProposalGet` + `Document[Proposal]` decode + the singular `proposal` render dispatch — RED-first unit tests — runProposalGet in proposal command file, wired via newProposalCommand; ~6 unit tests (detail/unknown-id/structured/list-flag tripwire ×7 flags/ExactArgs)
  - **Scope**: In the proposal command file, add `newProposalGetCommand(seam proposalSeam) *cobra.Command`: a guard-registered leaf (`Use:"get <prp-id>"`, `Args: cobra.ExactArgs(1)`, non-empty `Short`, `SilenceErrors`/`SilenceUsage`), declaring **no** list flags (so `--status`/`--role-id`/`--proposer-id`/`--proposed-after`/`--accepted-after`/`--first-page`/`--per-page` are cobra unknown-flag usage errors — the structural list-only guard, plan ADR-1); reads the persistent `--base-url`/`--output`; delegates to pure `runProposalGet`. `runProposalGet(cfg, id)` follows `runProjectGet`/043's `runTensionGet` verbatim (035-widened): `resolveRenderTarget(...)` before assembly; the id `url.PathEscape`-d and passed through unvalidated (plan ADR-3) so an unknown/invisible id surfaces the API's non-2xx via the shared classifier. Machine format: `Execute` into a raw `json.RawMessage` and emit the `{data: Proposal}` body verbatim via `output.RenderSuccess` (018). Human path: `Execute` into `glassfrog.Document[glassfrog.Proposal]` and render via `writeHuman(..., render.ResourceProposal, rt.format, render.ProposalView{Proposal: doc.Data})` — the **singular `proposal` render key** (T002), surfacing changes, the aggregate response summary, and available transitions (the latter printed, never invoked — spec non-behavior). Attach the leaf to the group via `MustRegister`. Never read `ctx.Cred.Token`.
  - **Acceptance criteria**:
    - `glassfrog proposal get prp_0123` reads the proposal and prints its status, changes, aggregate response summary, and available transitions; exits 0
    - An unknown/invisible `prp_ffff` surfaces the API status (APIError(3)/PermissionError(4)) — the id is not validated locally
    - Any list flag on `get` is a `UsageError(2)` via cobra's unknown-flag handling, **no request sent** (transport tripwire across all seven flags)
    - `-o json`/`yaml` emit the raw single-proposal payload (faithful incl. all change keys); `full`/`compact` render the singular `proposal` human projection; `-o <template-ref>` renders through a 035 user template
    - available transitions are printed but never invoked; only aggregate response counts appear (no per-person attribution)
    - No new `Outcome`/`ExitCode`/root flag; no token in any output; all branches run offline; `go build`/`go vet` clean
  - **Dependencies**: T004 (the group scaffold + `proposalSeam` reference); T001 (`Proposal` model + `Document[Proposal]`), T002 (singular `proposal` render key).
  - **Plan reference**: Phase 4 (Commands); ADR-1 (verb leaf, list-only-ness structural), ADR-3 (id pass-through), ADR-2 (reuse `Document[Proposal]` + the singular render)
  - **Interface references**: interface-cli.md — `proposal get` Surface, Output (single), Error Communication
  - **Scenario references**: proposal-reads.feature: "A single proposal is read with full detail", "An unknown proposal id fails with the API status", "A list filter on the single read is a usage error", "A read proposal shows only aggregate response counts", "The read surface surfaces transitions without invoking them"
  - **Risk**: ⚠️ Declare no list flags on `get` — that's how list-only-ness is enforced (no hand-rolled cross-combo guard; plan ADR-1); tripwire-test all seven. ⚠️ Pass the id through (no regex gate); the API 404s cleanly (plan ADR-3). ⚠️ Reuse the singular `proposal` render key (T002) — do not add a second singular key. ⚠️ Surface available transitions but NEVER invoke them; render only aggregate response counts (spec non-behaviors). ⚠️ Never read `ctx.Cred.Token`.
  - **Done**: `newProposalGetCommand` (`cobra.ExactArgs(1)`, no list flags) attached to the group; `runProposalGet` follows `runTensionGet` verbatim — `Document[Proposal]` decode → the singular `proposal` render key; id `url.PathEscape`-d, passed through. 6 unit tests (full detail/unknown-id/structured raw payload/list-flag tripwire ×7 flags/ExactArgs zero+two).

## Phase 5: Executable acceptance [Shared]

- [x] **T006** [Shared] Make the driving scenarios pass as executable acceptance — new `internal/cli` godog suite over `proposal-reads.feature`; un-`@wip` the behavioral scenarios, keep `@validation` held — TestProposalReadsFeatures (Paths → proposal-reads.feature only); 11 behavioral scenarios pass, 4 @validation held @wip — `TestProposalReadsFeatures` (Paths names only `proposal-reads.feature`); 11 behavioral scenarios pass (57 steps), 4 `@validation` kept `@wip`; reused shared exit-code/stderr/projection/query-param phrasings; step helpers return errors, never panic
  - **Scope**: Add godog step definitions for `features/proposal-write-flow/proposal-reads.feature` in a **new** `internal/cli` godog suite (e.g. `TestProposalReadsFeatures`) whose `Paths` names **only** that feature file (LEARNINGS: a suite points at its own file, never the `features/` directory). Drive `proposal list`/`proposal get` through the shared seam over a fake base `http.RoundTripper` returning canned `GET /proposals` (single-page, multi-page, mid-walk error, empty, filtered) and `GET /proposals/{id}` (found with changes/response_summary/available_transitions, 404) responses, plus a transport tripwire for the no-request paths (unsupported `--status`; list flag on `get`; positional on `list`). Remove `@wip` from the spec-derived + proposed behavioral scenarios; keep the 4 `@validation` scenarios `@wip` (held for validate). Grep existing `sc.Step(` registrations and reuse shared exit-code / stderr-substring / projection / query-parameter phrasings from the `me*`/`roles`/`projects`/`tension-reads` suites before writing new bindings; step helpers return errors, never panic.
  - **Acceptance criteria**:
    - Every non-`@validation` proposal-reads scenario has an executable, passing path; `@wip` removed from them
    - The 4 `@validation` scenarios keep `@wip`
    - The new suite's `Paths` names only `proposal-reads.feature`; all `internal/cli` godog suites run and report their own independent scenario counts
    - No real network (fake base transport / loopback only) and no real home/filesystem are touched; `go build ./...`, `go vet ./...`, and the feature suites run clean
  - **Dependencies**: T004 (`proposal list`), T005 (`proposal get`) — all behavioral scenarios must be implementable
  - **Plan reference**: Phase 5 (BDD); System Architecture; Cross-cutting (testing)
  - **Interface references**: interface-cli.md — Surface, Interactions, Error Communication
  - **Scenario references**: proposal-reads.feature: all behavioral Rule-block scenarios (the 4 `@validation` scenarios stay held for validate)
  - **Risk**: ⚠️ Suite scoping — point the suite at `proposal-reads.feature` only (not the directory); verify it reports its own count. ⚠️ Reuse shared step phrasings before writing new bindings; step helpers return errors, never panic (LEARNINGS). ⚠️ Cover the mid-walk-error, multi-page, empty-list, filtered, and no-request (tripwire) fakes so the completeness, empty, filter, and rejection scenarios genuinely exercise their paths. ⚠️ The single-read fake must carry `changes`/`response_summary`/`available_transitions` so the detail and anti-attribution scenarios are real.
