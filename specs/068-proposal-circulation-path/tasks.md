# Tasks: Proposal Circulation Path

**Feature**: 068-proposal-circulation-path
**Concretization**: Full context (plan + spec + interface + scenarios) — tasks carry scenario and interface references
**Inputs**: plan.md (single phase; ADR-1 both-form, ADR-2 new agent, ADR-3 both gated bodyless transitions in-subagent with independent confirmations, ADR-4 pure composition + reads-inform-never-gate, ADR-5 drift guard + two-write gated-membership invariant), spec.md (3 user scenarios), interface-spec.md, `features/unequipped-agent-operators/proposal-circulation-path.feature` (17 scenarios)

---

## Dependency Graph

Phase 1: Proposal-circulation path artifacts + drift guard (2 tasks, no phase dependencies; intra-phase: T002 depends on T001) [Shared]

2 tasks total | 0 phases parallelizable | Builder: pipeline (single active spec)

The plan is a single phase (ADR-1/2/3 land the two artifacts together; ADR-5's guard pins the leaves they name and the two-write gated-membership invariant). The two PR-sized units follow the plan's own suggested split: artifacts + registration first, drift guard second — the sibling-standard shape (065/066/067).

---

## Branching Guidance

**Pipeline mode**: `spec/068-proposal-circulation-path/base` → `spec/068-proposal-circulation-path/task-1`, `spec/068-proposal-circulation-path/task-2`

T002 depends on T001 (the guard pins the command leaves the artifacts name and asserts `proposal propose`'s and `proposal withdraw`'s membership in 063's gated set), so the two task branches land in sequence onto the spec base.

---

## Phase 1: Proposal-circulation path artifacts + drift guard [Shared]

- [x] **T001** [Shared] Author the proposal-circulation skill and the gated-transition proposal-circulator agent, single-source the composed-leaf list, and register the agent — skill + agent + commands.txt covering 16 of the feature's 17 BDD scenarios (T002 owns the 17th, the no-invented-surface drift check); auto-discovered from plugin/agents/, no plugin.json change — 16 scenarios passing (@wip removed); the two-write gate anchor (`ProposalCirculationGatedWrites`) landed with the BDD helpers in `internal/build/proposalcirculation.go` because the guardrail validation scenario consumes it; T002's guard cross-checks the same anchor
  - **Scope**: Add `plugin/skills/proposal-circulation/SKILL.md` (the thin, discoverable entry point: when to reach for it, the single-sourced workflow, the delegation-to-the-agent instruction, and the gated-writes note) and `plugin/agents/proposal-circulator.md` (the executor: identity/scope, the workflow it runs — *referencing* the skill's workflow, not restating a divergent copy — the confirmation contract, the composed leaves, and the circulation-record output contract). Single-source the composed-leaf list in `plugin/agents/proposal-circulation-commands.txt`. The agent is discovered by directory convention from `plugin/agents/` — **do not** add an `agents` key to `plugin/.claude-plugin/plugin.json` (the 063/064/065/066/067 auto-discovery convention; the manifest stays unchanged). Hand-authored, committed content; adds no CLI code. **Do not touch** 067's `proposal-drafter.md` or its leaf list — ADR-2 mints a new agent precisely so 067's validate-pinned no-propose/respond/withdraw fence stays intact.
  - **Acceptance criteria**:
    - `plugin/skills/proposal-circulation/SKILL.md` exists with YAML frontmatter `name` (`proposal-circulation`) + `description`; the `description` states *when* to reach for it (a draft proposal ready to enter circulation; a circulating proposal to monitor for progress toward acceptance; a circulating proposal to withdraw back to draft for amendment — both transitions confirmed gated writes), worded not to fire on "assemble changes / create the draft" (067), "record a no-objection / bring-to-meeting response" (069), "capture/refine/retire a tension" (066), "am I allowed to do X" (065), or "understand the governance around a concern" (064)
    - The skill body carries the when / workflow (`prp_` id + intent → ground via `proposal get <prp-id>` (status, `response_summary`, `response_deadline`, `available_transitions`) → where relevant, situate via `proposal list` (full walk) → by intent: advance via gated `proposal propose <prp-id>`, monitor without writing, or withdraw via gated `proposal withdraw <prp-id>` → return the circulation record; after a withdraw hand the `prp_` id back to 067, consent responses to 069) / delegation / gated-writes note, pointing at `glassfrog proposal <sub> --help` for per-command detail and at the orientation skill (062) for output/pagination/exit-code mechanics rather than restating them
    - The workflow states that a relevant in-flight walk pages through the **full** result set (Constitution VI: never a silent single-page cap), and that the reads **inform, never gate** — the `available_transitions` snapshot is narration, never a client-side precondition; the transition is issued and the server's `422` surfaced plainly (the 057/059 no-pre-gate invariant)
    - The gated-writes note states that `proposal propose` and `proposal withdraw` are governance writes gated by 063's confirmed write flow; the transitions are **bodyless**, so the confirmed command line is the complete payload; each transition confirms **independently** (never batched or pre-authorized); a declined confirmation means no transition; the path's only writes are these two — never `proposal create` (067) or `proposal respond` (069), which 063 gates regardless (plan ADR-3)
    - The agent is registered so the host discovers it and the skill's delegation resolves; if the agent is absent/unregistered, the skill's workflow remains usable as guidance with no CLI command broken (documented degradation)
    - `plugin/agents/proposal-circulator.md` exists with frontmatter `name` (`proposal-circulator`), `description`, and a **write-capable-but-fenced `tools` grant** that includes `Bash` (to invoke the composed reads and the two gated transitions) and excludes `Write`/`Edit` (no workspace mutation); the body carries Identity & scope (exactly two writes: `proposal propose` and `proposal withdraw`, each always through the gate, each confirmed independently; never `proposal create`/`respond`, never a tension write, never an authority verdict, never coaching; reads inform, never gate), Workflow (by reference to the skill), the Confirmation contract (narrate the proposal and what the transition will do — for a withdraw, that the server clears `proposed_at`/`response_deadline` and deletes prior responses, reflected in the returned record; the bodyless command line is the complete payload; declined = `action: declined`, an outcome not an error), Composed commands (exactly the four leaves), and the circulation-record Output contract (proposal?/situating?/action/handoff?/notes, the proposal exactly as the server last returned it, each element carrying its id; `action` ∈ `advanced`/`monitored`/`withdrawn`/`declined`/`none`)
    - The artifacts' only writes are `proposal propose` and `proposal withdraw`; no other proposal-write phrase appears as a command the path runs; a withdrawn draft's `prp_` id is handed back to 067, consent responses to 069, and authority questions to 065; the workflow steps are single-sourced in the skill and referenced by the agent — no second, divergent copy
    - The existing `plugin/skills/orientation/` (062), `plugin/skills/governance-navigation/` + `plugin/agents/governance-navigator.md` (064), `plugin/skills/constraint-discovery/` + `plugin/agents/constraint-navigator.md` (065), `plugin/skills/tension-processing/` + `plugin/agents/tension-processor.md` + `plugin/agents/tension-processing-commands.txt` (066), `plugin/skills/proposal-drafting/` + `plugin/agents/proposal-drafter.md` + `plugin/agents/proposal-drafting-commands.txt` (067), and `plugin/hooks/` (063, including `gated-commands.txt`) are untouched, and no `marketplace.json` is added (distribution is #70)
  - **Dependencies**: None
  - **Plan reference**: Phase 1; ADR-1 (thin skill + subagent), ADR-2 (new `proposal-circulator`, `plugin/agents/` reuse, no 067 reuse), ADR-3 (both gated bodyless transitions inside the subagent, independent confirmations, layered confirmation), ADR-4 (compose shipped commands, reads inform never gate, no CLI code)
  - **Scenario references**: proposal-circulation-path.feature — "A draft proposal advances into circulation", "An unconfirmed transition leaves the record untouched", "A rejected transition fabricates no state", "A stale snapshot does not stop a transition", "The circulator is reachable once the plugin registers it", "A missing circulator degrades the path to guidance", "The path routes both writes through the guardrail", "The path never pre-gates a transition client-side", "A circulating proposal is monitored as one picture", "A failed monitoring walk yields a partial picture", "A consent response is handed to the response side", "The path records no consent response", "The result is a synthesized circulation picture, not raw output", "A circulating proposal is withdrawn back to draft", "Two transitions in one session confirm twice", "The path circulates without judging authority or coaching"
  - **Interface references**: interface-spec.md — Surface (structural layout, `SKILL.md` / agent frontmatter, required sections, confirmation contract, circulation-record output shape, single-source leaf list), Interactions (skill→agent delegation flow, gated-transition steps, reads-inform-never-gate), Error Communication (declined confirmation, transition rejected, partial monitoring walk, response-side handoff, withdraw handoff, gated-write nuance, no-pre-gate nuance)

- [x] **T002** [Shared] Add the best-effort drift-guard test in `internal/build` — pins the composed leaves AND the two-write gated-membership invariant (extending 067's gate-posture pattern to a two-in-two-out path) — 1 validation scenario + standalone guard; both gated writes are anchored explicitly (e.g. `ProposalCirculationGatedWrites = {"proposal propose", "proposal withdraw"}`, cross-checked against the composed list) so a read/write swap across the gate is caught rather than satisfied by count, while the three registries stay source-derived — validation scenario passing (17/17 total); guard verified red-on-drift (dropping `proposal withdraw` from the gated set fails the build naming the leaf)
  - **Scope**: A new `internal/build` test asserting every leaf the skill/agent compose still exists in the CLI's command registry **and** the gate-membership posture holds: `proposal propose` and `proposal withdraw` are **each a member of** 063's gated proposal-write set, and the composed reads (`proposal get`, `proposal list`) are **not**. Best-effort and explicitly partial — pins the *existence* of the command leaves and their *gate-membership posture*, not their flags (deferred to `--help`), not the circulation-record prose, not the reads-inform-never-gate discipline (prompt-level, validation-scenario-verified), and not parser robustness. If an anchor proves infeasible to assert, state the reduced coverage rather than dropping it silently.
  - **Acceptance criteria**:
    - Test asserts the leaves the artifacts compose — `proposal get`, `proposal list`, `proposal propose`, `proposal withdraw` — each resolve to a real command in the CLI's registry, reading the composed set from the single-source leaf-list file (`plugin/agents/proposal-circulation-commands.txt`, T001), not a hard-coded copy
    - Test asserts `proposal propose` and `proposal withdraw` are **each present in** 063's gated set (source-derived from `plugin/hooks/gated-commands.txt`) — both confirmed-write-flow promises pinned structurally: either transition leaving the gated registry fails the build — and that the two composed reads are **absent from** it (monitoring must not start prompting)
    - The two gated writes are anchored as explicit per-leaf contract-facts cross-checked against the composed list — a count-only membership check (e.g. "exactly two leaves are gated") that a read/write swap could satisfy does not pass for this criterion (drift-guard boundary nuance: derive the sets, anchor the specific members)
    - Test fails loudly and names the offending leaf when one no longer exists in the shipped CLI or violates the gate-membership invariant on either side
    - All sides source-derived — composed set from the leaf list, live surface from the command registry, gated set from `gated-commands.txt` — never a hard-coded second copy (drift-guard-must-not-hardcode-the-SoT)
    - Any leaf or fact deliberately left uncovered is documented in the test, not omitted silently (no silent caps)
    - Reuses the `internal/build` config-guard home/idiom established by 062/063/064/065/066/067 (067's `proposal_drafting_guard_test.go` + `proposal-drafting-commands.txt` single-source are the concrete models; this guard asserts two memberships where 067 asserted one)
  - **Dependencies**: T001
  - **Plan reference**: Phase 1; ADR-5 (best-effort drift guard + two-write gated-membership invariant)
  - **Scenario references**: proposal-circulation-path.feature — "The path names no command the CLI lacks"
  - **Interface references**: interface-spec.md — Surface (single-source leaf list), Error Communication (drift guard red on missing leaf or gate-membership violation on either side; reduced coverage stated)
