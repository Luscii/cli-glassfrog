# Tasks: Subrole Filler Roll-up

**Feature**: 051-subrole-filler-roll-up
**Concretization**: Full context (plan + spec + interface-cli + scenarios)
**Inputs**: plan.md, spec.md, interface-cli.md, features/who-to-contact-for-a-role/subrole-filler-roll-up.feature

---

## Dependency Graph

Phase 1: The `subrole-actors` roll-up leaf (1 task, no dependencies) [Shared]
Phase 2: Executable acceptance (1 task, depends on Phase 1) [Shared]

2 tasks total | T001 startable immediately | Builder: pipeline — single cohesive leaf

> Plan-faithful: the plan's two ordered steps map 1:1 — the command leaf (T001) and the BDD suite (T002). This is the actor-shaped twin of Subroles Tension Roll-up (046): where Actor Directory (048) had to *add* the `actors` render path and the `validateKind` validator, 051 **reuses every data-layer seam unchanged** — no render phase, no validator phase, no schema phase, no new `Outcome`/`ExitCode`/root flag. The only genuinely new code is one command leaf whose request differs from the `actors` directory walk only in the path segment (`/roles/{role_id}/subroles/actors`), the role-keyed positional, and the leaf-`404`.
>
> **✅ The cross-spec dependency is landed on `main` and this task list is verified against the implementation.** **048 (#90)** ships the `actors` command (`internal/cli/actors.go` — `runActorsListWalk`, the two-track walked-list dispatch, `validateKind`/`supportedActorKinds`/`validateClosedFlagSet`), the `actors` render key (`render.ResourceActors` in `builtinResources` + `render.ActorsView` + `actors.{full,compact}.tmpl`), reusing the `glassfrog.Actor` model (011) and the `Page[T]`/`paging.All` walk (016/017). 007/009/010/011/015/016/017/018/020/025/031/032/035 are Complete with their packages shipped — `Page[T]`/`paging.All`, `RetryExecutor`, `classifyClientError`/`Diagnose`/`Outcome`/`ExitCode`, the persistent `--base-url`/`--output`/`-o`, the 035-widened `resolveRenderTarget`+`writeHuman`/`aggregateRawData`/`output.RenderSuccess` machinery, and 032's `reportFailure` chokepoint. 051 adds **no** new render key, model, validator, generic type, `Outcome`, `ExitCode`, or root flag.
>
> **The cross-role counterpart to Actor Directory (048).** `actors --role-id <id>` lists the actors filling **one** role; 051 reads `GET /roles/{role_id}/subroles/actors` (`listSubrolesActors`) — the one-level roll-up of the actors filling the anchor's direct sub-roles — as its **own** role-keyed read leaf (plan ADR-1: a distinct `ExactArgs(1)` command, **not** a `subroles` subcommand of the positional-bearing `actors`; the 026/050 own-command precedent). Story labels: the spec's four user scenarios — US1 (see who is staffing the circles inside), US2 (roll up the sub-roles' fillers in one read), US3 (tell people apart from agents via `--kind`), US4 (trust the roll-up is whole). The single leaf serves all four → `[Shared]`.

---

## Branching Guidance

**Pipeline mode**: `spec/051-subrole-filler-roll-up/base` → `spec/051-subrole-filler-roll-up/task-1`, `…/task-2` (one task branch per T-id, merged back into the spec base).

**Base branch**: 048 has landed on `main` (#90), so cut the 051 base from current `main` — the `actors` command, the `Actor` model, the `actors` render path, and `validateKind` are all present and reused as-is. 046 (the structural roll-up template) is also landed.

**Role-based awareness**: parallel Conductor workspaces may carry sibling actor specs (049 Actor Read is `Analyzed`, not yet merged; 050 Actor Assignments has landed on `main`, #118). Plan ADR-1 deliberately makes 051 a **distinct** command, so it touches **neither** `actors.go` (049 grows it) **nor** the `assignments` command (050) — there is no first-to-land race. 051 makes **no** change to `internal/glassfrog` (011's `Actor` reused), `internal/render` (048's `actors` key reused), or the validator set (048's `validateKind` reused) — the only new file content is the `subrole-actors` leaf in `internal/cli` and its BDD suite.

---

## Phase 1: The `subrole-actors` roll-up leaf [Shared]

- [x] **T001** [Shared] Add the `subrole-actors <role-id>` command — reuse the landed `runActorsListWalk` shape (048) with the request path swapped to `/roles/{role_id}/subroles/actors` and a required role-id positional; `--kind` only (no `--role-id`/`--query`); leaf-`404` surfaced verbatim, distinct from empty-`200`; reuses 011's `Actor`, 048's `actors` render key + `validateKind` unchanged — RED-first unit tests for every branch — **done**: `internal/cli/subrole_actors.go` + 19 unit tests; the `actors` list-walk was parameterized (extracted `actorsWalkConfig`, `runActorsListWalk(req)`) so the page loop/render/errors.As chain is single-sourced (plan ADR-3); drift noted in LEARNINGS — 049 has already landed, renaming 048's original `runActorsList` into `runActors`/`runActorsListWalk`; this task list now reflects the current `runActorsListWalk` name
  - **Scope**: In `internal/cli` (a new `subrole_actors.go` matching the pinned command token, or beside `runActorsListWalk` in `actors.go`), add the role-keyed roll-up leaf: a guard-registered command (`Use:"subrole-actors <role-id>"`, `Args: cobra.ExactArgs(1)`, non-empty `Short`, `SilenceErrors`/`SilenceUsage`), declaring `--kind`, `--first-page`, `--per-page`; reads the persistent `--base-url`/`--output`; delegates to a pure runner. **Reuse the landed `actors` directory runner shape** — the recommended implementation (plan ADR-3) is to **parameterize the request path + the filter set** on `runActorsListWalk` (add a path field and restrict the carried filters to `kind`) so the kind-validation, paging, completeness, render, and error logic is single-sourced; if a thin sibling runner is chosen instead, it must call the same helpers (no second copy of the page loop, render branch, or `errors.As` chain). The runner: resolve the render target (`resolveRenderTarget(seam, outputFlag, stderr)`) **first** and **validate `--kind` via the landed `validateKind`** (048) **before** assembly, so a bad template/format or bad kind is a fail-fast `UsageError(2)` with no request; build the `kind` query parameter only when `Changed()` and non-empty; the default path walks **`GET /roles/{role_id}/subroles/actors`** to completion using the landed two-track list pattern — machine format: walk `paging.All[json.RawMessage]` then `aggregateRawData(machineFmt, res.Records)` into the `{data:[…]}` document; human: walk `paging.All[glassfrog.Actor]` then `writeHuman(stdout, stderr, rt.tmpl, render.ResourceActors, rt.format, render.ActorsView{Data: res.Records})` (048's landed key — reused, NOT a new key). `--first-page` does one `Execute` into the corresponding `Page[…]` and writes a "more actors exist" stderr note (exit 0) on `HasNextPage`; a mid-walk `Result.Stop` renders the partial set, writes an "incomplete — <cause>" stderr note, and exits non-zero via `classifyClientError(Stop)`; `--per-page` sets `WithPageSize` (presence, not value). The `kind` filter must ride **every** page request of the walk, not just the first. The anchor `role_` id is `url.PathEscape`-d and passed through unvalidated (plan ADR-3) so a **leaf anchor's 404** (and an unknown id) surfaces the API's non-2xx via the shared classifier — **add no leaf-404 special case** (no "this role has no sub-roles" message; plan ADR-3) and **no recursion** into grand-children (one level only). Do **not** surface `--role-id`/`--query` (the endpoint offers neither). Guard-register the leaf and wire it explicitly in `main`/`Assemble`. Never read `ctx.Cred.Token`.
  - **Acceptance criteria**:
    - `glassfrog subrole-actors role_0123` walks every page of `GET /roles/{role_id}/subroles/actors` (`paging.All`) and prints the projection of all the direct sub-roles' fillers (each row carrying id, name, kind); exits 0
    - Sub-roles that carry no fillers print the `actors` empty line (`no actors`) and exit 0
    - A **leaf anchor** (or unknown id) returns `404` → surfaced as APIError(3)/PermissionError(4) naming the HTTP status, non-zero exit, **with no special "no sub-roles" message** — distinct from the zero-exit empty-list success
    - `--kind agent` sends `kind=agent`; an omitted/empty `--kind` sends nothing; the `kind` filter is present on every page request of a multi-page walk
    - `--kind robot` (unsupported) is a `UsageError(2)` naming the value + supported set (`agent`, `human`), **no request sent** (transport tripwire)
    - Rows are bare `Actor` records (id/name/kind); **no** `focus`/`elected_until` is projected (that is 047's assignment-shaped read)
    - `--first-page` against a multi-page roll-up prints one page, writes a "more actors exist" stderr note, exits 0
    - A mid-walk failure prints the partial set, writes an "incomplete — <cause>" stderr note, exits non-zero (classified from `Stop`)
    - `*AuthError{NoCredentials}` → UsageError(2); `*TransportError` → NetworkUnavailable(6); base-URL/`--output` errors → UsageError(2); `-o json`/`yaml` emit the aggregated `{data:[…]}` document, `full`/`compact` the human projection, `-o <template-ref>` a 035 user template
    - No `--role-id` or `--query` flag is declared on the leaf
    - No new `Outcome`/`ExitCode`/root flag/render key/validator; no token in any output; all branches run offline; `go build`/`go vet` clean
  - **Dependencies**: None at the spec level — 048's `actors` command + `validateKind` + `actors` render key + `runActorsListWalk` shape, 011's `Actor` model, and the shared seams are all landed on main. Startable immediately.
  - **Plan reference**: Phase 1 (Command); ADR-1 (a distinct role-keyed `ExactArgs(1)` leaf, not a subcommand of `actors`), ADR-2 (reuse the `Actor` model, `actors` render path, and `validateKind`; surface `--kind` only), ADR-3 (reuse the `runActorsListWalk` runner with the path swapped; leaf-404 surfaced verbatim, one level only)
  - **Interface references**: interface-cli.md — `subrole-actors` Surface, list flags, Output (actor rows, not assignment), Interactions (validation order + completeness + one-level read), Error Communication (leaf-404 row)
  - **Scenario references**: subrole-filler-roll-up.feature: "A circle's direct sub-roles' fillers are rolled up", "A leaf anchor fails with the API status", "A missing token fails as a not-authenticated usage error", "Sub-roles with no fillers are a clean success", "A kind filter narrows the roll-up to agents", "An unsupported kind is rejected as a usage error", "The roll-up walks every page to completion", "The first-page opt-out stops at one page and signals more", "A mid-walk failure yields a partial set flagged incomplete"
  - **Risk**: ⚠️ Reuse 048's landed `actors` render key, `validateKind`, and the `runActorsListWalk` runner — add **no** new render key, validator, or second copy of the page loop / render branch / `errors.As` chain (parameterize the path, per plan ADR-3). ⚠️ **Distinct command, not `actors subroles`** — do not turn the positional-bearing `actors` command into a runnable-parent-with-children (plan ADR-1); register a separate leaf. ⚠️ **No leaf-404 special-casing** — surface the `404` through the shared classifier; keep it distinct from the empty-`200` success (both pinned by tests). ⚠️ The roll-up is **one level only** — issue exactly the one paginated read; never recurse into grand-children. ⚠️ **Actor shape, not assignment shape** — project id/name/kind only; no `focus`/`elected_until`. ⚠️ Surface **only `--kind`** — no `--role-id`/`--query` (the endpoint offers neither). ⚠️ The `kind` filter must ride every page of the walk. ⚠️ Structured output is the aggregated `{data:[…]}` over `Page[json.RawMessage]`, NOT a decode-and-re-encode of `[]Actor`. ⚠️ `--kind` sent only when `Changed()` and non-empty. ⚠️ Resolve the render target and validate `--kind` BEFORE assembly so the tripwire confirms no request on rejection. ⚠️ Never silently truncate — every incomplete path writes an explicit stderr note (CONSTITUTION VI). ⚠️ Capture stdout/stderr in tests with a temp file, not `os.Pipe` (LEARNINGS). ⚠️ Never read `ctx.Cred.Token`.

## Phase 2: Executable acceptance [Shared]

- [x] **T002** [Shared] Make the driving scenarios pass as executable acceptance — new `internal/cli` godog suite over `subrole-filler-roll-up.feature`; un-`@wip` the behavioral scenarios, keep `@validation` held — **done**: `TestSubroleFillerRollUpFeatures` (Paths = only this feature file) — 9 behavioral scenarios pass (51 steps), 6 `@validation` scenarios kept `@wip`; leaf-404 (cannedTransport 404) and empty-200 (cannedTransport empty body) exercise distinct fakes with distinct outcomes
  - **Scope**: Add godog step definitions for `features/who-to-contact-for-a-role/subrole-filler-roll-up.feature` in a **new** `internal/cli` godog suite (e.g. `TestSubroleFillerRollUpFeatures`) whose `Paths` names **only** that feature file (LEARNINGS: a suite points at its own file, never the `features/` directory). Drive `subrole-actors` through the shared seam over a fake base `http.RoundTripper` returning canned `GET /roles/{role_id}/subroles/actors` responses (single-page, multi-page, mid-walk error, empty-`200`, kind-filtered) and a **leaf-anchor `404`**, plus a transport tripwire for the no-request path (unsupported `--kind`). Remove `@wip` from the spec-derived + architecture-informed behavioral scenarios; keep the 6 `@validation` scenarios (`A leaf 404 is distinct from an empty roll-up`, `The rolled-up rows are actor-shaped, not assignment-shaped`, `Default output carries no raw API envelope`, `The roll-up reads only the direct sub-roles`, `A rejected kind issues no request`, `The kind filter is carried on every page of the walk`) `@wip` (held for validate). **Reuse the sibling-suite step phrasings** (godog matches by text) from the actor-directory / role-fillers / subroles-tension-roll-up suites before writing new bindings — the connection-context, no-credential, exit-code, stderr-substring, kind-rejection, tripwire, first-page, mid-walk, kind-narrowing, and no-envelope steps already exist; add new bindings only for the subroles-specific steps (direct-sub-roles-filled setup, subroles actors endpoint, leaf-anchor 404, one-level read assertion, actor-shape-not-assignment assertion). Step helpers return errors, never panic; never hard-code the validator's supported-set order (it is alphabetically sorted).
  - **Acceptance criteria**:
    - Every non-`@validation` subrole-roll-up scenario has an executable, passing path; `@wip` removed from them
    - The 6 `@validation` scenarios keep `@wip`
    - The new suite's `Paths` names only `subrole-filler-roll-up.feature`; all `internal/cli` godog suites run and report their own independent scenario counts
    - The leaf-`404` scenario and the empty-`200` scenario exercise genuinely different fakes and assert different outcomes (non-zero failure vs zero-exit empty)
    - No real network (fake base transport / loopback only) and no real home/filesystem are touched; `go build ./...`, `go vet ./...`, and the feature suites run clean
  - **Dependencies**: T001 (the `subrole-actors` command) — all behavioral scenarios must be implementable.
  - **Plan reference**: Phase 2 (BDD); System Architecture (distinct role-keyed leaf); Cross-cutting (testing)
  - **Interface references**: interface-cli.md — Surface, Interactions, Error Communication
  - **Scenario references**: subrole-filler-roll-up.feature: all behavioral Rule-block scenarios (the 6 `@validation` scenarios stay held for validate)
  - **Risk**: ⚠️ Suite scoping — point the suite at `subrole-filler-roll-up.feature` only (not the directory); verify it reports its own count. ⚠️ Reuse shared step phrasings before writing new bindings; step helpers return errors, never panic (LEARNINGS). ⚠️ Cover the leaf-`404`, empty-`200`, multi-page, mid-walk-error, kind-filtered, and no-request (tripwire) fakes so the failure-vs-empty, completeness, filter, and rejection scenarios genuinely exercise their paths. ⚠️ Never hard-code the alphabetically-sorted supported-set order in any step assertion.
