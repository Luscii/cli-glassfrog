# Tasks: Proposal Creation

**Feature**: 055-proposal-creation
**Concretization**: Full context (plan + spec + interface-cli + interface-spec + scenarios)
**Inputs**: plan.md, spec.md, interface-cli.md, interface-spec.md, features/proposal-write-flow/proposal-creation.feature

---

## Dependency Graph

Phase 1: Models, change-source resolution, and the type floor (2 tasks: T001 models — no deps; T002 change-source + floor — no deps, parallel with T001) [Shared]
Phase 2: The proposal create command (3 tasks: T003 render — depends T001; T004 command — depends T001+T002+T003; T005 acceptance step — depends T004) [US1/US2/US3/US4]

5 tasks total | T001 and T002 startable immediately (parallel) | Builder: pipeline

> Plan-faithful: the plan's **two** phases map here directly — Phase 1 (models + change-source resolver + `type` floor: T001 model / T002 resolver+validator) and Phase 2 (the command: T003 render / T004 command). The godog acceptance work (T005) is the closing **acceptance step of Phase 2**, realizing plan's Cross-cutting testing note — not a separate numbered phase, since plan defines exactly two. **This is the CLI's second write** and the anchor of the governance write path; unlike Tension Capture (042) there is **no new transport mechanism** — the write body's `Content-Type` rides the **landed** `apiclient.Request.ContentType` (042), and no `If-Match` is sent. 055's genuinely exclusive machinery is the `--changes` source resolver, the `type` floor, and the write request-input; the `proposal` group, `Proposal` model, and singular `proposal` render key are **shared with 056** (first-to-land creates — see the ⚠️ note below). Story labels follow the spec's four user scenarios — US1 (create anchored to a tension), US2 (get back the `prp_` id + draft status), US3 (source the change set from a file/stdin), US4 (refuse a changeless/malformed proposal). The command (T004) realizes all four in one leaf, so it carries `[US1/US2/US3/US4]`; the model, resolver/validator, and render key are shared infrastructure `[Shared]`.
>
> **All cross-spec dependencies are landed on main.** Per STATUS.md, 007/009/010/011/015/016/017/018/019/020/032/034/035/042 are Complete with their packages shipped — `apiclient.Request` **including the `ContentType` field** (042), `(*Client).Execute`/`AssembleFromOS`/`NewClientFromOS` + the typed errors (010), `NewRetryExecutor` incl. the `isSafeMethod` 429 gate (017), the generic `Document[T]` (034), `classifyClientError`/`refineClientError`/`reportFailure`/`Diagnose`/`Outcome`/`ExitCode` (011/015/031/032), `output.ResolveSelectionFromOS`/`Selection`/`RenderSuccess` + the template source seam (035) + the `cli` `renderResult[T]` dispatch and the two-package `output`/`render` split (018/019/020), the persistent `--base-url`/`--output`/`-o`, the bounded stdin reader `readBoundedStdinN` (006/035) the change-source stdin arm reuses, the `tensionSeam` shape (042) the proposal seam extends, and the registration guard + `Assemble()` wiring (001). The 055 base cuts from current main with no sequencing caveat. **Existing main state 055 builds on**: there is **no** `proposal` command, `glassfrog.Proposal`/`ResponseSummary` model, or `proposal` render key in `main` *code* yet (056, which would also create them, is specified but unimplemented — see the ⚠️ note), and `apiclient.Request` already carries `ContentType` (042, used unchanged) and `IfMatch` (053, left `""`). 055 adds **no** new `Outcome` category, `ExitCode` case, generic envelope type, root flag, or `apiclient` change.
>
> **Second write, in the `proposal` namespace** (plan ADR-1). The `proposal` group + `create` leaf mirror the `tension`/`tension create` group/leaf; the rest of the write-flow (advance/respond/withdraw — deferred) adds sibling leaves under the same group.
>
> **⚠️ Concurrent sibling: Proposal Reads (056).** 055 and 056 both introduce the `proposal` group (`newProposalCommand`), the `glassfrog.Proposal`/`ProposalChange`/`ResponseSummary` response model, and the singular `proposal` render key. Design contract (plan ADR-1/ADR-4, Cross-cutting, Risks): **first-to-land creates, follower reuses-or-grows** (043→042). As cut from current `main` (no proposal code present), 055 creates all three. **If 056 merges first**, rebase and: (a) attach 055's `create` leaf to the existing group instead of creating it (T004); (b) reuse 056's `Proposal`/`ResponseSummary` response model — 055 still adds **only** its write request-input type (T001); (c) reuse 056's richer singular `proposal` render unchanged, dropping 055's thin view (T003). 056 additionally owns the plural `proposals` render, `list`/`get`, and `validateProposalStatus` — none of 055's concern. Structured `json`/`yaml` output is faithful regardless of model field coverage (`output.RenderSuccess` passes raw server bytes through), bounding any conflict to human render + cobra wiring. 055's exclusive surface — the write request-input, `resolveChangesSource`, the `type` floor — is conflict-free.

---

## Branching Guidance

**Pipeline mode**: `spec/055-proposal-creation/base` → `spec/055-proposal-creation/task-1`, `…/task-2`, … (one task branch per T-id, merged back into the spec base). Cut the base from current main (all dependencies landed).

**Role-based awareness**: parallel Conductor workspaces carry sibling proposal specs — chiefly **Proposal Reads (056)**, which shares the `proposal` group, the `Proposal`/`ResponseSummary` model, and the singular `proposal` render key (see the ⚠️ note: first-to-land creates, follower reuses/grows). 055 makes **no** change to other shared packages — it consumes `internal/apiclient` (incl. the landed `ContentType`/`IfMatch` fields) entirely unchanged, so a sibling editing `internal/apiclient` (053's `IfMatch`) or `internal/diagnostics`/`exitcode` (054's `412`/code 7) does not conflict with 055.

---

## Phase 1: Models, change-source resolution, and the type floor [Shared]

- [ ] **T001** [Shared] [P] Add the create request-input shape (055-exclusive) + reuse-or-create the shared `glassfrog.Proposal`/`ResponseSummary` model — decode/encode round-trip tests; `internal/glassfrog/proposal.go` + tests — ~6 tests (snake-case bind, null→empty, unknown-field tolerance, free-form change keys preserved, request marshal with verbatim changes), no scenarios at this layer
  - **Scope**: In `internal/glassfrog/proposal.go`. **Shared response model (056 coordination — create only if 056 hasn't landed it; otherwise reuse unchanged)**: the `Proposal` model matching the v5 `Proposal` schema with explicit snake_case JSON tags, tolerant of unknown/extra fields: `ID` (`prp_…`), `Type` (`proposal`), `Status` (`draft`/`proposed_outside_meeting`/`escalated`/`accepted`/`draft_with_conflicts`), `TensionID` (`ten_…`, nullable→empty), `CircleID` (`role_…`, nullable→empty), `ProposerID` (`per_`/`agt_…`, nullable→empty), `Changes []ProposalChange` (each `ID`/`Type` + a free-form `map[string]any` remainder, the `actions.go` precedent — 056 ADR-2), `ResponseSummary` (nested), `ExpectedResponseCount int`, `ReceivedResponseCount int`, `AvailableTransitions []string` (`propose`/`withdraw`), `ProposedAt`/`ResponseDeadline`/`AcceptedAt` (nullable→empty), `CreatedAt`, `UpdatedAt`; `ResponseSummary` is `Total`/`NoObjection`/`BringToMeeting` ints. **055-exclusive (always added by 055)**: the create request-input shape encoding `{ "proposal": { "tension_id": …, "changes": [<raw>,…] } }`, where the request `changes` is `[]json.RawMessage` carried **byte-for-byte** — deliberately distinct from the response `[]ProposalChange`, and never needed by 056. **No `proposer`** field (server derives from token) and **no `status`** field (server sets `draft`). Leaf package — imports no transport, no cobra. The token is never a field (CONSTITUTION II). The create response decodes into the landed generic `Document[Proposal]` — add no new envelope type.
  - **Acceptance criteria**:
    - A `{data: Proposal}` body decodes into `Document[Proposal]`; every field binds (snake_case tags verified); a null `tension_id`/`circle_id`/`proposer_id`/timestamp decodes to empty string; `response_summary` binds its three counts; free-form change keys are preserved on `[]ProposalChange`
    - Marshalling the request input emits `{"proposal":{"tension_id":"…","changes":[…]}}` with the supplied `changes` slice (`[]json.RawMessage`) **verbatim** (no reshaping, no added keys); no `status`/`proposer` keys
    - Unknown/extra response fields decode cleanly (forward-compatible)
    - `internal/glassfrog` imports neither `cli` nor `apiclient`; `go build`/`go vet` clean
  - **Dependencies**: None (references the landed `Document[T]`, 034).
  - **Plan reference**: Phase 1 (request-input + shared model); System Architecture (`internal/glassfrog`); ADR-4; conforms to 011 ADR-1
  - **Interface references**: interface-spec.md — `internal/glassfrog` proposal schema + request input
  - **Risk**: ⚠️ Explicit snake_case tags on every field (encoding/json does not bridge underscores). ⚠️ The **request** `changes` is `[]json.RawMessage` (verbatim send); the **response** `Changes` is `[]ProposalChange` (free-form remainder) — two distinct shapes, do not conflate. ⚠️ Never add a `status`/`proposer` request field. ⚠️ Nullable-as-empty-string convention, matching the landed models. ⚠️ **056 coordination**: if 056 already landed `proposal.go`, REUSE its `Proposal`/`ProposalChange`/`ResponseSummary` (grow only if thinner) and add **only** the request-input type — do not add a second `Proposal`.

- [ ] **T002** [Shared] [P] Add `resolveChangesSource` (reserved `stdin` / existing file / inline) + `validateChanges` (the type floor) — pure, fully offline tests; new helpers in `internal/cli/proposal.go` (or a focused `proposalchanges.go`) + tests — ~12 tests (stdin happy/TTY/empty-pipe; existing-file happy/unreadable/directory; inline happy; not-JSON; not-array; empty array; element-not-object; element-missing-type; extra-keys-preserved), no scenarios at this layer
  - **Scope**: In `internal/cli`, add two **new pure** helpers (plan ADR-2/ADR-3):
    - `resolveChangesSource(value string, stat func(string)(os.FileInfo,error), readFile func(string)([]byte,error), isTTY bool, stdin io.Reader) ([]byte, error)` — classify the `--changes` value: a trimmed value equal (case-insensitive) to `stdin` → read via `readBoundedStdinN(stdin, <changesCap>, "changes")` with the 035 TTY guard (a terminal stdin → usage error telling the operator to pipe) and the empty-pipe guard (an empty piped stream → usage error); else a value whose `stat` reports an **existing regular file** (not a directory) → `readFile`; else the value's own bytes (inline JSON). Read/stat/empty-pipe failures return a `UsageError`-bound error **naming the source**. Pure over injected `stat`/`readFile`/`isTTY`/`stdin`.
    - `validateChanges(raw []byte) ([]json.RawMessage, error)` — unmarshal into `[]json.RawMessage`; return a `UsageError`-bound error (naming the source/problem) if not valid JSON, not a JSON array, or empty; then probe each element by decoding into a minimal `struct{ Type string }` and reject an element that is not a JSON object or has a blank `type` ("every change must carry a `type`"). Return the verbatim slice on success — read only `type`, never other keys.
  - **Acceptance criteria**:
    - `--changes stdin` reads a piped array; a terminal stdin and an empty pipe each return a usage error (no panic), reusing the 006 bounded reader
    - A value that stats to an existing regular file is read from disk; a directory or unreadable path returns a usage error naming the source; any other value is treated as inline JSON bytes
    - `validateChanges` accepts a non-empty array of typed objects and returns it byte-for-byte; rejects non-JSON, a non-array, an empty array, a non-object element, and an object lacking a non-empty `type` — each a usage-error-bound error
    - Command-specific keys beyond `type` are preserved untouched in the returned slice (a test marshals the result and compares to the input)
    - Both helpers are pure (injected sources / bytes only) — tested with no real pipe or filesystem; `go build`/`go vet` clean
  - **Dependencies**: None (references the landed `readBoundedStdinN`, 006/035).
  - **Plan reference**: Phase 1 (change-source resolution + type floor); ADR-2 (`resolveChangesSource`), ADR-3 (`validateChanges`)
  - **Interface references**: interface-spec.md — `internal/cli` additions (`resolveChangesSource`, `validateChanges`)
  - **Risk**: ⚠️ Reuse `readBoundedStdinN` (006/035) for the stdin arm — do NOT write a second bounded reader. ⚠️ The file/inline split needs a regular-file guard (a directory is not a source). ⚠️ `validateChanges` reads ONLY each element's `type` — never parse or validate other keys (verbatim pass-through; typed builders are deferred). ⚠️ Every rejection is a `UsageError(2)` raised pre-assembly so the command's tripwire confirms no request. ⚠️ Reserved `stdin` is case-insensitive and beats a file literally named `stdin` (reachable as `./stdin`).

## Phase 2: The proposal create command [US1/US2/US3/US4]

- [ ] **T003** [Shared] Reuse-or-create the shared singular `proposal` render key — golden + registry-guard tests; `internal/render` + tests — ~4 tests if created (full golden, null-field markers, compact golden, change-count); **no work if 056's singular render has already landed** (reuse unchanged)
  - **Scope**: The created proposal renders through the **shared** singular `proposal` render key (056 coordination, plan ADR-4). **If 056's singular `proposal` view has landed, reuse it unchanged** — it already shows changes by type, the response summary, and available transitions; this task is then a no-op verification that the created proposal renders correctly through it. **If 055 lands first**, add the thin key: render key `proposal` (single, data `glassfrog.Proposal` via a `ProposalView{Proposal}`, mirroring the landed `TensionView{Tension}`), with `ResourceProposal` added to `builtinResources` for the registry-exhaustiveness guard (PR #10 `len`+comma-ok shape), and two `//go:embed` templates: `proposal.full` — `<prp_…>  [<status>]` then an indented block (`Tension`, `Circle`, `Proposer`, `Changes` = `len(.Changes)`, `Responses` = the aggregate counts, `Transitions` = `available_transitions`, `Created`, `Updated`) with 019's absence-guard discipline on the nullable `tension_id`/`circle_id`/`proposer_id`; `proposal.compact` — `<prp_…>  [<status>]  <N> change(s)`. Either way, depends only on `internal/glassfrog` + stdlib; must not import `cli`/`apiclient`.
  - **Acceptance criteria**:
    - The created proposal renders in both `full` and `compact` through the singular `proposal` key, surfacing the `prp_` id and `draft` status
    - A null `tension_id`/`circle_id`/`proposer_id` renders its explicit-absence marker (`(none)`), never `<no value>` or an invented value
    - The registry-exhaustiveness guard passes with the `proposal` key carrying both formats; golden tests pin each template (whether newly added or 056's)
    - `internal/render` imports neither `cli` nor `apiclient`; `go build`/`go vet` clean
  - **Dependencies**: T001 (the view references `glassfrog.Proposal`).
  - **Plan reference**: Phase 2 (render); System Architecture (`internal/render`); ADR-4
  - **Interface references**: interface-cli.md — Output; interface-spec.md — `internal/render` singular `proposal` key
  - **Risk**: ⚠️ **056 coordination**: if 056 already added the singular `proposal` key, REUSE it (do not add a second key); if 055 first, 056 later GROWS the template to render changes by type — keep it the single shared key. ⚠️ Explicit-absence guards for every nullable field; never invent a value. ⚠️ If created here, add `ResourceProposal` to the guarded set so exhaustiveness still holds.

- [ ] **T004** [US1/US2/US3/US4] Add the `proposal create <tension-id>` command — `proposal` group + `create` leaf + `proposalSeam` (incl. `readChangesSource`) + change read/parse/floor + body marshal + `Execute` + render dispatch + wiring — RED-first unit tests for every branch; new `internal/cli/proposal.go` + `Assemble()` wiring — ~16 unit tests (POST shape + verbatim changes, inline/file/stdin sources, missing-positional/missing-changes/empty/non-array/typeless/bad-source fail-fast no-request, auth/403/404/429-not-retried/transport classification, structured raw payload, group guard, end-to-end, ExactArgs arg-count); behavioral scenarios realized in T005
  - **Scope**: `internal/cli/proposal.go`. The `proposal` group (`newProposalCommand`) is **shared with 056** (plan ADR-1): if 056's group has landed, **attach the `create` leaf to the existing group** (do not redefine it); if 055 lands first, create `newProposalCommand(seam proposalSeam) *cobra.Command` — a guard-registered **non-runnable group** (`Use:"proposal"`, non-empty `Short`, no `RunE`), built with its `create` child attached **before** registration under root (so the guard's ">=1 child" rule holds). `newProposalCreateCommand(seam proposalSeam) *cobra.Command`: a guard-registered leaf (`Use:"create <tension-id>"`, `Args: cobra.ExactArgs(1)`, non-empty `Short`, `SilenceErrors`/`SilenceUsage`), declares `--changes`; reads the persistent `--base-url`/`--output`; delegates to pure `runProposalCreate`. Define `proposalSeam` — the 042 `tensionSeam` shape (`assemble`, `newClient`, `sleep`, `resolveSelection`, `readTemplateSource`) **plus** `readChangesSource(value string) ([]byte, error)`; prod binds the existing 035 selection/template binders + a `readChangesSource` that calls `resolveChangesSource` over real `os.Stat`/`os.ReadFile`/`term.IsTerminal(os.Stdin)`/`os.Stdin`; tests bind a fake `Execute` + tripwire + an injected `readChangesSource`. `runProposalCreate(cfg) (Outcome, error)`: resolve `--output` (020/035) **first**; the required positional `<tension-id>` is enforced by `ExactArgs(1)` (a missing/extra positional is cobra's arg-count `UsageError(2)`, pre-RunE, no request); require `--changes` presence (else `UsageError(2)` naming `--changes`, **no request**); read the change source via the seam and run `validateChanges` (else `UsageError(2)`, **no request**); assemble, build the executor, marshal `{proposal:{tension_id, changes}}` with the verbatim slice, and send **one** `Execute` `POST /proposals` with `ContentType: "application/json"` (landed 042 field) and **no** `IfMatch`, the tension id passed through unvalidated (plan ADR-1/§200). For `json`/`yaml`, `Execute` into a raw `json.RawMessage` and emit `{data: Proposal}` verbatim via `output.RenderSuccess` (018); for `full`/`compact`/template, `Execute` into `Document[Proposal]` and render via `renderFn(ResourceProposal, ProposalView{doc.Data})` (the single-read shape). On any failure route through `reportFailure` (032). Wire `MustRegister(root, newProposalCommand(...))` in `Assemble()`. Adds no new `Outcome`/`ExitCode`. Never read `ctx.Cred.Token`; issue the request with no client-side Premium pre-check.
  - **Acceptance criteria**:
    - `glassfrog proposal create ten_0123 --changes '[{"type":"CreateRole","name":"Scribe"}]'` POSTs `{"proposal":{"tension_id":"ten_0123","changes":[{"type":"CreateRole","name":"Scribe"}]}}` (changes byte-for-byte) with `Content-Type: application/json` and no `If-Match`; prints the created proposal (incl. `prp_` id, `draft` status); exits 0
    - `--changes changes.json` (existing file) and `--changes stdin` (piped) each source the array and POST it verbatim; an inline value is parsed as JSON directly
    - A missing `<tension-id>` positional, a missing `--changes`, an unparseable/non-array/empty change set, an element lacking `type`, or a bad stdin/file source is a `UsageError(2)` with the right message, **no request sent** (transport tripwire)
    - `glassfrog proposal` (no subcommand) prints help (group, no action); the registration guard accepts the group (≥1 child) and the leaf (action); zero or more than one positional fails `ExactArgs(1)`
    - `*AuthError{NoCredentials}` → UsageError(2); `*AuthError{CredentialError}` → RuntimeError(1); `*TransportError` → NetworkUnavailable(6); `*ResponseError` (403 Premium / 401 → PermissionError(4); 404 unknown tension / 422 rejected change → APIError(3); 429 → RateLimited(5)) via the shared classifier; base-URL/`--output` errors → UsageError(2)
    - A `POST` is never auto-retried on 429 (the 017 `isSafeMethod` gate) — a rate-limited create surfaces on first occurrence (no duplicate proposal)
    - The command issues the request without any client-side Premium feature check (the 403 is server-surfaced)
    - `-o json`/`yaml` emit the raw `{data: Proposal}` payload; `full`/`compact` render the `proposal` projection
    - No new `Outcome`/`ExitCode`/root flag and no `apiclient` change; no token in any output or request body; all branches run offline; `go build`/`go vet` clean
  - **Dependencies**: T001 (`Proposal` model + input shape), T002 (`resolveChangesSource` + `validateChanges`), T003 (`proposal` render key).
  - **Plan reference**: Phase 2 (the command); ADR-1 (group + leaf), ADR-2 (`readChangesSource` seam), ADR-3 (`type` floor + verbatim marshal); Cross-cutting (single-resource `Document[Proposal]`, no new outcomes, no transport change, §133 retry, Premium server-surfaced)
  - **Interface references**: interface-cli.md — Surface (group + `create`), `--changes` flag, Interactions (validation order), Error Communication; interface-spec.md — `internal/cli` additions
  - **Scenario references**: proposal-creation.feature: "A draft proposal is created from an inline change set", "A missing token fails as a not-authenticated usage error", "An unknown anchor tension fails with the API status", "A create on an organization without async proposals is refused", "A rate-limited create is surfaced, not silently re-sent", "The created proposal's id and status are present in structured output", "An invalid output format is rejected before any request", "The change set is read from a file", "The change set is read from piped stdin", "A missing change set is rejected as a usage error", "An empty change set is rejected as a usage error", "An unparseable change set is rejected as a usage error", "A change lacking a type is rejected as a usage error"
  - **Risk**: ⚠️ Reuse `classifyClientError`/`reportFailure`/`RetryExecutor`/`output.RenderSuccess`/`renderFn` — inline no second copy of the error chain, retry loop, or render branch. ⚠️ The seam extends `tensionSeam` with `readChangesSource` — do NOT fork a parallel seam shape. ⚠️ Resolve `--output`, require the positional + `--changes`, read the source, and run the floor BEFORE assembly so the tripwire confirms no request on rejection. ⚠️ Set `ContentType: "application/json"` (landed 042 field) — do NOT re-add an apiclient field; send NO `If-Match` (a create has no prior ETag). ⚠️ Marshal the validated `[]json.RawMessage` verbatim; never reshape or drop change keys. ⚠️ Issue the request without a client-side Premium check (the 403 is the server's to return). ⚠️ Build the group with its child before registering it (guard's ">=1 child" rule). ⚠️ **056 coordination**: if the `proposal` group already exists (056 landed), attach the `create` leaf to it — do not create a second group or a second `newProposalCommand`; render through the shared singular `proposal` view. ⚠️ Capture stdout/stderr in tests with a temp file, not `os.Pipe` (PR #10 LEARNINGS). ⚠️ Never read `ctx.Cred.Token`; no token in the request body.

## Phase 2 — acceptance step: Executable acceptance [Shared]

- [ ] **T005** [Shared] Make the driving scenarios pass as executable acceptance — new `internal/cli` godog suite over `proposal-creation.feature`; un-`@wip` the behavioral scenarios, keep `@validation` held — TestProposalCreationFeatures (Paths = proposal-creation.feature only) drives all behavioral scenarios green over a fake transport (201/403/404/429 + tripwire); the 429 fake proves exactly one POST (no retry); the 3 @validation scenarios stay @wip. Requires single-quote-aware arg splitting (the inline `--changes` JSON is single-quoted) + a proposal transport
  - **Scope**: Add godog step definitions for `features/proposal-write-flow/proposal-creation.feature` in a **new** `internal/cli` godog suite (e.g. `TestProposalCreationFeatures`) whose `Paths` names **only** that feature file (LEARNINGS: a suite points at its own file, never the `features/` directory). Drive `proposal create` through the seam over a fake base `http.RoundTripper` returning canned `POST /proposals` responses (201 with a created proposal, 403 async-proposals-disabled, 404 unknown tension, 429 rate-limit), plus a transport tripwire for the no-request paths (missing positional/`--changes`, empty/non-array/typeless change set), and an injected `readChangesSource` for the inline/file/stdin source scenarios. Remove `@wip` from the spec-derived + architecture-informed behavioral scenarios; keep the 3 `@validation` scenarios `@wip` (held for validate). Grep existing `sc.Step(` registrations and reuse shared exit-code / stderr-substring / structured-output / no-request / "request body carries" phrasings from the tension-capture and `me*`/`roles` suites before writing new bindings; step helpers return errors, never panic, and read parameterized values (status code, source) from world state, not hard-coded literals (LEARNINGS). **Argument parsing**: the inline `--changes` scenarios pass JSON wrapped in **single quotes** (`--changes '[{"type":"CreateRole"}]'`) — the realistic shell form. The landed `splitArgs` (`cross_model_search_bdd_test.go`) only groups on double quotes and toggles `inQuote` on every `"`, so it would split the embedded JSON quotes and corrupt the argument. This suite must parse the command string with **POSIX-style single-quote grouping** (single quotes group a token and preserve embedded double quotes literally) — either by extending/replacing `splitArgs` with a single-quote-aware splitter or by a suite-local one. Verify the inline scenarios yield exactly one `--changes` argv token equal to the JSON.
  - **Acceptance criteria**:
    - Every non-`@validation` proposal-creation scenario has an executable, passing path; `@wip` removed from them
    - The 3 `@validation` scenarios keep `@wip`
    - The 429 fake proves the create is surfaced once and not retried (no duplicate POST); the 403 fake proves the Premium refusal surfaces as the permission outcome; the tripwire fakes prove the no-request rejections; the inline/file/stdin steps each exercise their source path
    - A single-quoted inline `--changes '[{"type":"CreateRole"}]'` invocation parses into exactly one `--changes` argv token whose value is the literal JSON `[{"type":"CreateRole"}]` (embedded double quotes preserved) — proving the suite's splitter does single-quote grouping, not the naive double-quote toggle
    - The new suite's `Paths` names only `proposal-creation.feature`; all `internal/cli` godog suites run and report their own independent scenario counts
    - No real network (fake base transport / loopback only) and no real home/filesystem are touched (stdin/file sources injected); `go build ./...`, `go vet ./...`, and the feature suites run clean
  - **Dependencies**: T004 (the command must exist for the behavioral scenarios to be implementable).
  - **Plan reference**: Phase 2 (the command); System Architecture (single `Assemble()` wiring site); Cross-cutting (testing)
  - **Interface references**: interface-cli.md — Surface, Interactions, Error Communication
  - **Scenario references**: proposal-creation.feature: all behavioral Rule-block scenarios (the 3 `@validation` scenarios stay held for validate)
  - **Risk**: ⚠️ **Arg parsing** — do NOT reuse the landed `splitArgs` as-is: it groups only on double quotes and toggles `inQuote` on every `"`, so it corrupts the single-quoted inline JSON (`--changes '[{"type":...}]'`). Use a single-quote-aware (POSIX-style) splitter and assert the `--changes` token equals the literal JSON. ⚠️ Suite scoping — point the suite at `proposal-creation.feature` only (not the directory); verify it reports its own count. ⚠️ Reuse shared step phrasings before writing new bindings; step helpers return errors, never panic, and read the status code/source from world state, not a hard-coded literal (LEARNINGS). ⚠️ The 429 fake must assert exactly one POST (no retry on the non-idempotent method) so the no-double-create scenario genuinely exercises §133. ⚠️ Inject the change source (inline/file/stdin) rather than touching the real filesystem or a real pipe. ⚠️ Cover the 201, 403, 404, 429, and no-request (tripwire) fakes so the happy, Premium-refused, unknown-tension, rate-limit, and rejection scenarios each exercise their paths.
