# Tasks: Invalid-Create Outcome

**Feature**: 078-invalid-create-outcome
**Concretization**: Full context
**Inputs**: plan.md, spec.md, interface-cli.md, features/success-reported-for-a-dead-proposal/invalid-create-outcome.feature, features/success-reported-for-a-dead-proposal/post-create-validity-read.feature

---

## Dependency Graph

Phase 1: The outcome, end to end (3 tasks, no phase dependencies, strictly sequential: T001 → T002 → T003) [Shared]
Phase 2: Convention-documenting surfaces (1 task, depends on Phase 1) [Shared]

4 tasks total | 0 phases parallelizable, no `[P]` tasks — each task builds on the previous one's symbols | Builder: implement skill or manual Builder

Every task ends green: T001 ships the orientation row alongside the constant (the drift guard derives one from the other), and T003 ships the runner's `~@deprecated` filter alongside the behavior flip. No task leaves the suite red for the next one to fix.

## Branching Guidance

**Pipeline mode**: `spec/078-invalid-create-outcome/base` → `spec/078-invalid-create-outcome/task-1`, …

Simple two-phase build; role-based guidance not needed.

---

## Phase 1: The outcome, end to end [Shared]

- [x] **T001** [US1] Add the `InvalidCreate` outcome and code 8 at all three switch sites, with the orientation row that keeps CI green — 0 scenarios un-@wip'd (the feature's harness lands in T003), `String()` pinned by a new `dispatch_test.go` case
  - **Scope**: `internal/cli/dispatch.go` — `InvalidCreate` appended to the `Outcome` const block **and** its `String()` arm returning `"InvalidCreate"`. `internal/cli/exitcode.go` — `codeInvalidCreate = 8` in the const block, the `ExitCode` switch arm, and a **new** 078 paragraph appended after the 054 one. `internal/cli/errorenvelope.go` — the `kind()` arm returning `"invalid-create"`. `plugin/skills/orientation/SKILL.md` — the exit-code table gains a row whose code is written in backticks (``` `8` ```) and the range sentence becomes 0–8. The three table-test *functions* across those two files are extended in the same commit (`String()` has no table test of its own — see the criteria).
  - **Scope note — why the orientation edit is in *this* task, not the doc sweep**: `internal/build/operatororientation.go` derives the CLI's published code set by regexing `code(\w+)\s*=\s*(\d+)` out of `internal/cli/exitcode.go`, then `diffSets` it **bidirectionally** against the backticked digits in `plugin/skills/orientation/SKILL.md`. Adding the constant without the skill row reports `missing [8]` and fails `TestOperatorOrientationDriftGuard` — so the two edits are one atomic change, and Phase 2 cannot own half of it. The guard reads only the backticked digit, so this needs none of the sweep's final prose wording.
  - **Scope note — the 054 sentence is not edited**: `exitcode.go`'s existing "codeStaleWrite(7) — the first code beyond the originally-published 0–6 band" is a true historical claim about 004's published band (7 *is* the first code beyond 0–6). The 078 paragraph is **added after** it; that sentence stays byte-identical.
  - **Acceptance criteria**:
    - `ExitCode(InvalidCreate)` returns 8; `kind(InvalidCreate)` returns `"invalid-create"`; `InvalidCreate.String()` returns `"InvalidCreate"` (not `Outcome(8)`).
    - **Every length-guarded mirror list** gains the new entry: `exitcode_test.go`'s `publishedCodes`, `outcomeCodes`, and **both** inline `want` maps (there are two — one keyed by `Outcome`, one by name, each with its own `len` guard); `errorenvelope_test.go`'s `cases` table **and** its `allOutcomes` slice (manually maintained and length-guarded — the file's own comment warns it cannot detect a new enum constant on its own). A dropped entry then fails loudly; a missing one cannot pass silently.
    - `exitcode_test.go`'s header comment ("the **eight** frozen exit codes") is updated to nine — one of the five present-tense prose claims this task edits (enumerated below).
    - `plugin/skills/orientation/SKILL.md` documents code ``` `8` ``` in its table and no longer states the range as 0–7.
    - No existing constant, code, or token changes, and the two **historical** narrative sentences stay byte-identical: `exitcode.go`'s "the first code beyond the originally-published 0–6 band" and `exitcode_test.go`'s "Stale-Write Surfacing (054) added stale-write→7, the first code beyond the original 0–6". Both are true claims about 004's published band. The prose this task **does** edit, because each is a present-tense claim the new code falsifies: `exitcode_test.go`'s header count (eight → nine); `exitcode_test.go`'s "ExitCode maps the categories that have producers today…" enumeration; `dispatch.go`'s matching enumeration on the `Outcome` block; `exitcode.go`'s "the full 0–6 convention is published now"; and `exitcode.go`'s "the operational categories (codes 3–6) gain their cases with the future API client", which has been false since 015. All five are present-tense claims the new code falsifies, all sit in files this task already edits, and none is covered by T004 — whose sweep is scoped to surfaces **outside** `internal/cli`.
    - `go test ./...` and `gofmt -l .` are both clean **at the end of this task** — the drift guard is green because the skill row landed here. (The whole-suite gate matches the other three tasks and substantiates the dependency graph's "every task ends green".)
  - **Dependencies**: None
  - **Plan reference**: Phase 1, ADR-1: `InvalidCreate` takes previously-unused code 8
  - **Scenario references**: invalid-create-outcome.feature: "Scenario: The invalid-create code is new and renumbers nothing", "Scenario: The registry stays one-to-one after the new code" (@validation)
  - **Interface references**: interface-cli.md: § "Exit codes — the registry becomes 0–8"
  - **Risk**: ⚠️ Three switch sites, all with non-fatal defaults (`1`, `"runtime"`, `Outcome(%d)`) — an omitted arm ships a plausible-but-wrong signal rather than failing. The table-test additions are the tripwire, not optional polish; `String()` has no table test, so its arm is verified by the `"InvalidCreate"` criterion above.

- [x] **T002** [Shared] The failure-rendering chain: typed error, Diagnose arm, envelope extension — boy-scout: `TestRenderError_EachKindRendersWithoutError`'s kind list was a fifth stale enumeration (missing `stale-write` since 054), swept with the four
  - **Scope**: `internal/cli` — `*invalidCreateError{ProposalID, Alerts}` (token-free `Error()`), a `Diagnose` arm producing `Diagnostic{Category: InvalidCreate, Cause, NextStep}` + carried id/alerts (061 `Feature` pattern), `renderDiagnostic` extended to render one alert line per alert (`  <severity> <path>: <message>`) between cause and next step, `errorEnvelopeFor` mapping the carried id/alerts. `internal/output/error.go` — `ErrorDetail.ProposalID` + `ErrorDetail.ValidationAlerts` (`omitempty`) and the neutral `output.ValidationAlert{Severity, Path, Message}` — **all three fields carrying `omitempty`**, per plan ADR-3, so a key the server omitted stays omitted rather than being reconstructed as `""` (Principle VIII) — with the declared-here/populated-in-cli comment pattern.
  - **Acceptance criteria**:
    - `Diagnose(&invalidCreateError{…})` yields the interface-pinned cause ("the server accepted the create but reports proposal <id> not valid (read back after the create)") and next step (the grammar-reference + web-UI remedy), category `InvalidCreate`.
    - The machine envelope for this failure carries `kind`, `proposal_id`, and — **when the server attached at least one alert** — `validation_alerts` (server key spellings `severity`/`path`/`message`); no `status`, no `body`; `message` does not enumerate the alerts.
    - An invalid draft with **zero** alerts still fails and its envelope carries `kind` + `proposal_id` with the `validation_alerts` key **absent** (`omitempty` omits a zero-length slice — an empty array is not reachable and must not be asserted).
    - **Four** prose enumerations of the envelope's shape are updated, none of them tested by the `len`/comma-ok guards: `ErrorDetail`'s two spelled-out lists (the declaration-order enumeration and the "NextStep, Feature, Status, and Body are omitempty" sentence); `errorEnvelopeFor`'s bulleted field-mapping list in `errorenvelope.go`; and `internal/output/error.go`'s `Kind` taxonomy list, which is **already** missing `stale-write` and gains both that and `invalid-create`.
    - `errorenvelope_test.go`'s pinned `order` slice is **not** extended. It sits inside the plan-limit 403 subtest and `t.Fatalf`s on any listed key the envelope lacks — and a plan-limit envelope carries neither new key, so appending them would redden that subtest and contradict this task's own byte-identical criterion. Instead add a **new** declaration-order assertion over an *invalid-create* envelope covering `message → next_step → kind → proposal_id → validation_alerts`.
    - An invalid draft with zero alerts renders the human diagnostic exactly as every other single-line failure does (`cause — next step`, no alert block and no stray blank line); with one or more alerts the alert lines sit between them.
    - `go test ./...` and `gofmt -l .` clean at the end of this task.
    - Every other failure's envelope is byte-identical (both new keys absent — pinned by an existing-failure envelope test).
    - The human diagnostic renders cause, alert lines in server order (none when no alerts), then " — " + next step.
    - `internal/output` imports no model/transport package.
  - **Dependencies**: T001 (the `InvalidCreate` category exists)
  - **Plan reference**: Phase 1, ADR-2: typed error through the single Diagnose chain; ADR-3: envelope extension; ADR-5: cause and remedy
  - **Scenario references**: invalid-create-outcome.feature: "Scenario: Other failures carry no invalid-create envelope keys" (the `omitempty` uniformity this task's envelope extension owns), "Scenario: An invalid draft with no alerts still fails"
  - **Interface references**: interface-cli.md: § "stdout — machine formats … on the failure", § "stderr — human formats … on the failure"

- [x] **T003** [Shared] Classify the verdict at the create's two render branches and retire the superseded scenarios — 14 scenarios active in a new runner (4 `@validation` held `@wip`); 3 sibling scenarios excluded by `~@deprecated`; 6 Go tests re-pointed or inverted; DEPRECATION.md ticked
  - **Scope**: `internal/cli/proposal.go` — after `readBackProposalVerdict` on **both** branches: when `reason == "" && readBack.Valid != nil && !*readBack.Valid`, construct the typed error and `return reportFailure(…)` before any stdout write; the three success verdict states stay byte-identical (document emission + advisory untouched).
  - **Scope note — the machine branch must bind the decoded proposal**: that branch currently reads `_, readBackRaw, reason := readBackProposalVerdict(…)`, discarding the decoded `Proposal`. Bind it (`readBack, readBackRaw, reason := …`) so the trigger has a verdict to read. Do **not** substitute `created.Data.Valid` from the create's own POST document — 074 established the create response is not a verified carrier of the verdict fields, so that would read nil and silently never fire — and do **not** issue a second read (spec Non-Behavior 5).
  - **Scope note — retiring the three superseded scenarios (the mechanism, not just the tag)**: `@deprecate`/`@deprecated` are currently read by **nothing** — every runner filters `~@wip` only, so a retag alone changes no execution. Three scenarios in `post-create-validity-read.feature` are active (no `@wip`) and assert `exit with code 0` against a **not-valid** read-back — "A created-but-invalid draft surfaces the server's refusal", "Structured output carries the verdict alongside the created id", and "The compact line carries the validity token". They fail the moment this task's behavior lands. In the **same commit**: retag all three `@deprecate` → `@deprecated`, and add `~@deprecated` to the `Tags` filter in `internal/cli/post_create_validity_read_bdd_test.go` (the only runner loading that file, by exact path) so the tag finally carries the exclusion its lifecycle already specifies. The fourth `@deprecate` scenario ("Every verdict state is distinguishable in every output format") is `@validation @wip` — already excluded, so its retag needs no runner change.
  - **Scope note — the BDD harness does not exist yet and this task creates it**: no runner loads `invalid-create-outcome.feature` — no Go file references it, and the sibling runner's `Paths` names only its own feature file. Add `internal/cli/invalid_create_outcome_bdd_test.go`: a godog suite over the new file (`Tags: "~@wip"`, the house shape), its `ScenarioInitializer`, and step definitions for every step text the file uses, composed over the existing `fakeProposalSeam`/`proposalSeqTransport` helpers. **Diff this file's step texts against the sibling suite's registrations before writing any definition** — roughly two dozen are byte-identical (including every step of the two success re-pin scenarios), so an enumeration here would mislead. Every shared text reuses the sibling's implementation rather than gaining a near-duplicate. Two read-back fixture `Given`s are genuinely new. One `Given` is deliberately distinct: this file's "a user template referencing only proposal fields" is *not* the sibling's "…only the proposal fields the create rendered before the verdict existed" — the sibling asserts pre-verdict field paths still resolve, this one asserts the template is never invoked at all, so it gets its own definition and both wordings stay — and use **one** phrasing per assertion: the alert-carrying `Then` is worded identically in every scenario that uses it. Without this file the feature's scenarios never execute and T003's first criterion has nothing to satisfy it.
  - **Scope note — six landed Go unit tests pin the old outcome and must move with the behaviour**: `~@deprecated` is a godog tag filter and has no effect on Go tests. `internal/cli/proposal_readback_test.go`'s shared fixture `proposalReadBackBody` (line ~21) *is* the not-valid body (`"valid":false` with one alert), and six tests assert `Success` against it — `TestRunProposalCreate_HumanVerdictStates` (its `"not valid"` case), `TestRunProposalCreate_InvalidDraftFullOutput`, `TestRunProposalCreate_CompactCarriesVerdict`, `TestRunProposalCreate_MachineEmitsReadBackVerbatim`, `TestRunProposalCreate_MachineFourStateTable` (its `"not valid"` case), and `TestRunProposalCreate_MachineYAML` (which also asserts the stderr advisory this feature suppresses). **All six** additionally pin success-shaped output the failure path removes — the `Validity: not valid` line, the verdict-source advisory, the created id *on stdout*, the exact compact line, and stdout equalling the read-back document. Disposition, in this commit: for the two table-driven tests it is **not** enough to add an expected-`InvalidCreate` row, because their shared loop body asserts `outcome != Success` → `t.Fatalf` and then asserts success-shaped stdout/stderr **unconditionally** — the loop body itself must branch per case (the not-valid case expecting exit 8, the envelope, empty or envelope-only stdout, and no advisory). The four single-purpose tests either re-point at `proposalReadBackValidBody` (keeping their success-path coverage) or invert to assert exit 8 + the envelope + no advisory. `TestRunProposalCreate_PreChangeUserTemplateStillRenders` needs no change — it drives the valid body and a network error, both still successes.
  - **Scope note — the replacements**: every `@pending-deprecation` scenario in `invalid-create-outcome.feature` drops that tag (six of them, the compact failure scenario included — see the tracker), and **every** behavior scenario in the file drops `@wip` as its step definitions land, not only the tagged ones. The `@validation` scenarios keep `@wip`; the one that is `@validation @pending-deprecation @wip` ("The failure is distinguishable from every success state") **keeps `@validation @wip`** — it is held out for independent verification, so "active" does not apply to it. Tick `.score/memory/DEPRECATION.md`'s checklist accordingly.
  - **Acceptance criteria**:
    - All non-`@validation` scenarios in invalid-create-outcome.feature pass: exit 8 with id + alerts + remedy on stderr (human), envelope-only stdout with no server document and no `verdict_source` advisory (machine), exit 8 in `compact` as in `full`, exit 8 for an invalid draft with zero alerts, exit 0 unchanged for valid / valid-with-alerts / no-verdict / failed-read-back, trigger fires on `valid: false` with a non-empty transition set, template never invoked on the failure.
    - The machine-format failure fires for a `valid: false` read-back — i.e. the trigger is exercised on the `--output json` path, not only the human one.
    - No new request is issued: exactly 2 exchanges on the invalid path. This is asserted **both** at the transport (the `proposalSeqTransport` request tripwire the landed tests already use) **and** in Gherkin — the invalid-path scenario carries the sibling file's exact step text, "the create will have been followed by exactly one read of the created proposal", so one definition serves both suites. Neither assertion is optional: the spec's Consumption accord and Non-Behavior 5 forbid a second read, and nothing else would catch a Builder who reaches for one to normalize the alert shape.
    - `go test ./...` and `gofmt -l .` clean **in the same commit as the behavior flip**, on both halves of the collision: the three superseded *scenarios* are excluded by `~@deprecated`, and the six superseded *Go tests* are re-pointed or inverted per the scope note above. Neither half is a follow-up.
  - **Dependencies**: T001, T002
  - **Plan reference**: Phase 1, ADR-4: failure envelope on stdout, no advisory (narrowing of 074 ADR-5); Cross-cutting Concerns (trigger condition, buffer-then-write)
  - **Scenario references**: invalid-create-outcome.feature: every behavior scenario except the two owned by T002 (the envelope-uniformity and zero-alerts scenarios) and the registry ones referenced by T001 — all of which this task's harness must still execute; post-create-validity-read.feature: the four superseded scenarios (governance removal)
  - **Interface references**: interface-cli.md: § "The four verdict states and their outcomes", § "Reading the outcomes in a machine format"
  - **Risk**: ⚠️ The retag is inert without the runner's `~@deprecated` filter — that one-line `Tags` change is what makes this task's "suite green in the same commit" criterion attainable. Retagging alone leaves three failing scenarios.

## Phase 2: Convention-documenting surfaces [Shared]

- [x] **T004** [Shared] Sweep every remaining surface that enumerates the exit-code convention — every enumerated site plus TWO the list missed (`how-to-operate-safely.md`'s table beside its own range sentence; `docs/reference/proposals.md`'s exit table and its now-false "no proposal command introduces its own exit code"); `thenMeaningEachCode`'s bound derived from the CLI constants instead of raised; finding logged in LEARNINGS.md
  - **Scope** (the orientation skill's table row and range sentence already landed in T001 — this task covers everything else). The **property** each site must satisfy: *a surface that enumerates the convention lists every published code, and any stated range ends at the highest published code.* A literal grep for `0–7` is not sufficient — one of the sites below never says "0–7" at all.
    - `docs/guides/how-to-read-exit-codes.md` — **currently ends at code `6` with no row for `7`**, so it is already two codes stale. Add the missing `7` (stale write, 412 — landed with Stale-Write Surfacing) *and* the new `8`. Adding 8 alone would ship a guide in which 8 exists but 7 does not. The reserved notes on codes `3`–`6` are also false now and go with it (they read `*(reserved — see note)*` on code 3 and `*(reserved)*` on 4–6, with the "no command produces them yet" claim in the prose beneath the table).
    - `docs/reference/exit-code-convention.md` — **the canonical registry table**, rows `0`–`7`, plus the claim "All codes are **produced today**" and its per-code producer column. It never writes the range as "0–7", so no range grep finds it; it needs the code-8 row and a producer entry.
    - `docs/explanation/the-agent-operating-surface.md` and `docs/guides/agent-operators/how-to-operate-safely.md` — both write the range as `` `0`–`7` `` with the digits backticked, which defeats a plain `0–7` grep.
    - `docs/explanation/how-failures-are-reported.md` — it scopes format-aware failure rendering to "a transport, API, or decode error", and the invalid-create failure is none of the three (both exchanges succeeded) yet renders through that same chain. Widen the clause, and add the `invalid-create` kind if the document enumerates kinds.
    - `features/unequipped-agent-operators/operator-orientation.feature` — the step "the orientation will state the meaning of each code in the 0–7 convention" — **and** its paired regex in `internal/build/operator_orientation_bdd_test.go` — godog matches by text, so these two must change together or the step goes unmatched.
    - `internal/build/operator_orientation_bdd_test.go` — **two** sites inside `thenMeaningEachCode`: its loop bound `for code := 0; code <= 7` (raise it to the highest published code, or the guard silently stops asserting the newest one) **and** the failure message it returns, `"skill does not document exit code %d in the 0–7 convention"`, which states the range and goes false with it.
    - `internal/build/surface_self_containment_guard_test.go` — the `knownSafeTokens` entry `"exit codes 0–7 cover every outcome"`.
    - `plugin/skills/proposal-drafting/SKILL.md` — the write-path surface that drives `glassfrog proposal create` references orientation only for pagination and says nothing about validity. Add one in-surface line: a create can fail because the server judged the draft invalid, and the draft's id is in the failure. Keep it self-contained (076): no spec numbers, no repo paths.
  - **Acceptance criteria**:
    - Every site listed above is updated; no surface enumerating the convention omits a published code, and no stated range ends below the highest published code — including surfaces that write the range with backticked digits or state no range at all.
    - `docs/reference/exit-code-convention.md`'s producer column names the producing capability for code 8, and its "all codes are produced today" claim stays true.
    - `docs/guides/how-to-read-exit-codes.md` has a row for every code 0–8 with no "reserved" claim about a code that now has a producer.
    - The feature line and its step regex are byte-identical to each other; the loop bound covers the highest published code.
    - The plugin edits pass the 076 self-containment guard (in-surface names only).
    - `go test ./...` and `gofmt -l .` clean.
  - **Dependencies**: T003 (final wording is landed behavior)
  - **Plan reference**: Phase 2: Convention-documenting surfaces
  - **Interface references**: interface-cli.md: § "Consistency Notes" (convention-stating surfaces swept in the same change)
  - **Risk**: ⚠️ Only **one** of the four `internal/build` sites silently weakens — `thenMeaningEachCode`'s numeric loop bound, which a range-string grep cannot find. The other three are stale *text*: the step regex (a grep finds it, but it must move in lockstep with its feature line or godog leaves the step unmatched), the failure message `thenMeaningEachCode` returns, and `surface_self_containment_guard_test.go`'s string — the last being an entry in that guard's `knownSafeTokens` **fixture** corpus, so its strength is identical at 0–7 or 0–8 and it is a truthfulness fix, not a coverage one.
