# Checklist: Invalid-Create Outcome

**Feature**: 078-invalid-create-outcome
**Checked against**: CONSTITUTION.md (v1.1 — 13 principles, I–XIII)
**Artifacts checked**: spec.md, plan.md, interface-cli.md, tasks.md, features/success-reported-for-a-dead-proposal/invalid-create-outcome.feature, features/success-reported-for-a-dead-proposal/post-create-validity-read.feature (the sibling whose scenarios this feature retires), `.score/memory/DEPRECATION.md` § Pending Deprecations
**Checks**: 24 (23 pass, 1 fail) + 5 P2 considerations
**Generated**: 2026-08-20 (round 4 — re-derived from scratch against the current files; no round-3 verdict carried forward)

> Source note: no `accords/governance/` directory is deployed in this repo, so this checklist runs **constitution checks only** — the same standing as sibling specs 024/028/029/030/071/072/074. Done-criteria checks are skipped, not failed. One consequence is load-bearing for the severity column below and is recorded in Governance Notes: task-shape defects (a criterion that is unsatisfiable, self-contradictory, or scoped short of its own stated property) have no constitutional home in this repo, so they surface as P2 considerations regardless of impact.

> Calibration note: this feature reclassifies one existing outcome at one existing seam and extends a published registry. Twelve principles were calibrated to that shape; **VI (Size-Aware by Design)** produced zero applicable checks (see Governance Notes). Every principle in this constitution is phrased as a MUST or MUST NOT, so mechanical severity inheritance puts all 24 checks at P0.

> Verification note: every claim about landed code in this round was re-read from source, never inherited from round 3 or from an artifact's own assertion — `internal/cli/exitcode.go`, `exitcode_test.go`, `exitcode_bdd_test.go`, `dispatch.go`, `errorenvelope.go`, `errorenvelope_test.go`, `diagnostic.go` (`renderDiagnostic`), `me.go` (`reportFailure`), `proposal.go`, `proposal_readback_test.go`, `post_create_validity_read_bdd_test.go`, `stale_write_surfacing_bdd_test.go`, `internal/output/error.go`, `internal/output/selection.go`, `internal/glassfrog/proposal.go`, `internal/build/operatororientation.go`, `internal/build/operator_orientation_bdd_test.go`, `internal/build/surface_self_containment_guard_test.go`, `plugin/skills/orientation/SKILL.md`, `plugin/skills/proposal-drafting/SKILL.md`, `docs/guides/how-to-read-exit-codes.md`, `docs/reference/exit-code-convention.md`, `docs/explanation/how-failures-are-reported.md`, `docs/explanation/the-agent-operating-surface.md`, `docs/guides/agent-operators/how-to-operate-safely.md`, and every `Paths:`/`Tags:` declaration under `internal/` (32 godog suites, all naming a single feature file by exact path).

---

## Summary

| Severity | Count | Pass | Fail |
|---|---|---|---|
| P0 (blocking) | 24 | 23 | 1 |
| P1 (should fix) | 0 | 0 | 0 |
| P2 (consider) | 5 | — | — |
| **Total** | **24 checks** | **23** | **1** |

---

## Changes Since Previous Run

**Previous** (round 3): 1 P0, 0 P1, 4 P2 (1 failure)
**Current** (round 4): 1 P0, 0 P1, 5 P2 (1 failure)

The round-3 P0 is fixed. The round-4 P0 is a different defect, in a different task, found by re-deriving rather than carried forward.

**Resolved** — each re-verified against the current files and the landed code, never against an artifact's claim about itself:

- ~~**P0 (r3 CH-9)** — the Consumption accord's exchange count had no executable acceptance scenario, and T003's criterion phrased the Gherkin half as optional.~~ → **fixed**. `invalid-create-outcome.feature:30` now carries "the create will have been followed by exactly one read of the created proposal" inside the invalid-path scenario ("An invalid draft fails the create with its own exit code", whose Given is the not-valid read-back) — byte-identical to the sibling's `post-create-validity-read.feature:31`, so the existing definition at `post_create_validity_read_bdd_test.go:121` transfers verbatim. T003's fourth criterion is now unconditional: "asserted **both** at the transport … **and** in Gherkin … Neither assertion is optional." The conditional wording round 3 quoted ("If the new suite carries this as a Gherkin step") is gone from the file.
- ~~**P2 (r3-2)** — T001's completion gate was narrower than its three siblings'.~~ → **fixed**. T001 now gates on "`go test ./...` and `gofmt -l .` … **at the end of this task**", matching T002/T003/T004, with the parenthetical stating why ("The whole-suite gate matches the other three tasks and substantiates the dependency graph's 'every task ends green'").
- ~~**P2 (r3-4b)** — T004's scope for `docs/explanation/how-failures-are-reported.md` was conditioned on the document enumerating kinds, which it does not, so the stale scoping clause would survive the sweep.~~ → **fixed**. T004's bullet now names the clause directly ("it scopes format-aware failure rendering to 'a transport, API, or decode error', and the invalid-create failure is none of the three") and makes the widening unconditional; only the kind-enumeration half stays conditional, correctly. Re-verified the quoted clause exists at `how-failures-are-reported.md:10`.
- ~~**P2 (r3-3)** — the per-alert-key absence rule (a server alert omitting `path` rendering as `""` on the failure path while the success path omits it) was undecided in every artifact.~~ → **decided, not fully pinned**. Plan ADR-3 now states it: "The mirror's three fields carry `omitempty` so a key the server omitted stays omitted here too … a reconstructed entry must not turn an absent `path` into `\"\"`." The design no longer fabricates (CH-17 passes on it). What did not land is a criterion or a published field-contract line — see P2-3.

**Newly verified fixed** (named in this round's brief, each checked against current text plus landed code rather than against the round-3 record):

- **T001's prose scope** now owns exactly the four present-tense claims the new code falsifies, and exempts exactly the two historical ones. Re-verified all six sentences exist as quoted: `exitcode_test.go:5` ("names the **eight** frozen exit codes"), `exitcode_test.go:21–27` ("ExitCode maps the categories that have producers today: …" ending at StaleWrite→7), `dispatch.go:54–63` (the matching enumeration on the `Outcome` block, "The category named outcomes with a producer today: …"), `exitcode.go:8` ("Per ADR-2 the full 0–6 convention is published now"); exempt and correctly quoted: `exitcode.go:19–20` and `exitcode_test.go:8–9`, both true claims about 004's published band. One residue of this fix is P2-2.
- **T004 now names both sites inside `thenMeaningEachCode`**. Re-verified at `internal/build/operator_orientation_bdd_test.go`: the loop bound `for code := 0; code <= 7` (`:246`) and the failure message `"skill does not document exit code %d in the 0–7 convention"` (`:248`) are two distinct sites in the same function, and the paired step regex (`:93`) is a third site in the same file, correctly owned by T004's preceding bullet alongside `operator-orientation.feature:45`.
- **T003's disposition for the two table-driven tests** now requires changing the shared loop body, not a row. Re-verified against `proposal_readback_test.go`: `TestRunProposalCreate_HumanVerdictStates`' loop body does `if outcome != Success { t.Fatalf(…) }` (`:405`) and then asserts `wantValidity`, `wantSource`, the id on stdout, and the advisory **unconditionally** (`:408`–`:417`), so an added `InvalidCreate` row could not pass. `TestRunProposalCreate_MachineFourStateTable` (`:629`) has the same shape. The criterion's "All six additionally pin success-shaped output the failure path removes" is accurate, and the excluded test is correctly excluded — `TestRunProposalCreate_PreChangeUserTemplateStillRenders` (`:492`) drives `proposalReadBackValidBody`.

**Carried forward**: r3-4a (no executable human-format zero-alert scenario) — still open, re-assessed downward in risk; see P2-5.

**Closed, not carried**: the `--output stdin` synopsis token. Assessed against `internal/output/selection.go` (`const reservedStdin = "stdin"`, `:17`, with `ParseSelection` resolving the token to `TemplateStdin` at `:138`) and `internal/cli/root.go`'s `--output` help text, which documents the token independently. `stdin` is an accepted `--output` flag value; `supportedFormats` is the format-token list used in error messages, not the set of accepted flag values. The synopsis is correct. Recorded here so the finding is not resurrected a fourth time.

---

## Failures

### Principle VII — Working Software (1/2)

**P0** | CONSTITUTION.md VII: "Every commit and PR MUST include implementation together with its tests, and MUST validate and build. *Detection*: A commit or PR that fails lint/build/tests … is a violation."

**CH-15** → **tasks.md T002, fourth acceptance criterion — the prescribed test edit reddens the suite it is meant to keep green.**

- The criterion reads: "`ErrorDetail`'s doc comment carries **two** spelled-out field lists — the declaration-order enumeration and the 'NextStep, Feature, Status, and Body are omitempty' sentence — and both are updated, along with `errorenvelope_test.go`'s matching pinned list."
- Both doc-comment halves check out and are safely updatable: `internal/output/error.go:25–27` carries "The struct field declaration order is message → next_step → feature → kind → status → body", and `:21–22` carries the omitempty sentence.
- The third artifact does not behave like the other two. `errorenvelope_test.go`'s pinned declaration-order list is `order := []string{"message", "next_step", "feature", "kind", "status", "body"}` at `:192` — and it lives **inside** the subtest `t.Run("feature key present and naming the gate for a recognized plan-limit 403 (061)")`, over an envelope built from `apiclient.ExtractProblem(&apiclient.ResponseError{StatusCode: 403, …})`. That envelope carries neither `proposal_id` nor `validation_alerts`. The loop at `:194`–`:203` does `at := strings.Index(doc, key); if at < 0 { t.Fatalf("key %s missing from a recognized plan-limit envelope") }`.
- So appending the two new keys to that list — the literal instruction — makes `TestErrorEnvelopeFor` **fail**, in the same task whose seventh criterion is "`go test ./...` and `gofmt -l .` clean at the end of this task" and whose eighth is "Every other failure's envelope is byte-identical (both new keys absent)". The two criteria contradict each other on this artifact: criterion 8 is the reason criterion 4 cannot be carried out as written.
- The field placement is not an escape. The interface's pinned example (`interface-cli.md:44–55`) renders `message` → `next_step` → `kind` → `proposal_id` → `validation_alerts`, so the two fields must be declared after `Kind` (either before `Status` or after `Body`). Under **both** placements the plan-limit envelope still carries neither key, so extending that subtest's list fails either way.

*Failure scenario*: the Builder implementing T002 reads criterion 4, appends `"proposal_id"` and `"validation_alerts"` to `order` at `errorenvelope_test.go:192`, and runs the suite. `TestErrorEnvelopeFor/feature_key_present_and_naming_the_gate_for_a_recognized_plan-limit_403_(061)` fails with `key "proposal_id" missing from a recognized plan-limit envelope`. The task cannot go green while satisfying its own criterion. The likely recovery — reverting the list and moving on — silently drops the *real* coverage the criterion was reaching for: nothing then pins the key order of the invalid-create envelope, which is the one envelope the two new keys appear in.

*Remedy*: split the clause. (a) Keep the plan-limit subtest's `order` list at its six keys — it asserts the order of the keys **that** envelope carries, and is correct as it stands. (b) Require instead a new assertion over an **invalid-create** envelope pinning `message` → `next_step` → `kind` → `proposal_id` → `validation_alerts` (the interface's published order), which is where the new keys are observable. (c) Keep the two doc-comment lists in the criterion unchanged — both are genuinely updatable and both need updating.

---

## Passing Checks

### Principle I — Spec Fidelity (2/2)

- **CH-1** | Every request the feature issues maps to a published v5 operation. **PASS** — it issues none. `POST /proposals` and `GET /proposals/{id}` are 074's, unchanged and not re-issued; spec § Integration Boundaries, spec Non-Behavior 5, plan § System Architecture ("this capability adds no API call"), interface § Interactions ("The failure classification adds no request") and T003's criterion all state it. Re-verified at the seam: the trigger reads `readBack` and `reason`, both already returned by `readBackProposalVerdict` (`internal/cli/proposal.go:207` human, `:177` machine), so the failure envelope is assembled entirely from values in hand.
- **CH-2** | The undeclared verdict fields are never presented as contract-defined, and the design degrades rather than breaks if they disappear. **PASS** — `internal/glassfrog/proposal.go:48` types `Valid *bool` ("It is a POINTER, not a …"), and the trigger is `Valid != nil && !*Valid`. Spec § Behavioral Accord makes absence an explicit success ("The failure outcome is reserved for an explicit unfavourable verdict, never for the absence of one"). No 078 artifact calls either field published contract; interface § Consistency Notes only claims the alert entries "reuse the server's own key spellings". If the server stops carrying `valid`, exit 8 becomes unreachable rather than wrong — recorded as a premise dissolution in `.score/memory/DEPRECATION.md` ("that is a premise dissolution, and the whole outcome retires with a fresh entry naming the contract revision"), verified present this round.

### Principle II — Action Transparency (4/4, NON-NEGOTIABLE)

- **CH-3** | The failure names a cause and a next step in every rendering, and the remedy is reachable. **PASS** — interface § stdout field contract pins `message` "always" and `next_step` "always on this failure"; § stderr pins the same two strings ("one source, two renderings"); T002's first criterion pins both texts against the interface. Re-verified reachability: ADR-5 settled the remedy against the landed surface (`withdraw` moves a *circulating* proposal back to draft and deletes nothing), so the next step names the web UI rather than a command the caller cannot run — the remedy is achievable without tripping any sibling condition.
- **CH-4** | In a machine format, nothing an agent needs to recognize the failure is prose-only. **PASS** — `kind: "invalid-create"`, `proposal_id`, `message`, `next_step`, and `validation_alerts` are envelope keys, and exit `8` is the out-of-band signal. Re-verified the path: `reportFailure` (`internal/cli/me.go:252`) takes `format.MachineFormat()`, renders `errorEnvelopeFor(d, err)` through `renderErrorFn`, and `output.RenderError` supports JSON and YAML (`internal/output/error.go:67`). Interface § "Reading the outcomes in a machine format" tables all five rows; the failure is the only verdict-carrying outcome with a non-zero exit and the only one whose stdout carries `kind`.
- **CH-5** | The user-template path's failure rendering is pinned, and consistent with the landed code. **PASS** — interface § "stdout — user template … on the failure" states the template is never invoked and the caller gets the stderr diagnostic plus exit `8`. Re-verified: `internal/cli/proposal.go:149–152` records that "A user template (035) is a human render, so the structured branch is taken only for a built-in json/yaml selection (rt.tmpl is nil then, by construction in resolveRenderTarget)" — so `MachineFormat()` is false and `reportFailure` writes `renderDiagnostic(d)` to stderr. `renderDiagnostic` (`internal/cli/diagnostic.go:330`) returns cause plus " — " plus next step, so both facts are present. The feature file covers it (`:195`, "A user template does not render the failure", asserting the template is not rendered, the id is on stderr, and exit 8). The `stdin` template source resolves through the same rung (`internal/output/selection.go:138`) and therefore the same branch. *Scope note*: on any human format the diagnostic is prose only — the landed 032 convention for every CLI failure, not something 078 introduces, and the machine form is one flag away.
- **CH-6** | The exit code and the rendered outcome agree in every format — no success-shaped document under the failure code, and no failure document under a success code. **PASS** — spec Non-Behavior 8 states the rule with the two-channel rationale (II plus III); ADR-4 decides it (envelope on stdout, advisory suppressed, no verbatim document); interface § "What is no longer emitted on this path" republishes both suppressions; T003's criterion asserts "envelope-only stdout with no server document and no `verdict_source` advisory (machine)". The inverse direction is pinned too: the three success states keep the document plus advisory byte-identically, and the feature file re-pins two of them on the success side (`:108`, `:119`).

### Principle III — Fail Safe, Not Silent (3/3)

- **CH-7** | No failure condition is reported as success, and no error is swallowed. **PASS** — this is the feature's whole content: the accepted-but-dead draft 074 deliberately left exiting `0` now exits `8`. Nothing that previously failed now succeeds, and the read-back's own failures keep their landed never-the-command's-failure contract (interface § "The read-back's failures still never reach an exit code").
- **CH-8** | The `*bool` trigger distinguishes absence from `false` in both directions. **PASS** — plan § Cross-cutting Concerns states it exactly: `reason == "" && readBack.Valid != nil && !*readBack.Valid`. Absence never becomes failure (spec Non-Behavior 1: "failing on absence is the mirror image of reading absence as success"), and `false` never stays success. The `reason == ""` conjunct is load-bearing and correct: `readBackProposalVerdict` returns a non-empty reason on every path that did not answer (including the id-undeterminable short-circuit), and a valid-JSON-wrong-shape body leaves `Valid` nil — so no path can trip the failure on a read that did not happen. Both non-trigger states have scenarios (`:80` no validity field, `:90` unreachable read-back) and both assert "no failure envelope will be emitted".
- **CH-9** | Governance is never left partially applied and the failure never half-emits. **PASS** — the feature adds no mutation. The check sits "after `readBackProposalVerdict` … **before any stdout write**" (plan § Cross-cutting Concerns, interface § Interactions step 2, T003 Scope). Re-verified this is achievable on the machine branch: the stdout write at `internal/cli/proposal.go` happens only after `output.RenderSuccess`, downstream of the read-back call, so the check *replaces* the emission rather than following it. The created `prp_` id is never withheld (spec Non-Behavior 3; interface field table "Never withheld on this failure"; held-out validation scenario `:206`).

### Principle IV — Test-Driven Development (3/3)

- **CH-10** | Every accord bullet and published contract has an executable acceptance scenario. **PASS (newly)** — walked all thirteen accord bullets against the feature file: outcome classification → `:23`, `:169`, `:179`, `:80`, `:90`, `:129`, `:71`, `:60`; registry extension → `:100` plus held-out `:147`; failure rendering → `:27`, `:28`, `:29`, `:35`–`:45`, `:43` ("no server proposal document will be emitted"); **consumption → `:30`**, the round-3 gap, now closed with the sibling's exact step text. Nothing in the accord is now assertion-free.
- **CH-11** | The new feature file has an executable harness, scoped by a task. **PASS** — re-verified the gap is still real and still closed by exactly one instruction: `grep -rn "invalid-create-outcome.feature" internal/` returns nothing, and all 32 godog suites under `internal/` name a single feature file by exact path (no directory globs), so nothing picks the file up implicitly. T003's Scope note creates `internal/cli/invalid_create_outcome_bdd_test.go` with the suite (`Tags: "~@wip"`, the house shape), the initializer, and step definitions for every step text, instructing reuse of the sibling's implementation where the text already exists — which is now load-bearing for two steps, the connection-context `Given` and the exchange-count `Then`.
- **CH-12** | Tasks sequence tests with implementation rather than after it. **PASS** — every task's criteria are assertions, not descriptions. No user-facing behavior lands before its harness: T001 and T002 add dormant machinery (an enum constant nothing produces; two `omitempty` fields and a `Diagnose` arm nothing constructs), each pinned by unit criteria in the same commit; the observable behavior and its runner land together in T003. T001's table-test additions are named "the tripwire, not optional polish", and `String()`'s untested arm is pinned by an explicit `"InvalidCreate"` criterion — re-verified that `String()` (`dispatch.go:114`) has a non-fatal `Outcome(%d)` default and no table test.

### Principle V — Composition over Monolith (1/1)

- **CH-13** | Adding this feature requires no change to unrelated commands. **PASS** — every shared surface is extended additively: `ExitCode` (`exitcode.go:44`), `kind()` (`errorenvelope.go:24`) and `String()` (`dispatch.go:114`) gain arms with no existing arm edited; `ErrorDetail` gains two `omitempty` fields; `renderDiagnostic` gains alert lines that are absent when there are none. T002 pins the guarantee by test rather than by intent ("Every other failure's envelope is byte-identical … pinned by an existing-failure envelope test"). Re-verified that `stale_write_surfacing_bdd_test.go:350`'s `thenExistingCodesUnchanged` is a fixed 0–6 map with no length guard, so the addition cannot break it.

### Principle VII — Working Software (1/2 — see CH-15 above)

- **CH-14** | No task leaves the suite red for the next task to fix. **PASS** — the four couplings were each checked against the landed guard rather than accepted: (a) `internal/build/operatororientation.go:212` regexes `code(\w+)\s*=\s*(\d+)` out of `exitcode.go` and `diffSets` it **bidirectionally** (`:258`) against the backticked digits `` `(\d)` `` (`:303`) in `plugin/skills/orientation/SKILL.md`, so the constant and the skill row must move together — and T001 moves both; (b) `thenMeaningEachCode`'s `code <= 7` bound (`operator_orientation_bdd_test.go:246`) is forward-only, so T001's skill edit keeps it green while asserting less, and T004 raises it; (c) `operator-orientation.feature:45` and its step regex (`:93`) move together inside T004; (d) `surface_self_containment_guard_test.go:58`'s `"exit codes 0–7 cover every outcome"` is an entry in the `knownSafeTokens` **fixture** corpus, not a claim matched against the shipped skill, so it cannot redden between tasks. T003 reconciles both halves of the 074 collision in one commit and says so explicitly ("`~@deprecated` is a godog tag filter and has no effect on Go tests"; "Neither half is a follow-up").

### Principle VIII — No Fabricated Data (2/2)

- **CH-16** | No output value is synthesized, defaulted, or guessed. **PASS** — the id is the server's verbatim; the alerts carry "the server's own values" field-by-field; the slice-level absence rule is stated in both directions (`validation_alerts` omitted rather than rendered `[]`, ADR-3's "no contract may promise an empty array", interface's "Absence means 'the server attached none', never 'there were none to look for'") and pinned by T002's third criterion and the `:60` scenario ("the envelope will not carry a `validation_alerts` key"). The cause and next-step strings are CLI-authored diagnostic prose, never presented as server data. No verdict is derived locally (spec Non-Behavior 2, VISION Exclusion 2).
- **CH-17** | Per-alert-key absence is not turned into a fabricated empty string. **PASS (design)** — ADR-3 now decides `omitempty` on the mirror's three fields with the reason stated ("a reconstructed entry must not turn an absent `path` into `\"\"`"), which resolves the round-3 consideration at the level that owns decisions. `internal/glassfrog/proposal.go:70`'s `ValidationAlert` is three plain strings, so the mapping is a field-by-field copy and the mirror's tags are the only lever — and the decision now points the right way. What is not pinned by a criterion or published in the field contract is P2-3; the design itself no longer fabricates.

### Principle IX — Writes Require Explicit Intent (1/1)

- **CH-18** | No mutation occurs outside an explicit write command, and no read-shaped command mutates. **PASS** — the feature adds no request and changes only how an already-issued write's result is classified. Spec Non-Behavior 6 refuses to widen to `propose`, `withdraw`, or `respond`; interface § "What is deliberately absent" republishes it.

### Principle X — Respect API Limits (2/2)

- **CH-19** | The feature does not increase the request count against the per-organization budget. **PASS** — spec § Consumption, Non-Behavior 5, plan § System Architecture and interface § Interactions ("exactly 2 on a successful create with an attempted read-back. The failure classification adds no request") all state it, and it is now asserted twice: at the transport (`proposalSeqTransport`'s request tripwire) and in Gherkin (`invalid-create-outcome.feature:30`), with T003 declaring neither optional. The Builder-error path round 3 named — reaching for a second read to normalize the alert shape — is now caught by an executable scenario, not only by a criterion the Builder had to choose to honor.
- **CH-20** | No retry, poll, or wait is added, and no `If-Match` obligation arises. **PASS** — spec Non-Behavior 7 refuses to wait ("validity is the server's verdict on the change set as submitted and will not change without a new proposal"); interface § "What is deliberately absent" republishes it; the feature issues no update, so the optimistic-concurrency clause does not apply, and interface's exit-code table correctly marks code 7 "not reachable from this command (no `If-Match` on a create)".

### Principle XI — Governance via Proposals (1/1)

- **CH-21** | No command path mutates governance structure directly. **PASS** — no governance mutation is added and the gated `proposal create` leaf is unchanged: interface § Surface states "No flag is added, removed, or renamed" and refuses an opt-out by name ("there is **no** `--allow-invalid`, `--no-fail`, or equivalent"). The write-safety guardrail's contract facts are untouched because no command surface changed.

### Principle XII — Standalone Executable (1/1)

- **CH-22** | No new external dependency is introduced. **PASS** — the feature is one enum constant, one error type, one `Diagnose` arm, two struct fields and a render extension, over packages already imported. `internal/output` gains no import and T002 pins that ("`internal/output` imports no model/transport package") — re-verified the current imports are `bytes`, `encoding/json`, `fmt`, `sigs.k8s.io/yaml`, so the neutral `output.ValidationAlert` mirror is what keeps that true. The distributed artifact still needs nothing but network access.

### Principle XIII — Self-Contained Operating Surface (2/2)

- **CH-23** | Every task that edits `plugin/` scopes the edit and is covered by the surface guard. **PASS** — two tasks touch the surface, both scoped by path: T001 (`plugin/skills/orientation/SKILL.md` — the table row and the range sentence at `:67`, "convention is a fixed range, 0–7") and T004 (`plugin/skills/proposal-drafting/SKILL.md` — one in-surface line; re-verified that file says nothing about validity today, so the addition is genuinely new content). T004 states the constraint ("Keep it self-contained (076): no spec numbers, no repo paths") and carries the criterion "The plugin edits pass the 076 self-containment guard". T001 is covered mechanically: its `go test ./...` gate runs `internal/build`, where the surface walk lives.
- **CH-24** | No prescribed surface content carries a development-repository reference. **PASS** — the orientation row is specified as a backticked digit plus the interface's published row text ("the server accepted the write but reports the created draft not valid") and its agent reaction ("do **not** blind-retry the same change set …"), neither carrying a spec number or path; T001's Scope note records that the drift guard "reads only the backticked digit". The proposal-drafting line is specified semantically ("a create can fail because the server judged the draft invalid, and the draft's id is in the failure"). Cross-checked the guard's own fixture: the range phrasing itself is a legal operating-world token at either value.

---

## P2 Considerations

**P2-1 | T004's enumerated sweep misses three convention-stating surfaces, and all three are invisible to the grep T004 warns against.** T004's stated property is *"a surface that enumerates the convention lists every published code, and any stated range ends at the highest published code,"* and its scope note explains why the sites are enumerated rather than grepped ("A literal grep for `0–7` is not sufficient — one of the sites below never says '0–7' at all"). Three surfaces meeting that property are absent from both T004's list and plan Phase 2's:

- `docs/reference/exit-code-convention.md` — the **canonical registry reference** for this convention. Its "Code registry" table carries a row per code `0`–`7` (`:25`–`:34`), and the prose beneath asserts "All codes are **produced today**" (`:36`). After 078 lands, the reference document for the exit-code convention omits the newest code entirely. It never writes "0–7", so a range grep returns nothing on it — the exact failure mode T004 was designed to prevent. It is also the surface most likely to be read by an operator looking code `8` up.
- `docs/explanation/the-agent-operating-surface.md:56` — "the `0`–`7` exit-code meanings", a stated range that goes false when orientation documents `8`. Backticked, so `grep "0–7"` misses it.
- `docs/guides/agent-operators/how-to-operate-safely.md:62` — "Every invocation sets a process exit code in `$?`, in the fixed `0`–`7`". Same shape, same grep blind spot.

*Why this is P2 and not a failure*: T004's **criterion** is property-stated, not list-stated ("no surface enumerating the convention omits a published code, and no stated range ends below the highest published code"), so a Builder who honors the criterion rather than the bullet list must find them. The defect is in the enumeration the plan promised would replace the grep. *Cheapest fix*: add the three paths as T004 bullets, and record the two-backtick range form (`` `0`–`7` ``) as a second search pattern beside `0–7`.

**P2-2 | T001's criteria contradict each other on how much prose the task edits.** The third criterion ends "This is the one narrative sentence the task *does* change — see the freeze note below," while the fifth criterion enumerates **four** prose edits ("The prose this task **does** edit … `exitcode_test.go`'s header count …; `exitcode_test.go`'s 'ExitCode maps the categories that have producers today…' enumeration; `dispatch.go`'s matching enumeration on the `Outcome` block; and `exitcode.go`'s 'the full 0–6 convention is published now'"). The "one narrative sentence" claim was true of the round-3 text and became false when the round-4 fix widened the scope — the two criteria now disagree in the same task. A Builder reading top-to-bottom and taking the third criterion as the scope boundary edits one sentence and leaves three false present-tense claims in `internal/cli`, which T001 explicitly owns ("owned here because T004's sweep does not reach `internal/cli`"). The forward reference is also dangling: no criterion or scope note in T001 is labelled a "freeze note" — the intended target is the fifth criterion's byte-identical clause. *Fix*: drop the "this is the one narrative sentence" sentence and point the reference at the fifth criterion by its content.

**P2-3 | ADR-3's per-key `omitempty` decision is pinned by no criterion and published in no contract.** ADR-3 now decides that `output.ValidationAlert`'s three fields carry `omitempty` so an absent server `path` is not turned into `""` (the CH-17 concern). But T002's Scope names only "the neutral `output.ValidationAlert{Severity, Path, Message}` with the declared-here/populated-in-cli comment pattern" — no tags — and none of T002's eight criteria assert per-key absence, while the **slice-level** absence rule gets its own criterion and its own scenario. The interface's field contract is silent too: it says the entries carry "the same three keys the server's entries carry", which is compatible with the decision but does not state it. So the one thing standing between the plan's stated intent and a fabricated `""` is a plan sentence the Builder must read and remember. *Fix*: one clause in T002's second criterion (the mirror's three fields carry `omitempty`, so an alert key the server omitted is omitted here too), and one sentence in interface § stdout's field table.

**P2-4 | Two enumerations inside the very doc comments these tasks edit are not covered by any task's prose scope.** Both are the "spelled-out list drifts past a thematic check" shape, and both sit in files a task already opens:

- `internal/output/error.go:46–47` — `Kind`'s own field comment enumerates the taxonomy: "usage / runtime / network / api (plus the 015-widened permission / rate-limit)". It is already missing `stale-write` (054), and 078 adds `invalid-create`, so post-078 it names 6 of 9 tokens. T002 edits this struct's doc comment and counts "two spelled-out field lists" — accurately, since this is a *token* list, not a field list — but no task reaches it, and T004's sweep does not enter `internal/output`.
- `internal/cli/exitcode.go:41–43` — the `ExitCode` doc comment states "Only the categories with producers today are mapped explicitly; the operational categories (codes 3–6) gain their cases with the future API client," while the switch immediately below maps all of 3–6. T001 takes ownership of already-stale `internal/cli` convention prose by name (that is its stated reason for owning "the full 0–6 convention is published now"), and this sentence meets the same rule one comment block away, but is not in the list. (`dispatch.go`'s sibling claim — "The remaining reserved categories (permission/rate-limit, codes 4/5) gain an Outcome value when their producer lands" — sits inside the enumeration T001 *does* name, so it is arguably already covered.)

Neither is falsified by code 8 in the way the four listed sentences are; both are truthfulness debts a nearby edit makes cheap to settle. *Fix*: add both to T001/T002's prose scope, or state explicitly that pre-078 staleness outside the listed sentences is out of scope — either resolves the ambiguity.

**P2-5 | Still no executable scenario for the human-format zero-alert diagnostic.** T002's fifth criterion pins the shape ("renders the human diagnostic exactly as every other single-line failure does — `cause — next step`, no alert block and no stray blank line"), and interface § stderr pins it in prose ("When the server attached no alerts, no alert lines appear between cause and next step"). The only zero-alert scenario (`invalid-create-outcome.feature:60`) selects `--output json`, and all three human-format failure scenarios (`:23`, `:49`, `:71`) supply an alert — so the human zero-alert shape is asserted by a unit criterion and nothing executable. **Re-assessed downward from round 3's framing**: the risk is smaller than it reads, because `renderDiagnostic` (`internal/cli/diagnostic.go:330`) is the shared single-line renderer for *every* CLI failure, so a T002 edit that inserted a newline or a blank line on the empty-alert path would redden the existing human-failure tests across the suite. The gap is a coverage asymmetry inside this feature's own file, not an unguarded behavior. *Cheapest fix*: one `Then` on the existing `:60` scenario is not enough (it is a machine format) — either add a `full`-format zero-alert scenario, or accept the asymmetry deliberately and say so in T002.

---

## Governance Notes

**Principles producing zero applicable checks**:

- **VI (Size-Aware by Design)** — the feature issues no request at all: it reads no list, requests no page, and adds no truncation surface. The one document it would have emitted on this path is *withheld* in favour of a failure envelope, which is a rendering decision, not a truncation. No check applies.

**Missing done-criteria accords**: `accords/governance/` is not deployed in this repo — verified absent at the repo root this round. Consider creating `done-specify.md`, `done-plan.md`, `done-interface.md`, `done-scenarios.md`, and `done-tasks.md` to enable done-criteria checks. This is a repo-wide governance gap, not a finding against this feature — but it has a visible cost in this round, which is worth naming precisely: **three of the five P2s above (P2-1, P2-2, P2-3) are task-artifact defects with concrete failure modes, and their severity is capped at P2 because no constitutional principle governs task-criterion completeness, satisfiability, or internal consistency.** A `done-tasks.md` with Must-layer criteria (every acceptance criterion binary; criteria mutually satisfiable within a task; a scope list that satisfies the task's own stated property) would let this checklist rate them on their merits instead of filing them as considerations. The single P0 this round (CH-15) reached P0 only because its consequence happens to trip Principle VII's build-and-test detection.

**Structural mismatches**: none. All 13 principles carry a detection mechanism, which is what made mechanical evaluation possible.

**STATUS.md**: not updated by this run — the orchestrator owns `specs/STATUS.md` for this session, so the skill's post-step was deliberately skipped to avoid a concurrent write.
