<!-- Source: docs generated by Score document skill
     Capability: Templated Human Rendering
     Contributing specs: 019-templated-human-rendering
     Artifacts: specs/019-templated-human-rendering/interface-cli.md (Surface, Interactions, Error Communication), specs/019-templated-human-rendering/interface-spec.md (Surface, Interactions)
     Generated: 2026-06-09
     Type: reference -->

# Templated Human Rendering Reference

The two built-in human output templates, `full` and `compact`, and what each renders for every read command.

## Overview

Templated Human Rendering defines two human-readable output formats for the four read commands — `glassfrog me`, `glassfrog me roles`, `glassfrog me actions`, and `glassfrog me projects`:

- **`full`** — the standing, labelled, multi-line projection. This is the default output.
- **`compact`** — one line per record, with ids always present and nested collections rendered as counts.

This capability adds no command and no flag. The commands and their existing flags (`--include` on `me`; `--status` on `me actions` / `me projects`) are unchanged; only the rendering of a successful result is produced through these templates. Selecting `compact` from the command line is done with the `--output` flag (see [Output Format Selection](output-format-selection.md)).

## Common rules

Both formats follow these rules:

- **Ids always present** — the id is written unconditionally.
- **Explicit absence markers** — an absent or blank field renders its marker (`—`, `(none)`, `(no purpose set)`, `(no role)`) rather than an empty value.
- **No fabrication** — every rendered value traces to a field the result carried; no value the API did not return is invented.
- **Plain text** — output is plain text on stdout, with no HTML escaping.
- **No token** — the token is a request header, never a result field, so it cannot appear in rendered output.

## `full` format

`full` is field-equivalent to each command's projection — same fields, same order, same labels.

### `me` full

Labelled identity block. The roles section appears only when `--include roles` was given and the response carried roles.

```
actor:        <name> (<kind>) <actor-id>
organization: <name> (<org-id>)
access:       <access-level>
roles:
  - <name> (<role-id>)
```

### `roles` full

One block per role, with a blank line between blocks. Section headers always render; an empty section shows `    (none)`.

```
<name> (<role-id>)
  Purpose: <purpose | (no purpose set)>
  Domains:
    - <description>
  Accountabilities:
    - <description>
```

### `actions` full

Two lines per action. The description falls back to `—` when blank. The `tags:` segment appears only when tags exist.

```
<action-id>  [<status>]  <description | —>
  role: <role-id>   tags: <t1, t2>
```

### `projects` full

Two lines per project. `role` falls back to the no-role marker. `sub-projects` and `actions` render `yes` or `no`.

```
<project-id>  [<status>]  <description | —>
  role: <role-id | (no role)>   sub-projects: <yes|no>   actions: <yes|no>   tags: <…>
```

## `compact` format

`compact` is one line per record, carrying the essential identifying fields with ids always present, and rendering a nested collection as a count rather than enumerating it.

| Resource | `compact` line |
|---|---|
| `me` | `<actor-id>  <name> (<kind>)  org=<org-id>  access=<level>  roles=<N>` — `roles=<N>` present only when `--include roles` was given |
| `roles` | `<role-id>  <name>  domains=<N>  accountabilities=<N>` |
| `actions` | `<action-id>  [<status>]  <description | —>` |
| `projects` | `<project-id>  [<status>]  <description | —>` |

### Example

`me roles` with three roles:

```
# full                                       # compact
Marketing Lead (role_abc)                     role_abc  Marketing Lead  domains=1  accountabilities=2
  Purpose: Reach the right audience           role_def  Facilitator  domains=0  accountabilities=1
  Domains:                                     role_ghi  Rep Link  domains=0  accountabilities=0
    - Brand voice
  Accountabilities:
    - Planning campaigns
    - Measuring reach
```

## Empty result

When a read returns zero records, the command emits an explicit empty line — no fabricated row, no blank output. The same line is used in both `full` and `compact`.

| Resource | Empty line |
|---|---|
| `roles` | `No roles.` |
| `actions` | `No actions.` |
| `projects` | `no projects` |
| `me` | No empty case — `me` always resolves a single actor. |

The empty-line wording differs across the three list reads (`No roles.` and `No actions.` are capitalized with a trailing period; `no projects` is lowercase with no period). This inconsistency is inherited verbatim from each command's projection.

## Error behavior

Errors are not rendered through a template; they keep their existing cause-plus-next-step format on stderr.

| Condition | stdout | stderr | Exit code |
|---|---|---|---|
| Read succeeds, render succeeds | rendered `full` (or `compact`) output | — | `0` |
| Read succeeds, render fails | nothing (no partial output) | the render error message (token-free) | `1` |
| Read fails (transport / API / auth / decode) | nothing | existing cause-plus-next-step message | existing code (1/3/4/5/6) |

On a render failure, nothing is written to stdout — the result is rendered into a buffer and written only on success.

## Related references

- [Output Format Selection](output-format-selection.md) — the `--output` flag that selects `full` or `compact`.
- [Structured Serialization](structured-serialization.md) — the `json` and `yaml` machine formats.
