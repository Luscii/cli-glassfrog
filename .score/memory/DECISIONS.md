# Decisions

Architectural precedent from the specification pipeline. Each entry records a decision that future specs should follow or explicitly diverge from. Managed by the Score plan skill.

---

- Build the CLI in Go as a self-contained executable (from 001-command-registration, 2026-06-03)
  Chosen over Rust and compiled TS to satisfy CONSTITUTION XII natively (single self-contained binary, no runtime). Foundational language decision inherited by every later spec; built with GOOS/GOARCH cross-compilation and CGO_ENABLED=0 to avoid cgo where supported. ("Fully static" linking is platform-specific and is not the criterion — self-containment is.)

- Adopt cobra as the command framework; its command tree is the command registry (from 001-command-registration, 2026-06-03)
  Sibling capabilities (Argument Dispatch, Help & Version) become thin layers over cobra rather than separate subsystems. Later command specs register into the cobra tree.

- Enforce fail-loud registration through a guard wrapper, not cobra defaults (from 001-command-registration, 2026-06-03)
  cobra tolerates duplicate names and missing summaries; a Register/MustRegister guard validates (non-empty name+summary, leaf-has-action, group-has-children, no sibling collision) before AddCommand. All command registration must go through the guard — direct AddCommand is the bypass to watch.

- Wire commands explicitly in main, not via package init() side effects (from 001-command-registration, 2026-06-03)
  Deterministic ordering and a single legible source of truth for the command set (aligns with CONSTITUTION I). Adding a command = one wiring line + its own package; no existing command is edited.

- Route via cobra's built-in resolution; keep matching exact (from 002-argument-dispatch, 2026-06-03)
  Dispatch relies on cobra's Execute over the assembled tree rather than a hand-rolled matcher: exact match, unknown-command error + best-effort suggestion, and unknown-flag rejection are cobra defaults. cobra's EnablePrefixMatching (a package-global) MUST stay false — prefix/abbreviation matching is a non-behavior; pin it with a regression test.

- Dispatch classifies each outcome into a code-free category for Exit-Code Convention (from 002-argument-dispatch, 2026-06-03)
  Run returns Success / UsageError (two values, matching what the spec names). A distinct RuntimeError for a resolved command's own failure is deferred to Exit-Code Convention (004), the consumer that needs the distinction — until then a resolved command's error is returned uncategorized. Dispatch must NOT emit exit codes (that's 004); the entrypoint maps the category minimally (0 / non-zero) as a documented placeholder.

- Help & Version uses cobra's standard help rendering, not a custom template (from 003-help-and-version, 2026-06-03)
  Listing, per-command usage, flags section, and alphabetical sorting are cobra defaults; future command specs get standard help for free by setting a non-empty Short summary. Alphabetical order depends on the cobra package-global EnableCommandSorting staying true — pin it with a regression test (mirrors 002's EnablePrefixMatching pin). The spec's original "no flags section / no long description" non-behavior was narrowed (clarify 2026-06-03) to forbid only new *required* documentation data.

- Hide cobra's built-in `help` and `completion` commands; keep the `--help` flag (from 003-help-and-version, 2026-06-03)
  cobra injects `help` and `completion` outside the registration guard (LEARNINGS finding). Both *commands* are removed from resolution: replace the default help command with a hidden command under a **non-`help` name** (SetHelpCommand(&cobra.Command{Use: "__help_disabled", Hidden: true})) so the `help` token no longer resolves — `Hidden:true` alone only hides from listings, it does not disable `glassfrog help` — and set CompletionOptions.DisableDefaultCmd=true for `completion`. The `--help` *flag* is retained. Future specs that want a framework built-in surfaced must opt in explicitly.

- Exit codes flow through a single ExitCode(Outcome) registry; 004 is a pure category→code mapper (from 004-exit-code-convention, 2026-06-03)
  `internal/cli/exitcode.go` is the single source of truth: named constants pin the frozen convention 0 success / 1 internal / 2 usage / 3 API / 4 permission / 5 rate-limit / 6 network-unavailable, and `ExitCode(Outcome) int` maps categories with a Fail-Safe default of 1 (an unmapped/future category never yields 0). Producers classify into the `Outcome` category; 004 only maps — it never re-inspects the error (continues 002 ADR-2). Codes 3–6 are published as constants now but the live `Outcome` enum gains a category only when its producer exists; the future API client adds APIError/PermissionError/RateLimited/NetworkUnavailable + their ExitCode cases at this one site, taking the reserved codes. Never renumber an assigned code. Pin uniqueness, the no-shell-reserved range (126/127/128+N), and the exact values with regression tests (mirrors 002/003 package-global pins).

- RuntimeError resolved as the catch-all internal-error code (1); dispatch's default arm reclassified (from 004-exit-code-convention, 2026-06-03)
  002 deferred the distinct RuntimeError category to 004. Resolution: a resolved command whose own action errors is now classified `RuntimeError` (added to the `Outcome` enum, mapped to code 1) — `dispatch.Run`'s default arm returns `RuntimeError, err` instead of `Success, err`. The two deferral tests (`TestRun_RuntimeActionError_IsSuccessCategory`, `TestRun_RuntimeError_NotMisclassifiedAsArgError`) were updated to assert RuntimeError. Until the API client classifies finer, every resolved-command failure exits 1.

- `main` recovers from panics and exits 1 to avoid the Go panic→exit-2 collision (from 004-exit-code-convention, 2026-06-03)
  An unrecovered Go panic terminates with status 2, which collides with `UsageError = 2`. To honor the accord ("unexpected internal failure → 1, never 0/never aliased to usage"), `main` installs a deferred recover that writes the panic value + stack to stderr (Action Transparency) and `os.Exit(1)`. Future entrypoint changes must preserve this recover.

- Token concerns live in a new `internal/auth` package; the credentials-file read and write sides share one format module (from 005-credential-discovery, 2026-06-03)
  Token Authentication's three capabilities split as: Discovery (005) contributes the resolver + the shared `.glassfrogrc` reader; Credential Storage (006) adds the writer to the same package and MUST round-trip with that reader; Request Authentication (007) consumes the resolved credential (it lives with the API client, not in `internal/auth`). Resolution stays out of `internal/cli` — it registers no command and makes no API call (CONSTITUTION V).

- Credentials file format and env var: `.glassfrogrc` npmrc-style `key=value` with a `token` key, and `GLASSFROG_TOKEN` — both `[ASSUMED]` (from 005-credential-discovery, 2026-06-03)
  Hand-rolled minimal reader (no INI/dotenv dependency — CONSTITUTION XII), `#`/blank tolerant, unknown keys ignored (forward-compatible), a non-comment line without `=` is a parse error (not silently skipped). File name + env var are centralized constants and `[ASSUMED]` pending reconciliation with Credential Storage (006) — 006 must conform to or jointly revise this contract before both ship.

- Credential Discovery returns a code-free `Resolution{Token, Source, Path}`; absence is a normal outcome, broken files are errors (from 005-credential-discovery, 2026-06-03)
  Mirrors 002's "resolver classifies, consumer maps" split. `Source` is an enum (`Environment`, `File`, or `None`) with a separate `Path` (the file path when `Source` is `File`, empty otherwise); `None` is a non-error "nothing found"; a read/parse failure of an existing file is a typed error naming the path. The token value never appears in output, logs, or error strings (secret hygiene; CONSTITUTION II). 007 consumes this shape and reports `Source`/`Path` (never the token) as the active identity. No exit codes here.

- Filesystem/env-dependent resolvers inject their start/home roots rather than reading process globals (from 005-credential-discovery, 2026-06-03)
  The pure resolver takes `startDir`/`homeDir` (and reads the env var through a thin seam); a small production wrapper binds the real `os.Getwd`/`os.UserHomeDir`/`os.Getenv`. Keeps unit tests hermetic and — critically, given the secret — guarantees tests never read the developer's real `~/.glassfrogrc` (CONSTITUTION IV).

- The credentials-file writer lives in `internal/auth` beside Discovery's reader; a write→read round-trip test is the format contract (from 006-credential-storage, 2026-06-04)
  Storage adds `writeCredentials(path, token)` to `internal/auth`, sharing the one format module with 005's reader (no second implementation of the `.glassfrogrc` shape). The binding guard is a round-trip test: write a token, resolve it back through Discovery's reader. Because 005 is not yet implemented, whichever of 005/006 lands first creates the shared reader+constants to 005's interface-spec; the round-trip test pins the shape either way. `.glassfrogrc`/`GLASSFROG_TOKEN` stay `[ASSUMED]`, now jointly held — change once, in the shared constants.

- Operator-facing credential/transport concerns stay in their command/client layer; only the file concern lives in `internal/auth` (from 006-credential-storage, 2026-06-04)
  Storage is the first credential *command*: a guard-registered cobra leaf in `internal/cli` (explicitly wired in `main`, with an `Args` validator and a `Short`) that gathers input and delegates the file write to `internal/auth`. Mirrors how 007 will keep request-attachment with the API client, not in `auth` — `auth` stays command- and network-free (CONSTITUTION V). Exact command name/group/flags are interface-level, deferred.

- Connection Configuration lives in `internal/apiclient`; Base URL Resolution is its first capability and produces the base transport's base URL (from 008-base-url-resolution, 2026-06-04)
  Resolves the `[ASSUMED]` seam/package 007 flagged: 007's `AuthTransport` wraps Connection Configuration's base transport, and the resolved base URL is that transport's root. `internal/apiclient` (created by 007) is now the fixed home for connection/transport concerns — base URL, timeouts, retries — NOT `internal/auth` (which stays the credential-file, network-free concern) and NOT `internal/cli` (no command, no API call). The connection-context half (combine base URL + token, assemble the `http.Client`) is a deferred later spec.

- Base URL resolves on the precedence chain flag → env → `.glassfrogrc` `base_url` → built-in default, and always yields a value (from 008-base-url-resolution, 2026-06-04)
  Mirrors 005's env-first walk-up (reuses `candidateDirs` + nearest-wins) but adds a `--base-url` flag rung on top and a compile-time default backstop at the bottom — so, unlike 005's `Resolution`, there is NO `None`/absent outcome. Result is a code-free `BaseURL{Value, Source, Path}` with a `Source` enum (`Flag`/`Environment`/`File`/`Default`), continuing the 002/004/005/007 "producer classifies a code-free outcome, consumer maps it" split. A non-empty source supplying a non-`http(s)` URL is a typed code-free `BaseURLError` naming the source (no fall-through, fail loud); whitespace-only / absent key falls through; the default is never re-validated. Open downstream gap (mirrors 007's): 004 has no "bad configuration" code (code 4 is API-side) — the consuming-command spec decides.

- A second `.glassfrogrc` key (`base_url`) is read through 005's one shared reader, never a parallel parser; the token is never returned to non-secret callers (from 008-base-url-resolution, 2026-06-04)
  `internal/auth` exposes a network-free, secret-safe `base_url` file read over the existing `candidateDirs` walk + `parseCredentials` step, returning only the `base_url` value (+ path), never the token — so base-URL code paths never hold the secret. No second `.glassfrogrc` reader is written (LEARNINGS: two readers of one file drift). The `base_url` file-key constant lives in `auth` beside `tokenKey` (a format detail); `GLASSFROG_BASE_URL`, the `--base-url` flag name, and the default URL are `internal/apiclient` constants. The three names (`base_url`, `GLASSFROG_BASE_URL`, `--base-url`) are `[ASSUMED]`, jointly held with Credential Storage (shared file); the default URL is a fixed constant `https://glassfrog.com/api/v5` — the `/api/v5` path from the spec's `servers` block, the host `https://glassfrog.com` *inferred* from `info.contact.url` (not normatively specified by the OpenAPI spec; see risk H-1). `auth`'s scope broadens honestly from "the token" to "the `.glassfrogrc` file" while staying network- and command-free.

- Request Authentication is an `http.RoundTripper` middleware wrapping Connection Configuration's base transport; lives in the API-client package, consumes (never re-resolves) Discovery's `Resolution` (from 007-request-authentication, 2026-06-04)
  007's auth layer wraps the base transport so the base is reached only on the authenticated branch — "no request is ever sent unauthenticated" becomes structural, not caller discipline. It holds an injected `func() (auth.Resolution, error)` (prod = 005's resolver) and maps the outcome to a typed, code-free `AuthError{Kind}` — `NoCredentials` (Discovery `Source: None`) vs `CredentialError` (wraps Discovery's path-only read/format error); the consuming command does `errors.As` and maps to an exit code (007 decides none, continuing 002/005's producer-classifies/consumer-maps split). Code lives in the API-client package (`internal/apiclient` `[ASSUMED]`), NOT `internal/auth` (keeps auth network-free). The composition seam with Connection Configuration and the package name are `[ASSUMED]` — reconcile before either ships (parallel session); whichever of 007/Connection-Configuration lands first creates the package (mirrors 005/006). Open downstream gap: 004's frozen convention has no local "cannot authenticate" precondition code (code 4 is API-side rejection) — the consuming-command spec must decide.

- Interactive-input commands inject the stdin/TTY/env/dir seam, same as filesystem resolvers (from 006-credential-storage, 2026-06-04)
  Extends 005's injected-roots rule to the input side: a pure resolver takes the candidate sources (positional arg, stdin reader, an `isTTY` flag, env lookup) + `startDir`/`homeDir`; a thin production seam binds real stdin/TTY/`os.Getenv`/dirs and the non-echoing prompt. A single `isTTY` signal drives both "read piped stdin" and "prompt only when interactive". Keeps every precedence/interactivity branch hermetically testable and tests off the real terminal and home.

- The generic `.glassfrogrc` read/parse/walk lives in `internal/rcfile`; `auth` and `apiclient` are consumers (refactor, 2026-06-06)
  Supersedes the placement in 005 ADR-1 / 006 / 008 ADR-3 that put the shared `.glassfrogrc` reader, parser, and `candidateDirs` walk in `internal/auth`. Those decisions were right that there must be **one** reader (no parallel parser); they were wrong to home it in `auth`, which is the *token/secret* concern — the file format, the `key=value` parse, the nearest-wins walk, and the typed `FormatError`/`ReadError` are generic and have multiple consumers (token, base URL, future settings). `internal/rcfile` now owns them: `FileName`, `Settings`+`Value(key)`, `Parse`, `Read`, `ReadValue(path,key)`, and `Resolve(startDir,homeDir,key)` (the one walk). Every setting is retrieved through this one parameterized path, so consumers can't drift. `internal/auth` keeps the token domain (`Resolution`, `Source`, env short-circuit, the writer, secret hygiene) and re-exports `FormatError`/`ReadError` as type aliases for its credential-command consumers; the `token` key stays in `auth`, the `base_url` key moves to `apiclient` (each domain owns its key). Secret hygiene becomes structural: `rcfile.Resolve(…, baseURLKey)` returns only the base_url value, so a base-URL read can never come into possession of the token. `auth.ResolveBaseURLFile` (008) is deleted — `apiclient` calls `rcfile.Resolve` directly. Future consumers add a key constant in their own package and call `rcfile.Resolve`/`ReadValue`; they never write a second parser.

- Secret files are written atomically (temp-in-same-dir + chmod 0600 before bytes + rename) and merged line-preserving (from 006-credential-storage, 2026-06-04)
  An existing credentials file is parse-validated via the shared reader (malformed → format error, NO write), then rewritten at the line level — replace the `token=` value, append if absent, preserve every other line and comment. The write is atomic: a temp file in the same directory, `chmod 0600` (`[ASSUMED]`, POSIX-conditional) before any token bytes, then `rename` over the target — so a mid-write failure leaves the original intact and the secret is never briefly world-readable. Storage emits a code-free `Outcome` (UsageError for no-token/no-override, RuntimeError for write/format failures) mapped by the existing `ExitCode` site; no new codes, no `os.Exit` in the command (continues 002/004/005).

- Connection Context Assembly is a transparent aggregator: `Assemble` calls 008's and 005's resolvers once and always returns a code-free `ConnectionContext` carrying both outcomes (incl. errors) (from 009-connection-context-assembly, 2026-06-06)
  The connection-context half 008 deferred. `internal/apiclient`'s `Assemble(resolveBaseURL, resolveCred) ConnectionContext` invokes BOTH injected resolvers (no short-circuit on the first error — carry-both), packs `BaseURL`+`BaseURLErr` and `auth.Resolution`+`CredErr` verbatim, and returns the context with NO `error` of its own — assembly never fails, never refuses, decides no exit code (continues the 002/004/005/007/008 producer-classifies/consumer-maps split). Readiness is derived (`Complete()` = base URL present AND token present (`Source != None`) AND no errors), reported not decided. The refuse-to-call fail-safe stays in 007. Purely additive (`context.go`); no existing file changed. `AssembleFromOS(flagValue)` binds the real resolvers; the pure `Assemble` takes injected seams (005/008 ADR-5 pattern), fail-fast on nil (no nil-default — PR #20). Client assembly (the `http.Client`/base transport) is NOT here — it moves to Request Execution (010), refining 008's "connection-context half assembles the http.Client" forecast.

- The connection context is the single resolution point and carries the token; Request Execution (010) replays it into 007's `AuthTransport` rather than re-resolving — resolves the 007/009 `[ASSUMED]` seam (from 009-connection-context-assembly, 2026-06-06)
  `ConnectionContext` carries the full `auth.Resolution` (token included), so resolution happens once per invocation, at assembly. 010 will build `NewAuthTransport(baseTransport, func() (auth.Resolution, error) { return ctx.Cred, ctx.CredErr })` — a replay thunk over the context's cached credential, NOT a fresh Discovery walk — so identity+endpoint are stable across calls by construction and the two walks can't drift. 007's `authorize` still yields the token on the present branch and the typed `AuthError` (`NoCredentials`/`CredentialError`) on the absent/errored branches, so 007's fail-safe refusal fires at request time, unchanged — 007's code is untouched by 009 (the wiring lives in 010). Because the context holds a secret, it gets a value-receiver redacting `String()` (the `auth.Resolution.String()` precedent); readiness reasons are source/path-only, never the token. Settles the seam both 007 and 008 flagged `[ASSUMED]`; consider `/score:deprecate` to retire the open seam note.

- The API-client seam is a `Client` built once from the `ConnectionContext` via `NewClient(ctx, base) (*Client, error)` (with a `NewClientFromOS(ctx)` production seam binding the real base transport); `(*Client).Execute` is the single send seam (from 010-request-execution, 2026-06-06)
  Realizes the `http.Client` 008 forecast and 009 deferred. `internal/apiclient`'s `NewClient(ctx ConnectionContext, base http.RoundTripper)` (and its thin `NewClientFromOS(ctx)` production seam that binds the real base transport) is built ONCE per invocation (the context is already resolved once at assembly) and holds the configured `*http.Client` (base transport + 007's `AuthTransport` over the replay thunk + a request timeout); endpoint commands call `(*Client).Execute`, never a hand-built client — that is the "every command reinvents transport plumbing" problem this capability kills. 011–017 depend on this seam. Purely additive in `internal/apiclient`; no existing file changed; registers no cobra command (prints nothing, no `os.Exit`). The exact `Client`/`Execute`/request-descriptor API is interface-level (`/score:interface`).

- 010 owns the base-URL fail-fast (in `NewClient`); the token fail-safe stays in 007's `AuthTransport` at send time (from 010-request-execution, 2026-06-06)
  Resolves spec fork A (option b). `NewClient` returns `ctx.BaseURLErr` verbatim and builds nothing when the endpoint is missing/errored (010's own concern — fail loud at the source, not as a doomed wire call); it does NOT inspect the token. It wires `NewAuthTransport(base, func(){ return ctx.Cred, ctx.CredErr })` (009 ADR-2's replay thunk), and 007's `authorize` refuses at `RoundTrip` time on the absent/errored credential — so the two refusals stay split (no-endpoint = build-time/010; no-token = send-time/007) and the `NoCredentials`/`CredentialError` taxonomy is never re-decided outside 007. 007's code is untouched.

- The API client produces typed code-free errors (`TransportError`, generic non-2xx `ResponseError{status,headers,body}`, `DecodeError`); the consuming command adds the `Outcome` cases and maps them — `apiclient` never imports `internal/cli` (from 010-request-execution, 2026-06-06)
  Continues the producer-classifies/consumer-maps split (002/004/005/007/008/009) and refines 004's "the API client adds APIError/PermissionError/RateLimited/NetworkUnavailable + their `ExitCode` cases" forecast: the *client* (010) produces the typed errors but wires NO exit code (it has no command and no exit path); the first consuming *command* (011) does `errors.As` and adds the `Outcome` categories + `ExitCode` cases at `internal/cli`'s single registry, taking the reserved codes (transport → 6 network-unavailable; non-2xx → 3/4/5 once 015/017 classify). 010's non-2xx `ResponseError` is GENERIC and carries status+headers+body so 015 reads the detail and 017 reads the 429 rate-limit headers off the same value (010 never interprets or retries — one bounded `Do` with a timeout, no retry; 017 layers backoff above the seam). `errors.As(err, &authErr)` runs BEFORE wrapping a `Do` error so 007's fail-safe is propagated unchanged rather than mislabeled as transport. 010 never reads `ctx.Cred.Token` — the replay thunk is the token's only path.

- API response models live in a new `internal/glassfrog` schema package; the read commands decode into shared types, never command-local duplicates (from 011-identity-read, 2026-06-07)
  Resolves Q1. The `GET /me` (and every later read) body decodes into plain JSON-tagged structs in a NEW `internal/glassfrog` package — `MeResponse{Actor,Organization,Membership,Roles []Role}`, `Actor{ID,Name,Kind}`, `Organization{ID,Name}`, `Membership{AccessLevel,…}`, and a minimal `Role{ID,Name}`. Schema only — no transport, no cobra, no exit codes — so it is a leaf package both `internal/cli` and `internal/apiclient` import without a cycle, keeping `apiclient` transport-only (010) and `cli` free of domain types. `Role` is shared from day one: `me --include roles` decodes it AND My Roles (012) decodes it (012 grows the SAME type to the full role shape — accountabilities/domains/assignments — never a second type). 012–017 add their resource models (Action, Project, Proposal, …) here. Decoding is tolerant of unknown/extra fields (forward-compatible). The token is never a field (it is a request header, not a response field).

- `--base-url` registers as a persistent flag on the root command; API commands read it by cobra inheritance, not by re-registering (from 011-identity-read, 2026-06-07)
  Resolves Q2 and closes the "first consuming command registers the flag that triggers a real call" gap 008/010 forecast. The `--base-url` flag (name from the existing `apiclient.FlagBaseURL="base-url"` constant, so the precedence rung and the registered flag name can't drift; the usage/help string is an implementation detail kept consistent with the spec) is registered ONCE as a persistent flag on the root in the wiring layer; `me` and every future API command (012–017, proposals) read its resolved value via inheritance and pass it to `AssembleFromOS(flagValue)`. One definition for the whole CLI, no per-command duplication; global connection options live at the root. Cost: root help now shows a global-flags section (the affected 003 help tests are updated in the same change; permitted — 003's narrowed non-behavior forbids only new *required* documentation data, and a persistent flag with a description is optional) and the flag appears (inert) on non-API commands like `version`/`auth` (accepted).

- The read surface classifies 010's typed client errors through one `internal/cli` helper into the `Outcome` enum; 011 adds the operational categories + `ExitCode` cases at the single registry (from 011-identity-read, 2026-06-07)
  011 is the first consuming command, so it populates the codes 004 reserved: it adds `NetworkUnavailable` and `APIError` to the `Outcome` enum (`dispatch.go`) and their cases to `ExitCode` (`exitcode.go`), taking reserved codes 6 and 3, and introduces a shared `classifyClientError(err) Outcome` (`internal/cli`) — the single `errors.As` chain over 010's typed errors — reused verbatim by 012–017 (no per-command inlined chains). `ExitCode` stays a pure mapper (the classifier produces the category; `ExitCode` only maps; `default→1` fail-safe unchanged). The mapping (ADR-4): `*TransportError`→`NetworkUnavailable`(6); generic non-2xx `*ResponseError`→`APIError`(3, the residual "general API error" bucket — 015/017 later SPLIT 401/403→permission(4) and 429→rate-limit(5) as new cases WITHOUT renumbering 3); `*AuthError{NoCredentials}`→`UsageError`(2, "not authenticated — run `glassfrog auth login`", mirroring `runLogin`'s no-token→Usage); `*AuthError{CredentialError}`/`*DecodeError`→`RuntimeError`(1, mirroring `runLogin`'s FormatError→Runtime); base-URL error→`UsageError`(2, operator-supplied bad endpoint). No new exit codes invented — local/precondition failures reuse 1/2; reserved 3–6 are for genuine API-side/network outcomes. The classifier table test carries an exhaustiveness guard (PR #10 LEARNINGS: `len`+comma-ok so a dropped mapping fails loud).

- Each read command pairs a pure `run<Read>` + a pure `format<Read>` projection renderer behind an injected seam; the reshaped projection is the agent-legible default, `--output json` a future flag (from 011-identity-read, 2026-06-07)
  Extends 006's `runLogin`/`loginSeam` pattern to the read surface: a thin cobra `RunE` over an injected seam (prod binds `AssembleFromOS`/`NewClientFromOS` + the real base `http.RoundTripper`; tests bind a fake transport returning canned responses) delegating to a pure `runMe(cfg)` over injected values, with a pure `formatMe(MeResponse) string` projection renderer and a pure `validateInclude([]string)` that rejects an unsupported `--include` target BEFORE any request (fail-fast usage error, 002 invalid-input convention). Every branch (success-decode, non-2xx, transport, no-token, decode-fail, base-URL-error, include-rejection, roles-embed, empty-roles) runs offline; a tripwire fake asserts no request is issued on include-rejection. `formatMe`/`validateInclude` are unit-tested pure. Command-level scenarios become a NEW `internal/cli` godog suite (the `credstorage_bdd_test.go` pattern) pointed at its OWN feature file `features/self-service-reads/identity-read.feature`. The reshaped projection (actor id/name/kind, org id/name, membership access level; roles id+name when embedded) is the default; structured `--output json` is a deferred future cross-cutting flag (a Non-Behavior here).

- My Roles (012) conforms to Identity Read (011)'s landed self-service-read scaffolding rather than creating its own (from 012-my-roles, 2026-06-07)
  011 landed first and owns the shared scaffolding, so 012 (a parallel session) conforms — the "other conforms" half of the first-to-land rule (005/006, 007/008). `glassfrog me roles` (`GET /me/roles`) is a `NoArgs` subcommand registered under 011's **runnable** `me` command (a command that both runs and parents children — confirm the 001 guard permits this); it inherits the persistent root `--base-url` (011, name from `apiclient.FlagBaseURL`), routes 010's typed errors through 011's shared `classifyClientError` (adding NO new `Outcome` category and NO `ExitCode` edit), and **grows the shared `internal/glassfrog.Role`** (adds Purpose/Accountabilities/Domains) plus a `MyRolesResponse{Data,Meta{Pagination}}` wrapper — never a command-local type. Exit-code reconciliation (developer decision 2026-06-07): adopt 011's mapping — the local auth fail-safe maps to `UsageError`(2, NoCredentials, "run `glassfrog auth login`") / `RuntimeError`(1, CredentialError), mirroring `runLogin`; `PermissionError`(4)/`RateLimited`(5) stay reserved for 015/017. Supersedes 012's earlier pre-conformance plan (auth→4 / new `PermissionError`, a self-created `me` group), which never landed in code. Incompleteness signal (first-page-only + `has_next_page`) is a stderr note at exit 0.

- Paginated list reads share one `glassfrog.Pagination` + one list-envelope shape; each read adds only its resource model (from 013-my-actions, 2026-06-07)
  Extends 011 ADR-1 (API models live in `internal/glassfrog`) to list reads. The `{data: [T], meta: {pagination}}` envelope and the `Pagination` struct (`PerPage`, `HasNextPage`, `NextCursor`) are defined once — by My Roles (012), the first paginated `/me*` read — and reused by My Actions (013) and My Projects (014); each list read adds only its resource model (`Action`, `Project`), never a second pagination type or envelope. Whichever paginated read lands first creates the shared types in `glassfrog` (the 005/006/007 first-to-land-creates pattern); the others reuse. First page only + a "more results available" signal when `HasNextPage`; the multi-page walk is Pagination (016)'s, deferred.

- Spec-enumerated filter flags on the read surface are validated locally against the spec set before any request, reusing one validator across siblings (from 013-my-actions, 2026-06-07)
  Applies 011's `validateInclude` fail-fast precedent to filter flags. `--status` is checked against the spec's status set (`archived, cancelled, completed, current, scheduled, someday, waiting`, sourced from the `spec/glassfrog-api-v5.yaml` enum) by a pure `validateStatus` BEFORE context assembly or any request; an unsupported value → `UsageError`(2) naming the bad value and the supported set, with a transport tripwire asserting nothing was sent. Chosen over API pass-through during verification: pass-through returns an opaque `APIError`(3) indistinguishable from a real failure, and `/me/actions` does not even document a `400` for a bad status (silent-wrong-results risk). Introduced by My Actions (013), reused verbatim by My Projects (014); the set tracks the spec.

- A non-2xx is refined by a pure `internal/apiclient` function that wraps 010's `ResponseError` into a typed code-free error carrying the RFC 9457 detail; later response-interpretation capabilities follow the same shape (from 015-api-error-extraction, 2026-06-07)
  API Error Extraction adds `ExtractProblem(*ResponseError) *ProblemError` (the interface pinned these symbols — `ProblemError`/`ExtractProblem` — deliberately distinct from the `cli.APIError` *Outcome* to keep the `classifyClientError` mapping legible) in `internal/apiclient` — the home for response types (008/009/010), NOT `internal/glassfrog` (schema-only) or `internal/cli` (maps/presents). The typed error **wraps** 010's `*ResponseError` (via `Unwrap`) so `errors.As(err, &ResponseError)` still matches and the raw carrier (body/headers) stays reachable; consumers must branch on status (or check `*ProblemError`) BEFORE the bare `*ResponseError` arm. `ExtractProblem` is **total** (never returns an error, never panics): best-effort parse of the RFC 9457 Problem Details body (`type`/`title`/`status`/`detail`, NOT Content-Type-gated), graceful degradation to a `http.StatusText` fallback on an empty/HTML/non-conformant body (raw body always preserved), and the **HTTP status authoritative** over the body's `status` member. Response-side only — never reads the request token. (Rate-Limit Handling (017, landed) operates on the same `ResponseError` carrier from above the `Execute` seam — reading the 429 + rate-limit headers to retry/back off — but does NOT itself refine into a typed error; it defers the 429→rate-limit(5) classification to 015, see the next entry.)

- API Error Extraction fills the reserved exit codes 4 (permission) and 5 (rate-limit) by splitting the generic `APIError`(3) at the shared `classifyClientError` registry on status; the `apiclient` capability decides no exit code (from 015-api-error-extraction, 2026-06-07)
  Reconciles spec 015's "must not decide the process exit code / stay narrow" with the 004/011 forecast that "015/017 split 401/403→permission(4) and 429→rate-limit(5)": the split is **consumer-side** ("callers branch on the code"). `classifyClientError` (the ONE shared `internal/cli` chain, 011) maps the API error's status — **401/403 → `PermissionError`(4)**, **429 → `RateLimited`(5)** (taking the already-reserved `codePermissionError`/`codeRateLimited`), everything else → `APIError`(3); `PermissionError` and `RateLimited` join the `Outcome` enum (`dispatch.go` + `String()`) and `ExitCode` (`exitcode.go`); no code is renumbered. The split is purely additive (only 401/403→4 and 429→5 change, both from 3 — reserved codes, not a renumber), so 011–014 are otherwise unchanged. The extracted `detail` reaches the operator through the shared `formatClientErrorMessage`, refined **once** at the `reportClientError` chokepoint (`*ResponseError`→`*ProblemError`) so all reads benefit with no per-command edit and the category+message are computed from the same typed value; fallback to the existing "status N" wording when the body wasn't parseable, plus a per-class next-step hint (permission: "check the token's access / membership"; rate-limit: "the API is rate-limiting; retry later"). The classifier table test keeps its `len`+comma-ok exhaustiveness guard (PR #10). **429→rate-limit(5) ownership resolved (PR #37):** landed 017 does the 429 retry/backoff and its spec defers the *classification* to 015, so **015 classifies (exit 5), 017 handles (retry); 015 never backs off** — composition 010 sends → 017 retries → 015 classifies the final outcome. 403 plan-limit *messaging* stays with the Unsignalled Plan Limits problem (015 maps 403→permission, not to plan-availability guidance).
- The multi-page pagination walk lives in a new `internal/paging` package as a generic `All[T]` over an `Executor` seam; library-only, no command, no exit-code edit (from 016-pagination, 2026-06-07)
  Realizes the multi-page walk §109 deferred to 016. `internal/paging` imports `apiclient` (the one-method `Executor` seam that `*apiclient.Client` satisfies, plus its typed errors + `Request`/`Response`) and `glassfrog` (the envelope), keeping `apiclient` schema-agnostic (`Execute(out any)` unchanged) and `glassfrog` logic-free. `All[T any](reqCtx, ex Executor, req, opts...) Result[T]` issues one `Execute` per page through the seam; prod passes `*Client`, tests pass a fake `Executor` that decodes canned pages into `out` (hermetic, no HTTP). Like 010, purely additive: NO cobra command, NO `Outcome`/`ExitCode` change — the walker's `Result.Stop` is mapped by the *consumer*'s existing `classifyClientError` (§101). The walk is unbounded (no mandatory cap; a `WithMaxPages` option stays open) with a non-advancing-cursor guard: `has_next_page=true` while the cursor fails to advance — `next_cursor` blank/absent **or identical to the cursor just sent** — → a typed `MalformedPageError` (fail loud, never loop) that falls to the classifier's `default→RuntimeError(1)`, so no exit-code renumbering. The repeated-cursor arm (not just blank) is load-bearing: an API that ignores an unrecognized `cursor` param would otherwise loop the walker on the same page (PR #36 review #1).

- The shared paginated list envelope is the generic `glassfrog.Page[T]`; paging state is read from the decoded body's `meta.pagination`, NOT a response header — supersedes the per-resource concrete envelope (from 016-pagination, 2026-06-07)
  Refines §109. The canonical list envelope is generic `glassfrog.Page[T any]{Data []T; Meta Meta{Pagination}}` over the shared `Pagination{PerPage,HasNextPage,NextCursor}` — "each read adds only its resource model" expressed as the type arg `T`, with zero per-resource boilerplate. SUPERSEDES the per-resource concrete `MyRolesResponse{Data,Meta}` named in 012/013 plans (none landed — all *Analyzed* — so no refactor); when 012–014 implement they decode `Page[Role]`/`Page[Action]`/`Page[Project]`, single-page or walked. CORRECTS 010 plan §120 + `execute.go`'s comment ("016 reads Link/paging headers"): the v5 API carries paging in the response BODY at `meta.pagination` (012 already reads it there), so the walker decodes an envelope target — 010's `Execute` exposes only status+headers on `*Response`. Whichever of 016/012-014 lands first creates `glassfrog.Pagination` + `Page[T]`, the others reuse. Consider `/score:deprecate` on the per-resource-envelope note.

- Paginated reads surface an explicit `Result[T]{Records,Complete,Stop,Pages}` value, never a bare `([]T, error)` (from 016-pagination, 2026-06-07)
  Enforces 016's partial-flagged-incomplete contract (CONSTITUTION VI): a cut-short walk returns the records gathered so far with `Complete:false` and a non-nil `Stop` cause, so a caller can never mistake a partial set for the whole; the reflexive Go `if err != nil { return }` would discard the partial records — the precise footgun the explicit value prevents. `Complete == (Stop==nil)`; `Records` is always the gathered set; `Stop` holds a 010 typed error or `MalformedPageError`. Page size defaults to 500 (the API max, to conserve the rolling rate budget) via a `WithPageSize(n)` functional option; the walker clones the caller's `req.Query` per page and sets only `per_page`+`cursor` (preserving `q`/`include`), with no silent clamp — an out-of-range size surfaces the API's `400` as the `Stop` cause. The `--per-page` flag and consumer adoption (012–014 switching from first-page-signal to `paging.All`) are each the consuming command's own change.

- Cross-cutting request behaviors that must re-send or sleep (retry, backoff) wrap the `Execute` seam from ABOVE; the per-`Do` transport stack stays one-shot so `client.Timeout` keeps bounding a single attempt (from 017-rate-limit-handling, 2026-06-07)
  Rate-Limit Handling (017) is a retrying executor — `RetryExecutor` built by `NewRetryExecutor` (pinned by interface-spec; `(*RetryExecutor).Execute` mirrors `(*Client).Execute`'s signature) — in `internal/apiclient` that calls `(*Client).Execute` in a bounded loop and inspects 010's generic `*ResponseError` (`StatusCode==429`, `Retry-After`/`X-RateLimit-*` off `.Header`) — realizing 010's "017 layers backoff above the seam." Chosen over an `http.RoundTripper` retry middleware in the client stack: a transport-level retry would break 010's one-`Do`-per-`Execute` tripwire AND a `Retry-After` sleep inside `RoundTrip` would be counted against `client.Timeout` (which bounds one attempt), tripping the request timeout on a legitimate backoff. Above-`Execute` keeps each attempt one timed `Do` with the sleep BETWEEN independent attempts. 010 is untouched. The retry lives in ONE shared `apiclient` helper the read path routes its send through (the `classifyClientError` one-place discipline), not re-inlined per command.

- The retry/backoff layer's `sleep` and progress-note sink are INJECTED seams; `internal/apiclient` reaches for no `time.Sleep`/`os.Stderr` directly (from 017-rate-limit-handling, 2026-06-07)
  017's `RetryExecutor` (built by `NewRetryExecutor`) takes a `sleep func(time.Duration)` (so cap/backoff tests assert durations without ever blocking — CONSTITUTION IV) and an `io.Writer` progress sink (prod binds `cmd.ErrOrStderr()`, tests bind a buffer); fail-fast on nil (no nil-default — PR #20). The per-wait note carries only the wait/attempt/cap — never the token (017 never reads it; the `*ResponseError` holds only the response side). Extends the injected-OS-seam discipline (005/008/009/010) to a cross-cutting behavior that must sleep and emit diagnostics, keeping the transport package hermetic and command-free. Retry is also gated on `isSafeMethod` (GET/HEAD) so a non-idempotent write is never silently re-sent on a 429; caps (`MaxAttempts`, `MaxTotalWait`, `FallbackBackoff`) are named `[ASSUMED]` constants (tuning deferred, as 008/010 deferred their defaults).

- A 429 surfaced after retry exhaustion stays a generic `APIError(3)` until API Error Extraction (015) types it; rate-limit *handling* (retry) and rate-limit *classification* (exit code 5) are separate capabilities (from 017-rate-limit-handling, 2026-06-07)
  Resolves the 011 `classifyClientError` "015/017 refine it later" shorthand in favor of 015. 017's spec Non-Behavior forbids it from classifying/renaming the 429 into a meaningful typed error — that is 015's job (typing non-2xx responses). So **017 itself** adds NO `Outcome` category and NO `classifyClientError`/`ExitCode` edit, and `internal/apiclient` never imports `internal/cli`. The runtime classification of a capped-out 429 therefore depends on whether 015 has landed:
  - **Pre-015** (017 alone, 015's producer not yet present): a capped-out 429 stays a generic `*ResponseError` → `APIError(3)` via 011's then-unchanged classifier. Code 5 (rate-limit, reserved by 004) is unfilled.
  - **Post-015** (015 landed the producer in PR #37): 015 adds the 429→`RateLimited`(5) arm to the shared classifier, so a capped-out 429 classifies to rate-limit(5). 017 is unchanged either way — only 015 touches the classifier.

  Composition order is the same throughout: 010 sends → 017 retries/backs off → 015 classifies the final outcome. Mirrors the 2026-06-07 LEARNINGS (#33-r2): a read command must not interpret a status its Non-Behaviors reserve for a sibling.

- Structured/machine output lives in a new pure leaf package `internal/output`; the JSON/YAML serializers (018) and the `--output` selector (020) share it — human templating (019) is a SEPARATE `internal/render` package (reconciled 2026-06-08) (from 018-structured-serialization, 2026-06-08)
  First-to-land-creates the shared home (005/006/007 pattern). `internal/output` holds no cobra and no transport — imported by `internal/cli` without a cycle, like `glassfrog`/`apiclient`. 018 ships `Format` (`JSON`/`YAML`), `RenderSuccess(Format, json.RawMessage)`, and the `ErrorEnvelope` type + `RenderError`. Selection, decode-target choice, and the typed-error→envelope mapping happen at the `cli`/020 boundary; 018's renderers are pure encoders. (Reconciled 2026-06-08: 019 human rendering lives in a SEPARATE `internal/render` package, NOT an extension of `internal/output` — machine serialization and human templating are distinct concerns with different test shapes; 020 routes between the two packages by format.) 020 adds the flag + router.

- Structured output serializes the RAW response bytes (captured via a `json.RawMessage` decode target), never the tolerant typed `glassfrog` structs (from 018-structured-serialization, 2026-06-08)
  Spec fork B requires the raw payload verbatim, all fields intact; the typed structs ignore unknown fields (011 ADR-1) and `Execute` discards the raw bytes, and decoding numbers into a generic `any` would coerce to float64. So when a structured format is active the command decodes the 2xx body into `json.RawMessage` — a valid `out` for the EXISTING `Execute`, so 010 is unchanged — and `output.RenderSuccess` renders the bytes (JSON: stdlib normalize; YAML: `yaml.JSONToYAML(raw)`). The typed-struct path is retained for the human projection (019). "JSON ≡ YAML" holds by construction (YAML derived from the JSON bytes); no number-precision loss. The raw error body (`*ResponseError.Body`) is already bytes and nests into the envelope verbatim.

- YAML serialization uses `sigs.k8s.io/yaml` (`JSONToYAML` on raw bytes); a statically-linked Go module is NOT a CONSTITUTION XII violation (from 018-structured-serialization, 2026-06-08)
  Go has no stdlib YAML. `JSONToYAML([]byte)` transforms the raw JSON bytes, preserving fidelity + the same-data guarantee with no float64 coercion (chosen over `yaml.v3`, which would need manual json.Number handling, and over hand-rolling). CONSTITUTION XII governs RUNTIME installs on the operator's machine — a compiled-in module is fine. Developer-approved. First outside dependency the output surface introduces; record in PROJECT.md stack. Pulls `gopkg.in/yaml.v2` transitively.

- 018 defines the unified error envelope shape `{ error: { message, kind, status?, body? } }`; `kind` reuses the existing `classifyClientError` taxonomy, so 018 needs NOTHING from 015 (from 018-structured-serialization, 2026-06-08)
  Clarify resolved that every failure renders as one unified envelope 018 defines, in the active format, including bodiless failures (transport/auth/decode — `status`/`body` omitted). The typed-error→envelope mapping reuses the existing `errors.As` discrimination (kind from the classifier arms; `status`+raw `body` from `*ResponseError`; `message` from the token-free error string), and lives at the `cli`/020 boundary so `internal/output` need not import `internal/apiclient`. API Error Extraction (015, landed) supplies the widened taxonomy (`permission` 401/403, `rate-limit` 429) and `*ProblemError` detail that 020's mapping carries into `body`/`message` WITHOUT changing the envelope contract — same separation as 017↔015 (handling vs classification). No build dependency (`internal/output` does not import `internal/apiclient`). Exact field names are interface-level.

- Human result rendering uses Go stdlib `text/template` in a new `internal/render` package; built-in `full`/`compact` are embedded (`//go:embed`) templates per result type (from 019-templated-human-rendering, 2026-06-08)
  The Output Formatting cluster's human half. `internal/render` parses one template set ({me,roles,actions,projects}×{full,compact}) and exposes a buffer-then-render entry point keyed by resource + format name; it depends only on `internal/glassfrog` (the result structs) + stdlib, and must not import `cli`/`apiclient` (mirrors the apiclient-never-imports-cli layering). `text/template`, not `html/template` (plain-text CLI output, no auto-escaping). Data-fidelity guarantees (CONSTITUTION: present only API data) live in template text — `Option("missingkey=error")`, `{{if .X}}…{{else}}<marker>{{end}}` guards that render each landed explicit-absence marker (`—`/`(none)`/`(no purpose set)`/`(no role)`, or omit, e.g. `me`'s roles section) without inventing a *data value*, ids written unconditionally, an explicit per-command empty line inherited verbatim (`No roles.` / `No actions.` / `no projects`) for empty sets, and a `len`-based `roles=<N>` count for compact nested collections — pinned by golden/unit tests (full golden = the captured pre-019 projection; a registry exhaustiveness guard asserts every resource has both formats, PR #10 shape). Render errors are buffer-then-write → nothing partial on stdout, a built-in-template failure maps to `RuntimeError(1)` (like 011's undecodable-body). The standing CLI format is `full` (byte-equivalent to today); `compact` is built + unit-tested but reachable from no operator surface until 020 wires `--output`. 020 selects a built-in by name; 029 registers caller-supplied templates into a clone of the same set (no second engine). **Cross-spec reconciliation with 018 (resolved 2026-06-08):** human rendering keeps a *separate* `internal/render` package, distinct from 018's `internal/output` (machine JSON/YAML). Chosen over folding both into one package because human templating (`text/template`, absence markers, golden-pinned projections) and machine serialization (raw-bytes JSON/YAML, no float coercion) are different concerns with different test shapes — one mixed package has weaker cohesion. 020 routes between the two packages by selected format. 018's entry is amended to match.

- DIVERGENCE: the per-command pure `format<Read>` Go projection renderer (011 ADR-5) is superseded by the shared `internal/render` template seam; the per-command formatter becomes a per-result-type template (from 019-templated-human-rendering, 2026-06-08)
  011 ADR-5 (§103–104) established "each read pairs a pure `run<Read>` + a pure `format<Read>` projection renderer … the reshaped projection is the default, `--output json` a future flag." 019 keeps the `run<Read>` + injected-seam half and the "reshaped projection is the default" intent, but moves rendering out of `cli` into `internal/render`: `formatMe`/`formatMeRoles`/`formatMeActions`/`formatMeProjects` are deleted, their output becomes each read's `full` template, and future reads render through the seam rather than adding a `formatXxx`. Candidate for `/score:deprecate` so the superseded precedent is explicit.

- Output Format Selection's four-format vocabulary (`OutputFormat{Full,Compact,JSON,YAML}`) and its precedence resolver live in `internal/output`, mirroring `apiclient.baseurl` resolution (from 020-output-format-selection, 2026-06-08)
  Claims the selector slot 018 reserved in `internal/output` (the JSON/YAML serializers' home), keeping the whole output vocabulary in one leaf rather than in `apiclient` (a connection concern) or `cli` (command code 029/032 would then import). `ResolveFormat` is a pure core over injected (flag, env, rcfile walk) + a thin production OS/rcfile seam (005 inject-roots), resolving flag → `GLASSFROG_OUTPUT` → `.glassfrogrc` `output` → built-in default `full`, and always yielding a format — the same chain shape as 008's base URL. The `output` rcfile key is the THIRD `.glassfrogrc` key (after `token` 005, `base_url` 008), read through the one shared `internal/rcfile` reader, never a parallel parser; `internal/output` imports `internal/rcfile` (leaf→leaf, no cycle). `OutputFormat` exposes `IsStructured()` + a mapping to 018's `output.Format` for json/yaml; the human→`render`-format mapping stays in `cli` so `internal/output` never imports `internal/render`. Literal flag/env/key names + type spellings are interface-level. Consumed by 032 (failure rendering) and 029 (user templates).

- `--output` (`-o`) is a persistent flag on the root command, read by cobra inheritance; name from a centralized constant (from 020-output-format-selection, 2026-06-08)
  Silent conformance to 011 ADR-2 (`--base-url` persistent-root-flag): registered once in `root.go`, inherited by every current/future result-producing read via `cmd.Flags().GetString(...)`, accepted on either side of the subcommand by cobra's interspersed parsing (satisfies the spec's "anywhere on the path"). Flag name from an `output.FlagOutput`-style constant so the resolver rung and the registered flag can't drift (mirrors `apiclient.FlagBaseURL`). `-o` is a new short alias (no collision; `--include` is long-only). Inert appearance on non-result commands (`version`/`auth`) is the same accepted wart as `--base-url`.

- `internal/cli` owns the output dispatch; a shared generic render-dispatch selects decode target + renderer per resolved format, so `output` and `render` stay non-importing siblings (from 020-output-format-selection, 2026-06-08)
  `internal/output` (machine) and `internal/render` (human) never import each other; only `cli` imports both. A single generic dispatch (`renderResult[T]`-style) in `cli` owns the 018-ADR-2 decode-target choice — `json.RawMessage` + `output.RenderSuccess` when `IsStructured()`, else the typed struct + `render.Render(resourceKey, humanFormat, v)` — so the four reads delegate instead of each carrying the branch (the 016 generic-`All[T]` / 019 centralized-key discipline). A render error from either renderer maps to `RuntimeError(1)` via the existing classifier (buffer-then-write, nothing partial on stdout). Dispatch signature + human-format mapping are interface-level.

- An invalid/unreadable `--output` selector fails fast as `UsageError(2)` before any request, naming its source; resolution is independent of connection assembly (from 020-output-format-selection, 2026-06-08)
  `ResolveFormat` distinguishes absent-at-a-source (skip to next rung) from present-but-invalid-at-a-source (`*output.FormatError{Source,Value}`), and surfaces `internal/rcfile` read/format errors — the same loud-surfacing 008 applies to a malformed `base_url`. The read resolves the format FIRST (before assembly/request, so no wasted call — the `validateInclude` fail-fast shape), reports the token-free message to stderr, and returns `UsageError`; `classifyClientError` gains an `*output.FormatError`→`UsageError` arm symmetric with its base-URL arms (no new exit code). Case-insensitive parse over the four tokens (`JSON`/`Json`/`jSON`→JSON); only the four names valid in any casing. Output-format resolution is NOT in `AssembleFromOS` — it is a presentation concern resolved on the render path, orthogonal to the connection context (009).

- Adopt GoReleaser in build-only mode as the single canonical build mechanism; one `.goreleaser.yaml` `builds` block, no release/archive sections yet (from 021-self-contained-executable-build, 2026-06-08)
  Realizes the 001 foundational build decision (Go, `CGO_ENABLED=0`, GOOS/GOARCH matrix, self-containment-not-full-static). A single `builds` entry for `glassfrog` (`main: .`, `CGO_ENABLED=0`, four targets: darwin/linux × amd64/arm64 — NO Windows). Canonical invocations: `goreleaser build --snapshot --clean` (matrix, snapshot — no tag needed) and `goreleaser build --snapshot --clean --single-target` (local dev, host GOOS/GOARCH). Chosen over a Makefile/shell-script loop specifically to avoid a SECOND build path: 022 (Automated Release Pipeline) and the Homebrew Tap already need GoReleaser, so 022 EXTENDS this file with `archives`/`checksum`/`release`/`brew` rather than introducing a parallel build. `builds.ldflags` is left free of version logic as 023's (Version Embedding) injection seam. The build host needs Go + `goreleaser`, but neither is an ARTIFACT dependency — CONSTITUTION XII governs the produced binary's runtime, not the build host. Output lands under `dist/`. The `.goreleaser.yaml` file is intentionally PARTIAL after 021; 022 owns completing it, must not re-decide `builds`.

- Self-containment (CONSTITUTION XII) is verified by run + per-platform OS-only linkage inspection, guarded by a config-drift test; proven on the host target, cross-target breadth deferred to 022 (from 021-self-contained-executable-build, 2026-06-08)
  The spec's per-target contract: run a produced binary on a clean host of its own target and confirm it executes with only OS-provided libraries. Implemented in a dedicated `internal/build` package (NOT `internal/cli`): `TestSelfContainment_HostBinary` obtains a host-target binary via `HostBinary` (dist-artifact-preferred — `DiscoverDistBinary` reads `dist/artifacts.json`; else `buildHostBinary` with `CGO_ENABLED=0` so plain `go test` still runs), runs the produced binary with `exec.Command(bin, "version")` and asserts exit 0 (a no-network version-class command — it runs the external binary, NOT a TestMain re-exec), then inspects dynamic-library linkage via `extractDeps` (otool on darwin / ldd on linux) against `osOnlyViolations`'s per-platform OS allowlist (Linux: none / loader only — fully static; macOS: only `/usr/lib` + `/System` — `libSystem` is OS-provided, so self-contained ≠ fully-static). A companion config-guard — `CheckConfigGuard` over `LoadConfig`/`ParseConfig` in `internal/build/config_guard_test.go` — asserts the `.goreleaser.yaml` matrix is EXACTLY the four targets and `CGO_ENABLED=0` (change-detector rigor: fail on a MISSING target as loudly as an extra one — guards cgo-drift and Windows-creep at config level, independent of which arch the runner executes). Cross-target EXECUTION of the check (multi-arch runners / emulation) is 022's CI concern. (Corrected 2026-06-08 against the shipped `internal/build` package — the plan-era "reuses `internal/cli/smoke_test.go`" / `.goreleaser` notes did not match the implementation.)

- Complete the single `.goreleaser.yaml` with `archives`/`checksum`/`release` and trigger on `release: published`, attaching to the EXISTING release (from 022-automated-release-pipeline, 2026-06-08)
  Extends 021's build-only `.goreleaser.yaml` (does NOT touch `builds` or `ldflags` — 023's seam). Adds `archives` (one `tar.gz` per target, name carries tool/version/OS/arch), `checksum` (single `sha256` `checksums.txt`), and a `release` section configured to upload into the existing published release WITHOUT authoring notes or changing pre-release/latest status. Workflow `.github/workflows/release.yml` triggers on `release: { types: [published] }` with `permissions: contents: write`; GoReleaser derives the version from the git tag. Clean 022↔#30 seam: Release Drafting owns notes/draft/status, 022 owns binaries+checksums. Homebrew Tap (#36) later adds a `brew` section to the same file. Entirely within the repo (only `GITHUB_TOKEN`); no signing/notarization (integrity = checksums).

- Cross-target self-containment is a BLOCKING release gate via build-once + per-target verify matrix (from 022-automated-release-pipeline, 2026-06-08)
  Owns 021's deferred cross-target execution. Build/package once with `goreleaser release --skip=publish` → upload `dist/` as a CI artifact → fan out a 4-runner matrix (linux amd64/arm64, darwin amd64/arm64), each running 021's run+OS-linkage check against its target binary → publish ONLY if build + every leg passes. Extends the spec's atomicity from "build failure aborts" to "build-or-verification failure aborts"; the verified bytes are the published bytes. Native runners preferred, QEMU emulation the fallback. This is artifact verification (does the shipped binary run cleanly on its target?), distinct from the behavioral test suite (#24/#29).

- Fill the reserved `builds.ldflags` seam to inject `internal/cli.version` in `vX.Y.Z` shape; `.goreleaser.yaml` build config is now complete (from 023-version-embedding, 2026-06-08)
  Closes the seam 021 reserved and 022 left untouched. The single `builds.ldflags` entry becomes `-X github.com/Luscii/cli-glassfrog/internal/cli.version=<v-prefixed version>`, injected on EVERY GoReleaser invocation (snapshot via 021's `--snapshot`, release via 022's `goreleaser release` — GoReleaser derives the version from git, so no extra pipeline wiring). Injected shape carries the `v` prefix to match Go build-info's `Main.Version` form, so `--version` reads consistently across release builds and source installs. The exact template token (`{{ .Tag }}` vs `v{{ .Version }}`) is interface-level. The `internal/build` matrix config-guard parses but does NOT assert `ldflags`, so filling it does not trip that guard — but that also means a future blanking of `ldflags` is uncaught by the matrix guard; a focused config assertion that the real config injects a v-prefixed value into `internal/cli.version` is the regression guard (blank seam, stale symbol, malformed `-X`, or dropped `v` prefix). With this, `.goreleaser.yaml` is complete (builds=021, archives/checksum/release=022, ldflags=023); Homebrew Tap (#36) later adds only a `brew` section.

- `--version` value is resolved by a pure three-tier precedence (injected → Go build-info → placeholder), surfaced verbatim; the injected `version` var defaults to empty (from 023-version-embedding, 2026-06-08)
  A pure `resolveVersion(injected string, info *debug.BuildInfo, ok bool) string` in `internal/cli` (co-located with 003's version handling — no cycle reason to split, `runtime/debug` is stdlib) returns: injected if non-empty; else `info.Main.Version` VERBATIM when build info is available and non-empty (`(devel)`, pseudo-versions, real tags all pass through unchanged — confirmed decision, no normalization); else a non-empty placeholder constant (`0.0.0-dev`). Both 003 wiring sites (`configureHelpAndVersion` → `root.Version`, `newVersionCommand`) read the resolved value, preserving 003's byte-identical version-unify property. KEY CHANGE to 003: `var version` now defaults to `""` (empty = "not injected", the sentinel that triggers the build-info branch) instead of `"0.0.0-dev"`; the placeholder moved from the var default to the resolver's tier-3 return. 003's `TestVersionDefaultPlaceholder` relocates to assert the resolver's placeholder. Resolution is offline (no runtime network/VCS — CONSTITUTION XII corollary); 023 supplies the value, 003/cobra owns formatting (bare string, no commit/date metadata).

- `roles` is ONE runnable cobra command with an optional positional id (0 args → org-wide list, 1 arg → single read); the positional-id shape forecloses `roles <id> <subcommand>` (from 025-role-reads, 2026-06-08)
  First org-wide read (vs 012's token-scoped `me roles`) and the Governance Reads dependency root. `roles` registers once (001 guard + explicit `main` wiring) with `Args: MaximumNArgs(1)`; RunE branches on `len(args)`. Filters are list-only, `--include` is single-only; each cross-combo (filter+id, --include-without-id, >1 id) is a fail-fast `UsageError(2)` before assembly with a transport tripwire (011/013). CONSEQUENCE for the downstream per-role reads (Role Domains #33, Role Policies #34, Role Projects #38) and Organization Tree (#26): cobra can't distinguish a role id from a subcommand name, so those reads CANNOT be children of `roles` — they need a singular `role <id> domains` group or a flag-based surface, NOT `glassfrog roles <id> domains`.

- The shared `glassfrog.Role` grows to the full spec shape; `RoleDetail` embeds `Role` + optional related-resource fields; `Assignment`/`Policy`/`Note`/`SkillSummary` leaf models live in `glassfrog` for downstream reuse (from 025-role-reads, 2026-06-08)
  Conforms to 011 ADR-1 (one shared schema type, grown not duplicated; 012 already added purpose/accountabilities/domains). `Role` gains `type`/`parent_role_id`/`has_subroles`/`flags`/`fillers`/`tags`. `RoleDetail struct { Role; Assignments []Assignment; Subroles []Role; ParentRole *Role; Policies []Policy; Notes []Note; Skills []SkillSummary }` — nested subroles/parent_role are plain `Role` (no recursion); related fields stay nil/empty unless `?include`d (render guards each with an explicit-absence marker, never invents a value — 019). List decodes the generic `Page[Role]` (016); single decodes a `{data: RoleDetail}` document wrapper. `?include=skills` yields `SkillSummary` (no content). The per-role specs (#33/#34/#38) REUSE `Policy`/`Assignment`/etc. rather than redefining them — 025 sets that precedent.

- List completeness: default walks via `paging.All[Role]`; a `--first-page` opt-out does a single-page read — both reduce to `(records, complete)`; opt-out exits 0+signal, mid-walk failure exits non-zero+partial (from 025-role-reads, 2026-06-08)
  First consumer of 016's walker AND the first first-page opt-out. Chosen over `paging.WithMaxPages(1)` to preserve 016's `Result.Complete == (Stop==nil)` invariant (a cap is not an error). Default → `paging.All` (`Result.Records`/`Result.Complete`); opt-out → one `Execute` into `Page[Role]` (`Data`/`!HasNextPage`), reusing the landed 012–014 first-page-signal shape. Incomplete result writes ONE explicit stderr note. Exit code splits the two incompleteness causes: deliberate opt-out with more pages → exit 0 ("more available, re-run without --first-page"); mid-walk `Result.Stop != nil` (010 error / `MalformedPageError`) → render partial `Records` + stderr "incomplete — <cause>" + exit non-zero via `classifyClientError(Stop)`. Never silently truncates (CONSTITUTION VI). Adds NO new `Outcome`/`ExitCode`.

- Read commands validate closed-enum inputs locally (silent-wrong-results risk) but pass free identifiers through to the API (clean 404) — `--include` validated, role id not (from 025-role-reads, 2026-06-08)
  Refines the 011 `validateInclude` / 013 `validateStatus` fail-fast precedent with the inverse case. `--include` is a closed enum the API silently ignores when wrong (returns the role without the embed) → validate locally as `UsageError(2)` before any request (013's rationale). A role id is a free identifier and `getRole` documents only 401/404 (no 400), so a malformed/unknown id yields a clean not-found, not silent-wrong-results → pass it straight through; the API's non-2xx classifies via the shared chain (APIError(3)/PermissionError(4)). Principle for future id-taking reads: validate where the API would otherwise silently mislead; pass through where it reports cleanly.

- The pre-merge CI gate is a new `.github/workflows/ci.yml` on `pull_request: {branches:[main]}`; a lint-once job + an OS-matrix test job + a single `ci-success` aggregation job that branch protection requires (from 024-pr-validation, 2026-06-08)
  PR Validation ships NO app code — one workflow + a `.golangci.yml`, running the EXISTING `go test ./...` suite (unit + godog BDD suites + `internal/build` host self-containment + the `.goreleaser.yaml` config-guard test 022 relies on — so 024 invents no test logic). `permissions: contents: read` only (fork-safe). Three pieces map to the spec accord: (1) **lint** job runs ONCE (not per matrix) — gofmt-check + `go vet ./...` + `golangci-lint run` (pinned version; CI-host tool, NOT a CONSTITUTION XII artifact dependency, same standing as GoReleaser/`sigs.k8s.io/yaml`); (2) **test** matrix on OS `{ubuntu-latest, macos-latest}` (the two shipped families — exercises `internal/build`'s `otool`/`ldd` + `x/term` per-OS paths), Go from `go-version-file: go.mod`, amd64 only (cross-ARCH breadth stays at release time via 022, not per-PR); a Go-version axis is an `[ASSUMED]` extensible knob; (3) **`ci-success`** `needs:[lint,test]` is the ONE stable required-status name (mirrors 022's `publish needs:[build,verify]`) so branch protection survives matrix edits — required-by-name on matrix cells would drift. Enforcement ("report + enforce" per the spec) = GitHub branch protection requiring `ci-success`, a documented/scripted `gh api …/branches/main/protection` maintainer step (repo settings, the one part NOT enforced by a committed file — drift risk mitigated by the stable name + script; CI's read-only PR token can't self-grant it). Latest-commit semantics via a `concurrency` group on the PR ref + `cancel-in-progress`. **Main-Branch Verification (029) mirrors the `test` job** on `push: main` (post-merge net); keep the invocation plain `go test ./...` so 029 reuses it — whether via a shared reusable workflow is 029's call. Exact YAML (action versions, runner labels, job/step names, the `ci-success` assertion, `.golangci.yml` linter set, the branch-protection payload) is interface-level.

- Organization Tree's three reads are `tree` (optional positional id) + `subroles <id>` as two new sibling commands, NOT children of `roles` (from 026-organization-tree, 2026-06-09)
  Consumes 025 ADR-1's foreclosure (the optional-positional `roles <id>` shape can't host `roles <id> <subcommand>`). `tree` mirrors that shape — `MaximumNArgs(1)`, 0 args → `GET /tree` (`getOrgTree`, whole org), 1 arg → `GET /roles/{id}/tree` (`getRoleTree`, rooted subtree); `subroles` is `ExactArgs(1)` → `GET /roles/{id}/subroles`. Both guard-registered (001) + explicitly wired; `--depth` is tree-only, `--include` is validated per-read-set, each cross-combo (depth-on-subroles, >1 id, unknown include) is a fail-fast `UsageError(2)` before assembly with a transport tripwire (011/013/025). This does NOT create the `role` group — the downstream per-role reads (#33/#34/#38) stay free to choose their own surface. Exact command/flag spellings are interface-level (spec `[ASSUMED]`).

- `TreeNode` is a NEW recursive type in `internal/glassfrog` decoded from a `{data: TreeNode}` wrapper — distinct from `Role`/`RoleDetail`, and NOT paginated (from 026-organization-tree, 2026-06-09)
  The tree endpoints return one nested `TreeNode` (`children []TreeNode` — recursion is the point; constrained `flags` enum structural/elected/linked; `type:role`; `parent_role_id` on every node so a flattened consumer keeps the edge) in a single UNPAGINATED response, bounded only by `?depth`. Optional `accountabilities`/`domains`/`fillers` reuse the existing `Accountability`/`Domain`/`Actor` projections, nil unless `?include`d. Kept separate from `RoleDetail` (whose `Subroles` is flat `[]Role`, no recursion — 025 ADR-2) because the shapes differ (children/flags/type vs tags/related-resources). CONSEQUENCE: the recursive type drives a NEW recursive/depth-indented human render template in `internal/render` (every prior template is flat or one-level-nested); structured json/yaml output is unaffected (raw-bytes path, 018 ADR-2). Sets the precedent for any future tree-shaped read.

- Organization Tree's `subroles` read reuses 025's pagination + first-page-opt-out verbatim and SHARES the `RoleDetail` schema under first-to-land-creates — a coordination, not a hard dependency (from 026-organization-tree, 2026-06-09)
  `listSubroles` is paginated with per-child `?include` = `getRole`'s set, so the read decodes `Page[RoleDetail]` and the embeds land inline. Completeness is 025 ADR-3 exactly: default `paging.All[RoleDetail]` → `(records, complete)`; `--first-page` opt-out → one `Execute` into `Page[RoleDetail]`; deliberate opt-out with more pages exits 0 + "more available" note, mid-walk failure renders partial + "incomplete — <cause>" + exits non-zero via `classifyClientError(Stop)` (CONSTITUTION VI). `RoleDetail` + `Assignment`/`Policy`/`Note`/`SkillSummary` + the `Role` growth are the SAME schema 025 designs — neither is landed (025 is *Analyzed*), so whichever of 025/026 implements first CREATES them in `glassfrog`, the other reuses (005/006/016 pattern). The tree reads carry no such dependency (BACKLOG declares only 007/010); 026 is independent of 025 via the no-id whole-org `getOrgTree`. Adds NO new `Outcome`/`ExitCode`.

- The tree reads validate `--include` against the tree set `{accountabilities,domains,members}` even though the API SILENTLY IGNORES unknowns; `--depth` is an optional int (0 = root-only ≠ omitted) validated locally (from 026-organization-tree, 2026-06-09)
  Extends 025 ADR-4 ("validate where the API would otherwise silently mislead; pass free identifiers through") to two new wrinkles: (1) the tree endpoints' silent-ignore of bad `?include` IS the silent-wrong-results hazard, so reject-unknown locally as `UsageError(2)` — and the tree set differs from the subroles set, so TWO per-read validators (`validateTreeInclude` / `validateSubrolesInclude`), never one shared set that would accept cross-read values the API drops. (2) `--depth` is a BOUNDED int where `0` (root node alone) is a meaningful distinct value from "omitted = full tree", so it must be an OPTIONAL flag (pointer / `Changed()` check) sent only when set; a negative value is rejected locally, the API's `minimum:0` back-stops the rest. The role id is passed through to a clean `404` (025 ADR-4). Reuses 011's pure `validateInclude` fail-fast shape.

- The shared `go test ./...` matrix becomes a reusable workflow (`on: workflow_call`) that BOTH 024's `ci.yml` and 029's new `main-verify.yml` call — the test matrix is a single source of truth, not two hand-synced copies (from 029-main-branch-verification, 2026-06-09)
  Realizes 024's deferral ("029 mirrors the `test` job on `push: main`; whether via a shared reusable workflow is 029's call") by extracting the OS matrix (`{ubuntu-latest, macos-latest}`, `fail-fast: false`) + `setup-go` (`go-version-file: go.mod`) + plain `go test ./...` into `.github/workflows/test.yml`; 024's `ci.yml` `test` job becomes a `uses:` call and 029's `main-verify.yml` calls the same — so "green on main means the same as green on the PR" is structural, not a convention (the matrix-drift risk the spec named is closed). 024's required check is UNAFFECTED: branch protection requires only the stable `ci-success` context (024 ADR-4), and `ci-success`'s `needs:[lint,test]` still aggregates the called workflow's result; per-cell check names merely nest (`test / test (ubuntu-latest)`), and nothing requires those by name. 029 is the POST-MERGE net: `on: push:{branches:[main]}` (tags excluded structurally — no `tags-ignore` needed; 022 owns tags), `permissions: contents: read`, tests-ONLY (no lint — 024 owns it pre-merge), and CRUCIALLY **no enforcement layer** (no `ci-success`-style job, no branch protection) because post-merge code can't be blocked — the whole observable outcome is the failing run + the commit status GitHub attaches. Inverse of 024's concurrency: 029 does NOT `cancel-in-progress` (each merge commit gets its own verdict; "each merge verified against its own commit"), whereas 024 cancels superseded PR runs (only the tip matters). Adds NO Go package, command, or test. Sets the reusable-workflow-as-test-SoT precedent for any future CI capability (027/030).

- PR Administration is a SEPARATE `.github/workflows/pr-administration.yml` on `pull_request_target` (not extending 024's `ci.yml`); it labels fork PRs with `pull-requests: write` but NEVER checks out or runs PR head code (from 028-pr-administration, 2026-06-09)
  Spec C1 (label every PR incl. forks) needs a write-scoped token in the BASE-repo context → `pull_request_target`, which is incompatible with 024's deliberately read-only/fork-safe `pull_request` + `contents: read` gate, so the two stay separate files. Trigger activity types `opened|reopened|synchronize|edited` (`edited` is load-bearing — the PR title drives the semver-bearing labels, so a title edit must re-reconcile). Least privilege = `pull-requests: write` + `contents: read`, no secrets, no `id-token`. The security invariant (ADR-5): NO `actions/checkout` of PR head and no PR-supplied script — the labeler reads title + head branch + changed-file PATHS via the API only. Any future checkout under this trigger is a security change. Workflow is NOT a required check (024's `ci-success` stays the sole gate) so a labelling failure never blocks merge; per-PR `concurrency` + `cancel-in-progress` gives latest-state sync. Label catalog (ADR-4) is declarative `.github/settings.yml` (`labels:` block via the Probot "Settings" GitHub App — config-as-code, reviewed in PRs, self-reconciling; the ailign team convention), NOT a `gh` script — diverging from 024's imperative `scripts/setup-branch-protection.sh`; 024's branch-protection could later converge into the same `settings.yml` `branches:` block (out of scope for 028). The Settings app is a one-time repo/org prerequisite.

- Labelling uses pinned `srvaroa/labeler` + committed `.github/labeler.yml` (title+branch+files+sync in one config), NOT `actions/labeler` (no title matcher) or a hand-rolled script (from 028-pr-administration, 2026-06-09)
  Spec A3 needs labels from BOTH title/branch (semver-bearing) AND changed-file paths (non-semver) with authoritative sync (B1). `actions/labeler` has no PR-title matcher → can't derive conventional-commit semver categories; `srvaroa/labeler` expresses all three matcher kinds in one config and removes ONLY the labels it manages when they no longer match (so "scoped to managed set" / human-label-safe is structural, not configured). Pinned to a concrete version (024's golangci-lint / action-version discipline) so an upstream tag change can't silently alter labelling. Both developer-supplied reference repos (ailign-cli, terraform-vitals-template) use this same labeler + release-drafter shape.

- The managed label family is EXACTLY the seven release-note categories; breaking/features/fixes double as the semver signal (→ major/minor/patch); NO parallel `version:*` namespace (from 028-pr-administration, 2026-06-09)
  FEATURE-MODEL fixes seven categories (breaking/features/fixes/docs/infrastructure/dependencies/internal) that drive BOTH release notes AND the semver bump. Rather than ailign's two-family `version:*` + `type:*` scheme (redundant here, two families for 030 to reconcile, desync risk), the three semver-bearing category labels carry bump intent directly for Release Drafting (030) to resolve. This label SET is the cross-feature contract 028 PRODUCES and 030 CONSUMES — 028 owns producing/syncing the labels, 030 owns resolution (which semver label wins, default bump, render-vs-exclude). Exact label STRINGS are interface-level but are a stable contract; renaming one is a coordinated 028↔030 change, currently un-guarded until 030 ships (a config-guard test belongs to 030).

- The Install Script is POSIX `sh` (not bash) and resolves a release via the `releases/latest` redirect + deterministic asset URLs — NO GitHub API, NO `jq` (from 027-install-script, 2026-06-09)
  Sets the acquisition-channel pattern (precedent for #36 Homebrew, #37 NPM). `#!/bin/sh` + `set -eu` (no `pipefail`) for `curl | sh` portability — diverges deliberately from the repo's bash convention for maintainer-run `scripts/*.sh` because the audience is an arbitrary host. Default "latest stable" = follow `github.com/Luscii/cli-glassfrog/releases/latest` (302 → tag in Location header; GitHub excludes pre-releases from `latest` BY DEFINITION = the spec's "latest stable"); pinned version uses the tag verbatim (so a pinned pre-release works). Asset URLs are CONSTRUCTED from 022's pinned `name_template` — `glassfrog_<ver>_<os>_<arch>.tar.gz` + `glassfrog_<ver>_checksums.txt`, where `<ver>` is the tag WITHOUT the leading `v` (download path keeps `v`); so the script never parses release-asset JSON. HARD COUPLING to 022's template: a template change 404s downloads — pin it as a shared contract and encode the names in the test fixtures. The installer is shell using host tooling (downloader/`tar`/sha256) — NOT a CONSTITUTION XII concern (XII governs the distributed binary's runtime, the 021/022 build-host-vs-artifact distinction).

- The Install Script installs atomically (temp dir → verify → `mv`, trap-cleaned) and is covered by `shellcheck` + a Go exec-test via a download-base-URL seam (from 027-install-script, 2026-06-09)
  Atomicity mirrors 006's temp-file-then-rename secret-write discipline (CONSTITUTION I): all download/verify/extract happen in `mktemp -d` with an `EXIT` trap; only a checksum-verified binary is `mv`'d to the install dir (overwrite = the upgrade/downgrade path), so the only outcomes are "verified new binary installed" or "nothing changed". Default install dir is per-user, no sudo (`${XDG_BIN_HOME:-$HOME/.local/bin}`, `[ASSUMED]`); never edits shell profiles — prints the exact PATH export when the dir isn't on `$PATH`. Testability seam = an overridable download base URL (default `https://github.com`), same shape as 008's base-URL inject-seam, letting a Go exec-test drive the script against an `httptest` server (fake redirect + archive + checksums) for happy-path/checksum-mismatch/pinned-version WITHOUT network. `shellcheck` (sh dialect) is the static gate — a CI-host tool (standing of GoReleaser/`golangci-lint`), wired into 024's lint job or a dedicated step (interface call). Tooling detection probes curl→wget and sha256sum→`shasum -a 256`→`openssl dgst`, failing clearly before any download if none present.

- Failure diagnostics are consolidated into one cli-side `Diagnostic{Category, Cause, NextStep}` value produced by a total `Diagnose(err)` normalizer; future error-surface work extends this one chain (from 031-diagnostic-normalization, 2026-06-10)
  Folds the previously-split `classifyClientError` (category) + `formatClientErrorMessage`/`clientErrorNextStep` (cause + next step) in `internal/cli` into a single `errors.As` chain that sets all three fields per arm, so category and message are computed from the same matched value and cannot drift. `reportClientError` delegates to `Diagnose`; the three category-only callers (`me.go` output-format path, `roles.go`/`subroles.go` partial-result paths) use `Diagnose(err).Category` (or a thin `classifyClientError` shim). The value lives in `internal/cli` (carries `Outcome`, consumes apiclient errors — apiclient never imports cli). 004 reads `Diagnostic.Category`; Output-Aware Failure Rendering (032) reads the whole value to render per `--output`, sourcing `status`/`body` from the wrapped `*ResponseError` per 018. The 401-vs-403 next-step split and the rate-limit reset-window wording are folded in here. Exact symbol/field names are interface-level.

- DIVERGENCE: a decode error (undecodable 2xx body) is reclassified RuntimeError(1) → APIError(3) (from 031-diagnostic-normalization, 2026-06-10)
  Supersedes the shipped `classifyClientError` decode arm (pinned by `clienterror_test.go:38` `decode-is-runtime`) and the 011/019 "undecodable-body → RuntimeError(1)" wording. Clarified spec + developer decision: a 2xx the API returned in an unreadable shape is an API-exchange problem (exit 3 = the API's fault), distinct from a genuine CLI/internal fault (exit 1 = our fault — panic, render-template failure, fail-safe). The cause/next-step wording ("…may be an API change; report it") is unchanged — only the category moves. Render-template failures (019) stay RuntimeError(1), so the split sharpens the taxonomy. Candidate for `/score:deprecate` to retire the prior decode-classification precedent.

- Role Domains' two reads are two sibling leaf commands keyed by DIFFERENT id types — a role-scoped list (`GET /roles/{id}/domains`, role id) + a single read (`GET /domains/{id}`, domain id), each `ExactArgs(1)`, neither a child of `roles` (from 033-role-domains, 2026-06-10)
  Second consumer of 025 ADR-1's foreclosure (cobra can't tell a role id from a subcommand name) after 026's `subroles`. Unlike 025's `roles` (`MaximumNArgs(1)`, 0/1 args over ONE id space), the list and single key off different identifiers (`role_` vs `dom_`), so the clean optional-positional pattern doesn't fit — two required-positional siblings instead (the `subroles <id>` shape for the list + a singular single-read leaf). Rejected prefix-routing one positional (`domains <id>` dispatching on `role_`/`dom_`) and flag-for-list as divergent from the codebase's explicit-command style. Search flag is list-only, `--include` single-only; each cross-combo + wrong arg count is a fail-fast `UsageError(2)` before assembly with a transport tripwire (011/013/025/026). Does NOT create a `role` group. CONSEQUENCE: #34 Role Policies has the IDENTICAL shape (`GET /roles/{id}/policies` + `GET /policies/{id}`) and records the SAME two-sibling pattern in parallel (a sibling, not downstream — both 033 and 034 derive from 025 ADR-1's foreclosure + 026's `subroles`/`tree`); #38 Role Projects follows the shared precedent. Exact command names (plural-list/singular-single) + flag spellings are interface-level.

- Grow the shared `glassfrog.Domain` to the full standalone shape (+ optional `Policies []Policy`, nil unless `?include=policies`) and add a `DomainDocument{Data Domain}` wrapper — reuse the landed `Policy`, no `DomainDetail` (from 033-role-domains, 2026-06-10)
  Conforms to 011 ADR-1 / 025 ADR-2 (one shared schema type, grown not duplicated; reuse `Policy` rather than redefine — 025 set that). The landed `Domain` was the inline-on-Role projection (`{ID, Description}`); it gains `type`/`role_id`/`created_at`/`updated_at` (always present on both reads) + optional `Policies`. Rejected a separate `DomainDetail` (the `RoleDetail` shape) because `RoleDetail` earned its own type with SIX include-gated fields, whereas `Domain` has exactly one optional embed and the list/single shapes are otherwise identical — a second type would fork the model for one nilable slice. Single decodes `DomainDocument` (the `RoleDocument` sibling); list decodes generic `Page[Domain]` (016). Growth is ADDITIVE so the inline embeds on Role (025) and TreeNode (026) are unperturbed (those views render only `Description`; new fields decode unused — the 012→025 forward-compatible pattern). #34 reuses `Policy` + this single-read/document precedent.

- Expose the API's `q` full-text param as a LIST-ONLY search flag, sent only when the trimmed value is non-blank, carried on every page of the walk (from 033-role-domains, 2026-06-10)
  First CLI read to expose search (025 deferred `q` on `roles` deliberately; 033's defining conversation chose to surface it). Blank/whitespace term → no `q` sent (matches the API's own ignore semantics, observable as no-filter); non-blank → set as `q` on the walked request so search composes with walk-to-completion AND the `--first-page` opt-out for free (q is just another query value). Malformed query needs no handling — the API returns an empty list, flowing through the existing empty-result success path (exit 0), same boundary as "role controls no domains". Sets the precedent for future searchable list reads. Exact flag spelling (`--query`/`-q`/`--search`) is interface-level. List completeness itself reuses 025 ADR-3 / 026 verbatim (silent conformance, not re-decided).

- The per-role read surface is two sibling top-level commands — plural `<resource>s <role-id>` (the role-scoped list) + singular `<resource> <res-id>` (the standalone read), each `ExactArgs(1)` — NOT a `role` group and NOT children of `roles` (from 034-role-policies, 2026-06-10)
  Resolves the #34 half of the surface question 025 ADR-1 foreclosed and 026 ADR-1 left open. Role Policies ships `policies <role-id>` (`GET /roles/{id}/policies`) + `policy <pol-id>` (`GET /policies/{id}`); the plural/singular pair carries the role-id-vs-resource-id distinction in the name, exactly as 026's `subroles <id>` sits beside `tree`. KEY structural gain over 025's single-command branching: the list-only flags (`--query`/`-q`, `--first-page`, `--per-page`) are registered ONLY on the plural command, so passing one to the singular command is rejected by cobra's own unknown-flag handling as `UsageError(2)` — the spec's "list filter on the single read is a usage error" needs NO hand-rolled cross-combo guard (025 had to branch on `len(args)` + guard filter+id / include-without-id explicitly). Both guard-registered (001) + explicitly wired in `main`; inherit `--base-url`/`--output` (011/020). #33 (Role Domains: `domains <role-id>`/`domain <dom-id>`) records the SAME two-sibling pattern in parallel (a sibling, not downstream — both 033 and 034 derive from 025 ADR-1's foreclosure + 026's `subroles`/`tree`); #38 (Role Projects: `projects <role-id>`/`project <prj-id>`) follows the shared precedent. No `role` group is created — consolidating the siblings under one later would be a coordinated cross-spec change.

- The shared single-object envelope generalizes to a generic `glassfrog.Document[T]` (second single-read trigger), and `glassfrog.Policy` grows scope+timestamps for the standalone read (from 034-role-policies, 2026-06-10)
  Mirrors how 016 generalized the per-resource list envelopes into `Page[T]`: 025's named `RoleDocument{Data RoleDetail}` carried a comment inviting "a later read that wants the same shape" to generalize it — 034 is that read, so it introduces `Document[T any]{Data T}` and refactors `RoleDocument`→`Document[RoleDetail]` (alias if a clean rename proves noisy, to keep 025's call sites/BDD byte-stable). The list decodes `Page[Policy]` (016); the single decodes `Document[Policy]`. Separately, the landed `glassfrog.Policy` was minimal (`ID`/`Title`/`Body` — 025's embedded-on-role view only needed those); 034 GROWS it in place (adds `RoleID`/`DomainID`/`CreatedAt`/`UpdatedAt`, nullable role/domain modeled as plain strings like the existing nullable `Body`) per 011 ADR-1 (grow not duplicate), because the addressable `getPolicy` read's reason to exist is the full policy + its scope. Additive for 025 (its render reads only ID/Title/Body). Input handling is silent conformance to 025 ADR-4 — `--query` is free-text passed through as `q` (no enum validation), the id is passed through to a clean 404; no local validator since there is nothing closed-enum to check.

- The single command-execution failure chokepoint (`reportClientError`) becomes format-aware — structured envelope→stdout, human cause+next-step→stderr — and `output.ErrorDetail` gains an `omitempty` `next_step` field (from 032-output-aware-failure-rendering, 2026-06-10)
  The failure-side counterpart to 031's `Diagnose` + 020's `OutputFormat`: `reportClientError(stderr, err)` evolves to `reportFailure(stdout, stderr, format, err)` and routes per `format.MachineFormat()` — human (`full`/`compact`) keeps today's `renderDiagnostic(d)` on stderr (byte-stable), structured (`json`/`yaml`) builds 018's `ErrorEnvelope` and writes it to stdout (the channel an agent parses, so success and failure read the same way). The Diagnostic→envelope mapping (`errorEnvelopeFor`: `Cause`→`message`, `NextStep`→new `next_step` omitempty field, `Category`→`kind` via a 1:1 `Outcome`→token map, `status`/`Body` from the wrapped `*ResponseError` with `json.Valid` gating the body) lives in `internal/cli` — the only importer of both `internal/output` and `internal/apiclient`, keeping `internal/output` a transport-free leaf (018 invariant). `next_step` is an ADDITIVE omitempty field on 018's `output.ErrorDetail` (declared in `internal/output`, populated in `cli`), so 018's envelope tests are unperturbed. SCOPE = command-execution failures only (the ~19 `reportClientError` sites; the signature change makes the compiler enforce every site is threaded — no plain-text leak). OUT: usage errors (`dispatch.go` plain text) and the invalid-selector error (020) stay plain-text; the partial-walk incompleteness reporters (`reportIncompleteWalk`/`reportIncompleteSubrolesWalk`) keep their stderr note in EVERY format because a partial structured `{data:[…]}` document already occupies stdout (018 one-document-per-channel) — the one place a structured run emits text on the secondary channel, deliberate. Buffer-then-write→`RuntimeError(1)` retained for a structured render that can't complete. Exact `next_step`/`kind` token spellings are interface-level.

- User-template source recognition is a FLAG-ONLY extension of 020's resolver, which returns a discriminated selection (built-in OutputFormat | TemplateRef{File|Stdin}) (from 035-user-defined-template-output, 2026-06-10)
  Realizes the user-templates consumer 020's ADR named (019/020 prose called it "029" — a placeholder predating this feature's allocation; in this repo 029 is `029-main-branch-verification`, and User-Defined Template Output is 035). `-o`/`--output` is reused: at the FLAG rung only, a reserved token resolves to an OutputFormat (unchanged), `stdin` → TemplateRef{Stdin}, any other non-empty value → TemplateRef{File, path}. The env var and `.glassfrogrc output` key keep 020's four-token-or-`*FormatError` contract VERBATIM (operator-confirmed flag-only sourcing — a template is shaped to one resource type, so persisting one across heterogeneous reads never makes sense). Precedence stays centralized in `internal/output` (a new `ResolveSelection`-style entry beside `ResolveFormat`); `internal/output` carries only the path string / stdin marker, performs NO file/stdin I/O, and still never imports `internal/render`. The per-command `resolveFormat` seam widens from `(OutputFormat, error)` to the discriminated selection (mechanical change at every `--output`-capable read site + its fake — the `me*` reads plus `roles`/`role`/`tree`/`subroles`, `domains`/`domain`, `policies`/`policy`, and any future read). Reserved-names-win → a file named `full`/`stdin` needs `./full`. 032 (failure rendering) consumes this wider selection and must decide how a failure renders when a user template was selected (035 renders successes only). The user template is parsed into a CLONE of 019's built-in set (no second engine — silent conformance to the 019 precedent): same `funcMap`, same `Option("missingkey=error")`, so the anti-fabrication guard and the data-only sandbox hold by construction (text/template has no file/net/exec primitive and no such helper is added). File/stdin reads live behind the cli seam (005/006 inject precedent); the template is read+parsed BEFORE assembly (fail-fast, like 020's "resolve format FIRST").

- User-template failures (parse AND execute) map to `UsageError(2)`, via a typed render error distinct from the built-in `*render.RenderError` (from 035-user-defined-template-output, 2026-06-10)
  Sharpens the fault axis (DEPRECATION 2026-06-10: API's-fault 3 / our-fault 1 / usage 2): a built-in `*RenderError` is a CODE defect → `RuntimeError(1)`, but a user template is OPERATOR INPUT, so its failure is the operator's to fix → `UsageError(2)`, symmetric with a malformed `--output` (`*output.FormatError`) and `--base-url`. `internal/render`'s user-template entry returns an `errors.As`-discriminable error; the cli classifier gains a user-template-error → `UsageError(2)` arm beside the `*FormatError` arm. Parse failures are caught pre-call (fail-fast, no request); EXECUTION failures (an unguarded reference to an absent struct field/map key — undetectable at parse time because text/template is dynamically typed) are caught post-response and STILL exit 2 (a request is spent first — unavoidable, and consistent with "operator-input failure = usage error"). Absence-marker rendering is the template AUTHOR's job via the same `{{if .X}}…{{else}}—{{end}}` guards the built-ins use; the system's floor is anti-fabrication (missingkey=error fails loud, never silent fake data), not automatic marker injection.
- `search <query>` is a single-positional sibling leaf (`ExactArgs(1)`) that forwards the operator's query string BYTE-FOR-BYTE as the `query` param — the first read whose primary subject is free text, not an id (from 041-cross-model-search, 2026-06-11)
  `GET /search` requires a websearch-syntax `query`. Unlike 033/034's OPTIONAL list-filter `--query`/`-q` (passed through as `q`), search's query is the REQUIRED primary subject, so it is a positional, not a flag — and `ExactArgs(1)` makes both a missing query and a `>1` positional a fail-fast `UsageError(2)` (cobra's arg validator, no hand-rolled guard). CONSEQUENCE: a multi-word query MUST be quoted at the shell (`search "strategy review"`); an unquoted one is a `>1`-positional usage error — help text + examples must show quoting. Spec Fidelity (CONSTITUTION I): the API owns websearch interpretation, so the CLI never parses/escapes/normalizes/splits the string (continues the 033/034 free-text-pass-through stance, now for a required positional). Guard-registered (001) + explicitly wired in `main`; inherits `--base-url`/`--output` (011/020). Exact command spelling is interface-level.

- `glassfrog.SearchResult` is a NEW flat heterogeneous result type (not a resource projection); the render PRESERVES the API's relevance order verbatim and renders nullable fields as ABSENT, never fabricated (from 041-cross-model-search, 2026-06-11)
  First cross-type result row: one struct (`type` 8-value enum / `id` / `title` / `excerpt` nullable / `rank` float / `role_id` nullable) decoded from `Page[SearchResult]` (016) for ALL eight resource types, rendered under ONE new `search` render key with a per-row `type` badge so the operator drills in via `type`+`id`. 011 ADR-1's "grow the same type" does NOT apply — a `SearchResult` carries `rank`/`excerpt` no resource has, so reusing/growing `Role` would fork the model. DATA INTEGRITY (CONSTITUTION VIII): slice order = decode order = API relevance order (the walker appends pages in sequence), so the CLI NEVER re-sorts, de-dups, or filters — the ranking IS the answer; and a null `excerpt`/`role_id` renders as absent (no placeholder/empty string), not invented. Structured output (json/yaml) serializes `json.RawMessage` verbatim (018 ADR-2), so order/nullability need no machine-path handling. `--types` reuses the 025/026 ADR-4 reject-unknown local validator (`validateIncludeSet` shape) over the closed 8-value set, omitted when unset — silent conformance, except the landed `validateIncludeSet` hard-codes `--include` in its message, so a future searchable-enum read either parameterizes the flag name or adds a thin `validateTypes` sibling (interface-level). Sets the "preserve server ordering / render-nullable-as-absent / heterogeneous-result-key" precedent for future relevance-ranked or mixed-type reads.

- Even an UNBOUNDED, relevance-ranked read (`search`) walks all pages by DEFAULT with a `--first-page` opt-out — first-page-by-default was weighed and rejected (from 041-cross-model-search, 2026-06-11)
  Extends the 025/026 walk-by-default + `--first-page` completeness precedent to the case where diverging was genuinely tempting: a relevance-ranked search has a long low-relevance tail and the agent operator has bounded context, so first-page-by-default-with-a-"more exist"-signal (opt-in `--all`) was a real alternative (both satisfy CONSTITUTION VI). RESOLVED in the spec's clarify session (2026-06-11) in favor of cross-command symmetry + the strongest reading of VI (complete by default, never silently truncated): `paging.All[SearchResult]` to completion; `--first-page` does one `Execute` and exits 0 with a "more available" stderr note if more pages exist; a mid-walk failure renders the partial set + an "incomplete — <cause>" note and exits non-zero via `classifyClientError(Stop)` — the subroles model parameterized on `SearchResult`. The operator narrows a broad query with `--types` or `--first-page`. Future relevance-ranked/unbounded reads inherit this resolution rather than re-litigating it.

- The composable setting resolver lives in a new `internal/resolve` leaf: a kind-tagged `Source` walked lazily into a code-free `Resolution{Value, Provenance{Kind, Origin}}` (from 039-source-composed-resolution, 2026-06-11)
  Generalizes the precedence skeleton 005/008/020 each re-implement. `internal/resolve` imports only `internal/rcfile` (for the file source's nearest-wins walk — §74, never a parallel parser) and no domain package, so the domain packages adopt it in 040 with no cycle. `Resolve(sources ...Source) (Resolution, error)` walks in list order (first = highest precedence), stops at the FIRST source that yields (lazy — a lower source that would error is never reached once a higher one yields), and returns the winning raw value + provenance; a trailing `Default(value)` always yields (`Kind: Default`), and no-source-no-default is `Kind: None` with nil error (the 005 absent-is-normal outcome). Sources: `FromFlags` (presence-based yield per cobra `Changed()` — a supplied flag wins even with an empty value; list-walk over aliases like `--output`/`-o`), `FromEnv` (non-empty yield; list-walk over names), `FromFile(startDir, homeDir, key)` (delegates to `rcfile.Resolve`, surfaces its typed read/format errors verbatim with no fall-through — §170 loud-surfacing), `FromStdin(read, isTTY)` (bounded read reusing 006/§71's stdin+`isTTY` seam; yields only when piped and non-empty). OS access is injected per source (conforms to §49/§71 inject-the-seam), with a thin `…FromOS` binding for the real `os.Getenv`/`os.Getwd`/`os.UserHomeDir`/bounded `os.Stdin`+`term.IsTerminal`. Novel rule: at most one `Stdin` source per composition — a second is a wiring bug and PANICS (conforms to the PR #20 nil-seam fail-fast convention), keeping `Resolve`'s returned error exclusively about runtime resolution failures. The resolver emits no diagnostics and never auto-formats `Value` (secret hygiene for the token setting). Exact Go signatures / type spellings / constructor names are interface-level.

- The resolver is setting-agnostic: value validation moves OUT to the call site; `Resolve` returns the raw winning value + provenance and runs no validator (from 039-source-composed-resolution, 2026-06-11)
  Inverts the in-resolver validation 008/020 use (`isUsableURL`, `ParseFormat`). The OBSERVABLE present-but-invalid-fails-loud-no-fall-through behavior (§170) is preserved by construction: resolution is first-NON-EMPTY (not first-VALID), so an invalid high-precedence value still wins and the caller rejects it using `Provenance.Origin` to phrase the error (`--base-url` / `GLASSFROG_BASE_URL` / file path) — never silently superseded by a lower source. CONSEQUENCE FOR 040: the call-site retrofit must relocate `isUsableURL`/`ParseFormat` to the token/base-URL/output sites and validate the resolved value THERE (the load-bearing change, behavior-preserving with the existing suites green); the resolved default is returned unvalidated (today's "default valid by construction, never re-validated"). The three divergent per-domain `Source` enums collapse onto the one `Provenance` shape — their removal is 040's work, not 039's.

- `actors` is the first FLAG-ONLY read — a top-level list command keyed purely on optional filters (`cobra.NoArgs`), not on a positional id (025/026/033/034/038) or a required positional query (041); the people/agents question is resolved as ONE `actors` command with a `--kind human|agent` filter over the unified ungated `/actors`, NOT separate `people`/`agents` commands (from 048-actor-directory, 2026-06-11)
  `GET /actors` lists actors with three optional filters (`kind`/`role_id`/`q`) and no required input — so the command's whole subject is the org, narrowed by flags, and a positional is a fail-fast `UsageError(2)` via cobra's `NoArgs` validator (no hand-rolled guard). `/people` (`?kind=human`) and `/agents` are aliases over `/actors`; `--kind` selects either through the one endpoint with no capability lost, so a second command would fork the discovery surface — and `/agents` is `ai_integration`-gated and deferred (PROJECT) while `/actors?kind=agent` reaches agents ungated. CONSEQUENCE: Actor Read (049) and Actor Assignments (050) likewise read `/actors*` rather than the gated aliases; they may grow `glassfrog.Actor` in place (011 ADR-1) for the footprint embeds (`?include=roles,assignments`) the directory deliberately omits. `--kind` is the one closed-enum input validated locally (reject-unknown, `validateIncludeSet`/`validateStatus` shape — same `--include`-hardcoded-message factoring note as 041 ADR-3); `--role-id`/`--query`→`q` pass through (025 ADR-4). `Actor` (011) is reused AS-IS (no schema growth, 038 ADR-2); a new `actors` render key (full+compact, homogeneous rows) mirrors 041's `search` key addition. Completeness is silent conformance to 025 ADR-3 (walk-by-default + `--first-page`). Exact command/flag spellings are interface-level.

- The first write threads a request body's `Content-Type` via a NARROW `ContentType string` field on `apiclient.Request` (not a general `Header` map) — `Execute` sets the header only when non-empty (from 042-tension-capture, 2026-06-11)
  Every landed command is a bodyless GET; `apiclient.Request` had `Method`/`Path`/`Query`/`Body io.Reader` but NO header field and `Execute` set no `Content-Type`. The first write (`POST /roles/{id}/tensions`) needs `Content-Type: application/json` or the API ignores the body and answers 422 (the silent-wrong-result class §113/§200 designs against). DECISION: add `ContentType string` to `Request`; `Execute` does `if req.ContentType != "" { httpReq.Header.Set("Content-Type", req.ContentType) }` after build, before `Do`. Reads pass `""` and are byte-identical (pinned by a header-present/absent test). Chose the narrow field over a general `Header http.Header` bag per the codebase's anti-speculation idiom (008/010 deferred defaults; 017 caps `[ASSUMED]`) — the only second consumer in sight is `If-Match` for Clobbered Changes, which is explicitly deferred AND forbidden by 042's non-behaviors. When a real second header lands, generalizing `ContentType`→`Header` is a contained refactor with that consumer to ground it. Sets the write-body transport precedent for the rest of the write path (proposals, operational writes).

- `tension create <role-id>` is a NON-RUNNABLE `tension` group parenting a `create` leaf — reserving the `tension` namespace for future reads/edits; conforms to the `auth`/`auth login` group pattern (from 042-tension-capture, 2026-06-11)
  First write command + first multi-verb noun namespace. The 001 guard requires a command be EITHER a leaf (action) OR a group (≥1 child), never both; the codebase has runnable-parent (`me`/`me roles`) and non-runnable-group (`auth`/`auth login`) idioms. DECISION: non-runnable `tension` group + `create` leaf, both assembled then attached via `Register` (group built with its child before being registered under root, so the ">=1 child" rule holds at attach time). `tension` alone prints help. The write flags (`--body`/`--label`/`--meeting-type`) live ONLY on `create`, so a future `tension get`/`list`/`update` adds its own leaf with its own flags and passing `--body` to a read is a cobra unknown-flag `UsageError(2)` for free (the structural guard 034/038 rely on). Chosen over a flat `tension-create` leaf, which would foreclose the `tension <verb>` family. No `role` group is created (consistent 026/033/034/038). Future tension reads/edits/delete conform to this namespace. INPUT handling is silent conformance to §113/§200: a new `validateMeetingType` (closed enum {tactical, governance}, set from the spec) mirrors `validateStatus` as a pre-assembly fail-fast `UsageError(2)` with a transport tripwire; a missing/trim-empty `--body` is likewise a pre-request `UsageError(2)`; the role id passes through to a clean 404/422. Adds NO new `Outcome`/`ExitCode`. The 201 `{data: Tension}` decodes into the existing generic `Document[Tension]` (§143/§254 — no per-resource envelope); a new `Tension` model lives in `internal/glassfrog` (011 ADR-1). POST is never auto-retried on 429 (silent conformance to §133 `isSafeMethod`), so capture cannot double-create. Exact command/flag spellings, the `Content-Type` constant location, and render template are interface-level.

- The call-site retrofit is SURFACE-STABLE: each setting keeps its per-domain output type and `Source` enum, mapping `resolve.Provenance` back internally — DIVERGING from 039's forecast that 040 would remove the divergent enums (from 040-resolution-call-site-retrofit, 2026-06-12)
  Announced divergence from the 039 "value validation moves to the call site" entry's tail ("the three divergent per-domain `Source` enums collapse onto the one `Provenance` shape — their removal is 040's work"). RESOLVED with the developer in favor of keeping `auth.Resolution`/`auth.Source`, `apiclient.BaseURL`/`BaseURLSource`, `output.Selection` (035's discriminated format-or-template type), and the typed errors exactly: each resolver becomes a thin adapter that composes `resolve.Resolve(...)`, takes the winner, and maps `resolve.Provenance.Kind`→its existing enum member (+`Origin`→`Path`/error label); `resolve.Provenance` is an internal intermediate never exposed past the three resolver functions. The output retrofit targets 035's `ResolveSelection`/`resolveSelection` (NOT the pre-035 `ResolveFormat`/`resolveFormat`) and preserves the flag-rung template branch. WHY: 039 recorded NO DECISIONS entry, so its "remove the enums" line is an unratified forecast, and §74 ratifies "`internal/auth` keeps `Resolution`/`Source`"; removing the enums would ripple into `Identity` (transport.go) and `ConnectionContext.Complete()`/`Ready()`/status (context.go) — exactly the readiness logic 039's risk register flags as high-impact drift. CONSEQUENCE: the type-change blast radius is confined to the three resolver files; 039's "one uniform provenance vocabulary" end-state is DEFERRED to a possible later cleanup spec (candidate for `/score:deprecate` of the 039 forecast). Future settings adopting `resolve` follow this adapter pattern (compose + map back to a code-free domain type) rather than exposing `resolve.Resolution` directly.

- The base-URL and output-format flag rungs adopt cobra `Changed()` presence in place of value-emptiness; the presence bit is threaded from each `RunE` down through the resolvers (from 040-resolution-call-site-retrofit, 2026-06-12)
  Fixes the long-standing inconsistency (LEARNINGS / PRs #61/#73: validate on `Changed()`, not the parsed value) at the moment of adopting `resolve.FromFlags` (presence-based by design). Today `ResolveBaseURL`/`ResolveSelection` receive only `flagValue string` and treat `TrimSpace(flagValue) != ""` as "supplied", so an explicit `--base-url ""`/`--output "  "` silently falls through; under presence semantics a supplied flag WINS even when empty/whitespace and is validated → fails loud (for `--output`, via 035's flag-rung classification — a degenerate empty template selection — not a token `*FormatError`). ONE observable behavior change (covered by the spec's edge-case scenarios). The presence bit (`cmd.Flags().Changed(apiclient.FlagBaseURL)` / `Changed(output.FlagOutput)`, correct on these inherited persistent root flags — same flagset that already serves `GetString`) threads through `AssembleFromOS`/`ResolveBaseURLFromOS`/`ResolveBaseURL` and the `resolveSelection` seam (035's widening of 020's `resolveFormat`; all declarations + impl) into `FromFlags`; signatures change with no default-value overload so the compiler flags every un-threaded call site. The token path is unaffected (no `--token` flag). Future flag-bearing settings thread presence, never value-emptiness.

- A call site adopting `resolve` keeps its OWN OS seam and feeds it into `resolve`'s injected constructors, rather than routing through `resolve`'s `…FromOS` binding (from 040-resolution-call-site-retrofit, 2026-06-12)
  The `…FromOS` entrypoints (`auth.Resolve`, `ResolveBaseURLFromOS`, `ResolveSelectionFromOS`) keep sourcing the real dirs/env exactly as today and pass them into `resolve.FromEnv(getenv, name)` / `resolve.FromFile(startDir, homeDir, key)`; `resolve.FromFile` delegates to the same `rcfile.Resolve` the sites call today. The seam SHAPE differs by site: `internal/auth` and `internal/apiclient/baseurl.go` own `var getenv`/`getwd`/`userHomeDir` and derive their own dirs, whereas the output path (035) owns only a `getenv` seam and `ResolveSelectionFromOS(flagValue, startDir, homeDir)` RECEIVES the dirs — the cli `productionSeam.resolveSelection` derives them via `os.Getwd`/`os.UserHomeDir`. 040 does NOT relocate that derivation into `internal/output`. WHY: preserves each site's hermetic test pattern (controlled `getenv`, temp-dir rcfile — never the real `~/.glassfrogrc`, CONSTITUTION IV) with minimal churn, and keeps the secret-bearing token path free of any dependency on `resolve`'s binding surface. `resolve`'s own `FromOS` helpers stay unused by these three sites (they exist for a future stdin-consuming setting).

- Tension `status` is its OWN closed enum (`unprocessed`, `processed`, `archived`) validated by a NEW `validateTensionStatus` — do NOT reuse the action/project `validateStatus` set (from 043-tension-reads, 2026-06-12)
  Tension Reads' `tension list <role-id> --status` filter is a closed enum, but a DIFFERENT vocabulary than the action/project statuses `validateStatus` (`internal/cli/status.go`) covers (`archived`/`cancelled`/`completed`/`current`/`scheduled`/`someday`/`waiting`). Reusing `validateStatus` would accept invalid tension statuses (`current`) and reject valid ones (`unprocessed`/`processed`) — a correctness bug. DECISION: a new `validateTensionStatus` over the tension status set, placed WITH the tension command code (next to 042's landed `validateMeetingType`/`supportedMeetingTypes` in the tension file, NOT in the shared `internal/cli/status.go`), as a pre-assembly fail-fast `UsageError(2)` with a transport tripwire — silent conformance to the validate-closed-enum-locally pattern (025 ADR-4 / 038 ADR-3 / 042 ADR-3's `validateMeetingType`), a new validator instance over a new set, NOT a new pattern. Location verified against 042 on main (#91): tension-domain validators live with the tension code, not in `status.go` (which owns the action/project `validateStatus`/`supportedActionStatuses`). Future tension edits (Tension Update 044's `--status`, which also accepts `archived`) reuse this set. The set tracks the vendored `spec/glassfrog-api-v5.yaml`.

- Tension Reads (`tension list <role-id>` + `tension get <ten-id>`) are TWO verb leaves under 042's existing `tension` group, reusing the `Tension` model + singular render and adding only a plural `tensions` list render path (from 043-tension-reads, 2026-06-12)
  The list-by-role + read-by-id read pair is the structural twin of 038-role-projects, but the command surface is the `tension <verb>` grammar (not 038's plural/singular noun pair) because 042 ADR-2 already claimed `tension` as a group with a `create` leaf — a bare `tension <ten-id>` would collide with subcommand dispatch (the cobra constraint 038 ADR-1 cites). DECISION: add `list <role-id>` + `get <ten-id>` leaves (both `ExactArgs(1)`) to 042's `tension` group; list-only flags (`--status`/`--first-page`/`--per-page`) live only on `list`, so `--status` on `get` is a cobra unknown-flag `UsageError(2)` for free. Reuse 042's `Tension` model + `Document[Tension]` + singular `tension` render UNCHANGED (011 ADR-1, grow-not-duplicate); add only the plural `tensions` list render (`ResourceTensions` + `tensions.full`/`tensions.compact` + `TensionsView`) — mirrors 038 ADR-2/ADR-4 with plural/singular roles reversed (038 added the singular because 014 had the list; 043 adds the list because 042 has the singular). 042 has LANDED on main (#91): verified the `tension` group (`newTensionCommand`), `Tension` model, `Document[Tension]`, and singular render (`ResourceTension`/`TensionView`/`tension.{full,compact}.tmpl`) are present and reused as-is; 043 extends `newTensionCommand` to attach the two read leaves and reuses 042's `tensionSeam` (identical to `projectsSeam`; paging is a `paging.All` body call). 035 (User-Defined Template Output) also landed, so the reads use the widened `resolveRenderTarget`+`writeHuman`/`aggregateRawData`/`output.RenderSuccess` flow and inherit `-o <template-ref>` support. Adds NO new `Outcome`/`ExitCode`; list completeness is 025 ADR-3 (`paging.All` + `--first-page` opt-out, never silent-truncate).

- Release Drafting is a dedicated `.github/workflows/release-drafting.yml` on `push:main` running pinned `release-drafter` + a committed `.github/release-drafter.yml`; NOT an extension of 029's `main-verify.yml` (from 030-release-drafting, 2026-06-12)
  Closes the pipeline cluster's drafting stage. Mirrors 028's separate-file precedent (its `pr-administration.yml` is split from 024's `ci.yml` over divergent permissions/trigger): drafting needs `contents: write` (+ `pull-requests: read`) to maintain the draft release, incompatible with 029's deliberately `contents: read` post-merge test net, so they stay separate files. The action is version-pinned (024/028 action-pinning discipline). NOT a required check (`ci-success` from 024 stays the sole gate) — a drafting failure is administrative and blocks nothing (spec non-blocking guarantee). Per-`main`-ref `concurrency`+`cancel-in-progress` keeps the draft on the latest tip. Clean seam with 022: 030 owns notes/draft/version/status, 022's `release.yml` (on `release:published`) honors them and attaches binaries. release-drafter realizes the bump via `version-resolver` (breaking→major / features→minor / fixes→patch, `default: patch`, highest-wins native); first release computes from a 0.0.0 base (a feature-bearing first release yields v0.1.0 natively — the spec floor; a patch-only first release is the gap flagged in plan Risks). Categories map the seven 028 labels to note sections (no new note taxonomy). Exact YAML (action version, templates, section titles, prerelease step) is interface-level.

- Release Drafting ADDS AN EIGHTH MANAGED LABEL (exclusion) to 028's catalog+labeler, diverging from 028's recorded "EXACTLY seven managed labels" decision (from 030-release-drafting, 2026-06-12)
  ANNOUNCED DIVERGENCE from "The managed label family is EXACTLY the seven release-note categories … NO parallel namespace (028, 2026-06-09)". The spec excludes spec/feature-only PRs (changes confined to `specs/`+`.feature`) from notes AND the bump, and release-drafter filters by LABEL ONLY (no path-based exclusion). Developer chose (030 plan resolve) to ship the full behaviour now via a coordinated 028 change rather than defer or post-filter 030-side. VERIFIED constraint: `srvaroa/labeler`'s `files:` matcher is any-file-matches and has no native all-files-confined-to mode; `negate` inverts a whole label block. Realization: a new managed label (working name `no-release-note`; exact string interface-level) added to `.github/settings.yml` + `.github/labeler.yml`, applied via a `negate`-over-noteworthy matcher (label applies when NO changed file matches the code/docs/infra/deps patterns the existing seven labels enumerate = the complement of noteworthy = spec/feature-only churn); 030's `release-drafter.yml` lists it under `exclude-labels`. 028's "seven" is now EIGHT — the new invariant is re-pinned by 030's config guard (below). Candidate for `/score:deprecate` of the seven-only decision. Edge cases (PR touching only a stray non-noteworthy file, empty PR) are also-excluded (plan Risks).

- The 028↔030 label contract is now guarded by a config-drift test in `internal/build`, asserting all eight labels agree across release-drafter.yml, labeler.yml, and settings.yml (from 030-release-drafting, 2026-06-12)
  Closes 028's standing note: "this label SET … currently un-guarded until 030 ships (a config-guard test belongs to 030)". Joins the existing `internal/build` config guards (021 `.goreleaser` matrix, 022 release-workflow) — same `sigs.k8s.io/yaml` parse + change-detector rigor (a missing label fails as loudly as an extra one). Asserts: (a) release-drafter `categories` labels == the seven semver/category labels in labeler.yml+settings.yml; (b) the eighth exclusion label is present in settings.yml+labeler.yml and in release-drafter `exclude-labels`; (c) the resolver major/minor/patch buckets are exactly breaking/features/fixes. Runs in the existing `go test ./...` matrix (024 pre-merge, 029 post-merge) — a rename/drop/add in one file without the others reddens CI rather than silently mis-drafting. The one piece of Go in the feature; adds no runtime package (conforms to `internal/build`'s "system under test is the pipeline itself" charter).

- Release Drafting sets the draft's pre-release/latest status AUTOMATICALLY from the resolved version (0.x → pre-release, ≥1.0.0 → latest) via a post-draft step, not a static config flip (from 030-release-drafting, 2026-06-12)
  Spec (clarified 2026-06-12) derives status from the version; 022 honors whatever the draft carries. release-drafter's `prerelease` is a static boolean with no version-conditional mode but exposes a `major_version` output, so a step after the drafting step marks the draft pre-release when `major_version == 0`, else latest (via `gh release edit` on the draft; exact invocation interface-level). Considered + rejected: static `prerelease: true` flipped by hand at the 1.0.0 cut (not the automatic rule the spec commits to). Fallback if editing an unpublished draft's flag proves infeasible at implement = the static config, which still satisfies the project's current 0.x phase (plan Risks). Status lives in exactly one place (the draft) — the seam 022 reads.

- Homebrew distribution is a GoReleaser **formula** (not a cask) published to a **dedicated tap repo** (`Luscii/homebrew-cli-glassfrog`), via a post-publish `tap` job using a **cross-repo write token** — the FIRST credential beyond `GITHUB_TOKEN` in the release pipeline (from 036-homebrew-tap, 2026-06-12)
  Reverses the Feature Model's original "Homebrew cask committed within this repository (no separate tap repo)" intent (FEATURE-MODEL updated). Two drivers: (1) a **formula** installs the pre-built binary on macOS AND Linux brew (cask is macOS-only), subsuming the cask and adding Linux; (2) a **separate tap repo** lets GoReleaser's brew publisher push `Formula/glassfrog.rb` straight to it, so NOTHING is committed to this repo's branch-protected `main` (024's `ci-success`) — the in-repo alternative forced a per-release PR/push to protected main. The `brews` block goes in the SAME `.goreleaser.yaml` as builds(021)/archives+checksum+release(022)/ldflags(023) — conforms to 022's "brew section in the same file" (DECISIONS 179) — but DIVERGES from 022's "entirely within the repo (only `GITHUB_TOKEN`)" property: the tap push needs a token with `contents: write` on the OTHER repo (`GITHUB_TOKEN` can't write cross-repo). Baseline = fine-grained PAT scoped to only the tap repo (App token the no-expiry upgrade), injected only into the `tap` job's env. ARCHITECTURE: keep the existing `build → verify → publish (gh release upload)` spine UNCHANGED — `goreleaser release --skip=publish` + `gh release upload` still own assets and the GitHub release (the GoReleaser-doesn't-publish boundary, DECISIONS 178). The NEW `tap` job is `needs:[publish]`, runs GoReleaser's brew publisher ONLY (GitHub-release publisher disabled), and is gated stable-only by a JOB-LEVEL `if: !github.event.release.prerelease` — do NOT rely on GoReleaser's `brews.skip_upload: auto` (keys off a semver `-rc` suffix, which can diverge from the GitHub pre-release flag #30 sets). Formula content (per-platform url/sha256, install/test stanzas) is GoReleaser-authored from the same archives/checksums 022 publishes (single build-truth). Sets the acquisition-channel pattern alongside 027 (install script) for #37 (NPM Wrapper) — a sibling channel that will face the same "publish to a registry the in-repo token can't reach" shape. Config-drift guarded by extending `internal/build`'s `.goreleaser.yaml` config-guard with a focused `brews`-targets-the-tap assertion (spirit of 023's ldflags regression-guard). Exact `brews` fields, formula DSL, tap/install command strings, and GoReleaser skip-flags are interface/tasks-level.

- The npm channel is a hand-rolled OSS packaging path (umbrella + 4 per-platform `optionalDependencies`, binary bundled), GENERATED in CI from a single in-repo source — not committed scaffolding — and published from `release.yml` reusing the verified `dist/` via npm OIDC trusted publishing (from 037-npm-wrapper-package, 2026-06-12)
  Sets the *binary-bundling* acquisition-channel precedent (sibling to 027's *download-at-install* shell channel; precedent for a future Scoop/Docker/etc. channel). Rejected GoReleaser Pro's native `npms` pipe (paid, Pro-only since v2.8 — would migrate the whole release tooling off OSS) in favor of hand-rolled OSS. Topology: a main `glassfrog` umbrella lists four `glassfrog-<os>-<cpu>` packages as `optionalDependencies` pinned `=X.Y.Z`; npm's `os`/`cpu` resolution installs only the host's (binary bundled in its tarball = offline-capable primary path); a zero-dependency CommonJS launcher (`bin/glassfrog`) execs the resolved binary with full argv/stdio + exit-code/signal passthrough (honors 004) and is the runtime backstop when postinstall is skipped (`--ignore-scripts`); a `postinstall.js` fallback downloads+verifies from the release when no optional dep is present, and refuses (clear message, place nothing, non-zero) on an unsupported platform (Windows/other) — both mirror 027. SILENT CONFORMANCE to 027 ADR-1/ADR-2 (the named `037` follower): construct asset URLs from 022's `name_template` (`glassfrog_<ver>_<os>_<arch>.tar.gz` + `_checksums.txt`, `<ver>`=tag without `v`), sha256-verify against the checksums file, download-base-URL seam (default `https://github.com`) for hermetic `node --test`. DEVELOPER CONSTRAINT (diverges from the ailign-cli reference, which commits N per-platform dirs): the per-platform package directories are GENERATED at release time by an in-repo generator into a gitignored dir — only the launcher, postinstall, umbrella template, and generator are committed (one copy of each, testable). npm `cpu` differs from GoReleaser arch (`amd64`→`x64`); the generator owns the map. PUBLISH: a dedicated `npm-publish` job appended to 022's `release.yml`, `needs:[build,verify]` (no npm release without a verified release; byte-parity — bundles the dist/ bytes the self-containment matrix already gated, so the install-time URL fallback is end-user-only), platform-packages-FIRST then umbrella (pinned optional deps must exist first), auth via GitHub OIDC `id-token: write` + `npm publish --provenance` — no long-lived `NPM_TOKEN` (022's no-external-secrets ethos). One-time maintainer cost: npmjs.com trusted-publisher registration per package name before first publish (like 024's branch-protection bootstrap). Version = tag minus `v` (npm semver); pre-release tags publish as npm prereleases. JS is dependency-free, Node built-in test runner only, isolated under one dir — no CONSTITUTION XII bearing (governs the distributed Go binary's runtime, not the host packager; 021/022 build-host-vs-artifact distinction). Package name/scope, the four platform names, package.json metadata, and message wording are interface-level.

- Tension Update's partial-update body is a NEW `TensionUpdateInput` (all four fields `omitempty`, INCLUDING `status`) — a sibling of capture's `TensionInputBody`, not a reuse of it (from 044-tension-update, 2026-06-13)
  The vendored `updateTension` PATCH body shares the `TensionInput` schema with `createTension`, but the two impose DIFFERENT client contracts on that wire shape. Capture's landed `TensionInputBody` (042) is deliberately `{Body (no omitempty), Label omitempty, MeetingType omitempty}` with NO `status` field — a documented server-ownership invariant (creation cannot claim a status the server auto-computes). Update is the opposite: `status` is editable (the API allows the `archived` transition on PATCH, then recomputes on save), `--body` is OPTIONAL (checked only when supplied), and every field rides only when supplied (true partial update). DECISION: a new `TensionUpdateInput`/`TensionUpdateBody` with all four fields `omitempty` incl. `Status`, plus `NewTensionUpdateInput`. Because every SUPPLIED value is guaranteed non-empty (blank `--body` rejected; `--status`/`--meeting-type` closed-enum non-empty; `--label` rides only when non-empty), `omitempty` + plain strings faithfully expresses "send only what was supplied" without pointer fields. Chose FORK over growing the shared type (the 011 ADR-1 grow-not-duplicate reflex governs RESPONSE models — Policy/Domain grown additively; request inputs are constructed per-operation and 042 chose a status-free shape ON PURPOSE) because adding `Status`/`Body omitempty` to `TensionInputBody` would erase capture's type-level "cannot send server-owned status" guarantee. Capture's `TensionInputBody` is left BYTE-STABLE; the `Tension` response model, `Document[Tension]`, and singular render are reused unchanged. Sets the write-path's first EDIT-input shape — proposals/operational writes facing partial-update bodies follow this fork-when-contracts-diverge rule.

- Tension Update requires AT LEAST ONE editable field fail-fast (resolved send-set, presence + non-empty) and rejects a blank `--body` only when supplied — both pure, pre-assembly, with a transport tripwire (from 044-tension-update, 2026-06-13)
  Update adds two preconditions capture lacked: (a) "an update that changes nothing is meaningless" — no editable field flag ⇒ usage error, no request; (b) a supplied `--body` must be non-blank, but `--body` is OPTIONAL here so the blank check fires only when the flag is present (`Changed()`). DECISION: compute the send-set from `Changed()` + non-empty value (capture's `labelSet`/`meetingTypeSet` + `omitempty` rule, extended to body and status); if empty ⇒ `UsageError(2)` naming the four flags. ORDER: resolve `--output` → (if `--body` Changed) reject whitespace-only body as `UsageError(2)` naming `--body` (specific message FIRST, so `--body "   "` is not swallowed by the generic precondition) → validate `--status`/`--meeting-type` (reusing the LANDED `validateTensionStatus` (043) + `validateMeetingType` (042) — a new CONSUMER of existing closed-enum sets, NOT a new validator/copy; 043's entry already named 044 as the next consumer) → require non-empty send-set → assemble/send. Rejected counting `Changed()` flags only (ignoring values): `update <id> --label ""` would pass yet marshal an empty `{tension:{}}` no-op. Rejected server-side `422`: the precondition is knowable locally (§113). CONSEQUENCE: `--label ""`/`--status ""`/`--meeting-type ""` resolve to "no field sent" and do NOT satisfy the precondition on their own, so the no-op intent holds beyond the literal no-flags case; no field-clearing-to-null affordance is introduced (spec non-behavior — empty omitted, never sent as null). No new `Outcome`/`ExitCode`. PATCH is never auto-retried on 429 (017 `isSafeMethod` is GET/HEAD only — `retry.go:65`), so update cannot double-edit. Sets the partial-update precondition pattern for the rest of the write path; the `ten_` id passes through unvalidated to a clean 404/422 (042/043 ADR-3, §200).

- Role Fillers is a SINGLE top-level `fillers <role-id>` command (`ExactArgs(1)`) — the role-scoped list half of the 034/038 pattern standing ALONE, no plural/singular pair, because the API exposes no `GET /assignments/{id}` (from 047-role-fillers, 2026-06-13)
  First role-scoped list read with no singular sibling. The per-role-read precedent (034 ADR-1, followed by 033/038) pairs a plural list with a singular standalone read keyed on a different id kind; `/assignments/{id}` carries only the administrative `PATCH`/`DELETE` (no GET), and assignment administration is out of scope (PROJECT) — so there is no singular read to pair. Rejected inventing a `filler <asgn-id>` (would fabricate API surface, violating "Spec is the contract") and a `role` group (lockstep cost 033/034 declined). The command name `fillers` speaks the practitioner's question while the model/endpoint stay the "assignment" resource noun. CONSEQUENCE: Actor Assignments (050) reads the same `Assignment` resource from the actor end (`/actors/{actor_id}/assignments`) as its own command and faces the same single-read-absence. No new `Outcome`/`ExitCode`; command/flag spellings are interface-level.

- Role Fillers reuses the landed `glassfrog.Assignment` (025) AS-IS — no schema growth — and offers NO list filters and NO `--include` flag (the endpoint accepts neither beyond the default `actor` include + pagination) (from 047-role-fillers, 2026-06-13)
  `Assignment` (roles.go, grown by 025 for `?include=assignments` and reserved there for "the future standalone assignment reads") already carries `focus`/`elected_until` + the embedded `actor{id,name,kind}` the default `include=actor` returns — so the projection that surfaces focus + elected_until (beyond 025's actor-reference-only embed) needs no model change (011 ADR-1 / 038 ADR-2 reuse-not-fork). Adds only a `fillers` list render path (`render.ResourceFillers` + `FillersView{Data []Assignment}` + `fillers.full`/`fillers.compact`), mirroring 048's `actors` and 043's `tensions` plural render; nullable `focus`/`elected_until` get explicit-absence markers. Diverges from 038 ADR-3 (`--status`) and 048 (`--kind`) the OTHER way — silent conformance to 034 ADR-3's "validate nothing": `--include`'s only non-default value (`role`) is redundant when the caller named the role, so the smallest input surface in the read family (no validator, no filter plumbing). List completeness is 025 ADR-3 (`paging.All` + `--first-page`). Reuses the `projectsSeam`-shaped list seam; structured output aggregates raw bytes (`aggregateRawData`). 050 reuses `Assignment` likewise.

- Subroles Tension Roll-up is a fifth read leaf `tension subroles <role-id>` under 042's `tension` group, reusing the LANDED `Tension` model, `tensions` render path, and `validateTensionStatus` with ZERO new model/render/validator — only the request path differs from `tension list` (from 046-subroles-tension-roll-up, 2026-06-13)
  `GET /roles/{role_id}/subroles/tensions` (`listSubrolesTensions`) is a one-level roll-up (direct sub-roles only, not transitive) returning the SAME paginated `{data:[Tension],meta}` shape as `listRoleTensions`. DECISION: add a `subroles` leaf (`ExactArgs(1)`) to `newTensionCommand`'s group beside `create`/`list`/`get`/`update`, structurally a clone of the landed `newTensionListCommand` with the request path swapped to `/roles/{role_id}/subroles/tensions` (recommended: parameterize the path, since status-filter/paging/completeness/render/error logic is byte-identical). Chosen the new verb OVER a `--subroles` flag on `tension list` (define-session decision): 043's non-behavior #1 scoped `list` to a role's OWN tensions and declared the roll-up its own capability — one verb = one endpoint. The verb name does NOT collide with the top-level `subroles <role-id>` command (026 `listSubroles`): different command paths. REUSE-ONLY at the data layer (silent conformance to 011 ADR-1 / 043 ADR-2/ADR-3 / 044's new-consumer pattern): `--status` is a new CONSUMER of 043's landed `validateTensionStatus` set (`unprocessed`/`processed`/`archived`), the `tensions` render path (`ResourceTensions`/`TensionsView`) and `Tension` model are reused unchanged — no schema/render/validator phase. The leaf-anchor `404` (API returns 404 when the anchor has no sub-roles) is surfaced VERBATIM through the shared `classifyClientError`/`Diagnose` chain (no "this role has no sub-roles" special-case — VISION Exclusion 1), kept DISTINCT from the genuine empty-list `200` (sub-roles exist but carry no tensions → `0`-exit empty-set line); both pinned by BDD + transport tripwires. Completeness is 025 ADR-3 (`paging.All` + `--first-page`). Adds NO new `Outcome`/`ExitCode`. The `role_` id passes through to a clean 404 (042/043 ADR-3). Exact command/flag spellings and path parameterize-vs-fork are interface-level.

- Tension Discard treats a `404` as SUCCESS (idempotent soft-delete) — the first command to fold a non-2xx into the success path, intercepting the `ResponseError` before `reportFailure` (from 045-tension-discard, 2026-06-13)
  `deleteTension` is documented not REST-strict idempotent: first `DELETE` of a live tension → `204`, re-delete → `404`, and an already-discarded tension is indistinguishable from a never-existed one ("treat 404-following-204 as success"). A single CLI call issues one `DELETE` and cannot know if a prior `204` happened, so the choice is binary. DECISION: after `Execute(reqCtx, req, nil)`, `errors.As(err, &respErr)`; a `*ResponseError` with `StatusCode == http.StatusNotFound` is folded into the success end-state (the tension is gone), NOT routed to `reportFailure`. ONLY `404` is diverted — `401`/`403`/`429`/other non-2xx and transport/not-authenticated all still classify normally (so a `403` on an existing tension is never swallowed). Rejected treating `404` as `APIError(3)` (like `get`/`update`): re-discard would then fail, contradicting the API guidance and breaking retry-safety for the agents that drive the CLI. CONSEQUENCE: discard is retry-safe and a re-run always exits `0`; the accepted cost — a mistyped/never-existed id reports success — is a spec non-behavior, softened by the `204`-vs-`404` stderr advisory. Every prior command routes ALL non-2xx through the shared classifier; this is the first exception. Sets the idempotency shape for future deletes (e.g. proposal withdraw).

- Tension Discard SYNTHESIZES a command-local `{data:{id,discarded}}` result for the bodyless success and routes it through the existing 018/019/020/035 flow — the first write whose stdout result is built client-side, not decoded from the server (from 045-tension-discard, 2026-06-13)
  A successful `deleteTension` is bodyless (`204`, and the `404`-as-success also carries nothing): `Execute(…, nil)` drains and returns a bare `*Response`. But the family contract (and spec) is to PRODUCE structured data rendered in the effective format (`full`/`compact`/`json`/`yaml`), so `-o json`/`-o <template>` must still yield a parseable result. DECISION: build a small command-local result from the id (NO server-owned fields like `discarded_at` — the body never provided them), marshal to `json.RawMessage` → `output.RenderSuccess` for a built-in machine format, or render a new minimal `render.TensionDiscardView{ID}` + `ResourceTensionDiscard` + `tension-discard.{full,compact}.tmpl` via `writeHuman` (035-aware) otherwise — buffer-then-write, identical for `204` and `404`. Rejected silent/plain-text success and stdout-empty (both fork the family's render contract — `-o json/yaml/<template>` would break). The `204`-vs-`404` distinction rides a one-line advisory on STDERR (informational, not an error, exit stays `0`) so stdout is byte-identical for both — a small extension of the failures-only stderr convention (031/032). NO `internal/glassfrog` model and NO transport change (bodyless `DELETE`, `ContentType` stays `""`): the first family member that touches neither package. Sets the bodyless-success synthesized-result pattern for future writes (e.g. proposal withdraw).

- Actor Assignments is a SINGLE top-level `assignments <actor-id>` command (`ExactArgs(1)`) — the actor-end mirror of Role Fillers (047) reading the same `Assignment` resource — and GROWS `glassfrog.Assignment` with an embedded `role` object, the one substantive divergence from 047's no-growth reuse (from 050-actor-assignments, 2026-06-13)
  `GET /actors/{actor_id}/assignments` (`listActorAssignments`) is the actor-end twin of 047's role-end `/roles/{role_id}/assignments`: same `Assignment` resource, paginated `{data:[Assignment],meta}`, no query filter beyond `include` + pagination, and the same single-read-absence (`/assignments/{id}` carries only the admin `PATCH`/`DELETE`, no GET) — so one `assignments <actor-id>` command, no plural/singular pair, no `actor` group, ExactArgs(1) (cobra validator, no hand-rolled guard). DECISION: 047 ADR-2 forecast "050 reuses `Assignment` likewise", but that assumed the embed was already present — true for the role end (default `include=actor`, already on the model) but NOT the actor end, whose default `include=role` returns an embedded role object the model lacks. So 050 GROWS `Assignment` in place to add an embedded `Role` struct (`{id,type,name,purpose,parent_role_id}`, tagged `json:"role"`, mirroring the existing `actor` block; nullable purpose/parent as plain strings) — silent conformance to 011 ADR-1 / 025 ADR-2 (grow not duplicate, NOT a new `AssignmentDetail` type) and exactly what 048 (§280) anticipated for the actor-side embeds. Growth is ADDITIVE/forward-compatible: 025's `?include=assignments` and 047's `fillers` projection read only the actor block, so the new `role` field decodes unused there (012→025 pattern; a model test asserts those paths leave it zero-valued). Adds a new `assignments` list render path (`render.ResourceAssignments` + `AssignmentsView{Data []Assignment}` + `assignments.full`/`assignments.compact`) mirroring 047's `fillers` and 048's `actors`; nullable `focus`/`elected_until` + role `purpose`/`parent` get explicit-absence markers. NO filters, NO `--include` (only non-default value `actor` is redundant when the caller named the actor — silent conformance to 047 ADR-3 / 034 ADR-3 "validate nothing"); actor id passes through to a clean 404. List completeness is 025 ADR-3 (`paging.All` + `--first-page`); structured output aggregates raw bytes (`aggregateRawData`). Adds NO new `Outcome`/`ExitCode`. Exact command/flag spellings and whether `include=role` is sent explicitly are interface-level.

- Actor Read GROWS 048's `actors` command from `cobra.NoArgs` to `cobra.MaximumNArgs(1)` — the single-command optional-positional 025 shape (0 args → directory list, 1 arg → single by-id read) — superseding 048 ADR-1's `NoArgs` sub-decision (from 049-actor-read, 2026-06-13)
  `GET /actors/{id}` (`getActor`) is the by-id drill-in the directory (048) deferred to this spec; the natural surface is the actor analogue of `roles`/`roles <id>` (025 ADR-1), one noun on one command. 048 ADR-1 chose `cobra.NoArgs` and rejected an optional positional — but the positional it rejected was a free-text NAME/QUERY (belongs on `--query`/`-q`), NOT an id subject; 048 explicitly left the by-id read to 049. DECISION (announced divergence from 048 ADR-1): widen `actors.go`'s `Args` to `MaximumNArgs(1)` and branch `RunE` on `len(args)` — the 0-arg directory branch (048) is preserved verbatim, the 1-arg branch is the single read. Filters (`--kind`/`--role-id`/`--query`) stay list-only, `--include` stays single-only; each cross-combo (filter+id, `--include`-without-id, >1 positional) is a fail-fast `UsageError(2)` before assembly with a transport tripwire (025 ADR-1 discipline). Rejected a separate singular `actor <id>` command (forks the noun surface, breaks the `roles`/`roles <id>` symmetry) and a `--id` flag (a path-id subject is a positional read, not a filter). 048 is merged on `main` (#90 — `actors.go` ships with `cobra.NoArgs`), so 049 grows the already-landed command — no first-to-land race (STATUS.md's `Ready` label for 048 is stale; #90 never bumped it). CONSEQUENCE (025 ADR-1 foreclosure): cobra cannot distinguish an actor id from a subcommand name under an optional positional, so `actors <id> <subcommand>` is impossible — Actor Assignments (050, `GET /actors/{id}/assignments`) takes a FLAG/SEPARATE-command surface, NOT `actors <id> assignments` (its standalone read is distinct from this spec's `--include assignments` embed). Candidate for `/score:deprecate` on 048 ADR-1's `cobra.NoArgs`. No new `Outcome`/`ExitCode`; exact flag spellings are interface-level.

- Actor Read adds `glassfrog.ActorDetail{ Actor; Roles []Role; Assignments []Assignment }` (the 025 `RoleDetail` shape) for the single read's `{data: ActorDetail}` document, reusing the FULL landed `Role`/`Assignment` as-is, and a singular `ResourceActor` render key — no growth of `Actor`/`Role`/`Assignment` (from 049-actor-read, 2026-06-13)
  `getActor` returns `Actor` with optional `roles []Role`/`assignments []Assignment` embeds (present only when `?include`d); 048 ADR-2 reused bare `Actor` and left the embed fields to this spec. DECISION (silent conformance to 011 ADR-1 grow-not-duplicate + 025 ADR-2): add `ActorDetail` embedding the UNCHANGED `Actor` plus the two optional embed slices — exactly as `RoleDetail` embeds `Role` + its related resources — decoded from the `{data: ActorDetail}` document on the single read, while the directory list (048) keeps decoding `Page[Actor]`. The embedded `Role` (full shape) and `Assignment` are the landed 025 types, NOT new leaf models, so the footprint (each role's purpose/accountabilities/domains, plus assignments) is assembled entirely from existing types. Rejected hanging the embed slices directly on `Actor` (spreads single-read concerns onto every actor/`me`/directory consumer — the split 025 ADR-2 avoided). RENDER: a new singular `ResourceActor` key (`actor.full`/`actor.compact`), the established singular-detail shape (`ResourceRole`/`ResourceDomain`/`ResourcePolicy`) and natural sibling of 048's plural `ResourceActors`; distinct from `ResourceMe` (the org/membership-framed identity document). Embeds are guarded by explicit-absence markers (019), rendered only when `?include`d. `--include` is validated locally against the per-read set `{roles, assignments}` (reject-unknown, 025 ADR-4 closed-enum-validate-locally), distinct from the role include set (026 two-validator precedent); the actor id passes through to a clean `404` (025 ADR-4 — `getActor` documents only 401/404, no silent-wrong-results). NO pagination: `getActor` is a single resource, embeds arrive inline in one `Execute` (the 011/025-single-read shape, not `paging.All`). (Actor Assignments (050) reads the same `Assignment` resource from the actor end but GROWS it with an embedded `role` object for its `include=role` default — 050 ADR-2; it is a list read with its own `AssignmentsView`/`Page[Assignment]`, not a consumer of this spec's single-read `ActorDetail`.)

- Version capture is a verbatim derived accessor `func (r *apiclient.Response) Version() string` reading the `ETag` response header off the `Header` the `*Response` already carries — no stored field, no `Execute` change, no rendered output (from 052-version-capture-on-read, 2026-06-14)
  The Optimistic Concurrency foundation (Clobbered Changes). `Execute(...) (*Response,error)` already propagates `*Response{StatusCode,Header}` up through the `executor` interface + `RetryExecutor` to every command; single reads just discard it (`if _, err := exec.Execute(...)`), so the `ETag` is already in scope — what was missing is a named, tested seam. DECISION (ADR-1): an accessor on `Response` returns `Header.Get("ETag")` (case-insensitive match) VERBATIM — no unquoting, no weak-validator (`W/"…"`) stripping, no normalization — because a later `If-Match` (053) must echo exactly what the server sent or risk a spurious 412; absent header → `""` (consumer reads as "no version", empty == absent, both unusable as a precondition). Rejected a `Version`/`ETag` field populated in `Execute` (adds stored state + an Execute change for a trivially-derived value, against the anti-speculation idiom) and a `glassfrog` domain wrapper (ETag is transport metadata, not a body field; a domain wrapper invites rendering it — forbidden by the spec's no-output non-behavior). Keeps `Execute` and all existing reads byte-identical; transport metadata stays in the transport layer so the render path never sees the version. The exact accessor name/signature is interface-level (`interface-spec.md`).

- Version Capture on Read delivers the read-side mechanism ONLY — wires no existing read call site and does NOT add an `If-Match` field to `apiclient.Request`; consumption + the request-side change are deferred to Guarded Writes (053) (from 052-version-capture-on-read, 2026-06-14)
  DECISION (ADR-2, silent conformance to the 039 resolver → 040 call-site-retrofit split and the 042 narrow-field precedent): 052 ships `Response.Version()` + its unit tests and nothing else. Rejected retrofitting existing single reads (`tension get`/`role get`) to capture now — each CLI invocation is one process that exits after rendering, and with nothing consuming (053 absent), nothing surfaced (non-behavior), nothing persisted (non-behavior) the captured value evaporates unused = dead state. The only sensible consumer is an in-process read-then-write = 053, which performs its pre-write read, calls `resp.Version()`, and sends it as `If-Match`. Per 042's "generalize/add `If-Match` when a real consumer lands", the `apiclient.Request` `If-Match` field belongs to 053, not here. CONSEQUENCE: 052 is a tiny, zero-risk, behavior-preserving, externally-invisible addition whose accessor is intentionally unused until 053 (a documented foundation, next consumer named on the roadmap `053 → requires 052`, contract proven by its own tests independent of any consumer). Collection/list reads yield no per-resource version BY CONSTRUCTION — the pagination walker / `aggregateRawData` path has no version seam and is untouched.

- Subrole Filler Roll-up is a DISTINCT role-keyed read leaf (`ExactArgs(1)` on the anchor role id) — NOT a `subroles` subcommand of the positional-bearing `actors` command — reusing the LANDED `Actor` model, `actors` render path, and `validateKind` with ZERO new model/render/validator; only the request path and the leaf-`404` differ from 048's directory walk (from 051-subrole-filler-roll-up, 2026-06-14)
  `GET /roles/{id}/subroles/actors` (`listSubrolesActors`) is a one-level roll-up (direct sub-roles only, not transitive) of the actors filling an anchor's children, returning the SAME paginated `{data:[Actor],meta}` shape as `listActors` (cross-linked assignments excluded; its only query filter is `kind` — no `role_id`, no `q`). It is to Actor Directory (048) what Subroles Tension Roll-up (046) is to Tension Reads (043). DECISION (ADR-1): make it its OWN top-level role-keyed read leaf, NOT `actors subroles <role-id>` — 046's `tension subroles` rhyme does NOT transfer because `tension` is a non-runnable GROUP while `actors` is a runnable leaf that 049 (pending) grows to `MaximumNArgs(1)`; hosting a subcommand under it would force the runnable-parent-with-children shape 025 ADR-1's foreclosure avoids and create an ordering dependency on 049/050. Silent conformance to 026 (`subroles <role-id>` is its own command beside `roles`) and 050 (`assignments <actor-id>` is its own command beside `actors`). The distinct-command choice REMOVES any coupling to the pending 049/050 — 051 touches neither `actors.go` nor the `assignments` command. REUSE-ONLY at the data layer (silent conformance to 011 ADR-1 / 048 ADR-2/ADR-3/ADR-4 / 046's new-consumer pattern): `--kind` is a new CONSUMER of 048's landed `validateKind` set (`human`/`agent`), the `actors` render path (`ResourceActors`/`ActorsView`) and `Actor` model are reused unchanged — no schema/render/validator phase. Surfaces `--kind` ONLY (the endpoint offers no `role_id`/`q` — 048's directory filters are NOT carried). The leaf-anchor `404` (anchor has no sub-roles) is surfaced VERBATIM through the shared `classifyClientError`/`Diagnose` chain (no "this role has no sub-roles" special-case — VISION Exclusion 1), kept DISTINCT from the genuine empty-list `200` (sub-roles exist but carry no fillers → `0`-exit empty-set line); both pinned by BDD + transport tripwires. Returns bare `Actor` rows, NOT the assignment-shaped read of Role Fillers (047) — no `focus`/`elected_until` projected. Completeness is 025 ADR-3 (`paging.All` + `--first-page`, `kind` carried on every page). Recommended: parameterize the landed `runActorsList` with the path + base query rather than fork a sibling runner. Adds NO new `Outcome`/`ExitCode`. The `role_` id passes through to a clean 404. Exact command/flag spellings (leading candidates `subrole-fillers`/`subrole-actors <role-id>`) and path parameterize-vs-fork are interface-level.

- Guarded Writes adds a narrow `IfMatch string` field to `apiclient.Request`, sent by `Execute` as the `If-Match` header VERBATIM only when non-empty — the request-side mirror of 042's `ContentType`, resolving 042/052's deferred "add `If-Match` when a real consumer lands" without opening a general `Header` bag (from 053-guarded-writes, 2026-06-14)
  The send half of Optimistic Concurrency (052 captures via `Response.Version()`, 053 sends, 054 surfaces the `412`). DECISION (ADR-1): add `IfMatch string` to `Request`; in `Execute`, immediately after the `ContentType` block (`execute.go:128`), `if req.IfMatch != "" { httpReq.Header.Set("If-Match", req.IfMatch) }`. Value sent VERBATIM (no quoting/unquoting/weak-validator `W/"…"` handling/normalization) — the precondition must echo the server's token byte-for-byte (the value 052 captured unchanged) or risk a spurious 412; empty `IfMatch` sets no header so the write proceeds unconditionally (last-write-wins) — the empty-as-absent symmetry with 052's `Version()` sentinel. The set is METHOD-AGNOSTIC (depends only on the field being non-empty), so a `DELETE` (Tension Discard) is guarded like a `PUT`/`PATCH`; the caller populates `IfMatch` only on requests it intends to guard. Rejected a general `Header http.Header` bag on `Request` (over-generalizes for one header against the anti-speculation idiom 042 chose; invites setting transport headers the layer owns, e.g. 007's `X-Auth-Token`) and a dedicated `ExecuteIfMatch` method (forks the single `Execute` seam 010 ADR-1 every command calls through). `If-Match` and `Content-Type` are independent fields — a JSON-bodied guarded write carries both. The general bag waits for a genuine SECOND request header to justify it. Exact field name/doc-comment are interface-level (`interface-spec.md`).

- Guarded Writes delivers the SEND mechanism ONLY — wires no production write command and interprets no `412`; the per-command read-then-write retrofit and distinct `412` surfacing are deferred (from 053-guarded-writes, 2026-06-14)
  DECISION (ADR-2, silent conformance to the 039 resolver → 040 call-site-retrofit split and 052 ADR-2): 053 ships `Request.IfMatch` + the conditional send in `Execute` + tests and nothing else. The FEATURE-MODEL places the write commands (Tension Update 044, Tension Discard 045, Proposal write-flow) "opting into" this Client-Foundation mechanism with "retrofitting each write call-site … per-command work, not a capability here" — so each retrofit (an extra pre-write read, the read-failure path, whether an unguarded write is signalled) is its OWN spec with its own scenarios. Rejected retrofitting the existing write commands now (inflates this spec, couples the shared mechanism to specific commands, against the split the roadmap draws). A refused guarded write returns the EXISTING generic `*ResponseError{StatusCode:412,…}` (`execute.go:151`) surfaced through the landed 004/015 diagnostic+exit-code path — 053 neither classifies nor relabels it; distinct clobber surfacing so the operator can re-read is Stale-Write Surfacing (054), which refines that `412`. CONSEQUENCE: 053 is a tiny, low-risk, behavior-preserving, externally-invisible addition (the `IfMatch` field zero-values to `""`, every landed read/write stays byte-identical) whose field is intentionally unused until a command retrofits — a documented foundation, consumers named on the roadmap (`053 → requires 052`; write-command retrofits → require 053), contract proven by its own tests independent of any consumer.

- Stale-Write Surfacing adds exit code `7` + a `StaleWrite` Outcome by classifying `412` at the existing registry sites (from 054-stale-write-surfacing, 2026-06-14)
  DECISION (ADR-1, silent conformance to 004's single `ExitCode(Outcome)` registry / never-renumber / new-category→new-unused-code rule and 015's split-`APIError`-by-status-at-`categoryForStatus` precedent): branch `412 Precondition Failed` out of the generic `APIError`(3) bucket into a distinct `StaleWrite` Outcome (`dispatch.go`, new iota after `RateLimited`) mapped to a new `codeStaleWrite = 7` (`exitcode.go`), via a `http.StatusPreconditionFailed` arm in `categoryForStatus` — mirroring the 401/403→4, 429→5 arms. `7` is the FIRST code added beyond 004's originally-published 0–6 band; it is anticipated growth, not a contract break (the extension rule designed for it). Classification is status-driven ONLY — never conditioned on whether this CLI sent `If-Match`, the command, or the resource — so it stays decoupled from 053 and every write command. ADR-2: a `412` arm in `nextStepForStatus` (re-read for current version → retry, replacing the misleading generic "check that the token has access" step) and a `412`-aware synthesized cause (API `detail` surfaced when present, else a precondition-failed/changed-since-read fallback — never fabricated). Purely additive: every other status keeps its category/code/cause/next-step; 032 rendering unchanged (Diagnostic keeps its three-field shape). `exitcode_test.go` must pin `7` uniqueness + no-renumber + no-shell-reserved, with comma-ok lookups (zero-value-trap guard). Sets the precedent for adding any future exit-code category beyond 0–6.

- Proposal Reads creates the `proposal` command GROUP via `newProposalCommand(seam)` and adds `list` (`cobra.NoArgs`, the CLI's first GLOBAL non-role-scoped paginated list) + `get <prp-id>` (`ExactArgs(1)`) as verb leaves — verb grammar (NOT a plural/singular noun pair), coordinating first-to-land-creates / follower-reuses with the concurrent Proposal Creation (055) (from 056-proposal-reads, 2026-06-14)
  The read half of the Governance Proposals solution; twin of Tension Reads (043) but no `proposal` code exists yet (055 is concurrent in a parallel workspace). DECISION (ADR-1, conformance to the 042 ADR-2 / 043 ADR-1 verb-grammar precedent, adapted to a group that does not yet exist): the verb grammar is FORCED — the proposal surface is verb-rich (create/propose/withdraw/respond), so a bare `proposal <prp-id>` would collide with subcommand dispatch (the 038 ADR-1 cobra constraint). 056 introduces `newProposalCommand(seam)` (the `newTensionCommand` shape) building a non-runnable group and guard-registers `list`+`get` under it. `proposal list` is GLOBAL — `GET /proposals` takes no path id, so `cobra.NoArgs`, with the circle as an optional `--role-id` FILTER not a positional (contrast `tension list <role-id>`); `paging.All` over a query-only (no path-id) descriptor is already supported, a usage difference not a machinery change. List-only flags (`--status`/`--role-id`/`--proposer-id`/`--proposed-after`/`--accepted-after`/`--first-page`/`--per-page`) live only on `list`; passing any to `get` is cobra's unknown-flag `UsageError(2)` (the structural guard, no hand-rolled check). `--status` is validated locally via a NEW `validateProposalStatus` over the proposal status set (`draft`/`proposed_outside_meeting`/`escalated`/`accepted`/`draft_with_conflicts` — incl. `draft_with_conflicts`, which the FEATURE-MODEL prose omits) in the proposal command file (the `validateTensionStatus`/`validateMeetingType` location, NOT shared `status.go`); the other four filters + the `prp_` id pass through to a clean server `400`/`404` (025 ADR-4 closed-enum-validate-locally / pass-free-identifiers). CONCURRENT COORDINATION (055): first-to-land defines `newProposalCommand`+the group; the follower adds its leaves (043's relationship to 042's `newTensionCommand`). NOTABLY proposal READS are NOT Premium-gated — no plan-gate `403` expected, no special-casing (that signal is the write path's Plan-Limit Signalling). Adds NO new `Outcome`/`ExitCode`; exact flag spellings are interface-level.

- Proposal Reads establishes `glassfrog.Proposal` with a free-form `ProposalChange` (`map[string]any` remainder, the `actions.go` precedent) + an aggregate-only `ResponseSummary`, and adds BOTH the plural `proposals` and singular `proposal` render paths — shared with Proposal Creation (055) under grow-not-duplicate (from 056-proposal-reads, 2026-06-14)
  No `Proposal` model exists; both reads decode the SAME schema (list many, `getProposal` one) and 055 returns it on `201`. DECISION (ADR-2, silent conformance to 011 ADR-1 grow-not-duplicate + the 042/043 model+render pattern): add `internal/glassfrog/proposal.go` — `Proposal` (id/type/status, nullable tension_id/circle_id/proposer_id + lifecycle timestamps → empty string, `Changes []ProposalChange`, `ResponseSummary`, expected/received_response_count, `AvailableTransitions []string`, created/updated_at; forward-compatible decoding). The `changes[]` wrinkle: `ProposalChange` is `additionalProperties:true` with NO per-type schema (typed change construction is the deferred Unguided Change Construction problem), so it keeps `ID`/`Type` + a free-form remainder (`map[string]any`, the landed `actions.go Permissions` precedent) — faithful pass-through, never interpreted (spec non-behavior). `ResponseSummary` is `Total`/`NoObjection`/`BringToMeeting` ints ONLY — no per-person field exists, enforcing the anti-attribution non-behavior at the type level. Decoded via existing generics: `Document[Proposal]` (single), `Page[json.RawMessage]`/`Page[Proposal]` (walked list). RENDER: add BOTH paths (the `ResourceProjects`/`ResourceProject` plural/singular shape, both in `builtinResources` for the exhaustiveness guard) — `ProposalsView`+`ResourceProposals`+`proposals.full`/`.compact` and `ProposalView`+`ResourceProposal`+`proposal.full`/`.compact`; `proposal.full` shows changes BY TYPE (body verbatim — CONSTITUTION VI, no truncation), the response_summary counts, expected/received counts, and available_transitions; `*.compact` summarize to one line; empty-set line on the list. Structured `json`/`yaml` is FAITHFUL REGARDLESS of model field coverage — `aggregateRawData` (list) / `output.RenderSuccess` (single) emit the raw server bytes, so a field the model omits still appears under `-o json`. CONCURRENT COORDINATION (055): first-to-land adds `proposal.go` + the singular render; if 055 lands first with a thinner singular render, 056 GROWS it to surface changes/response_summary/available_transitions (never a second type). No transport change (bodyless GETs, `ContentType` stays `""`); no other model touched. The single read is NOT paginated (embeds arrive inline in one `Execute`). Adds NO new `Outcome`/`ExitCode`; field names + template text are interface-level.
- Proposal Creation is a non-runnable `proposal` group + `create` leaf (`proposal create <tension-id>`), reserving the `proposal` namespace for the whole write-flow (list/get/propose/withdraw/respond) — silent conformance to the 042 tension group/leaf precedent (from 055-proposal-creation, 2026-06-14)
  The anchor of the governance write path: `POST /proposals` (`createProposal`), Premium-gated. DECISION (ADR-1): build `proposal` as a non-runnable group parenting `create` (assembled with its child before `Register`, so 001's group ">=1 child" guard holds), exactly as `tension`/`tension create` (042 ADR-2). The positional `<tension-id>` is the anchor (one required positional, `ExactArgs(1)`, the 042 shape); `--changes` lives on the `create` leaf so a future `proposal get`/`propose` adds its own leaf and rejects `--changes` as a cobra unknown-flag for free. The `proposal` group is SHARED with the concurrent Proposal Reads (056), which also creates it for `list`/`get`: CONCURRENT COORDINATION — first-to-land creates `newProposalCommand`, the follower attaches its leaf(s) to the existing group (043's relationship to 042's `newTensionCommand`). The namespace hosts the rest of the write-flow (057 advance, 058 respond, 059 withdraw). No If-Match (a create has no prior ETag — 052/053/054 untouched); the body's `Content-Type: application/json` rides the landed `Request.ContentType` (042 ADR-1) with no transport change. The `ten_` anchor passes through to a clean 404 (042/044 ADR-3, §200).

- Proposal Creation resolves `--changes` from a reserved `stdin` keyword, an existing regular file, or inline JSON — a pure `resolveChangesSource(value, stat, readFile, isTTY, stdin)` behind a seam, reusing the landed `readBoundedStdinN` (006/035) and DIVERGING from 035's template-source classifier by adding an os.Stat existing-file check + an inline-JSON fallback (from 055-proposal-creation, 2026-06-14)
  DECISION (ADR-2): the change set comes from one `--changes` flag resolved by convention (clarified): `stdin` (reserved, any casing) → the bounded stdin reader with the 035 TTY/empty fail-fast; a value that stats to an existing regular file → `os.ReadFile`; anything else → the value's own bytes as inline JSON. 035's `TemplateRef` classifier is NOT reused verbatim — a template's non-`stdin` value is ALWAYS a file (no inline form), so it does no existence check; the change set's primary case is a small inline array on argv, so it needs the stat-based file/inline split. Reading lives behind the seam (035 ADR-4 — pure over injected `stat`/`readFile`/`stdin`); a stat/read/empty-pipe failure is `UsageError(2)` naming the source, pre-request. A file literally named `stdin` is reachable as `./stdin` (035's reserved-name escape). A regular-file guard avoids reading a directory as a change set. Sets the CLI's first JSON-body-from-source input pattern; future free-form-body writes follow this stdin/file/inline resolution.

- Proposal Creation validates the change set against a `type` floor and passes it through VERBATIM above the floor (request `changes` is `[]json.RawMessage`) — the explicit ANTI-044 case: free-form raw pass-through because the API is schema-free here, NOT a typed input fork (from 055-proposal-creation, 2026-06-14)
  `ProposalChange` is `type` plus open, command-specific keys with no per-type schema in the spec. DECISION (ADR-3): parse the source bytes as a JSON array; reject (`UsageError(2)`, no request, transport tripwire) if not valid JSON, not an array, empty, or any element is not an object or lacks a non-empty `type` (the one key the schema requires — §113 validate-what-is-knowable-locally). Each element stays `json.RawMessage` so it marshals into `{proposal:{tension_id, changes}}` byte-for-byte, preserving every command-specific key untouched and unread. ORDER: resolve `--output` → read+parse source → floor → assemble. Where 044 FORKED a typed `TensionUpdateInput` because client contracts diverged, 055 keeps an UNtyped raw pass-through because there is no per-type schema to model; typed per-change builders are the deferred Unguided Change Construction problem (spec non-behavior), and the floor is the seam they would later attach to. Malformed-but-typed changes reach the server and surface as a clean 422. No new `Outcome`/`ExitCode`.

- Proposal Creation grows `internal/glassfrog` with a `Proposal` response model (v5 `Proposal` schema: `prp_` id, status, tension_id, changes, response_summary, available_transitions, …) decoded through `Document[Proposal]`; machine renders raw `{data}`, human renders the SHARED singular proposal view (with 056) surfacing the `prp_` id + `draft` status; 055's exclusive add is the write request-input (from 055-proposal-creation, 2026-06-14)
  DECISION (ADR-4, silent conformance to 011 ADR-1 grow-not-duplicate + 042's created-resource render + §143/§254 generic envelope): add `glassfrog.Proposal` (forward-compatible decoding) + the `{proposal:{tension_id, changes []json.RawMessage}}` request input; decode the 201 `{data: Proposal}` into the generic `Document[Proposal]` (no per-resource envelope); machine formats emit raw `{data}` via `output.RenderSuccess`, the human path renders a proposal view surfacing the load-bearing `prp_` id and `draft` status, plus anchor `tension_id`, change count, and `available_transitions`. Rejected raw-JSON-only (forks the 019/020 human-view contract). CONCURRENT COORDINATION (056 Proposal Reads): the `Proposal`/`ProposalChange`/`ResponseSummary` response model and the singular `proposal` render are SHARED — first-to-land creates, follower reuses-or-grows (043->042). The response `changes` decode as `[]ProposalChange` (056 ADR-2, free-form `map[string]any` remainder); if 056 lands first 055 reuses its model + richer singular view unchanged. 055's EXCLUSIVE addition regardless of order is the write request-input `{proposal:{tension_id, changes []json.RawMessage}}` (verbatim send) — 056 has no write and never adds it.

- Response Recording adds `proposal respond <prp-id> --response <no_objection|bring_to_meeting>` as a verb leaf under the (coordinated) `proposal` group, with a REQUIRED closed-enum `--response` validated locally via a NEW `validateProposalResponse` (omitted value is the required-error, since consent has no default) — coordinating first-to-land-creates / follower-reuses with concurrent 055/056 (from 058-response-recording, 2026-06-15)
  The consent-window write of the proposal flow; the single-write shape of 042 (Tension Capture) on the `proposal` group. DECISION (ADR-1, conformance to 042 ADR-2/056 ADR-1 verb-grammar group + 042 ADR-3/025 ADR-4 closed-enum fail-fast): a `respond` leaf (`cobra.ExactArgs(1)` for the `prp_` id) under the `proposal` group, guard-registered + wired, delegating to a pure `runProposalRespond(cfg)` over the family seam. The value rides a REQUIRED `--response` flag (chosen over a second positional, which reads as another id and gives no flag name for the usage error — the `--body`/`--meeting-type`/`--status` flag-input idiom). A new pure `validateProposalResponse` (the `validateMeetingType`/`validateProposalStatus` shape — `supportedProposalResponses` map + sorted `supportedProposalResponseNames()`, in the proposal command file NOT shared `status.go`) rejects an OMITTED `--response` (no default consent answer) AND an unsupported value as `UsageError(2)` naming the requirement/value + supported set, BEFORE any context assembly/request (transport tripwire asserts nothing sent); `--output` resolves first, then `--response`. The `prp_` id is `url.PathEscape`'d one segment, passed through to a clean `404` (no local id regex). The server derives the responding person from the token (007) — the body carries NO person field. CONCURRENT COORDINATION (055/056): first-to-land defines `newProposalCommand`+the group; followers attach their leaves; `--response` lives only on `respond` (cobra unknown-flag guard for free). Adds NO new `Outcome`/`ExitCode`; exact spellings are interface-level.

- Response Recording establishes `glassfrog.ProposalVote` (a NEW response schema, distinct from 056's `Proposal`) + a tiny `{response:{value}}` request input, and a dedicated recorded-response render path surfacing the parent `proposal_status` (the auto-acceptance signal); reuses 042's `ContentType` write-body seam and sends NO `If-Match` (append-create, not a guarded edit) (from 058-response-recording, 2026-06-15)
  The `201` returns `{data: ProposalVote}` — id (`prr_`), type (`proposal_response`), nullable `proposal_id`, `proposal_status` (the parent proposal's status at response time, `accepted` when this response triggered auto-acceptance), `value`, created/updated_at — a DIFFERENT schema from `Proposal`. DECISION (ADR-2, silent conformance to 011 ADR-1 grow-not-duplicate for RESPONSE models + the 042 single-write `Document[T]` render + §322 per-operation request input): add `ProposalVote` + a `ProposalResponseInput`/`ProposalResponseBody` (single non-omitempty `Value`, the 042 `TensionInputBody` shape, + a `NewProposalResponseInput` constructor) to `internal/glassfrog/proposal.go` (the shared file; APPEND under grow-not-duplicate — `ProposalVote` is a new type in every landing order, no grow/shrink negotiation with 055/056's `Proposal`). The `201` decodes into the existing generic `Document[ProposalVote]` (§143/§254 — no per-resource envelope). RENDER: a `ProposalVoteView`+`ResourceProposalResponse`+`proposal-response.full`/`.compact` (the `ResourceProject` singular shape), registered in `builtinResources` for the exhaustiveness guard; `proposal-response.full` surfaces the `prr_` id, recorded `value`, anchoring `proposal_id`, and (LOAD-BEARING) the parent `proposal_status` rendered so `accepted` is legible as "consent window closed"; `.compact` is id+value+status one line. Structured `json`/`yaml` via `output.RenderSuccess` over raw `{data}` bytes (018) — faithful regardless of model coverage. DECISION (ADR-3, silent conformance to 042 ADR-1 + 053's opt-in `IfMatch` contract): `runProposalRespond` sets `Request.ContentType="application/json"` (reusing 042's seam as intended for "the rest of the write path") and leaves `Request.IfMatch` EMPTY — recording a response is an append-CREATE with no prior `ETag` of the vote to guard, and the double-response case is the server's one-per-person `422`, NOT a `412`; guarding here would send a no-op/spurious precondition (rejected). CROSS-CUTTING: Premium `403` → `PermissionError(4)` via the shared chain with NO plan-limit special-casing (Plan-Limit Signal is a separate spec); one-per-person `422` → `APIError(3)`, NOT retried, NOT folded into success (contrast 045's idempotent `404`-as-success); `POST` never auto-retried on `429` (017 `isSafeMethod`), so recording cannot double-fire. Adds NO new `Outcome`/`ExitCode`; field names + template text are interface-level.

- Advance to Circulation DECODES the server-returned `{data: Proposal}` on a `200` and renders it through the shared singular `proposal` path — decode-and-render, the INVERSE of discard's synthesize-on-bodyless-success (from 057-advance-to-circulation, 2026-06-15)
  `proposeProposal` returns `200` with the full advanced `Proposal` (now `proposed_outside_meeting`, carrying the server-computed `response_deadline`, the proposer's auto-recorded implicit `no_objection` in `response_summary`/`received_response_count`, and updated `available_transitions`) — the OPPOSITE of discard's bodyless `204` (045 §339, which synthesized `{data:{id,discarded}}` because there was nothing to echo). DECISION (ADR-2, silent conformance to the 056/055 `Proposal` model + singular `proposal` render): `runProposalPropose` decodes the `200` into the sibling-owned `Document[Proposal]` and dispatches on the resolved render target EXACTLY as `proposal get` does — `output.RenderSuccess` over the raw `{data: Proposal}` bytes for a built-in machine format (faithful regardless of model field coverage, so any omitted field still appears under `-o json`), `writeHuman` over the decoded `ProposalView`/`ResourceProposal` otherwise (buffer-then-write → a render failure leaves stdout empty, `RuntimeError(1)`). The command adds NO `internal/glassfrog` model and NO `internal/render` surface — it is the first proposal WRITE to be a pure CONSUMER of both, where the reads/creation own them. It surfaces the proposal as returned and NARRATES NONE of the side effects (notifications, deadline computation, implicit response) — visible only through the returned data (spec non-behavior). Rejected synthesizing a thin id+status result (the discard shape): it would discard server-confirmed data the operator/automation want and fork the render path for no gain. CONSEQUENCE: the two writes now BRACKET the write-output design space — a write whose success echoes a server resource decodes-and-renders (057); a write whose success is bodyless synthesizes (045). Bodyless POST + decode-response is also a new transport COMBINATION (createTension is POST+body+decode, discard is bodyless-DELETE+no-decode) needing no transport change (`Method` free string, `ContentType` optional from 042 §283, `Execute` decodes `out`). Sets the decode-and-render pattern for the other proposal transitions (Withdraw, Response Recording) that also return a `Proposal`.

- Advance to Circulation treats the `propose` transition as SERVER-AUTHORIZED — no client pre-check of `available_transitions`, `404`/`422` are REAL FAILURES (the INVERSE of discard's `404`-as-success), and the Premium `403` routes through the shared classifier with NO plan-gate special-casing (from 057-advance-to-circulation, 2026-06-15)
  `proposeProposal` is allowed ONLY when `propose` is in the proposal's `available_transitions` (server-enforced with `422`) and is Premium-gated (`403` when async proposals disabled); documented non-2xx: `401`/`403`/`404`/`422`. DECISION (ADR-3): (a) the command does NOT pre-read the proposal to check `available_transitions` — it issues the one bodyless `POST` and lets the server's `422` enforce (a pre-read forks the server's authority, adds a round-trip, and risks a stale snapshot); (b) it INTERCEPTS NO status — every non-2xx routes through the landed `reportFailure`/`refineClientError`/`classifyClientError` chain unchanged (`404`/`422`/other → `APIError(3)`, `401`/`403` → `PermissionError(4)`, `429` → `RateLimited(5)`, transport → `NetworkUnavailable(6)`), the EXACT OPPOSITE of discard's `errors.As`-for-`404`-as-success divert (045 §336) — because an advance has NO idempotent re-run (re-proposing a circulating proposal is a genuine `422`; a `404` is a real missing proposal), so both must surface loudly; (c) the Premium `403` gets NO bespoke "not available on your plan" message — it is a generic `PermissionError(4)` like any refusal, leaving the plan-gate signal to the separate Plan-Limit Signalling capability (which will refine the `403` centrally the way 054 refined the `412` for all writes — NOT per-command here). A `429` on this `POST` is never auto-retried (017 `isSafeMethod` is GET/HEAD-only), so the advance cannot double-fire. Adds NO new `Outcome`/`ExitCode`. CONSEQUENCE: establishes the INVERSE-of-discard precedent — a write whose success has no idempotent end-state treats `404`/`422` as real failures, bracketing §336 (non-2xx IS success) against this (non-2xx is NOT). Sets the no-pre-check + real-failure + generic-403 template for the other server-authorized proposal transitions (Withdraw, Response Recording).

- Feature-Gate Recognition ships as a STANDALONE, UNWIRED pure recognizer — a `Gate` enum + a static `(method, path-template)→gate` registry + `RecognizeFeatureGate(method, path, status) Gate` in `internal/apiclient` (sibling to `problem.go`) — that supplies the OPERATION dimension a status-only classifier lacks; the central 054-style 403 refinement is deferred to Plan-Limit Signal (#61) (from 060-feature-gate-recognition, 2026-06-15)
  Recognition is recognition-ONLY: it renders nothing, changes no exit code, alters no error chain, and is NOT wired into `reportFailure`/`Diagnose` — #60 depends only on 015 (it interprets the status 015's `ProblemError` carries), while #61 depends on BOTH #60 and Diagnostic Normalization (031) because #61 owns the wiring + the "not available on your plan" wording. DECISION (ADR-1, conforms to the 057/058 reservation that the Premium `403` be refined "centrally the way 054 refined the 412 — NOT per-command"): the decisive difference from 054 is that a plan-gate `403` is NOT status-only — every `403` is `PermissionError(4)` today and only the GATED OPERATIONS are plan-limits, so the refinement needs an operation dimension that 054's `412`→StaleWrite (status-only, "regardless of the command") never needed. #60 provides that dimension as a tested leaf function; #61 performs the central diagnostic-site refinement using it and takes on the new cost of threading the failed request's method/path to the central `Diagnose` site (054 needed only status, so had no such cost). Rejected wiring it inertly now (adds live-path code + threading with no observable effect, risks the no-error-chain-change non-behavior) and rejected per-call-site `FeatureGateError` wrapping (the single `refineClientError` site re-extracts a fresh `ProblemError` and would DISCARD an outer wrapper — 015 ADR-4). DECISION (ADR-2): key on HTTP method + path TEMPLATE matched segment-wise (`{seg}` = one-segment wildcard, query ignored) against an explicit per-operation registry transcribing the spec's gate metadata — the four Premium async-proposal writes (`POST /proposals`, `…/{id}/propose`, `…/{id}/withdraw`, `…/{id}/responses`); `withdraw`/`responses` are registered though their commands aren't built yet (recognition keys on the OPERATION, not the command, so it's correct the moment the request issues). Explicit rows (not a `POST /proposals*` prefix) prevent a future non-gated POST from mis-recognizing. DECISION (ADR-3): model BOTH gate kinds in the `Gate` type (`GatePremiumAsyncProposals`, `GateAIIntegration`) but REGISTER only the reachable Premium family — `GateAIIntegration` is modeled-but-unregistered (the deferred agent/skill endpoints have no command per PROJECT scope), pinned by a guard test asserting no registered row is `GateAIIntegration`; a future `ai_integration` command extends by adding rows, not changing the type. DECISION (ADR-4): a non-`None` `Gate` means the `403` is CONSISTENT WITH a known plan gate — a SUSPICION, never a verdict — because the `403` body is not self-identifying and a genuine permission `403` on a gated op is indistinguishable; #61 must word the diagnostic as possibility ("may not be available on your plan"), never certainty. CONSEQUENCE: establishes that an operation-aware refinement of an otherwise status-only classifier is split into a tested recognizer (the operation dimension) + a dependent signal spec (the central site edit); sets the registry + possibility-not-certainty template for any later gate work.
