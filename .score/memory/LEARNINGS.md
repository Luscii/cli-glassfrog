# Learnings

Operational patterns discovered during implementation. Each entry records a non-trivial finding that future specs should account for. Managed by the implement skill via memory-protocol.md.

---

## Findings

### 2026-06-03 — cobra injects `completion` and `help` commands outside the registration guard
(Found during [T004](../../specs/001-command-registration/tasks.md) implementation)

- **Type**: design-issue
- **Location**: `internal/cli` (cobra root command), surfaces in `glassfrog --help` / command listing
- **Severity**: low — expected cobra behavior; no impact on Command Registration's guarantees, but it affects sibling capabilities.
- **Description**: cobra automatically adds built-in `completion` and `help` subcommands to the root. These are framework-provided and do **not** pass through our `Register`/`MustRegister` guard, so they are not subject to the fail-loud rules and they appear in the command listing alongside guard-registered commands.
- **Suggested action**: The **Help & Version** spec (and **Argument Dispatch**) should decide explicitly how to treat cobra's built-ins — list them, hide them, or replace cobra's default help — rather than assuming every visible command came through the guard. If the built-ins are unwanted, disable `completion` via `cmd.CompletionOptions.DisableDefaultCmd = true`. For `help`, note the subtlety surfaced in PR #5 review: `SetHelpCommand(&cobra.Command{Hidden:true})` only hides the command from listings — `glassfrog help` **still resolves**. cobra has no clean "no help command" switch: `InitDefaultHelpCmd` (called inside `ExecuteC`, after `Assemble`) always attaches *some* help command when the root `HasSubCommands()`, so a placeholder must occupy the slot. **The right placeholder carries an empty `Use` (`SetHelpCommand(&cobra.Command{Hidden: true})`) — not a non-`help` name.** 003 implemented `Use: "__help_disabled"` first, but PR #6 review caught that a *named* placeholder is itself a resolvable invented path: `glassfrog __help_disabled` resolved as a hidden child and returned `Success` with empty output, contrary to the no-invented-commands / no-fabricated-usage non-behaviors. An empty-`Use` placeholder has no typeable token — `glassfrog help` and `glassfrog __help_disabled` are both unknown commands, and even a degenerate empty argument is rejected as unknown rather than silently succeeding; the `--help` flag is unaffected. Resolved by 003 (ADR-2) as implemented in PR #6. **Test note**: assert the *invariants* — built-ins absent + alphabetical relative order (`roles` before `version`) — rather than pinning the full top-level set (`"roles,version"`), so adding a legitimate command later does not break the help test (PR #6 review).

### 2026-06-03 — cobra rejects unknown subcommands only at the root, not under nested groups
(Found during [T002](../../specs/002-argument-dispatch/tasks.md) implementation)

- **Type**: design-issue
- **Location**: `internal/cli/dispatch.go` (`Run`), surfaces for any `glassfrog <group> <typo>` invocation.
- **Severity**: medium — without handling, an unknown subcommand under a group fails open (prints the group's help, returns no error), violating the dispatch contract that every unmatched token is a `UsageError`.
- **Description**: cobra's default arg validator (`legacyArgs`) only emits an `unknown command` error when the command has **no parent** (the root). A non-runnable nested group (e.g. `roles`) given an unknown subcommand (`lst`) returns `flag.ErrHelp` *before* arg validation runs, so cobra prints the group's help and `Execute` returns `nil`. Setting `Args: cobra.NoArgs` on the group does **not** fix this — `Find` skips `legacyArgs` once `Args` is non-nil, and the `!Runnable() → flag.ErrHelp` early return still pre-empts `ValidateArgs`. Our registration guard also forbids giving a group a `RunE` (a command is leaf-with-action XOR group-with-children), so the group cannot be made runnable to intercept the args. Dispatch therefore detects the swallowed token itself: after `ExecuteC`, when the executed node is a non-runnable group and `executed.Flags().Args()` (cobra's own flag-stripped positional remainder) is non-empty, `Run` synthesizes `unknown command "<token>" for "<path>"` and classifies `UsageError`. **Because cobra returned `nil` here it printed no error of its own, so `Run` must also write the synthesized error (plus a `Run '<path> --help' for usage.` pointer) to `executed.ErrOrStderr()` — otherwise the operator sees only the group help and never which token was unrecognized (PR #4 review, round 1).** This stderr write belongs in `Run`, not the entrypoint: blanket-printing returned errors in `main.go` would double-print the errors cobra already emits itself (root unknown command, unknown flag).
- **Suggested action**: **Help & Version (003)** owns help-text rendering and the keep/hide of built-ins; if it customizes the help command or the not-runnable behavior, re-verify this swallow-detection still fires. **Exit-Code Convention (004)** maps the `UsageError` category — note that the nested-group unknown-subcommand path produces `UsageError` via a *synthesized* error (not a cobra-native one), so any error-type inspection in 004 must not assume a cobra sentinel.

### 2026-06-03 — cobra leaf commands accept arbitrary positional args unless they declare an `Args` validator
(Found during PR #4 review of [002-argument-dispatch](../../specs/002-argument-dispatch/tasks.md))

- **Type**: design-issue
- **Location**: leaf command constructors (`version.go`, `roles.go`); classification in `internal/cli/dispatch.go` (`Run`).
- **Severity**: medium — without an `Args` validator a leaf silently ignores extra positional tokens (`glassfrog version extra` runs the action and drops `extra`), violating the Invalid-input accord ("unexpected input is never silently ignored; the command does not run").
- **Description**: a cobra command with no `Args` field defaults to `ArbitraryArgs`, accepting any positional tokens and passing them to the action. Each leaf must declare what it accepts: leaves with no positional arguments set `Args: cobra.NoArgs`; a leaf taking a fixed argument (e.g. a future `roles get <role-id>`) sets `cobra.ExactArgs(n)`. The acceptance contract is per-command, so dispatch can't enforce it generically — but it must *classify* the rejection. cobra runs `ValidateArgs` before the action, so when `Run`'s runnable-command arm sees a non-nil `err`, it re-checks `executed.ValidateArgs(executed.Flags().Args())`: non-nil means the command refused an unexpected arg (`UsageError`, action never ran); nil means the action ran and the error is its own runtime failure (`Success`, with the error returned — `RuntimeError` deferred to 004).
- **Suggested action**: every new leaf must declare an `Args` validator matching its positional contract. Consider whether the **Command Registration** guard should *require* an explicit `Args` validator on leaves (fail-loud) so this can't be forgotten — today it is a convention, not an enforced rule. **Mirror the contract in every test fixture**: when production leaves gained `NoArgs`, the unit-test tree (`dispatchTree`'s `mark`) was updated but the BDD `recordingLeaf` was initially missed (PR #4 review, round 2). A test fixture that omits the validator silently diverges from production and can let a future scenario pass extra positional tokens that real commands would reject — so when you add a per-command constraint, audit *both* the unit-test tree and the BDD harness for parity.

### 2026-06-04 — `SilenceErrors` swallows pre-`RunE` arg errors: print them from dispatch, NOT the `Args` func (it is re-invoked)
(Found during PR #16 rounds 3–4 review of [006-credential-storage](../../specs/006-credential-storage/tasks.md); code in `internal/cli/authcmd.go`, `internal/cli/dispatch.go`)

- **Type**: design-issue
- **Location**: `internal/cli/dispatch.go` (`Run`, arg-rejection branch) and any leaf that sets `SilenceErrors`/`SilenceUsage` and relies on `RunE` to emit its own operator-facing messages.
- **Severity**: medium — round 3: a usage error (`auth login a b`) gave a bare exit code 2 with **no message** (silent crash); round 4: the round-3 fix then printed the message **twice**.
- **Description**: `auth login` sets `SilenceErrors`/`SilenceUsage` so `runLogin` controls every message (path-only, never the token). But cobra runs the `Args` validator (`MaximumNArgs(1)`) *before* `RunE` — a rejected positional returns straight out of `ExecuteC`, never reaching `RunE`, and `SilenceErrors` suppresses cobra's own dump. `dispatch.Run` classifies it as `UsageError` (→ code 2) but printed nothing. This is the trap hiding *behind* the line-27 learning ("declare an `Args` validator"): declaring it is necessary but not sufficient once you silence cobra. **Round 3 fixed it the wrong way** — by printing inside a custom `Args` func. That double-printed, because `dispatch.Run` *re-invokes* `executed.ValidateArgs(...)` to classify the rejection (tell an arg error apart from a runtime error), so a side-effecting validator runs its print twice. **Round 4** is the correct shape: keep `Args` side-effect-free (`cobra.MaximumNArgs(1)`), and emit the `Error: <cause>\nRun '<path> --help' for usage.` line once from dispatch's arg-rejection branch, **gated on `executed.SilenceErrors`** so commands that let cobra print (the root sets only `SilenceUsage`, so `version`/`roles` print via cobra) don't double-print.
- **Suggested action**: (1) surface a silenced command's *pre-`RunE`* failures (arg validation, pre-action flag checks) at the single authoritative classification site, not in the validator. (2) **A predicate the framework calls AND your own code re-calls must be side-effect-free** — printing/logging/mutating in a cobra `Args` func (or any validator re-invoked for classification) duplicates the side effect; put it at the one site that runs once. (3) **Test the message AND its count:** assert `strings.Contains(output, …)` on the operator-facing text (here `accepts at most 1 arg` + `--help`) so the silent-exit path can't return, AND assert `strings.Count(output, …) == 1` so the double-print can't return — the round-3 test checked only `Contains`, which passed against the double-print. General rule for silenced cobra commands: every operator-triggerable error needs a test pinning *some* stderr output and its exact count, because `SilenceErrors` makes both "no message" and "doubled message" passing-looking failure modes.

### 2026-06-03 — new scenarios in the shared feature file must reuse the existing step vocabulary
(Found during PR #7 review of [004-exit-code-convention](../../specs/004-exit-code-convention/tasks.md))

- **Type**: convention
- **Location**: `features/no-runnable-cli/` (the shared problem-driven directory — one per-capability feature file per CLI-Skeleton spec, all wired to one godog step set); step definitions in `internal/cli/dispatch_bdd_test.go`.
- **Severity**: low — no behavior impact, but novel step phrasing forces the Builder to write duplicate godog bindings for assertions that already exist, and clutters the shared file.
- **Description**: the `features/no-runnable-cli/` files accumulate scenarios across specs (001–004 so far, one capability file each), all wired to one godog step set. godog matches steps by exact text, so phrasing that merely rewords an existing assertion silently demands a *new* binding for the same check — e.g. `dispatch will classify…` vs the existing `it will classify the outcome as a usage error`; a combined `route to a help outcome classified as success` vs the existing split `route to a help outcome` + `the outcome will be a success`; `…command whose action completes its work is registered` vs `…command is registered at the top level`. Copilot flagged four such cases on PR #7 (all accepted). Only genuinely-new assertions (004's `Then the process will exit with code N`) legitimately need new bindings. Two further rules surfaced: (1) keep feature wording **implementation-agnostic** — "runtime default status 2" leaked Go-runtime detail into a file whose own `@validation` scenario forbids naming implementation tech; use plain `code 2`; and (2) make repeated assertions **uniform** (`it will not exit with code N`, not a mix of `will never` / `will not`) so they share one parameterized binding.
- **Suggested action**: before adding scenarios to a shared feature file, grep the existing step registrations (`sc.Step(` in the `*_bdd_test.go` for that package) and phrase Given/When/Then to match an existing step whenever the assertion already exists; reserve new phrasings for genuinely new behavior. Keep step text behavioral and tech-agnostic, and keep repeated assertion wording identical so one regex binding covers all instances. **Match the exact anchored regex, including prefixes — approximate vocabulary is not enough** (PR #7 round 2): round 1's fix changed `route to a help outcome classified as success` to `dispatch will route to a help outcome`, but the registered step is `^route to a help outcome$` (no `dispatch will` prefix), so it *still* needed a new binding. The existing 002 pattern is `dispatch will resolve to the "X" group` + `route to a help outcome listing "..."` + `the outcome will be a success` — copy a working multi-step sequence verbatim from a sibling scenario rather than reconstructing it. **Also sweep the source `# Source:` / `# Proposed:` comments for the same tech-agnostic rule** — round 1 fixed the step text `runtime default status 2` → `code 2` but left the matching scenario comment saying "runtime default 2", which round 2 then flagged (symmetric-site miss). The reliable check is to run the godog suite (or `--dry-run`) so unbound steps surface as missing-definition errors instead of waiting for a reviewer. **Put command flags/arguments inside the quoted invocation, never in prose around it** (PR #62, 026 subroles first-page scenario): a step like `runs "glassfrog subroles role_0123" with the first-page opt-out flag` forces a one-off binding, whereas `runs "glassfrog subroles role_0123 --first-page"` is captured by the generic `runs "<cmd>"` step that every other governance-read scenario already uses (e.g. `role-reads.feature`: `glassfrog roles --first-page`). The same symmetric-site rule applies across artifacts: the fix also aligned the spec's first-page *driving scenario* to the concrete `--first-page` (its sibling `--depth` scenarios were already concrete), so the spec, feature file, and interface don't drift on the flag spelling.

### 2026-06-04 — a resolved command had no way to emit a UsageError; 006 added a typed-error seam to dispatch
(Found during [T004](../../specs/006-credential-storage/tasks.md) implementation)

- **Type**: design-issue
- **Location**: `internal/cli/dispatch.go` (`Run` default arm) + `commandUsageError`; produced by `internal/cli/authcmd.go` (`runLogin`).
- **Severity**: medium — without it, the interface-cli mapping for Credential Storage (no-token / blank / existing-no-overwrite → `UsageError`/code 2) was unreachable: dispatch mapped every resolved-action error to `RuntimeError`/code 1.
- **Description**: through 002/004, the only `UsageError` sources were cobra-level — unknown command/flag (flag-error hook, non-runnable-node resolution, synthesized nested-group token) and a leaf's own `Args` validator rejection. A *runnable* leaf whose action returned an error was always classified `RuntimeError` (the default arm). But 006's `auth login` must classify several **invocation** faults its action only discovers at runtime (no token resolvable from any source; a blank token; an existing credential with no `--overwrite`) as `UsageError`. An `Args` validator can't express these (they depend on stdin/TTY/env/existing-file state, not positional args). Resolution (ADR-5/DECISIONS): a small unexported `commandUsageError{err}` wrapper that a resolved action returns; dispatch's default arm does `errors.As(err, &*commandUsageError)` *after* the `ValidateArgs` re-check and returns `UsageError` for it, else `RuntimeError`. The leaf sets `SilenceErrors`/`SilenceUsage` and writes its own cause+next-step message (path only, never the token), so the wrapped error is for classification, not display.
- **Suggested action**: **Request Authentication (007)** and any future command that distinguishes operator-fixable invocation faults from internal failures should return `commandUsageError` for the former rather than inventing a parallel mechanism or a second exit-code path — the category→code mapping stays in the single `ExitCode` registry (004). If 007's API client adds the reserved operational categories (APIError/Permission/RateLimited/Network, codes 3–6), those are a *different* axis (the client classifies them into the `Outcome` enum directly); `commandUsageError` is only the command-action→`UsageError` bridge. Pin any new command-originated category with a focused dispatch test (see `TestRun_CommandUsageError_IsUsageErrorCategory`).

### 2026-06-04 — an error-classifier helper must discriminate every typed source and scope its claims
(Found during PR #16 review of [006-credential-storage](../../specs/006-credential-storage/tasks.md))

- **Type**: design-issue
- **Location**: `internal/cli/authcmd.go` (`classifyAuthError`), consumed by `runLogin`.
- **Severity**: low — wrong-but-not-dangerous operator message; no behavior or exit-code impact.
- **Description**: `classifyAuthError` originally branched on `*auth.FormatError` and treated *everything else* as a "write error … the filesystem is unchanged". Two callers feed it errors that are neither: (1) the pre-write guard's `auth.ReadCredentialsFile(target)` returns a wrapped *read* error for an unreadable existing file — reported as a "write error / check write permission" though no write was attempted; and (2) the interactive "both" choice writes multiple targets in a loop, so a failure on a later target occurs *after* an earlier target was already written and its "Stored…" line printed — making the global "filesystem is unchanged" claim false. Fix: three branches — `*FormatError` (not overwritten), `*auth.WriteError` (claim scoped to the single path: the atomic temp+rename guarantees *that path* was not partially written; no global filesystem claim), and any other error reported as a read/access failure naming the path. Copilot caught it in round 1.
- **Suggested action**: when a single helper maps several error sources to one operator-facing message, **discriminate every typed error the producers can return (`errors.As` per type), not just the one obvious type** — a catch-all "else" silently mislabels the others. And **scope state claims to what the code actually guarantees**: an atomic single-file write guarantees only *that file* is intact, so a per-target loop must not claim the whole filesystem is unchanged. The `auth` package already exposes `FormatError`/`WriteError` precisely so callers can tell them apart — use both. Companion to the user-level "throw a named error class so the catcher can discriminate" learning: this is the receiving end (the classifier must actually branch on each type).
- **Producer-side mirror (round 5):** a classifier can only be as accurate as the typed errors it is handed — so the **producer must emit the accurate type at the error's origin**. `WriteCredentials` wrapped a *pre-write* `os.ReadFile` failure (permission denied, or a directory at the path — anything but not-exists) as `*WriteError`, even though no write was attempted. That mislabels at the source: a downstream classifier branching correctly on `*WriteError` would still print "write error" for what is really a read/access failure. Fix: return 005's `*ReadError` from that branch, so the type matches the operation that actually failed and the CLI's `classifyAuthError` read branch ("could not read … no changes were made") fires. **Rule: name a returned error after the operation that failed, not the operation the function is named for** — a writer that fails while *reading* its existing input returns a read error, not a write error. Round 1 fixed the consumer; round 5 fixed the producer feeding it. When auditing typed-error flows, walk both ends: every `errors.As` branch in the classifier AND every `return &XxxError{...}` site in the producers, confirming each names the operation that genuinely failed.

### 2026-06-04 — a writer of a shared file must honor the reader's multi-entry resolution rule
(Found during PR #16 review of [006-credential-storage](../../specs/006-credential-storage/tasks.md))

- **Type**: design-issue
- **Location**: `internal/auth/write.go` (`mergeTokenLine`); reader is `readCredentialsFile` (005).
- **Severity**: medium — a write could silently leave the *old* token effective and leave the stale secret on disk.
- **Description**: the shared reader is **last-`token=`-wins** (`if key == tokenKey { token = value }` over all lines). `mergeTokenLine` replaced only the *first* `token=` line and `break`ed. On a file with two `token=` lines, the write updated the first while the reader returned the second (old) value — the new token never took effect, and the old secret stayed in the file. The single-token round-trip test didn't exercise it. Fix: replace the first token entry in place and **drop every later `token=` line**, collapsing to one entry; added a multi-token regression test.
- **Suggested action**: when a writer edits a file a separate reader parses, **pin the reader's conflict-resolution rule for duplicate keys and make the writer converge on the same effective value** — last-wins reader ⇒ the writer must ensure the surviving/last entry is the new one (collapsing duplicates is the safe move; it also avoids leaving stale secrets). Test the duplicate-key case explicitly: a happy-path single-entry round-trip will not catch the divergence. Companion to the shared-format-module / round-trip DECISIONS.

### 2026-06-04 — validate the bytes you act on, not a re-read: share one parse step between read and write
(Found during PR #16 round 4 review of [006-credential-storage](../../specs/006-credential-storage/tasks.md); code in `internal/auth/credentials.go`, `internal/auth/write.go`)

- **Type**: design-issue
- **Location**: `internal/auth/write.go` (`WriteCredentials`) and `internal/auth/credentials.go` (`readCredentialsFile` → `parseCredentials`).
- **Severity**: medium — a check-then-act TOCTOU: the "malformed → no write" guarantee was validated against a *different* read of the file than the one being merged.
- **Description**: `WriteCredentials` did `existing, _ := os.ReadFile(path)` (the bytes it merges), then called `readCredentialsFile(path)` to validate — which **re-reads the file from disk**. Validation and merge thus operated on two snapshots; a concurrent edit between the two reads could let a malformed file into the merge output (or reject a now-valid one), and it doubled the I/O. The reader-vs-writer "don't drift" intent (ADR-1) was honoured at the *function* level (both call the shared reader) but violated at the *byte* level (different reads). Fix: extract `parseCredentials(path, data []byte)` — the shared, **I/O-free** parse step. `readCredentialsFile` reads then parses; `WriteCredentials` parses the already-read `existing` bytes it is about to merge. One snapshot, one read, no race. Bonus: a pure parse step can only return `*FormatError` (no `*ReadError`), which simplified the writer's error handling.
- **Suggested action**: whenever a function reads bytes, validates, then writes/transforms those bytes, make validation and the action consume the **same in-memory snapshot** — never re-open/re-read between check and act. The clean factoring is a pure `parse(data)` / `validate(data)` helper that takes bytes (or an `io.Reader`), with the single `os.ReadFile` at the boundary; both the read path and the write path call the pure helper. Re-reading "to be sure it's still valid" is the classic TOCTOU anti-pattern — it widens the race window instead of closing it. Generalises beyond files to any check-then-act over mutable shared state.

### 2026-06-04 — harden the credential command's input path: reject newline-bearing tokens, bound piped stdin
(Found during PR #16 review of [006-credential-storage](../../specs/006-credential-storage/tasks.md))

- **Type**: design-issue
- **Location**: `internal/cli/authlogin.go` (`usableToken`), `internal/cli/authlogin_seam.go` (`gatherInputs`).
- **Severity**: low–medium — a crafted token could inject extra `.glassfrogrc` keys; an unbounded pipe could exhaust memory.
- **Description**: (1) `usableToken` trimmed only surrounding whitespace, so a token containing an embedded `\n`/`\r` (from stdin/env/prompt) would be written verbatim into the line-structured `.glassfrogrc`, splitting into multiple lines and injecting keys (e.g. `abc\nevil=value`). Now rejected as unusable → UsageError. (2) `gatherInputs` used `io.ReadAll(os.Stdin)`, reading an arbitrarily large pipe into memory. Now bounded via `io.LimitReader` (64 KiB) with an error past the cap. **(3) Defense-in-depth at the shared API boundary (PR #16 round 3):** the upstream `usableToken` guard was the *only* place the newline rule was enforced, but `auth.WriteCredentials` is a reusable package API that interpolates `token` straight into a `token=<token>` line — a future/other caller passing an unvalidated token would re-open the injection. `WriteCredentials` now rejects `strings.ContainsAny(token, "\r\n")` up front with the exported sentinel `ErrTokenNotSingleLine` and **no write** (the file is left untouched, the token value never appears in the error).
- **Suggested action**: any value written into a **line- or delimiter-structured file** must be validated against the structural delimiters (reject embedded newlines for a `key=value` line format) before persisting — trimming surrounding whitespace is not enough. And **bound reads of external input** (`io.LimitReader`) when the expected payload is small; never `io.ReadAll` an untrusted/unbounded stream. Both are the input-side analogue of the escape/validate-before-interpolate discipline. **Enforce the structural rule at the writer/API boundary, not just at the most-upstream caller**: when a shared helper interpolates a value into a structured format, it must reject delimiter-bearing input *itself* (fail without writing, with a typed/sentinel error and no secret in the message) even when today's only caller validates upstream — the caller's guard protects the caller, not the next caller. Same shape as the "designate a SoT helper and grep siblings" discipline, but applied to a write API: the helper owns its own input contract.

### 2026-06-03 — when triage edits one Score artifact, propagate the decision to its siblings in the same change
(Found during PR #7 review of [004-exit-code-convention](../../specs/004-exit-code-convention/plan.md))

- **Type**: convention
- **Location**: the Score artifact set for a spec (`spec.md` ↔ `plan.md` ↔ `interface-*.md` ↔ `tasks.md` ↔ `validate.md` ↔ feature files) — they restate the same decisions at different altitudes, and `validate.md` additionally *cites the code and tests* by file/line.
- **Severity**: low per round, but it manufactures extra review rounds: a one-sided edit leaves the siblings contradicting the changed file, and the reviewer flags the drift next pass.
- **Description**: PR #7 had two rounds caused purely by incomplete propagation. Round 2 introduced a design decision in `tasks.md` (extract a testable `cli.Main() int`; defer operational categories to `@validation` per ADR-2) but did **not** update `plan.md`. Round 4 then flagged that `plan.md`'s Implementation Strategy, Testing strategy, ADR-4 mechanism, and even the System-Architecture diagram still described the old `os.Exit(cli.ExitCode(...))`-in-`main` approach and a full category↔code one-to-one test — all contradicting the round-2 `tasks.md`. The same applies to the clarify→plan/interface direction (the earlier crash-diagnostic carve-out had to be threaded through spec + plan + interface). A decision lives in several artifacts; changing it in one is a half-edit. **The `code → artifact` direction bites too**: PR #11 changed `resolve.go` during triage (whitespace-only `GLASSFROG_TOKEN` now falls through via `strings.TrimSpace`). Round 2 propagated that to `interface-spec.md` but not to `validate.md`, whose conformance traces still quoted the old `token != ""` predicate — three round-3 comments. The same round's directory-vs-`0o000` test fix removed an assertion that `validate.md` still cited by line (`resolve_test.go:212`), leaving a dangling test reference. A triage **code** fix that changes observable behavior is also a half-edit until the artifacts that *describe or cite* that code (especially `validate.md`'s traces and any `file:line` references) are re-synced.
- **Suggested action**: after changing a decision in any Score artifact **or fixing code during triage**, grep the sibling artifacts in the same spec dir for the old approach (the function name, the mechanism phrase, the predicate like `token != ""`, the test claim, ADR titles, diagram labels, and `file:line` citations) and update every occurrence in the same commit. Treat ADRs, diagrams, "Implementation Strategy / Testing strategy" sections, **and `validate.md` conformance traces / test-line citations** as first-class propagation targets, not just prose. When a triage fix touches code, immediately ask "which artifacts quote this predicate, cite this test line, or describe this behavior?" Cheapest when done proactively — the reviewer will otherwise surface each lagging sibling one round at a time. **Prefer test/function *names* over `file:line` citations in long-lived artifacts** (`validate.md`, learnings): line numbers drift on every edit, and a `grep` for a single stale citation can miss sibling occurrences. PR #11 proved both halves — a `resolve_test.go:212` citation went stale when the directory-test fix removed the assertion, and fixing only the first of *two* occurrences (line 53, missing line 103) cost an extra round; switching to test-name references (`TestReadCredentialsFile_MalformedLineIsFormatError`) removes the drift class entirely. When you must fix a brittle citation, grep the artifact for *every* occurrence of that exact `file:line`, not just the one the reviewer flagged. **Two more drift targets after a plan/tasks *rewrite* (e.g. a conformance pass that changes the task decomposition or resolves a hazard): (a) task IDs** — `analyze.md` and `checklist.md` cite `T00N` by number, so collapsing a 5-task list to 3 (PR #33 conformance to 011) left `analyze.md` K3 saying "T001–T005" and `checklist.md` citing "T003/T004/T005" against a `tasks.md` that now defined only T001–T003; grep `T0\d\d` across the validation artifacts after any re-decomposition. **(b) risk residual states** — when a hazard's control is applied (e.g. H-3's RC-3 next-step fix landed in round 2), update the register *row* (Yellow→Green) **and** the per-hazard detail (drop "currently"/"once applied" framing) in the *same* edit as the residual-summary line; PR #33 left the H-3 row Yellow and the detail pre-fix while the summary said "resolved," giving the implementer contradictory acceptance guidance. The guard re-run refreshes these, but a manual artifact edit must grep the register, the detail, and the summary together. **(c) ordering / superlative claims** — a "first / only / the X-est" assertion written before a sibling spec existed turns false when the sibling lands; scrub it across the *full* artifact set, not just the plan. PR #33 round 2: conforming 012 to 011 removed the "first command to issue a real API call" claim from `plan.md` but left it in the feature-file narrative **and** the `interface-cli.md` intro — Copilot flagged both. After a reframe that drops an ordering claim, grep the superlative term (`first`, `only`) across `spec`/`feature`/`interface`, not only where you last edited. **(d) STATUS `Last Skill` after a guard re-run** — a `guard --pre` re-run that skips an already-done step (risk, when `risk.md` exists and covers the findings) leaves `Last Skill` = the last skill that *ran* (`analyze`), which under-represents the completed guard depth and diverges from siblings; set it to the deepest completed guard skill (`risk`, matching 011) since the stage (`Analyzed`) is identical either way. **(e) renumber/finding-resolution within a single open PR — sweep the PR description too, and re-sync cross-artifact finding counts.** PR #52 confirmed the `T0\d\d` rule (a) within one PR's own lifecycle: a *shape*-stage `tasks.md` restructure (3 separate task branches → one RED→GREEN increment) renumbered T001=config-guard-test / T002=config / T003=self-containment-verification and *resolved* the checklist VII P1, but stale IDs survived in `checklist.md`'s passed-check lines and `analyze.md`'s K3/K5/H4 — **5 of 7 Copilot threads**. The other 2 were count-desync: resolving VII flipped `checklist.md` to 7/7, but `analyze.md`'s "Checklist context: N/M" Governance Note still said 6/7, and the **PR description**'s guard summary still said "analyze 16/16" (it was 15/16 — a K5 P1 accepted out-of-scope). Lessons: (i) the `analyze.md` "Checklist context" line mirrors `checklist.md`'s pass total — when a triage/restructure resolves a checklist finding, update that mirror in the same commit; (ii) the **PR body is a drift target on par with the spec-dir artifacts** — its guard-summary counts/IDs go stale the moment you renumber or resolve mid-PR, and AI reviewers read it as source-of-truth, so `gh pr edit` belongs in the same triage batch (companion to the global prose-grep-includes-PR-description rule); (iii) run the `T0\d\d`/count sweep proactively the instant you renumber tasks or change a pass count — not after the reviewer flags each site one at a time. **(f) The sweep applies at INITIAL authoring of a multi-artifact set, not only after an edit — a cross-cutting fact stated in plan+interface+risk+tasks can be born inconsistent (or self-contradictory within one sentence), and guard's `analyze` coherence checks (H1 terminology / H3 scope) operate at too coarse a grain to catch a single contradictory clause.** PR #71 (034, define→guard, no code) shipped two such drifts straight out of authoring: (1) tasks.md's landed-state note said `Policy` was "(grown by T001)" and `RoleDocument` "(generalized to `Document[T]` by T001)" — present-tense phrasing for a *future* T001 action — while the **same line** had already correctly called `Policy` "minimal `ID`/`Title`/`Body`"; (2) plan.md ADR-3 consequences said an empty `--query` "is sent as an empty `q`" while interface-cli.md, risk.md RC-10, **and the very tail of that sentence** ("sent only when `Changed()`") relied on *omitting* `q`. Both passed the guard (analyze 16/16) and were caught by Copilot in round 1. Discipline: before handing a freshly-authored spec set to `/score:guard`, enumerate each fact that appears in ≥2 artifacts — especially (a) **present-vs-future state** ("already exists" / "grown by TNNN" / "will be") and (b) **empty/optional-input handling** (sent-vs-omitted, the same single-rule-across-sources shape as the trim-empty entry below) — pick the canonical statement, and grep the set (and each sentence's own clauses) to confirm they agree. Authoring-time reconciliation is the same two-second sweep as the edit-time one; the guard will not do it for you at clause granularity. **PR #76 (032, define→shape→guard, no code) added a third recurring shape and a direction worth naming: (c) edge-case error-handling — graceful-degrade vs. hard-fail.** A `shape`-phase ADR (plan ADR-4: a non-JSON API error body is *omitted* from the structured envelope so the rest still renders) introduced observable behavior the earlier-authored `spec.md` contradicted in **two** places at once: the accord implied the body is always included when present, and a later accord bullet cited "a raw error body that is not valid JSON" as an *example of the render failing* (hard-fail) — the exact opposite of the ADR's graceful-omit. The plan/interface/feature/tasks all agreed on omit; the spec was the lone outlier, self-contradictory across its own two bullets. This is the **downstream-refines-then-upstream-drifts** direction (most other entries are forward plan→tasks propagation): when a `plan`/`interface` ADR *changes or sharpens observable behavior* an upstream artifact already described, the spec's pre-existing statement on that edge case silently becomes a contradiction. It passed my own `/score:guard` analyze (marked C2/H3 "pass") — reconfirming the coarse-grain point: analyze's H1/H3 will not catch "spec says fail, plan says omit" on a single edge-case clause. Copilot caught all three in round 1, then posted a **stale duplicate** of the L41 comment from a re-review that raced the fix push — when re-triaging, read the *current pushed* line before re-fixing; a comment can be obsolete on arrival. Discipline addition: when a shape-phase ADR resolves an edge case (malformed input, partial failure, missing optional), grep the spec for any pre-existing statement about that same edge case (here: "valid JSON", "not valid", "fails", "omitted", "verbatim") and reconcile it — the ADR is the new SoT for that behavior, and the spec must not still describe the old/contradictory handling.

### 2026-06-04 — a godog suite must point at its own feature file, not the whole `features/` directory
(Found during [T003](../../specs/005-credential-discovery/tasks.md) implementation)

- **Type**: design-issue
- **Location**: `internal/cli/bdd_test.go` (`TestFeatures`) and any future package that adds a godog suite; the shared `features/` directory.
- **Severity**: medium — left unaddressed, removing `@wip` from a second spec's scenarios breaks the *first* suite, not the new one, with a confusing "step undefined" failure in an unrelated package.
- **Description**: 001–004 all live in `features/no-runnable-cli.feature`, run by the single `internal/cli` godog suite whose `Paths` globbed the whole `../../features` directory with `Tags: "~@wip"`. Credential Discovery (005) is the first spec to add a **second** feature file (`features/unauthenticated-access/credential-discovery.feature`, originally `features/unauthenticated-access.feature` before spec 006 moved it into a per-spec subdirectory) owned by a **different** package (`internal/auth`, since Discovery has no cobra surface — plan ADR-1). While 005's scenarios were all `@wip` they were skipped everywhere, but the moment T003 un-wipped them the directory-globbing `cli` suite picked them up and failed with undefined steps — the `auth` step definitions live in the `auth` package, not `cli`. godog binds steps per-suite, so a scenario can only be served by the suite whose initializer registered its steps. Fix: scope each suite's `Paths` to the specific feature file(s) it defines steps for — `cli` → `no-runnable-cli.feature`, `auth` → `unauthenticated-access/credential-discovery.feature`.
- **Suggested action**: when a spec adds a feature file owned by a new package (a capability with its own package and no cobra surface is the tell), give that package its own godog `TestSuite` pointed at *only* its feature file, and confirm every other suite's `Paths` names a specific file rather than the directory. Run both suites with `-count=1 -v` and check the `N scenarios (N passed)` summary so a mis-scoped path surfaces as an undefined-step failure immediately, not after a reviewer notices the cross-package break.

### 2026-06-04 — the "usable token" rule (non-empty after trimming) applies to every credential source uniformly
(Found during PR #11 review of [005-credential-discovery](../../specs/005-credential-discovery/spec.md); code in `internal/auth/resolve.go`)

- **Type**: convention
- **Location**: `internal/auth/resolve.go` (env short-circuit) and `internal/auth/credentials.go` (`readCredentialsFile`); the contract is shared with Credential Storage (006) and consumed by Request Authentication (007).
- **Severity**: medium — a blank credential resolving as usable would skip real discovery and hand a whitespace token downstream.
- **Description**: the spec's "usable token" assumption ("whitespace-only values are treated as absent") and the plan's risk mitigation ("non-empty **after trimming**") are a single rule that must hold across *all* sources, but the env path was implemented with a bare `token != ""` while the file reader already trimmed. So `GLASSFROG_TOKEN="   "` short-circuited to `SourceEnvironment` with a blank token instead of falling through. The file side and the env side are sibling empty-handling predicates for the same concept; they drifted because the rule was applied only where it was most obvious (the file reader). Fix: guard the env short-circuit with `strings.TrimSpace(token) != ""`; the non-blank value is still used as-is per the interface "used as-is" wording — only the usability *test* trims.
- **Suggested action**: when 006 (Storage) writes the file and 007 (Request Auth) consumes the `Resolution`, keep the "non-empty after trimming" definition uniform — Storage should not persist a whitespace-only token, and any new credential source must apply the same trim-for-usability test. When adding an empty/blank predicate for a credential, grep the sibling sources (`resolve.go` env branch, `readCredentialsFile`) for the established `strings.TrimSpace(x) != ""` shape rather than inventing a bare `!= ""`.
- **Refinement (2026-06-06, PR #25 triage — `internal/rcfile`)**: after the shared `.glassfrogrc` reader was extracted into `internal/rcfile`, this rule is embodied in one place — `rcfile.Settings.Value(key)`, the usability predicate for *every* key (token, base_url, future settings). It must **self-enforce** the trim rule (`strings.TrimSpace(value) != ""`), not rely on `Parse` having trimmed the stored value: `Settings` is exported and can be built directly, so a bare `value != ""` reports a whitespace-only value as usable and diverges from the method's own docstring. Copilot caught exactly this on PR #25. The earlier env-vs-file drift and this one are the same class — an exported predicate whose contract isn't enforced at its own boundary. Companion to the user-level rule "a helper that claims an output contract must enforce it itself, not trust upstream guarantees."

### 2026-06-04 — godog step helpers must return errors, never panic, so After hooks run
(Found during PR #11 review of [005-credential-discovery](../../specs/005-credential-discovery/spec.md); code in `internal/auth/bdd_test.go`)

- **Type**: convention
- **Location**: `internal/auth/bdd_test.go` (`mkdir`/`seed` helpers); applies to every godog suite in the project (`internal/cli`, `internal/auth`, and future ones).
- **Severity**: low — the failing operations (temp-dir creation) are near-impossible in practice, but the pattern is a latent flakiness trap.
- **Description**: BDD suites here mutate package-global seams (`getwd`/`userHomeDir`) and the process env (`GLASSFROG_TOKEN`) per scenario, restoring them in an `sc.After` hook. A helper that `panic()`s on error inside a step can bypass that After hook, leaving the globals mutated and the temp tree undeleted — state that leaks into later scenarios as confusing, order-dependent failures. Returning the error from the helper keeps the step on godog's normal failure path, where After always runs. This is the godog-specific instance of the project's broader rule that test helpers earn the same no-silent-failure / safe-cleanup rigor as production code.
- **Suggested action**: in godog step helpers, return `(…, error)` and propagate through the `Before` hook (which returns `(context.Context, error)`) and the Given/When/Then step funcs (which return `error`). Never `panic()` in a step or a helper a step calls. When a scenario must mutate a global, pair the mutation with an `sc.After` restore and verify cleanup can't be skipped by an early exit.

### 2026-06-04 — test a fail-loud read error with a directory at the path, not chmod 0o000
(Found during PR #11 review of [005-credential-discovery](../../specs/005-credential-discovery/spec.md); code in `internal/auth/resolve_test.go`, `internal/auth/bdd_test.go`)

- **Type**: convention
- **Location**: `internal/auth/resolve_test.go` (`TestResolve_UnreadableFileFailsLoud`) and `internal/auth/bdd_test.go` (`givenNearestUnreadable`); applies to any test that needs `os.ReadFile`/`os.Open` to fail on an existing path.
- **Severity**: low — but `0o000` tests are non-portable and force a skip-if-root guard, eroding coverage exactly where it is supposed to be strongest.
- **Description**: the unreadable-credentials-file cases made the file unreadable via mode `0o000`. That is environment-dependent — root can still read a `0o000` file (so CI-as-root or a local root shell silently skips the case), and Windows permission semantics differ — so the test needed a `t.Skip`/guard and still didn't reliably exercise the path. Placing a **directory** at the expected file path instead makes `os.ReadFile` fail deterministically across platforms and privilege levels: it returns a path-only `*PathError` that is **not** `os.ErrNotExist`, which is exactly the resolver's fail-loud branch condition (`!errors.Is(err, os.ErrNotExist)`). The resolver doesn't distinguish permission-denied from is-a-directory — both are "exists but unreadable" — so the directory proxy fully exercises the branch. Bonus: `os.ReadFile` never reads file content into its error, so the read path can't leak a secret regardless; content-leak hygiene belongs on the *format-error* test (where content is actually parsed), not here.
- **Suggested action**: when a test needs an existing-but-unreadable file, create a directory at that path (`os.Mkdir(path, 0o755)`) rather than `os.WriteFile(path, …, 0o000)`. Drop any skip-if-readable guard, and add an assertion that the error is **not** `os.ErrNotExist` so a regression that mis-classifies it as absence fails loudly. Put the token-never-in-error assertion on the format/parse test, not the read test. **The same directory-at-`.glassfrogrc` tripwire also enforces *negative* properties**: a test asserting "no file was read" / "short-circuits the filesystem" (e.g. the env-first hit) is tautological if it only checks the output — the happy path produces the same result whether or not a stray read happened. Place an unreadable directory at the *nearest* search location so any unintended `readCredentialsFile` call errors and the test fails loudly (PR #11: `TestResolve_EnvFirstShortCircuitsFiles` only checked Source/Path until a tripwire at `startDir/.glassfrogrc` made the no-read guarantee load-bearing). General rule: to test that something *doesn't* happen, install a tripwire that fails when it does, rather than asserting on output that looks identical either way.

### 2026-06-04 — a struct that carries a secret should implement a redacting String()
(Found during PR #11 review of [005-credential-discovery](../../specs/005-credential-discovery/spec.md); code in `internal/auth/resolve.go`)

- **Type**: convention
- **Location**: `internal/auth/resolve.go` (`Resolution`); applies to any secret-bearing type in the auth domain — Credential Storage (006) and Request Authentication (007) handle the same token.
- **Severity**: medium — the spec's secret-hygiene non-behavior and the held-out "token never in output" validation scenario forbid leaking the token; a struct that holds it one `fmt.Errorf("%+v", x)` away from a log line is a latent breach.
- **Description**: `Resolution{Token, Source, Path}` carries the secret `Token`. Nothing prints it today, but tests already format the struct (`fmt.Errorf("got %+v", got)`) and 007 will log the active identity — any `%v`/`%+v`/`%s` on the value would dump the token. A value-receiver `String() string` that renders `Source`/`Path` (the safe parts) but reports the token as `<redacted>` (present) / `<none>` (absent) makes those verbs safe at near-zero cost; field access (`res.Token`, needed to attach the header) is unaffected. Scope note: `fmt.Stringer` covers `%v`/`%+v`/`%s` — the accidental-logging verbs; `%#v` still dumps fields, but that verb is not an accidental path, so `fmt.Formatter` (full-verb control) is overkill here.
- **Suggested action**: give any struct that holds a credential/secret a value-receiver `String()` (and document it on the struct) that redacts the secret while keeping the safe-to-display fields, and pin it with a test asserting `%v`/`%+v`/`String()` omit the secret but keep the rest. Value receiver so both `T` and `*T` are covered. Reserve `fmt.Formatter` for when `%#v` coverage is genuinely required.

### 2026-06-04 — weigh "nil-default the injected dependency" review suggestions against ownership non-behaviors and fail-loud
(Found during PR #20 triage of [007-request-authentication](../../specs/007-request-authentication/spec.md); code in `internal/apiclient/transport.go`)

- **Type**: convention
- **Location**: `internal/apiclient/transport.go` (`NewAuthTransport`); applies to any constructor in this project that takes injected seams (a base `http.RoundTripper`, a resolver func, an OS seam).
- **Severity**: low — the suggested change compiled and "worked", but would have quietly breached two spec boundaries.
- **Description**: Copilot suggested making `NewAuthTransport` fail-safe by defaulting a nil `base` to `http.DefaultTransport` and converting a nil resolver into an error-returning resolver (so callers get an `AuthError{CredentialError}` instead of a panic). Declined the fix (kept the panic, documented the non-nil precondition) because all three legs conflict with 007's spec: (1) defaulting `base` would make 007 *own* the HTTP transport — base URL, timeouts — which spec § Non-Behaviors and ADR-1 explicitly assign to Connection Configuration, and `http.DefaultTransport` has no timeouts; (2) silently substituting a working default is quiet degradation where CONSTITUTION III (Fail-Safe) wants failing *loud* — a nil seam is a wiring bug a panic surfaces immediately; (3) mapping a nil resolver to `CredentialError` conflates a programming bug with a genuine broken-credentials-file outcome, polluting the deliberately-distinct `NoCredentials`/`CredentialError` taxonomy (ADR-4). The general trap: generic "be defensive about nil" review suggestions optimize for *robustness-in-isolation*, but for an internally-wired constructor whose args are always supplied deliberately at integration, fail-fast on nil is both idiomatic Go and the spec-aligned choice; silent defaulting can smuggle in a capability the spec says the component must *not* have.
- **Suggested action**: when a reviewer (often Copilot) proposes nil-defaulting or silent fallback for an injected dependency, before accepting, check it against (a) the spec's ownership non-behaviors — does the default make this component own something another capability owns? — and (b) the fail-loud principle — does it convert a loud wiring-bug signal into quiet degradation? If either, decline the *defaulting* but honour the reviewer's real point by documenting the precondition on the constructor (e.g. "base and resolve are required and must be non-nil; RoundTrip panics otherwise") and the rationale, keeping fail-fast behaviour. Reserve nil-tolerant defaulting for genuine public-API convenience seams (like `http.Client{}`'s documented nil-`Transport` default), not internal constructors.

### 2026-06-07 — Go idioms and pinned signatures embedded in spec-pipeline artifacts must be valid and surface-accurate
(Found during PR #29 review of [010-request-execution](../../specs/010-request-execution/spec.md) — a spec-only PR, no implementation yet; reinforced by PR #33 review of [012-my-roles](../../specs/012-my-roles/tasks.md) and PR #38 review of [017-rate-limit-handling](../../specs/017-rate-limit-handling/plan.md))

- **Type**: convention
- **Location**: `specs/<NNN>/{plan,tasks,risk,interface-spec}.md` and `.score/memory/DECISIONS.md` — anywhere a Score artifact embeds a code snippet or a function signature as Builder/future-spec guidance.
- **Severity**: low — no behaviour impact (spec-only), but an invalid idiom or a drifted signature in guidance propagates a mistake into the implementation PR if not caught.
- **Description**: PR #29 embedded the invalid single-arg shorthand `errors.As(&AuthError)` / `errors.As(*AuthError)` (Go's `errors.As` requires `errors.As(err, &target)`) as implementation guidance in plan.md, tasks.md, risk.md, and DECISIONS.md; Copilot flagged 5 instances. Separately, the DECISIONS.md precedent entry recorded the seam as `NewClient(ctx)` while the interface accord and tasks had already pinned `NewClient(ctx, base) (*Client, error)` + `NewClientFromOS(ctx)` — signature drift in the durable cross-spec record. Both classes mislead their readers: the Builder copies the snippet, and future specs read DECISIONS.md as the contract. The `errors.As` shorthand also recurred at sibling sites the reviewer did **not** flag (plan.md ADR-4 phase note, risk.md traceability index, a third DECISIONS.md entry) — found only by grepping the whole artifact set, including a re-grep *after* editing.
- **Suggested action**: (1) any code snippet in a spec artifact must be valid, copy-pasteable form — `errors.As(err, &target)`, not `errors.As(&Type)`. (2) When interface/tasks pin a signature, the DECISIONS.md entry and the plan prose must reflect that **pinned** surface, not the plan's architectural shorthand — DECISIONS.md is the durable contract future specs inherit (plan may stay architectural and defer the exact API to interface, but the precedent record must not). (3) On review of any embedded-idiom/signature comment, grep **every** artifact (`plan`/`tasks`/`risk`/`interface-spec`/`DECISIONS`) for the form — siblings recur across files — and re-run the grep after editing to catch instances the first pass missed. (4) **Grep the misattributed *symbol* across all its textual surfaces, not just the one flagged call form.** A "wrong receiver/parameter" error (e.g. a method described as using `ctx` when only the constructed `Client` is in scope) recurs as signature drift, code-example drift, **and** prose-description drift — different phrasings of one conceptual mistake. PR #29 needed a round 2 because round 1 grepped the call forms (`errors.As(...)`, `NewClient(ctx)`) but missed `ctx.BaseURL.Value` in the `Execute` *join description*; the round-2 grep keyed on the symbol (`ctx.BaseURL.Value` / `join(ctx`) and found four unflagged siblings. When a fix is "X doesn't have access to Y," grep for **every** mention of Y in X's vicinity — signature, example, and prose alike. (5) **A struct described in an artifact that decodes external JSON must carry explicit `json:"…"` tags for any snake_case field** — Go's `encoding/json` matches case-insensitively but does **not** bridge underscores, so `HasNextPage` never binds `has_next_page` and a derived predicate (`incomplete()`) silently stays false. PR #33: `tasks.md` T001 called the `Role` growth "JSON-tagged" but described the `MyRolesResponse{Meta{Pagination{HasNextPage,PerPage}}}` wrapper with bare Go field names — left literal, the pagination signal would never decode and a paginated list would read as complete (CONSTITUTION VI). When an artifact specifies a decode target for the Glassfrog API (snake_case throughout — `has_next_page`, `per_page`, `created_at`, …), spell out the tags and require a decode test over a real snake_case payload that asserts the tagged fields populate. (6) **A decode model a cross-spec contract designates as SHARED must be a NAMED reusable type that mirrors the FULL API schema — including documented optional / forward-looking fields the current slice won't use yet.** An inline anonymous struct, or one omitting an optional field, forces churn on the shared type when a later consumer needs it. PR #33 round 3: 012 is the designated creator of `glassfrog.Pagination` (013/014 reuse it), but the artifacts specified an inline `Meta{Pagination{HasNextPage,PerPage}}` omitting the API's `next_cursor` (documented "present when `has_next_page` is true"); Copilot flagged it across five locations (plan diagram, ADR, phase text, tasks scope, decode test). Fix: a named `Pagination{PerPage,HasNextPage,NextCursor}` referenced by the wrapper, `next_cursor` decoded (unused until 016). When a sibling-spec contract names your spec as the creator of a shared type, model the whole schema up front — and conform proactively when that contract lands, rather than deferring, or the reviewer surfaces each omission one location at a time. (7) **When reconciling a pinned-name drift, enumerate the FULL set of identifiers the interface accord pins and grep EACH against the plan/DECISIONS — don't stop at the first drift you happened to notice.** The interface-spec (the SoT for the API surface) pins a *set* of names; the plan, written earlier with provisional sketch names, can drift on *several* of them at once. Fixing only the one you spotted leaves the rest for the reviewer. PR #38: a `/score:guard` analyze pass caught and reconciled `ExecuteWithRetry`→`RetryExecutor` across plan.md + DECISIONS.md, but grepped only that symbol — it missed the sibling helper `retryAfter`→`parseRetryAfter` (5 occurrences in plan.md), which interface-spec.md and tasks.md had pinned. Copilot opened 4 separate threads for it. The discipline: after interface pins the surface, build the identifier list from interface-spec (`RetryExecutor`, `NewRetryExecutor`, `RetryPolicy`, `DefaultRetryPolicy`, `isSafeMethod`, `parseRetryAfter`, …) and grep the plan/DECISIONS for each plausible provisional spelling (the camelCase/no-prefix/free-function variants), not just the one drift that surfaced — a single sweep closes the whole class instead of one reviewer round per missed name.

### 2026-06-07 — fail-fast belongs in the pure constructor; the OS seam degrades gracefully — and argument-preconditions are checked first

(Found during PR #30 review of [010-request-execution](../../specs/010-request-execution/spec.md); code in `internal/apiclient/client.go`)

- **Type**: convention
- **Location**: `internal/apiclient/client.go` (`NewClient`, `NewClientFromOS`); applies to any pure-constructor + OS-seam pair in this project (mirrors `Assemble`/`AssembleFromOS`, `NewAuthTransport`).
- **Severity**: low — both fixes were small, but each masked a failure mode (a non-deterministic panic, and a panic on a replaced global).
- **Description**: Two accepted Copilot comments refined the fail-fast posture the [PR #20 entry](#) set. (1) **Precondition ordering**: `NewClient` documented that a nil `base` panics, but the `if ctx.BaseURLErr != nil { return … }` early-return ran *before* the `base == nil` check — so a nil-transport wiring bug was masked (returned the error instead of panicking) exactly when the context also carried a base-URL error. The panic became non-deterministic in the wiring-bug case. Fix: check the argument precondition first, unconditionally, so a wiring bug fails the same way regardless of input; legitimate behaviour (non-nil base + `BaseURLErr` → error verbatim) is unchanged. Pinned by `TestNewClientNilBasePanicsEvenWithBaseURLError`. (2) **Unchecked type assertion on a mutable global**: `NewClientFromOS` did `http.DefaultTransport.(*http.Transport).Clone()` — an unchecked assertion that panics if a dependency reassigned the global to a different `RoundTripper` type. Fix: comma-ok (`if t, ok := base.(*http.Transport); ok { base = t.Clone() }`) and fall back to `http.DefaultTransport` directly. This does **not** contradict PR #20's "don't nil-default the injected base": that rule guards the *pure constructor* (`NewClient`), whose injected `base` is a deliberate integration wiring point that must fail loud on nil. The *OS seam* (`NewClientFromOS`) is precisely the layer whose job is to *pick* a real transport, so graceful fallback is correct there — and safe, because the `*http.Client.Timeout` still bounds the request, so the fallback never reintroduces the untimed-transport hazard. Two rejected comments (ignored `io` errors) were declined on spec grounds: the non-2xx `io.ReadAll` error is best-effort because that path already returns a `ResponseError` and the spec requires status+headers survive for 015/017 (a 429 must reach 017 even with a truncated body), and the bodyless-2xx `io.Copy` drain is an optimization whose failure can't affect a success the caller declined the body for.
- **Suggested action**: in a constructor that both validates argument preconditions and has business-logic early-returns, **order the precondition (panic) checks before the early-returns** so wiring bugs fail deterministically. Distinguish the **pure constructor** (fail-fast/panic on a nil injected seam — PR #20) from the **OS seam** (may degrade gracefully when picking a real dependency, *provided* an outer bound like the client timeout still holds). Never use an unchecked type assertion on `http.DefaultTransport` (or any reassignable global) in a public seam — use comma-ok with a sensible fallback. When rejecting an "unchecked `io` error" comment, justify it against the spec: is the path already an error outcome, must a signal (status/headers) survive for a downstream sibling, and did the caller opt out of the body?
- **Update (2026-06-08, PR #43 Copilot review of 017)**: the constructor-precondition discipline extends from **nil seams** to **value-type config whose fields carry documented floors/invariants**. `NewRetryExecutor` fail-fasted on nil `client`/`sleep`/`progress` but accepted the `RetryPolicy` value unchecked — yet its fields document floors (`MaxAttempts ≥ 1`, non-negative `MaxTotalWait`/`FallbackBackoff`). An out-of-floor value isn't a nil-pointer crash; it's worse — *silent* surprising behavior: a negative `FallbackBackoff` is a wait `time.Sleep` ignores and the `waited+wait > budget` check can't catch (negative never exceeds), and `MaxAttempts < 1` makes the loop give up immediately. Fix: validate the value's documented floors in the same constructor block as the nil checks and panic on violation (same wiring-bug class); `DefaultRetryPolicy` and all callers satisfy them, and a test pins the panic per invalid field. Rule: when a constructor documents preconditions on a *value* parameter (floors, ranges, "must be non-empty"), enforce them at construction next to the nil-seam checks — a documented invariant the constructor doesn't enforce is a footgun that surfaces far from the wiring site, and "it's a value not a pointer" is not a reason to skip the check.

### 2026-06-07 — compose URLs with net/url, not string concat — and mind that 008's isUsableURL admits query/fragment bases

(Found during PR #30 round-2 review of [010-request-execution](../../specs/010-request-execution/spec.md); code in `internal/apiclient/execute.go`)

- **Type**: convention
- **Location**: `internal/apiclient/execute.go` (`buildURL`, was `joinURL`); applies to any site that joins a path/query onto the 008-resolved base URL.
- **Severity**: low–medium — only bites a base carrying its own query string or fragment (the Glassfrog default has neither), but when it bites it produces a silently malformed URL, not a loud failure.
- **Description**: `Execute` joined the base and path with `strings.TrimSuffix/TrimPrefix` + `"/"` and then appended `"?" + req.Query.Encode()`. That assumes the base has no `?`/`#`. But 008's `isUsableURL` (`baseurl.go`) validates a base on **scheme + host only** — it accepts `https://example.com/api/v5?x=1` and `…#frag` as usable. Under the concat join those bases mangle: the path lands *after* the `?`/`#` (`…?x=1/me`), a second `?` is appended, and the base's own query params are dropped/overridden. Copilot caught it. Fix: parse the base with `url.Parse`, append `req.Path` onto `u.Path` only, merge `req.Query` into `u.Query()` (preserving the base's existing params), and let `u.String()` reattach the fragment — a parse failure returns a `TransportError` guard (the base is 008-validated as parseable). No behaviour change for the normal no-query/no-fragment base; `TestExecuteJoinsURLPreservingBaseQueryAndFragment` pins the rich case. General rule (companion to the structural-encoding entry in the user memory): build composite URLs/keys with the structural API (`net/url`), never string concatenation — *especially* when an upstream validator admits inputs richer than the concat assumes. When consuming a sibling capability's "usable/valid" guarantee, read what that validator actually checks (here: scheme+host, **not** the absence of query/fragment) rather than assuming the stricter property you need.
- **Suggested action**: when joining onto a resolved base URL, use `url.Parse` + path-append + `u.Query()` merge + `u.String()`, not string concatenation. Before relying on an upstream "is-valid" predicate, read its body to learn exactly which properties it guarantees — and handle the inputs it admits but you didn't expect (query/fragment on a base, trailing data, etc.). Pin the richer-than-default input with a test, since the happy-path default won't exercise it.

### 2026-06-07 — operational outcomes need a dispatch error-channel carrier to reach exit codes 3/6
(Found during [T004](../../specs/011-identity-read/tasks.md) implementation)

- **Type**: design-gap
- **Location**: `internal/cli/dispatch.go` (the `Outcome` derivation in `Run`); surfaces for any command producing an operational outcome (`APIError`, `NetworkUnavailable`).
- **Severity**: medium — without it the acceptance "*ResponseError → APIError(3)" / "*TransportError → NetworkUnavailable(6)" is unsatisfiable: those outcomes would silently collapse to exit code 1.
- **Description**: The exit code is computed centrally as `ExitCode(dispatch.Run's Outcome)` (`entrypoint.go: runToExitCode`). But `dispatch.Run` re-derives the `Outcome` from the `RunE` **error channel**, and it only knew two carriers: `*commandUsageError → UsageError`, everything-else → `RuntimeError`. So `runMe` returning `APIError`/`NetworkUnavailable` had no way to surface those categories — a command cannot set the exit code itself (producer-classifies / consumer-maps). T004's scope sketched the RunE mapping as only `Success/UsageError/RuntimeError` (the `runLogin` pattern), which is complete for 006 (login never produces operational outcomes) but **not** for the first API command. Resolved by adding a general `*outcomeError{outcome, err}` carrier mirroring `commandUsageError`: the command wraps its operational outcome, `dispatch.Run` unwraps it (`errors.As`) and returns the carried category verbatim. The command writes its own token-free message (it sets `SilenceErrors`), so dispatch only reads the category.
- **Suggested action**: 012–017 (the sibling reads) reuse `outcomeToDispatchError` (in `me.go`) verbatim — when a later spec adds a finer operational `Outcome` (015 splits `APIError` into permission(4)/rate-limit(5)), no dispatch change is needed: the carrier already conveys any `Outcome`. A future spec that adds an `Outcome` category but routes it through the bare-error channel would silently map to `RuntimeError(1)` — the rule is "operational outcomes travel via `*outcomeError`, not a bare error." The `me_test.go` `TestMeCommand_ExitCodesAcrossOutcomes` table pins 0/2/3/6 through the full `Run`→`ExitCode` path so a regression in the carrier fails loud.

### 2026-06-07 — multi-value flag rejection: quote each item, agree the noun in number
(Found during PR #32 triage of [011-identity-read](../../specs/011-identity-read/spec.md); code in `internal/cli/me.go` `validateInclude`)

- **Type**: convention
- **Location**: `internal/cli/me.go` (`validateInclude`); applies to any usage error built from a multi-value flag (`StringSliceVar`) or a slice of offending items.
- **Severity**: low — message clarity, not behavior.
- **Description**: `--include` is a `StringSliceVar`, so several unsupported targets can arrive at once (`--include foo,bar`). The first cut reported every rejection in the singular ("target") and quoted the whole comma-joined list as ONE string — `unsupported --include target "foo, bar"` — which reads as a single bogus target named `foo, bar`. Copilot flagged it on PR #32 (accepted). Fix: quote each offending item individually and switch the noun on count (`target` vs `targets`) → `unsupported --include targets "bar", "foo" — supported: roles`. Keep the items sorted so the message is deterministic.
- **Suggested action**: whenever an error names items drawn from a slice/multi-value flag, build the list with per-item quoting (`fmt.Sprintf("%q", item)` joined by `, `) and make the noun agree in number — never quote the joined list as a single token. Pin a multi-item test case (plural noun, each item quoted, the joined list NOT quoted as one) alongside the single-item one. Companion to the existing self-correcting-error-message guidance (give the model/operator enough context to fix the input).

### 2026-06-07 — distinct Gherkin phrasings → one shared step handler is correct, not a duplicate
(Found during PR #32 triage of [011-identity-read](../../specs/011-identity-read/spec.md); code in `internal/cli/identity_read_bdd_test.go`)

- **Type**: convention (refines the 2026-06-03 "phrase Given/When/Then to match an existing step" entry)
- **Location**: `internal/cli/*_bdd_test.go` step initializers.
- **Severity**: low — review-noise / maintainability.
- **Description**: when a feature file genuinely uses two phrasings for the same assertion (here `it will exit with a non-success result` and `the command will exit with a non-success result`), binding BOTH `$`-anchored regexes to one handler (`w.exitedNonSuccess`) is the right move — godog matches each regex independently, no identical regex is registered twice, and `$`-anchoring means no Gherkin line matches more than one, so there is no overwrite or ambiguity. A reviewer (Copilot, PR #32) mis-flagged the two adjacent `sc.Step(... w.exitedNonSuccess)` lines as a "duplicate step definition"; rejected, because the regexes differ. The deeper fix lives upstream: the feature wording should be uniform (the 2026-06-03 entry's "keep repeated assertion wording identical"), which removes the need for two bindings — but scenario prose is the Verifier's domain, so the Builder binds both rather than rewriting `.feature` content.
- **Suggested action**: to tell a real duplicate from a shared handler, grep `grep -oE 'sc\.Step\(`...`' <file> | sort | uniq -d` — empty output means distinct regexes (fine). Reserve the concern for genuinely identical regex strings. When authoring scenarios (`/score:scenarios`), prefer one canonical phrasing per assertion so the step set stays minimal.

### 2026-06-07 — an ordering/discrimination test must use an input matching BOTH arms, or it passes vacuously
(Found during PR #32 triage of [011-identity-read](../../specs/011-identity-read/spec.md); code in `internal/cli/clienterror_test.go`)

- **Type**: testing
- **Location**: any test that pins the ORDER of an `errors.As` / type-switch / precedence chain (e.g. `classifyClientError`'s "match *AuthError before the rcfile arms").
- **Severity**: medium — a vacuous test gives false confidence; the invariant it claims to protect can regress silently.
- **Description**: `TestClassifyClientError_AuthBeforeRcfile` wrapped a nil-cause `*AuthError{CredentialError}` and asserted the classifier returned RuntimeError. But the chain contained NO rcfile error, so `errors.As(err, &rcfile.ReadError)` was false — the rcfile arms could never have matched regardless of order. The test passed whether the AuthError arm came first or last: it pinned nothing. Copilot flagged it twice on PR #32 (imports + the vacuity). The fix builds the REAL overlapping shape — an `*AuthError` whose `Unwrap()` exposes an `*rcfile.ReadError`, obtained via the production `AuthTransport`/`Execute` path (a context with an rcfile `CredErr`) — and asserts `errors.As` matches BOTH types before asserting the classifier's verdict. Now flipping the arm order fails the test.
- **Suggested action**: when a test's whole point is "X is checked before Y," the input MUST satisfy both X's and Y's predicates simultaneously — otherwise reorder-and-rerun would still pass and the test is a tautology. Sanity check: "if I swapped the two arms under test, would this test fail?" If not, the input doesn't exercise the ordering. Prefer constructing the real wrapped error via the production path over hand-rolling a chain, so the overlap matches what ships. Companion to the existing zero-value change-detector trap (PR #10) — same family: a test that passes for the wrong reason.

### 2026-06-07 — command RunE error messages must carry their own cause (the entrypoint discards Run's error)
(Found during PR #32 round-3 triage of [011-identity-read](../../specs/011-identity-read/spec.md); code in `internal/cli/me.go` + `internal/cli/authcmd.go`)

- **Type**: convention
- **Location**: any cobra `RunE` in `internal/cli` that prints a stderr message and `return err`.
- **Severity**: low–medium — only bites rare wiring/OS failures, but when it does the operator gets a bare exit code with no diagnosable cause.
- **Description**: `entrypoint.go:runToExitCode` deliberately does `outcome, _ := Run(root, args)` — it DISCARDS Run's returned error (004 renders no text; the resolved command owns its stderr message). So a `RunE` that prints a GENERIC message (`could not read the --base-url flag`) and then `return err` loses the underlying cause entirely: dispatch propagates the error, but the entrypoint throws it away, so only the generic line reaches the operator. Copilot flagged the `me.go` `--base-url` `GetString` site on PR #32 (accepted). The same pattern existed at three sibling sites in `authcmd.go` (`gatherInputs`/`homeDir`/`startDir`) from spec 006; swept in the same commit since 006 is otherwise frozen. Fix: interpolate the cause — `fmt.Fprintf(stderr, "…: %v\n", err)`.
- **Suggested action**: in any `internal/cli` `RunE`, when you print a message and return the error, include the cause with `%v` (these wiring errors are OS/stdin/cobra-type failures — never the token, so they are safe to print). The error return still travels for `errors.As` classification, but treat the stderr message as the ONLY thing the operator will see. When fixing one such site from review, grep the package for the sibling shape (`Fprintln(... ErrOrStderr()` immediately followed by `return err`) and sweep them together.

### 2026-06-07 — an error-message "next step" must not interpret a status the spec's Non-Behaviors reserve for a sibling capability
(Found during PR #33 round-2 review of [012-my-roles](../../specs/012-my-roles/interface-cli.md))

- **Type**: convention
- **Location**: `interface-*.md` Error Communication tables (and any `RunE` message) for a read command whose spec Non-Behaviors defer API-error classification to a sibling spec (015 API Error Extraction, 017 Rate-Limit Handling).
- **Severity**: low (spec-only here), but it encodes a contract contradiction that would propagate into the implementation: the message would classify a status the command is forbidden to classify.
- **Description**: an earlier Action-Transparency fix (every error names a cause **and** a next step) over-reached — the non-2xx `APIError`(3) message was given *status-keyed* guidance (`403` → "you may lack permission"; `429` → "rate limit hit; wait before retrying"). But 012's spec Non-Behavior says the command "must not interpret a non-2xx response into a specific, meaningful API error" — that is 015's job (and 429-as-rate-limit is 017's). The remediation for one principle (Action Transparency) silently violated a Non-Behavior. Copilot flagged it. Fix: keep the message generic — name the HTTP status (the cause) + a **generic** recovery next step ("check the token has access and retry, or consult the status"), with no per-status interpretation; classification stays with 015/017.
- **Suggested action**: when adding or editing an error message to satisfy a "name the next step" rule, check it against the spec's **Non-Behaviors** before committing — if a Non-Behavior reserves interpreting/classifying that error for a sibling capability, the next step must stay generic (status named, generic recovery), never a per-status diagnosis. A fix for one principle can breach another; the cheapest guard is re-reading the spec's Non-Behaviors whenever a fix adds interpretive prose to a failure path.

### 2026-06-07 — merging `main` into a long-lived spec-PR is a staleness trigger: sweep the spec's status claims in the merge commit
(Found during PR #33 rounds 3–4 review of [012-my-roles](../../specs/012-my-roles/plan.md))

- **Type**: process
- **Location**: any spec-PR (`specs/<NNN>/{plan,tasks,risk}.md`) that stays open while sibling/dependency specs land on `main` and get merged in.
- **Severity**: low per round, but it manufactures whole extra review rounds — PR #33 round 3 (013/014 merge → the shared-`Pagination` contract + stale "first" claims) and round 4 (#32 merge → "011 scaffolding not yet implemented" across 7 sites) were *both* pure post-merge staleness the reviewer surfaced one location at a time.
- **Description**: when `main` is merged into an open spec-PR, the merge brings sibling/dependency changes that silently falsify this spec's prose: dependency-status claims ("011's scaffolding recorded, not yet implemented", "gated on X", "whichever implements first"), ordering/superlative claims ("the first command…"), and cross-spec contracts (013/014 designating 012 as the creator of shared `glassfrog.Pagination`). The author treats `git merge` as done once conflicts are resolved, but the *conflict-free* artifact prose is exactly what goes stale — no marker, no compile error. Copilot then re-reviews the updated PR and flags each stale site individually.
- **Suggested action**: treat **"merge `main` into the PR"** as a propagation trigger (companion to the global dependency-status-drift sweep). In the *same* merge commit, grep this spec's artifacts for status claims that the merged reality may have changed — `not yet implemented`, `recorded but not`, `gated on`, `whichever.*first`, `unbuilt`, `first command`, `first .* read`, and any shared-type/contract the newly-merged sibling specs now define — and reframe them to the post-merge truth (`implemented on main`, `resolved`, `satisfied`). Cheapest done proactively in the merge; otherwise it returns as a full review round. After editing, re-grep (the bare terms, not specific phrasings) since these claims recur across plan/tasks/risk.

### 2026-06-07 — the registration guard validates a command at its OWN registration, so a runnable parent must be registered as a leaf BEFORE its children attach
(Found during [T002](../../specs/012-my-roles/tasks.md) implementation of 012-my-roles)

- **Type**: design-pattern
- **Location**: `internal/cli/app.go` (`Assemble`), `internal/cli/registry.go` (`Register`), and any future self-service sub-read (`me actions` 013, `me projects` 014) that attaches a leaf under the runnable `me` command.
- **Severity**: low — there is a clean ordering that satisfies the existing guard with no guard change; the trap is only that the obvious wiring (attach the child inside the parent's constructor) trips it.
- **Description**: `Register` enforces "a command is either a leaf (has an action) XOR a group (has ≥1 child), never both" — but it checks this against the **child** being registered, evaluated at the moment of *that* `Register` call. `me` (011) is a runnable leaf; 012 needs `me roles` as a child of `me`, making `me` both runnable and a parent. If `me` gained the `roles` child *before* `MustRegister(root, me)` ran (e.g. attaching it inside `newMeCommand`), the guard would evaluate `me` as both-leaf-and-group and panic. The resolution is purely **ordering**: register `me` under the root *while it is still childless* (the guard sees a valid runnable leaf), then `MustRegister(me, newMyRolesCommand(...))` — that call validates the `roles` child (a clean leaf) and attaches it; `me` is never re-validated, so it quietly becomes runnable-with-children. cobra runs the parent's `RunE` for `glassfrog me` and the child's for `glassfrog me roles`, both fine.
- **Suggested action**: 013 (`me actions`) and 014 (`me projects`) attach the same way — in `Assemble`, keep the `meCmd := newMeCommand(...); MustRegister(root, meCmd)` then `MustRegister(meCmd, newMy<X>Command(...))` ordering; do NOT attach the sub-read inside `newMeCommand`. Pin it with a registration test that asserts `me.RunE != nil` AND `me` has the child (the `TestMyRolesCommand_MeIsRunnableWithChildren` shape) plus a `TestAssemble_Wires…` smoke test, so a future reorder that moves the child attach before the parent registration fails loud. If the guard is ever changed to validate parents on child-attach, this ordering trick breaks — at that point the guard should explicitly permit runnable-with-children (a runnable parent satisfies both predicates).

### 2026-06-07 — model nested API objects as named types (not anonymous-struct slices); assert spec-pinned strings by equality (not substring)
(Found during PR #35 triage of 012-my-roles — two accepted Copilot patterns)

- **Type**: convention
- **Location**: `internal/glassfrog/` (schema models) and any `*_test.go` / `*_bdd_test.go` asserting a string an interface artifact pins verbatim.
- **Severity**: low — both are maintainability/regression-strictness improvements, not bugs.
- **Description**: (1) **Named nested types over anonymous-struct slices.** 012's `Role` first declared `Accountabilities []struct{ Description string }` and `Domains []struct{ Description string }` — identical anonymous element types. That forces every caller/test that constructs one to repeat the exact anonymous literal (`roleWith` in `myroles_pure_test.go` did, twice) and blocks the element type from growing fields or methods. Fixed to distinct named types `glassfrog.Domain` / `glassfrog.Accountability` (kept *separate*, not one shared `RoleItem`: they are different governance concepts that will diverge as the schema grows). This **extends** the existing cross-spec-shared-type learning (the PR #29/#33 embedded-idiom entry, item 6) from *top-level shared* models down to *nested element* types: in the `glassfrog` schema package, prefer a named type for any API object, even a single-field one, even when it isn't cross-spec-shared yet. (2) **Equality, not substring, for spec-pinned strings.** `interface-cli.md` pins the incompleteness note verbatim, but both the unit test and the BDD step asserted only `strings.Contains(stderr, "incomplete")` — which silently tolerates any wording drift away from the pinned line. Fixed to compare the full stderr line against the `incompleteRolesNote` constant (accounting for the `Fprintln` trailing newline). A `Contains` of a generic substring is not a regression guard for a verbatim-pinned string.
- **Suggested action**: 013/014 add `Action` / `Project` models with their own nested objects — declare each nested API object as a named type in `internal/glassfrog/` from the start; never an anonymous-struct slice. When an interface/spec pins a user-facing string verbatim (note text, error line, label), assert it by **equality against the source-of-truth constant** (full line, trailing newline accounted for), not `Contains` of a fragment — otherwise CI stays green while the pinned wording drifts. Reuse the rendering constant (`incompleteRolesNote`) in the test rather than re-typing the literal, so the constant stays the single source of truth.

### 2026-06-07 — once /score:interface pins a Go symbol, sweep plan.md + DECISIONS.md to the pinned name in the same change; reconcile cross-spec contradictions at merge time
(Found during PR #37 triage of 015-api-error-extraction — 15 of 18 Copilot threads were naming drift; 2 cross-spec drifts surfaced when main merged 016/017)

- **Type**: process / cross-artifact consistency
- **Location**: spec-pipeline artifacts (`specs/NNN-*/plan.md`, `interface-spec.md`, `.score/memory/DECISIONS.md`) and the merge step that pulls landed sibling specs into an in-flight spec branch.
- **Severity**: low (doc/consistency), but high-friction — it generated 18 review threads + a tangled merge.
- **Description**: (1) **Interface-pinned names must propagate immediately.** 015's plan deliberately used the *working* name `APIError`/`ExtractAPIError` and flagged "interface pins it." Once `/score:interface` actually pinned `ProblemError`/`ExtractProblem`, the un-swept plan + DECISIONS drew **15 Copilot threads** (one per occurrence, incl. the ASCII diagram and ADR titles). The producer rename had to preserve the unrelated `cli.APIError` *Outcome* and `APIError(3)` exit-code refs — so it is a *targeted* sweep (`*APIError`→`*ProblemError`, `ExtractAPIError`→`ExtractProblem`, `APIError{`→`ProblemError{`, struct-literal + method + diagram-label forms), not a blind replace-all. (2) **Sibling specs can each defer a shared concern to the other**, and the contradiction only appears when `main` merges. 015 said "017 owns the 429→rate-limit(5) split"; landed 017 said the inverse ("a capped-out 429 stays APIError(3) until 015's producer exists"). Both pointed away — so exit code 5 had *no* owner. Surfaced only because the merge brought 017's DECISIONS entry next to 015's. (3) **A resumed/compacted session can leave a stale half-merged index** (`UU` entries with conflict markers but **no `MERGE_HEAD`**); committing it would fabricate a non-merge commit that doesn't link `origin/main`. Recovery: `git reset --hard HEAD` to the clean committed tip, then re-merge. A *diverged remote PR branch* (parallel `main` merges pushed by another session, lacking your local-only fix) integrates cleanly with `git merge origin/<branch> -X ours` when the only unique local content is your known-good edits and the conflicting hunks are "your edited version vs the pre-edit version."
- **Suggested action**: the moment `/score:interface` pins a symbol name, grep the whole spec dir **plus DECISIONS.md** for the working name and sweep it (preserving same-spelling-different-meaning tokens like a sibling-package Outcome) — don't defer to review. **This extends beyond *names* to any *representation* the interface/tasks pin** — config-field values, required command flags, command forms — and the sweep runs *up* from the earlier-written artifacts (plan, DECISIONS) to the later authoritative contract. PR #52 round 2: `plan.md` + `DECISIONS.md` were written with the working forms `./main.go` and a bare `goreleaser build` *before* interface-spec/tasks pinned `main: .` and `goreleaser build --snapshot --clean`; the un-swept plan/DECISIONS drew **6 Copilot threads** (one per occurrence across System Architecture, ADR, Implementation Strategy, and the DECISIONS precedent). The fix-all-occurrences grep (`main\.go`, `goreleaser build` without `--snapshot`) caught a fourth plan.md site the reviewer hadn't flagged. Companion drift in the same round: a `tasks.md` restructure that reused the label "Phase 1" collided with `plan.md`'s Phase 1/Phase 2 — rename the tasks grouping (e.g. "Increment 1, spans plan Phases 1–2") while keeping the per-task Plan-reference fields citing plan's phases. When `/triage` or any merge pulls a landed sibling spec in, grep that sibling's DECISIONS entry for references to THIS spec (and vice-versa) and reconcile shared-concern ownership explicitly; if a shared exit code / behavior ends up owned by neither, flag it as an open scope decision rather than letting each spec assume the other has it. On a confusing git state mid-merge, inspect `.git/MERGE_HEAD` + `git ls-files -u` before committing; never commit an index with unmerged entries as a normal commit. **(4) A scope fold-in sweep must reach the document HEADER, INTRO, and SUMMARY lines — not just the body tables — and the sibling Guardian artifacts.** Folding `429→RateLimited(5)` into 015 updated the interface-spec *tables* but left the plan-reference header ("reserved exit code 4") and the intro paragraph ("only PermissionError") behind, and `analyze.md` (a sibling Guardian artifact) still reported the pre-fold-in checklist counts + the resolved K5 as failing — Copilot flagged all of them. When a capability's exit-code/feature set changes, grep the *whole* artifact set (incl. `analyze.md`, `checklist.md`, `risk.md`, and every doc's header/intro/summary) for the old narrower statement, not only the obvious mapping table. **(5) Each fix-push re-triggers Copilot, which re-flags issues a *concurrent* commit already fixed** — Copilot reviewed the fold-in commit `488cc3e` before the refresh `e76ea34` landed, so 6 of 8 "new" threads were already-fixed staleness with fresh thread IDs. Before re-doing work on a fresh `/triage` batch, diff the flagged lines against the *current* file state (and check whether a later commit already addressed them) — reply "already fixed in <hash>" + resolve rather than re-editing.

### 2026-06-07 — 013's spec artifacts say "my actions" / "the `my` parent"; the implemented convention is `me actions` under the runnable `me` command
(Found during [T003](../../specs/013-my-actions/tasks.md) implementation of 013-my-actions)

- **Type**: process / spec-staleness
- **Location**: `specs/013-my-actions/{plan,tasks,interface-cli,interface-spec}.md` (prose) vs. `internal/cli/app.go` + `internal/cli/my_actions.go` (implementation).
- **Severity**: low — the intent was fully recoverable and a prior learning already anticipated the right shape; the trap is only that a Builder following the 013 prose literally would invent a competing `my` parent and fork the surface.
- **Description**: every 013 artifact was written assuming My Roles (012) would introduce a top-level `my` parent command (`glassfrog my roles` / `glassfrog my actions`), and 013 would attach an `actions` leaf under it. But 012 actually shipped as `glassfrog me roles` — a `roles` leaf under the runnable `me` command (commit 959be80; `app.go` wires `MustRegister(meCmd, newMyRolesCommand(...))`). There is no `my` parent. The 2026-06-07 registration-guard learning already encoded the correct convention, naming "`me actions` 013" and "`me projects` 014" explicitly. So the 013 prose was stale-on-arrival, not a live design choice. Resolution: built `me actions` as a sibling leaf of `me roles` under `me`, mirroring 012 exactly (reused `meSeam`, `classifyClientError`, and the signal-on-stderr `incompleteActionsNote` convention). Creating a `my` parent would contradict the sibling `me roles` and fork the self-service-read surface — the very thing ADR-4 option 3 rejected.
- **Suggested action**: **014 (My Projects)** has the identical drift in its artifacts ("my projects" / "the `my` parent") — implement it as `me projects` under `me`, not under a `my` parent, mirroring `me roles`/`me actions`. When `/score:validate` runs against 013/014, read the command surface as `glassfrog me <noun>` (the spec prose's `my <noun>` is the stale label, the code and the registration-guard learning are the truth). If anyone later genuinely wants a `my` parent, that is a deliberate rename of the whole self-service-read group (`me roles` + `me actions` + `me projects`) through one spec, not a per-leaf divergence. **The drift is not only the command path — it extends to internal symbol names the spec/tasks invented that the code never used.** PR #39 review round 2 (Copilot) caught three residue sites the first `my`→`me` alignment sweep missed because they are *different token forms*: the camelCase identifier `myParent` in a `MustRegister(myParent, …)` example (the sweep matched `my actions` / `` `my` `` / `my parent` but not the glued camelCase token), and a planned-but-unused seam/type symbol `myActionsSeam` / `glassfrog.ActionList` (the code reuses `meSeam` and named the envelope `MyActionsResponse`). When aligning or validating these artifacts, sweep **every** token form — spaced, backticked, camelCase identifiers, and sibling symbol names — and verify each named Go symbol (`*Seam`, decode types, parent vars) against the actual code, not just the user-facing command string. Project instance of the global convention-sweep rule (the sweep/validation pattern must be a superset of the rule, covering alternate spellings and placeholder forms).
- **Update (2026-06-07, 014 implementation)**: 014 shipped exactly as prescribed — `me projects` as a third sibling leaf under the runnable `me` command (`app.go` wires `MustRegister(meCmd, newMyProjectsCommand(productionSeam{}))`), reusing `meSeam`, `validateStatus`, `classifyClientError`, and 012's stderr more-results signal (`incompleteProjectsNote`). The 014 artifacts' "my projects" / "`my` parent" prose is the stale label; the code surface is `glassfrog me projects`. No `myProjectsSeam`/`glassfrog.ProjectList` symbols were invented — the reused `meSeam` and `glassfrog.MyProjectsResponse` follow the 013 naming. tasks.md T002/T003 completion notes record the drift inline for the validate pass.

### 2026-06-07 — validate.md coverage tables: one row per behavioral scenario, and the "N of N" count must equal the feature file's non-@wip scenario count
(Found during PR #39 Copilot review of 013-my-actions validate.md)

- **Type**: convention / Guardian-artifact authoring
- **Location**: `specs/<NNN>/validate.md` § Driving Scenario Coverage (and any coverage/conformance table that states an "N of N" count) — produced by `/score:validate`.
- **Severity**: low — a self-consistency/clarity defect in the conformance record, not a code issue; but it undermines the artifact's credibility (a reviewer reasonably asks "is it 10 or 11?").
- **Description**: 013's validate.md § Driving Scenario Coverage collapsed the two architecture-informed error scenarios (malformed base URL; malformed credentials file) into a single table row, headed the section "Pass (10 of 10 scenarios covered)" — yet the prose immediately said "all 11 behavioral scenarios pass executably via `TestMyActionsFeatures`". The feature file has 11 non-`@wip` behavioral scenarios, so the "10 of 10" count (driven by the collapsed row count) contradicted both the prose and the executable suite. Copilot flagged the inconsistency. Fix: split the collapsed row into one row per behavioral scenario (→ 11 rows) and restate the status as "11 of 11 behavioral scenarios covered", so the count, the table, and the godog suite's reported scenario count all agree.
- **Suggested action**: when writing a validate.md coverage table, **emit one row per behavioral (non-`@wip`) scenario in the feature file — never merge two scenarios into one row** — and make the "N of N" header equal that scenario count (cross-check it against the godog suite's own reported count, e.g. "11 scenarios (11 passed)"). If you want to distinguish the spec's driving scenarios from the feature file's concretized set, say so explicitly ("the spec's 7 driving/edge scenarios concretize into 11 behavioral scenarios") rather than letting two different numbers sit unreconciled across adjacent sentences. **014 (My Projects)** will produce a structurally identical validate.md — apply this from the first draft. (Project instance of the global hand-count-vs-generated-count discipline: a hand-maintained "N of N" must be reconciled against the artifact it counts.)

### 2026-06-07 — command request tests must assert the endpoint PATH, not just query params and call count
(Found during PR #39 Copilot review of 013-my-actions)

- **Type**: convention / test-coverage
- **Location**: `internal/cli/*_test.go` command tests over the shared `cannedTransport` (me / me roles / me actions, and the future me projects).
- **Severity**: low — a coverage gap, not a live bug; but it lets a whole class of regression (wrong endpoint) pass silently.
- **Description**: the `me actions` tests asserted the `?status=` query parameter and the request count (`tr.calls == 1`), but never that the request actually went to `/me/actions`. The shared `cannedTransport` recorded `lastQuery` but not the path, so a regression that changed `runMyActions`'s `Request{Path: "/me/actions"}` to a different endpoint would still pass every test. Fix: added a `lastPath` field to `cannedTransport` (`c.lastPath = req.URL.Path` in `RoundTrip`) and asserted `strings.HasSuffix(tr.lastPath, "/me/actions")` in the success test. (`HasSuffix`, because `apiclient.Execute` joins the base URL's path prefix — e.g. `/api/v5` — ahead of the request path.)
- **Suggested action**: any command test that drives a request through the `cannedTransport` should pin the **endpoint path** (`strings.HasSuffix(tr.lastPath, "/me/<noun>")`) in at least the success case, alongside the existing query/call-count assertions. The path is the command's core contract with the API; query + count alone don't pin it. **This applies to the godog acceptance step too, not just the unit test** (PR #39 review round 3): a step whose Gherkin phrasing *claims* the request reached an endpoint (e.g. "the request will go to the my-actions endpoint" → `requestWentToEndpoint`) must actually assert `HasSuffix(lastPath, "/me/<noun>")`, not merely `calls == 1` — otherwise the acceptance suite's endpoint claim is unverified even when the unit test covers it. Match the assertion to what the step *says* it checks. **014 (My Projects)** reuses this transport — assert `/me/projects` the same way in both the unit tests and the acceptance step. (`me` and `me roles` predate the `lastPath` field; add the assertion opportunistically if those tests are touched.)
- **Update (2026-06-08, PR #43 Copilot review of 017)**: the discipline extends to **every new fake transport**, not just the shared `cannedTransport`. 017 added a *sequencing* fake base (`seqMeTransport`, for the 429-then-200 retry test) that omitted path recording, so the new retry test pinned call count + backoff but not that `runMe` still targets `/me` — exactly the gap this entry warns about, reintroduced by a fresh fake. Fix: give any new request-recording fake a `lastPath` field set in `RoundTrip` from the start, and assert `strings.HasSuffix(tr.lastPath, "/me<...>")` in at least the success path. When you add a fake transport, port the `lastPath` field over with it — don't wait for review to flag the missing endpoint assertion.

### 2026-06-07 — when parsing an integer that gets scaled into a fixed-width type, bound it against overflow, not just against negative/non-numeric
(Found during PR #43 Copilot review of 017-rate-limit-handling)

- **Type**: code-correctness / parser-robustness
- **Location**: `internal/apiclient/retry.go:parseRetryAfter` (and any future header/config integer that is multiplied by a unit — page sizes, byte counts, timeouts).
- **Severity**: low likelihood (the API sends `Retry-After` ≤ ~3600s), but it silently defeats a safety cap, so worth pinning as a discipline.
- **Description**: `parseRetryAfter` parsed `Retry-After` with `strconv.Atoi` (returns `int` — 64-bit on the build platforms) and returned `time.Duration(secs) * time.Second`. `time.Duration` is `int64` nanoseconds, so any value above `math.MaxInt64 / 1e9` (≈ 9.2e9 seconds ≈ 292 years) overflows when scaled and **wraps to a negative/garbage duration**. The existing guard only rejected `secs < 0` and non-numeric input — it did not catch a *positive* input that becomes negative *after scaling*. A wrapped-negative `wait` then (a) bypasses the total-wait budget check (`waited+wait > MaxTotalWait` is false for a negative wait), (b) makes `time.Sleep(negative)` return immediately, so the loop burns attempts with no real backoff, and (c) prints a confusing negative-duration progress note. The function's own contract ("absent / non-integer / negative / HTTP-date → unusable → fall back to FallbackBackoff") *should* have covered this — overflow is just another flavor of "unusable". Fix: parse with `strconv.ParseInt(v, 10, 64)` (platform-independent) and reject `secs > maxRetryAfterSeconds` (a named `const = int64(math.MaxInt64) / int64(time.Second)`), treating overflow as unusable → fallback. Tests: an overflow case (rejected) and a largest-non-overflowing case (accepted, scaled correctly).
- **Suggested action**: whenever you parse an integer that will be **multiplied/scaled into a fixed-width target** (`time.Duration` ns, a byte count, a millisecond timeout), the validity check must bound the input against the target type's range **after scaling**, not merely reject negativity/non-numeric. The clean shape: derive a named `maxXSeconds`/`maxXBytes` constant from `math.MaxInt64 / int64(scale)` and reject above it, folding "would overflow" into the same "unusable → safe default" branch the parser already has for the other garbage forms. Use `ParseInt(_, 10, 64)` over `Atoi` so the bound is explicit and platform-independent. This is the integer-overflow sibling of the existing URL/semver parser-robustness discipline (a parser must never emit a value that downstream code will treat as a *valid* but nonsensical quantity). 015 (API Error Extraction) and 016 (Pagination) parse their own numeric fields on the same seam — apply the same overflow-bounding there.

### 2026-06-07 — hardening a re-issuing walker over a caller's request: own-the-param means Del-then-Set, guard nil options, and never re-send a consumed Body
(Found during PR #41 Copilot review of 016-pagination — three rounds)

- **Type**: convention / API-boundary hardening
- **Location**: `internal/paging/paging.go` `All[T]` (the per-page query build, the functional-option loop, and the per-page request copy); transferable to any walker/helper that **re-issues a copy of a caller's request struct in a loop** and claims ownership of a subset of its fields, and any functional-option API.
- **Severity**: low–medium — #1 and #3 were latent correctness traps (a partial/wrong request produced silently); #2 a latent panic. None fired in existing tests because the live consumers are well-behaved (bodyless GETs, no stray cursor, no nil option).
- **Description**: a function that loops re-issuing `pageReq := req` (a shallow struct copy) over a caller-supplied `apiclient.Request` shares every field of that struct across iterations — and three of them bit, each caught by Copilot: (1) **`cursor` ownership via conditional Set.** The walker claimed sole ownership of the `cursor` query param but only *conditionally set* it (`if cursor != "" { q.Set("cursor", cursor) }`). The internal `cursor` var is `""` on page 1, so the conditional never fires there — a stale `cursor` in the caller's `req.Query`, copied by `cloneQuery`, **survived into the first request**, starting the walk mid-stream → an apparently-complete result that is actually partial. Ownership of a key you copy from the caller requires an explicit **`q.Del("cursor")`** before the conditional set. (2) **nil functional option.** `for _, opt := range opts { opt(&cfg) }` panics on a nil `Option` (common when a caller appends conditionally); guarded with `if opt != nil`. (3) **consumed `io.Reader` Body.** `req.Body` is an `io.Reader` the first page's `Execute` consumes; the shallow copy shares it, so re-issuing page 2+ re-reads an exhausted reader and sends an empty/partial body — a wrong request, silently. The walker can't rewind an `io.Reader`, so it now **stops loud with a typed `UnrewindableBodyError` before following the next page** when `req.Body != nil` (retaining the page(s) gathered, ADR-3); a single-page body request is unaffected.
- **Suggested action**: when a helper **re-issues a copy of a caller's request struct in a loop**, audit every field of that struct for shared/single-use state, not just the obvious one: (a) **map/slice fields it claims to own** — clone, then `Del`+set each owned key (never a conditional `Set` guarded by an internal "do I have a value yet" flag, which skips the first iteration); test by passing the owned key in the caller's input and asserting the first outgoing request omits it and the caller's map is untouched; (b) **`io.Reader`/`io.Closer`/channel/other single-use fields** — these cannot be safely re-sent; fail loud (typed error, partial retained) when re-issue would be required, rather than send a consumed value; test multi-iteration-with-body stops loud AND single-iteration-with-body succeeds; (c) **functional-option loops** — guard `if opt != nil` and pin with a `nil`-in-the-middle test. (The nil-option guard is distinct from the project's "fail-fast on nil **seam**" rule — that governs required injected dependencies; an optional configurator should be skipped, not fatal.) Meta: all three landed across three review rounds of the SAME PR — a shallow-struct-copy-in-a-loop is a cluster hazard; when you write one, sweep all of its fields for shared state in the first pass.

### 2026-06-07 — landed reads sampled 401/403 as their "generic non-2xx"; 015's split made those samples reclassify, so re-point them to 500
(Found during [T002](../../specs/015-api-error-extraction/tasks.md) implementation)

- **Type**: cross-spec drift / test-fixture staleness
- **Location**: `internal/cli/{me,my_actions,my_projects,myroles}_test.go`, `internal/cli/{my_actions,my_projects}_bdd_test.go`, `internal/cli/identity_read_bdd_test.go`, and `features/self-service-reads/{my-roles,identity-read}.feature`.
- **Severity**: low — the behavioural change is intended and was forecast by 004/011 and tasks.md's cross-spec note; the trap is only that the landed tests/features encoded the *old* classification using a now-reclassified status as their arbitrary sample.
- **Description**: API Error Extraction (015) split the generic `APIError`(3) bucket at `classifyClientError` — **401/403 → `PermissionError`(4)** and **429 → `RateLimited`(5)**. Several landed reads (011–014) had chosen **401** or **403** as the arbitrary representative of "a generic non-2xx" in their tests and one feature, asserting `APIError`/exit 3. After 015 those samples flip to permission/4, breaking the suites even though the reads' own behaviour is unchanged. Two flavours of fix, both preserving the original *intent* ("a generic non-2xx → exit 3"): (1) where the status was an arbitrary sample (unit tests; my-actions/my-projects BDD Given steps that say "a non-2xx response" generically; the `me`/`me actions`/`me projects`/`me roles` unit tests' `api-error` table rows), **re-point the sample from 401/403 to 500** (a 5xx is genuinely generic post-split) — a code-only change, no feature edited; (2) where a landed **feature prose** hard-coded the status (`my-roles.feature`: "a 403 response" / "name the 403 status" / "exit with code 3"), the .feature itself had to be edited 403→500 to keep the scenario faithful — the only non-@wip .feature edit outside 015's own file this slice made. The `identity-read.feature` "unusable token → 401" scenario keeps its 401 (a bad token *is* now a permission failure); its step `surfacedNon2xxWithStatus` was relaxed from "must be APIError/3" to "must be an API-family non-success category (APIError/PermissionError/RateLimited) carrying the status", matching the scenario's generic prose and surviving the reclassification.
- **Suggested action**: when a spec **splits or reclassifies a shared outcome/exit-code registry** (here `classifyClientError`/`ExitCode`), grep every landed test and feature for the literal status values it newly carves out (`401`, `403`, `429`) **before** running the suites — each hit that asserts the old category is a fixture to re-point (to a status still in the residual bucket) or a feature to update. Prefer re-pointing arbitrary samples to a status that stays generic (a 5xx) over retrofitting the new category into a scenario that predates it — that keeps the old spec's scenario testing what it meant and lets the new spec's own scenarios own the new category's coverage. Watch for the status being pinned in **three** places per read: the unit test, the BDD Given step, and (sometimes) the feature prose + the Then assertion — they must move together. **Pagination (016) and Rate-Limit Handling (017)** build on this same `classifyClientError` seam; if either later reclassifies another status, repeat this sweep. (Project instance of the global "after designating or changing a shared registry, grep every sibling site" discipline.)

### 2026-06-08 — a retry/backoff loop with an injected sleep seam must check ctx.Err() before sleeping (time.Sleep ignores context)
(Found during PR #43 Copilot review of 017-rate-limit-handling)

- **Type**: code-correctness / responsiveness
- **Location**: `internal/apiclient/retry.go:(*RetryExecutor).Execute` — the bounded 429 backoff loop; any future loop that waits between attempts (016 Pagination if it ever backs off; a write-retry path).
- **Severity**: low impact (waits are cap-bounded), but a real responsiveness defect for an interactive CLI / cancellable agent run.
- **Description**: 017 injects `sleep func(time.Duration)` (prod binds `time.Sleep`) so cap/backoff tests never block. But `time.Sleep` does **not** observe `context.Context` — so if the caller cancels (Ctrl-C) or the deadline fires *between* a 429 response and the next attempt, the loop still emitted the progress note and slept the full backoff before the next `client.Execute` could fail-fast. The per-attempt `client.Execute` honors ctx (it flows to `http.Do`), but the *gap between attempts* did not. Fix: check `reqCtx.Err() != nil` immediately before the note/sleep and, if set, surface the current outcome unchanged (`return resp, err`) — consistent with the other give-up branches (exhaustion, over-budget). Keeps cancellation responsive without making the injected sleep itself ctx-aware (the seam stays a plain `func(time.Duration)` for hermetic tests). Test: a base that cancels the ctx during its round trip, then returns a 429 → the loop surfaces the 429 with no sleep, no note, exactly one attempt.
- **Suggested action**: any loop that sleeps between iterations behind an injected (ctx-unaware) sleep seam must gate the sleep on `ctx.Err()` — either check before sleeping (minimal, keeps the seam a plain `func(time.Duration)`) or make the wait ctx-aware (`select { case <-time.After(d): case <-ctx.Done(): }`, but that reintroduces real time in tests unless the seam is richer). Prefer the pre-sleep check when the next iteration's first action already honors ctx and will fail-fast. The per-call timeout/cancellation covering the *work* does not cover the *wait between calls* — they are separate windows.

### 2026-06-08 — `me` compact always emits `roles=<N>`: a pure renderer can't observe the `--include` flag the interface keyed the count on
(Found during [T001](../../specs/019-templated-human-rendering/tasks.md) implementation)

- **Type**: interface-vs-implementation tension (resolved by uniform behaviour)
- **Location**: `internal/render/templates/me.compact.tmpl`; the `render.Render(resource, format, data)` signature in `internal/render/render.go`.
- **Severity**: low — `compact` is reachable from no operator surface until 020 wires `--output`, and no scenario/test pins the `me`-without-`--include` compact case.
- **Description**: `interface-cli.md` specifies the `me` compact line as `…  roles=<N>` with "`roles=<N>` present only when `--include roles` was given". But `render.Render` is pure over the decoded result struct (`glassfrog.MeResponse`) — it receives **no signal of whether `--include roles` was passed**, and the API only populates `MeResponse.Roles` when the embed was requested, so an empty `Roles` slice is indistinguishable between "embed not requested" and "embed requested, actor fills no roles". A `{{if .Roles}}` guard can't separate them (it is false for both nil and empty). The spec's own "Empty and absent data" accord also says compact renders `roles=0` on the record's line for an *empty embed*, which contradicts "present only when `--include` was given" for the not-requested case. Resolved by **always emitting `roles={{len .Roles}}`** (0 when none): it satisfies the only scenario ("Compact counts a nested collection" → `roles=3`), satisfies the accord's explicit `roles=0`-for-empty-embed statement, and is the simpler uniform rule (every `me` compact line carries the count). The one "wrong" case — emitting `roles=0` when `--include` was *not* given — is unreachable from the CLI today and untested.
- **Suggested action**: when **020 Output Format Selection** wires `--output` and makes `compact` reachable, decide deliberately whether the `me` compact line should suppress `roles=` when `--include roles` was not requested. If so, the include signal must reach the renderer — either pass it as part of the data (a small view value the read assembles) or widen the render API; a pure renderer over `MeResponse` alone cannot honour the "only when `--include` was given" clause. Until then, the uniform always-count behaviour stands and is pinned by `TestRender_MeCompact_NestedCollectionAsCount`.

### 2026-06-08 — ADR-3 says "record sigs.k8s.io/yaml in PROJECT.md's stack", but PROJECT.md has no stack/dependencies section
(Found during [T001](../specs/018-structured-serialization/tasks.md) implementation)

- **Type**: design-issue
- **Location**: `specs/018-structured-serialization/plan.md` ADR-3 / Risks ("record it in PROJECT.md's stack"); `PROJECT.md`.
- **Severity**: low — the dependency is recorded authoritatively in `go.mod`/`go.sum`; the plan's mitigation just points at a section that does not exist.
- **Description**: ADR-3 (and the Risks section) instruct the Builder to record the new `sigs.k8s.io/yaml` dependency in "PROJECT.md's stack". This project's PROJECT.md is a Prelude/Score *domain* artifact — Domain / Vocabulary / Actors / Scope / Constraints — with no Stack, Dependencies, or Tech section. Adding one would invent structure the discovery pipeline never established (and would be the Builder redesigning a discovery artifact, not implementing a task). The dependency is recorded where it belongs: `go.mod` (`sigs.k8s.io/yaml v1.4.0`, promoted from indirect to direct by `go mod tidy` once imported in non-test code). 018 added no `accords/` and touched no PROJECT.md.
- **Suggested action**: 019 and 020 (which also extend `internal/output` and may add deps) should treat `go.mod` as the dependency source of truth and ignore the "record in PROJECT.md's stack" clause unless a stack section is deliberately added to PROJECT.md first. If a tech-stack record is genuinely wanted, that is a PROJECT.md revision (Prelude `project` skill), not an implement-time edit.

### 2026-06-08 — 020 dispatch: structured path can't see the typed struct, so the incompleteness note is human-only; and an output-key rcfile error must not borrow `--base-url` wording
(Found during [T005](../specs/020-output-format-selection/tasks.md)/[T006](../specs/020-output-format-selection/tasks.md) implementation)

- **Type**: design-decision (two related, both deliberate)
- **Location**: `internal/cli/render.go` (`renderResult[T]`); `internal/cli/me.go` (`reportFormatResolutionError`); `internal/cli/me_roles.go`/`me_actions.go`/`me_projects.go`.
- **Severity**: low — both are reasoned interim positions, not defects; flagged so 032 (Output-Aware Failure Rendering) and 029 (User-Defined Templates) inherit the rationale rather than re-deriving it.
- **Description**:
  1. **Incompleteness note is emitted on the human path only.** The dispatch picks the decode target from the format: a `json.RawMessage` (verbatim 2xx bytes, 018 fidelity) for `json`/`yaml`, the typed `*T` for `full`/`compact`. The list reads' "this list may be incomplete" note keys on `resp.Meta.Pagination.HasNextPage` — a *typed-struct* field the structured path never decodes. Rather than decode twice (wasteful, and re-parsing pagination from raw forks the contract), the note rides the human path only; the structured document already carries the API's pagination metadata in-band, so a machine consumer reads completeness from the document itself. The note is passed to the dispatch as an optional `func(T) string` consulted only after a successful human render.
  2. **`reportFormatResolutionError` deliberately does NOT reuse `reportClientError`/`formatClientErrorMessage`.** A format-resolution failure can be a `*output.FormatError` (invalid selector) OR an `*rcfile.ReadError`/`*rcfile.FormatError` (unreadable `.glassfrogrc` while reading the `output` key). `formatClientErrorMessage`'s rcfile arm appends "— correct --base-url, GLASSFROG_BASE_URL, or the .glassfrogrc base_url", which would misdescribe an *output-key* rcfile error as a base-URL problem. So the resolution path writes the error's own text (the `FormatError` names source+value+supported set; the rcfile error names the file) and pairs it with `classifyClientError` (both → `UsageError(2)`), keeping category and message consistent without the base-URL wording. ADR-4's "classifyClientError gains an arm" is honoured (the `*output.FormatError` arm), but the *message* is the error's own, not the base-URL-worded formatter.
- **Suggested action**: **032** owns rendering command *failures* in the selected format; when it wraps `json`/`yaml` failures in 018's envelope it will need the resolved format on the failure path too — reuse the same resolve-first seam (`meSeam.resolveFormat`) the reads already call, and decide there whether a structured failure should carry a structured pagination/incompleteness signal (the human note's machine analogue). If a future read needs the incompleteness signal under a structured format, surface it from the raw document (it is already present) rather than decoding the typed struct a second time.

### 2026-06-08 — 020 left the `me` compact `roles=<N>`-when-`--include`-absent question to the renderer's owning spec (out of selection scope)
(Found during [T006](../specs/020-output-format-selection/tasks.md) implementation; closes the question 019's LEARNINGS handed to 020)

- **Type**: scope-boundary decision
- **Location**: `internal/render/templates/me.compact.tmpl` (unchanged by 020); `specs/020-output-format-selection/interface-cli.md` (Consistency Notes — "020 does not normalize 019's rendering concerns").
- **Severity**: low — `me` compact is now reachable via `--output compact`, but no scenario pins the `me`-without-`--include` compact case, and the always-`roles=<N>` behaviour is the uniform rule 019 settled on.
- **Description**: 019's LEARNINGS asked 020 to "decide deliberately whether the `me` compact line should suppress `roles=` when `--include roles` was not requested", noting a pure renderer over `MeResponse` cannot tell "embed not requested" from "embed requested, no roles". 020's deliberate decision: **leave it unchanged.** interface-cli.md scopes 020 to *selection and dispatch* and explicitly states 020 does not normalize 019's rendering concerns (the same stance it takes on 019's flagged empty-line inconsistency); changing the template here would edit a renderer 019 owns and would touch sibling goldens outside 020's selection surface. The always-`roles={{len .Roles}}` behaviour stands, now reachable and exercised end-to-end by the compact-selector scenario (via `me roles`, not the `me`-include edge).
- **Suggested action**: if the `me` compact `roles=`-suppression is genuinely wanted, it belongs to a `internal/render` change (019's owner) or **029 User-Defined Template Output**, which extends the same engine — and it requires the `--include` signal to reach the renderer (a small view value the read assembles, or a widened render API), exactly as 019's entry noted. It is not an output-selection concern.

### 2026-06-08 — build-behavior scenarios are verified through the config-guard proxy, not by running goreleaser
(Found during [T002/T003](../../specs/021-self-contained-executable-build/tasks.md) implementation)

- **Type**: scope-boundary / verification-strategy decision
- **Location**: `internal/build/selfcontained_bdd_test.go` (the godog step mappings); `internal/build/{config,linkage,hostbinary}.go`.
- **Severity**: low — all 9 executable scenarios pass; 3 @validation scenarios held for validate. The decision affects *how* the build-matrix scenarios are proven, not whether the build works (the real `goreleaser build` was run and produced all four binaries + dist/artifacts.json during implementation).
- **Description**: Spec 021's feature scenarios fall into three families. Self-containment scenarios ("runs on a clean host", "rejects a foreign dependency", per-target allowlist, "local build") map directly to executable Go logic: a host-target binary is obtained (dist/artifacts.json manifest-first, `go build` host fallback) and its linkage inspected against a per-platform OS-only allowlist. Config-drift scenarios (cgo enabled, windows target) run `CheckConfigGuard` over in-memory mutated configs. But the **build-behavior** scenarios ("produces all four target binaries", "a failed target fails the whole build / no partial set", "foreign-target binaries build from any host") cannot run goreleaser inside `go test` — T003's contract is that `go test ./...` must run *without* goreleaser installed (host-build fallback). So those three are mapped to assertions over the parsed real `.goreleaser.yaml`: the matrix is exactly the four targets (config-guard passes), the four targets live in one atomic `builds` entry (GoReleaser aborts the whole build on any target's failure — no partial dist), and `CGO_ENABLED=0` is set (the lever that enables cross-compilation from any host). This is the plan's chosen in-process proxy (ADR-2: "the config-guard reads the config and asserts the matrix"); actual artifact production is verified by the documented `goreleaser build --snapshot --clean` invocation and CI (022).
- **Suggested action**: 022 (Automated Release Pipeline) owns running goreleaser in CI and pointing the self-containment check at real `dist/` artifacts across multiple target hosts. When 022 lands, the build-behavior scenarios *can* be promoted from config-proxy assertions to artifact-level checks if desired (e.g. a CI-only test that runs `goreleaser build` and asserts 4 binaries). The @validation scenarios ("the matrix is exactly the four supported targets", "shared build entry point", "needs only the API at runtime") remain held for the validate skill.

### 2026-06-08 — linkage inspection must name missing deps and distinguish benign tool exits from real failures
(Found during [PR #54 review](../../specs/021-self-contained-executable-build/) triage)

- **Type**: parser/tooling robustness
- **Location**: `internal/build/linkage.go` (`parseLdd`, `extractDeps` linux + darwin branches)
- **Severity**: medium — both bugs weakened the self-containment check on Linux without failing any test, so they would have shipped silent.
- **Description**: Two gotchas in shelling out to `ldd` for Linux linkage inspection. (1) `ldd`'s missing-dependency line is `libfoo.so.1 => not found` — the right side is the literal string `"not found"`, NOT empty. The first cut skipped only the empty-right-side case, so it appended `"not found"` as the dependency and lost the library name — exactly the most important violation (a required library that isn't present) rendered unnameable. Fix: on `=>`, capture the left-side name and, when the right side is empty or `"not found"`, report the NAME. (2) `ldd` exits non-zero for a statically-linked binary (`"not a dynamic executable"`) — the self-contained case — so the first cut discarded ALL `ldd` errors with `out, _ :=`. That meant a missing/unexecutable `ldd` produced empty output and a false PASS (zero deps) without inspecting linkage at all. Fix: suppress the non-zero exit ONLY when the output reports static/non-dynamic (shared `lddReportsStatic` helper); surface every other error. **General rule**: when a CLI exits non-zero for a *legitimate* state, detect that specific state and suppress only then — never blanket-drop the error. And when parsing tool output with a sentinel (`=> not found`), preserve the identifying token, don't collapse it to the sentinel. **Symmetric-sibling follow-up (PR #54 round 2):** the round-1 ldd fix used `CombinedOutput()` + embedded the output in the error, but the `extractDeps` darwin branch still used `Output()` (drops `otool`'s stderr — the non-Mach-O / missing-file diagnostics). Both branches of the same `switch goos` shell out and should surface stderr identically; fixing one and leaving the sibling is the classic missed-symmetric-call-site, caught in round 2. When you change error-handling in one branch of a tool-invocation switch, apply it to every branch in the same pass.
- **Companion (testing)**: tests that shell out to platform-specific tools (`otool`/`ldd`) must skip on unsupported `runtime.GOOS` (e.g. Windows) so `go test ./...` stays green on dev hosts off the target matrix — repo precedent already exists in `internal/auth/auth_test.go`. Pure-logic tests (parsers, allowlist, config-guard) carry no such guard and run everywhere, preserving coverage. Relevant to 022, which extends `internal/build` linkage inspection across more targets/hosts.

### 2026-06-08 — paths gated on a tool-produced artifact (dist/) are invisible to normal `go test` — parameterize and test the core
(Found during [PR #54 round 4](../../specs/021-self-contained-executable-build/) triage)

- **Type**: test coverage / testability
- **Location**: `internal/build/hostbinary.go` (`discoverDistBinary` / `discoverDistBinaryIn`)
- **Severity**: low-medium — the manifest-first discovery path is a real contract (HostBinary prefers it; 022 consumes `dist/`) but had zero test coverage, so a parsing/selection regression would have shipped silent.
- **Description**: `HostBinary` prefers a `dist/artifacts.json`-listed binary and only falls back to a host `go build` when the manifest is absent. But `dist/` is gitignored and produced only by goreleaser, so in every normal `go test` run the manifest branch is skipped and the fallback runs — the discovery logic (manifest parse, host GOOS/GOARCH selection, stale-reference fallback) was never exercised. The fix that makes it testable: the function read the repo root itself via `RepoRoot()`, coupling it to the real tree; extract a root-parameterized core (`discoverDistBinaryIn(root)`) so a test can point it at a `t.TempDir()` with a hand-written `artifacts.json` + referenced file. **General rule**: when a code path is gated on a precondition that only a build tool produces (a `dist/` artifact, a generated file, a cached download), normal tests silently take the *other* branch — extract the logic behind the precondition into a pure/parameterized helper and test it directly, and cover the fallback/stale cases too. Relevant to 022, which adds more `dist/`-consuming paths (archives, checksums, release upload).

### 2026-06-08 — loop completeness: scan every entry before deciding (both an all-must-hold guard AND a find-first-valid search)
(Found during [PR #54 round 5](../../specs/021-self-contained-executable-build/) triage; extended round 6)

- **Type**: guard / search-loop correctness
- **Location**: `internal/build/config.go` (`checkCgoDisabled`); `internal/build/hostbinary.go` (`discoverDistBinaryIn`)
- **Severity**: medium — both holes silently produced the wrong result (a drifted config passing; a usable binary going undiscovered).
- **Description**: Two duals of the same loop-termination pitfall, both caught on this PR.
  (1) **All-must-hold guard** — `checkCgoDisabled` returned `nil` (pass) on the FIRST `CGO_ENABLED=0` entry, so a config with `CGO_ENABLED=0` then `CGO_ENABLED=1` passed even though a duplicate env key resolves to the LAST value (cgo re-enabled). A guard enforcing "all entries satisfy P" must scan every entry, fail on the first *violation*, and return pass only after the full scan (and, where presence matters, only if ≥1 entry was seen). Early-returning on the first *satisfying* entry is the bug.
  (2) **Find-first-valid search** — `discoverDistBinaryIn` returned `false` (fall back) as soon as the FIRST host-target Binary entry's file was missing, abandoning the search instead of `continue`-ing to a later valid host-target entry. A search for "the first entry satisfying P" must `continue` past non-satisfying candidates, returning the not-found result only after the full scan. Early-returning on the first *non-matching* candidate is the bug. (Ironically introduced by the round-4 stale-fallback fix; caught round 6.)
  Rule of thumb: in a decide-from-a-list loop, the only thing that justifies an early return is the *conclusive* case (a guard's violation, a search's match); every *inconclusive* case must `continue`. Especially load-bearing for env/flag lists where duplicate keys are legal and last-wins. Same family as the change-detector-rigor learnings (assert presence AND absence).
- **Companion (consuming tool-produced paths):** `discoverDistBinaryIn` joined `artifacts.json`'s `a.Path` to root and then *executed* the result; round 6 added a containment guard (resolved path must stay under `<root>/dist/`) against a `..`-escape, even though the manifest is our own goreleaser output. When a path from a tool's output feeds into exec/open, validate containment at the boundary — cheap, and catches malformed/hand-edited manifests too.
- **Companion (test robustness):** godog step funcs that indexed `w.cfg.Builds[0]` without a length guard would panic the suite on a drifted (zero-builds) config; route them through a `singleBuild() (Build, error)` helper so drift yields a readable assertion, not an index-out-of-range panic.

### 2026-06-09 — A spec Error (driving) scenario was tagged @validation in the feature file
(Found during [T001](../../specs/023-version-embedding/tasks.md) implementation)

- **Type**: process-issue
- **Location**: `features/runtime-dependent-distribution/version-embedding.feature` ("Version determination needs no network or VCS at runtime"); `specs/023-version-embedding/spec.md` (Error scenarios)
- **Severity**: low — no behavioral impact; the property is independently pinned, and the scenario is still verified, just on the held-out (validate) path rather than the builder path.
- **Description**: spec.md categorizes "the version determination never reaches out at runtime" under **Error scenarios** (a driving scenario → should be plain `@wip`, implemented and un-wipped by the Builder). The scenarios phase tagged it `@validation @wip` (held out for the validate skill) instead. The Builder left it as-is rather than re-tagging: the only sanctioned `.feature` edit during implement is `@wip` removal, not changing `@validation` categorization, and the resolver's no-I/O property is already pinned by the pure `resolveVersion` unit tests (it takes build info as a parameter and performs no network/VCS access), so coverage is not lost. Net effect: T001 un-wipped 5 driving scenarios instead of 6; "never reaches out at runtime" stays held out for validate. Rule of thumb for the scenarios phase: a spec's *Error* scenarios are driving (`@wip`), only its *Validation Scenarios* section gets `@validation @wip` — categorize from the spec's own section, not from whether the scenario "feels like an invariant."

### 2026-06-09 — "too loose" guards/assertions: validate a structured flag's grammar and value shape, surface a run helper's outcome/error, and make a BDD step's Then as strong as the scenario it claims
(Found during PR #58 review triage — spec [023](../../specs/023-version-embedding/tasks.md))

- **Type**: review-finding
- **Location**: `internal/build/config.go` (`CheckVersionInjection`/`ldflagsInjectVersion`); `internal/build/version_injection_test.go`; `internal/cli/version_embedding_bdd_test.go` (`assembleAndRun`, `veThenIdentifiesCommit`)
- **Severity**: low–medium — each lets a broken state pass a guard/test silently.
- **Description**: Five duals of "too loose," caught by Copilot across four triage rounds on the 023 PR.
  (1) **Guard parser — tokenize, don't substring-match.** `CheckVersionInjection` first accepted any ldflags entry where `strings.Contains(f,"-X") && Contains(target+"=")`. That is too permissive for a release guard: it accepts the malformed concatenated `-Xsym=val` form (which the Go linker does NOT parse as `-X` — single-dash flags need `-X sym=val` or `-X=sym=val`, never `-Xsym`), and it can false-positive when the symbol appears in some unrelated flag (`-extldflags …target=…`). Fix: tokenize the entry (`strings.Fields`) and accept only the two real `-X` forms — `-X` as its own token followed by `<target>=…`, or a `-X=<target>=…` token. **General rule**: a guard asserting the *presence* of a structured token (a linker flag, an env assignment, a CLI arg) must validate the token's actual grammar, not test for a loose substring — substring-presence both accepts invalid forms and false-positives. Same family as the spec-pinned-string equality-not-`Contains` learning above (2026-06-07, item: "Equality, not substring") — both are "a `Contains` check is not a real guard." Pin the tightening with drift cases that include the malformed form (expect-violation) AND a symbol-outside-the-flag false-positive case.
  (2) **A run-and-capture test helper must not swallow the outcome/error.** `assembleAndRun` did `outcome, err := Run(...)` then `_, _ =` — discarding both — so an end-to-end `--version`/`version` scenario could pass on the output comparison even if the command returned a `UsageError` (with output still written). Fix: return an error from the helper and fail the step loudly on a non-`Success` outcome or a non-nil error; callers propagate it. Companion to the production-side learning above (2026-06-07, "command RunE error messages must carry their own cause — the entrypoint discards Run's error"): the entrypoint legitimately drops Run's *error* (004 renders no text), but a *test* helper asserting success must check it. Test helpers earn the same no-silent-failure rigor as production code.
  (3) **A BDD step's Then-assertion must be as strong as the scenario's stated outcome.** The step `the reported value will identify the exact commit` was wired to `veThenNotEmpty` (non-emptiness) — but the scenario's whole point is commit-identification for a pseudo-version, and a non-empty check passes for *any* version. Fix: a dedicated `veThenIdentifiesCommit` that asserts the value's final `-`-delimited segment is a commit-length hex string (`>=7` lowercase hex — the Go pseudo-version `vX.Y.Z-<ts>-<commit>` shape). **General rule**: when a Gherkin Then names a specific property ("identifies the commit", "is sorted", "redacts the token"), the step impl must verify *that* property, not a weaker proxy (non-empty / contains / count>0). A weaker proxy is the same "a `Contains` check is not a real guard" failure as (1) and the 2026-06-07 equality-not-substring entry — now seen on the assertion side of a shared feature step (steps are reused across scenarios, so an under-strength impl silently weakens every scenario that maps to it).
  (4) **A config guard must validate the required value shape, not only the field's presence.** After tokenization, `CheckVersionInjection` still accepted `-X …/internal/cli.version=1.0.0`; that targets the right symbol with a real linker flag, but violates 023 ADR-4's `v`-prefixed release/snapshot shape. Fix: after extracting the `<target>=<value>` argument, require `strings.HasPrefix(value, "v")`, and pin both valid `v1.0.0` / `v{{ .Version }}` cases and a missing-prefix drift case. **General rule**: a guard for a declarative contract must enforce every behaviorally relevant part of the contract — target, syntax, and value shape — not just the easiest observable part.
  (5) **A test's failure message must describe what it actually checks — don't overclaim.** `veThenVersionCommandSame` compared two *trimmed* values (`assembleAndRun` applies `strings.TrimSpace`) but its failure message said they must be "byte-identical". The assertion was correct; the message overstated it. Fix: reword to "must report the same value" and note that the raw byte-for-byte parity is pinned elsewhere (003's `TestVersionFlagAndCommandParity`, which compares untrimmed output). **General rule**: an assertion's message names the property being verified — when the check is deliberately looser than a stronger named property (trimmed-equality vs raw byte-identity), say so, and point at where the stronger property is actually guarded. The inverse of (3): there the *check* was too weak; here the *message* was too strong.

### 2026-06-09 — forward Score artifacts (plan/interface/tasks/checklist) must be authored against the EXISTING codebase + STATUS, not a clean-slate assumption
(Found during [PR #59 round 1](../../specs/025-role-reads/) triage — Copilot caught 4 instances)

- **Type**: process / artifact fidelity
- **Location**: `specs/025-role-reads/` (plan.md, interface-cli.md, interface-spec.md, tasks.md, checklist.md)
- **Severity**: medium — the artifacts told the Builder to *create* files/keys that already exist, which would cause path/key collisions and a broken `me roles` contract if implemented verbatim.
- **Description**: The 025 define→shape→guard artifacts were written as if the org-wide `roles` surface were greenfield. Four collisions with shipped main code that a quick read of the tree would have caught: (1) `tasks.md` asserted spec **019 is "Complete"** but `specs/STATUS.md` shows it at **"Ready"** (its `internal/render` package is on main, but the prose contradicted STATUS); (2) every artifact said T002 **adds** `internal/cli/roles.go`, but that file **already exists on main** as a wired (`Assemble()`) `roles` *group* with `list`/`get` "not yet implemented" stubs — 025 must **replace** the stub (and its `list`/`get` verbs, which the chosen `roles`/`roles <id>` positional shape supersedes), not create new; (3)+(4) plan/interface/tasks planned a **`roles` render key**, but `render.ResourceRoles` (`"roles"`, with `roles.full/compact.tmpl`) is **already used by `glassfrog me roles`** (`me_roles.go`) — reusing it would clobber that contract. Fixed by reframing as "replace the stub" + introducing distinct new render keys (`org-roles`, `role`) and aligning the 019 wording with STATUS.
- **Suggested action**: before writing forward artifacts for a capability, **grep the codebase for the command package, render/resource keys, and symbol names the spec will touch, and read `specs/STATUS.md` for the real stage of every cited dependency** — never assert "new file", "adds key X", or "dependency Y is Complete" without confirming against the tree + STATUS. Concretely for this repo: (a) `git ls-files internal/cli | grep <noun>` and read any existing stub before claiming a command file is new; (b) `grep -rn "Resource" internal/render/render.go` to see which render keys are taken before naming one (the `/me*` reads already own `me`/`roles`/`actions`/`projects`); (c) quote STATUS's literal stage string, don't paraphrase it to "Complete". This is the forward-artifact sibling of the existing **`my`→`me` symbol-drift** finding (line ~291, PR #39) and the user-level "verify against shipped code / verify reuse claims against source" learnings — that one was stale-rename drift in already-shaped artifacts; this one is clean-slate authoring that ignored what already shipped. When triaging multi-artifact specs, run the same grep across **all** artifacts (the fix's stale references surfaced in `checklist.md`'s Composition check too — symmetric-site sweep).
- **Round 2 (3 more instances, same root cause — fidelity to schema/types/landed-behavior):** Copilot caught three further artifact-vs-reality gaps that the same "verify before asserting" discipline covers, extended to data shapes and copied prose: (1) **API schema fidelity** — `interface-spec.md` modeled `Note` as `ID`, `Body`/`Text`, `CreatedAt`, but `spec/glassfrog-api-v5.yaml`'s `Note` has required `title` + `body` and **no `text` field** → grep the schema (`awk`/`grep` the `<Type>:` block in `spec.yaml`) for the real required field names before writing a model row, don't guess from the type name. (2) **Go-type fidelity in example snippets** — the interface-spec example used `Query:{"include": ...}`, an invalid Go map literal, when `apiclient.Request.Query` is `url.Values` (`internal/apiclient/client.go`) → example code in an interface accord is implementation guidance and must be type-correct/copy-pasteable; match the real field types (`url.Values{"k": {"v"}}`), don't use shorthand. (3) **Copied-accord staleness vs landed siblings** — `spec.md`'s Failure accord said non-2xx "exits with the generic API-error outcome; does not interpret which kind", copied from the `/me*` reads' pre-015 framing, but 025 ships *after* API Error Extraction (015) landed and its own interface-cli error table surfaces `PermissionError`(401/403)/`RateLimited`(429); the spec contradicted its own downstream artifacts → when reusing accord prose from a sibling spec, re-read it against capabilities that have landed since (the classifier now splits the status) and against this spec's own plan/interface, not just the sibling's spec. Rule of thumb: an interface/spec artifact's every concrete claim — a model's fields, an example's types, a failure-outcome set — must be checked against its authoritative source (`spec.yaml` schema, the real Go type, the landed shared behavior), exactly as the command/key/STATUS claims were in round 1.
- **Round 3 (2 more — a symmetric-site miss + a render-convention miss):** (1) The round-2 `Note` model fix (interface-spec.md row → `title`/`body`) was **not swept across artifacts**, so `interface-cli.md`'s single-role *output example* still rendered `- <note text>` — a contradiction with the just-corrected model row. **A schema-field fix in one artifact (a model table) must immediately grep every other artifact for the old token (`note text`/`text`) — including rendered-output examples, not just model rows.** This is the symmetric-site grep discipline (CLAUDE.md) applied to a same-PR fix: fixing the model row should have triggered `grep -rn 'note text\|`text`' specs/<spec>/` before committing round 2. (2) The `compact` output example used a name-first, em-dash, comma-separated line, but the repo's compact templates (`internal/render/templates/{roles,actions,projects}.compact.tmpl`) are **resource-id-first, double-space-separated `key=value` fragments** — when an artifact shows example output for a render format, match the existing template convention for that format (read a sibling `.compact.tmpl`/`.full.tmpl` first). Both are the same fidelity family; round 3 is the signal that after a fidelity fix, the *sweep* (every artifact, every output example) matters as much as the fix. (PR #59 reached 3 triage rounds — diminishing returns; the durable fix is to run these greps during authoring, not triage.)

### 2026-06-09 — implementing the first walking command: format changes rendering not fetch depth; nil-vs-empty drives guarded include sections; a stub→leaf swap rewrites a sibling test's premise
(Found during 025-role-reads implement — T002/T004/T005)

- **Type**: implementation decision + structural-drift adaptation
- **Location**: `internal/cli/roles.go` (`runRolesList`/`runRoleGet`); `internal/render/render.go` (`RoleView`) + `templates/role.*.tmpl`; `internal/cli/smoke_test.go`
- **Severity**: low — no behavioral surprise, but three judgment calls worth pinning for the downstream per-role reads (#33/#34/#38) and Organization Tree (#26).
- **Description**: Three decisions the artifacts left implicit, resolved against the landed precedent:
  (1) **Output format must change rendering, NOT fetch depth — json and human walk identically.** `roles` is the first command to use `paging.All`. The accords say "walk to completion by default" *and* "`-o json` emits the raw payload (018) verbatim". My first cut resolved the collision by making the structured branch a single first-page `Execute` (raw, in-band meta) while only the human path walked — mirroring the single-page `me roles` precedent. **The user rejected this as unpredictable**: `roles` (human) showed every role while `roles -o json` showed only page one. Corrected so *every* format walks to completion (and `--first-page` limits *all* formats to one page); the format changes only how the gathered set renders. To keep 018 fidelity on the structured path without a single verbatim payload-across-pages, the walk runs over `paging.All[json.RawMessage]` — each role's bytes are preserved verbatim — then the per-role messages are aggregated into a synthesized `{ "data": [ <raw>, … ] }` document (only the envelope is synthesized; no field dropped, no number coerced). Per-page `meta` is dropped (an aggregate has none) and completeness is signalled on **stderr** in *both* formats (the same mid-walk note + non-zero exit), so neither relies on in-band meta. Lesson for the downstream walking reads (#26 org tree, etc.): a paginated read's `-o json` must fetch the same set as its human output — aggregate raw page bytes, don't emit just page one. (The single read is naturally consistent: one object, no pagination, raw `{data: RoleDetail}` verbatim.)
  (2) **First-page failure vs mid-walk failure are different outcomes.** `paging.All` returns `Result{Records, Complete, Stop}`. A `Stop != nil` with **zero** records (first page failed: transport/auth/404) is a clean read error → `reportClientError`, nothing on stdout (matches `me*`). A `Stop != nil` with **≥1** record (mid-walk) renders the partial set THEN writes the incomplete note + exits non-zero via `classifyClientError(Stop)`. Rendering the partial set unconditionally (the accord's literal "render Result.Records") would print "No roles." on a first-page transport error — wrong. Branch on `len(Records)==0`.
  (3) **The API can't distinguish "include not requested" from "requested-but-empty" in the body — the request set must.** `?include=policies` returning `[]` and no `?include` at all both decode `Policies` to an empty/nil slice; Go templates can't tell nil from empty-non-nil reliably. So the `role` render data is a `render.RoleView{Detail, Requested map[string]bool}` carrying the validated `--include` set; the template guards each section with `{{if index .Requested "policies"}}` (omit when unrequested) and shows `(none)` inside it (marker when requested-but-empty). Any future read that conditionally embeds includes needs the same request-set companion, not just the decoded slice.
  (4) **Replacing a stub group with a runnable leaf rewrites a sibling test's premise.** `roles` was a stub *group* (`roles list`/`roles get`); 025 makes it a runnable *leaf*. `smoke_test.go`'s "bare group help → exit 0" case ran `roles` expecting help (groups print help, exit 0). A runnable `roles` with no token now runs the list → `*AuthError{NoCredentials}` → exit 2. The case moved to `auth` (still a pure group). Rule: when a spec converts a group to a leaf (or vice-versa), grep sibling tests for bare invocations of that command name — the group-prints-help / leaf-runs-action premise flips, and the assertion is silently testing the wrong thing otherwise.
- **Round 2 (PR #61 triage, Copilot — 2 more):** (5) **An error helper that classifies + messages off a *refined* error must also RETURN the refined error.** `reportIncompleteWalk` did `refined := refineClientError(stop); … classifyClientError(refined), stop` — returning the raw `stop`, so the returned value disagreed with the note text and outcome, and a downstream `errors.As(err, &ProblemError)` saw the generic `*ResponseError` on a mid-walk 401/403/429. `reportClientError` already returns the refined value; the rule is the whole invariant: **category, message, AND returned error all derive from the one refined value** — when you copy that pattern into a new helper, copy the return too (`return classifyClientError(refined), refined`). Pinned by `TestReportIncompleteWalk_ReturnsRefinedError`. (6) **Detect "flag was set" by presence (`cmd.Flags().Changed`), not by a non-default value sentinel — and apply by presence too.** `--per-page` validation used `perPageSet: cfg.perPage != 0`, so `--per-page=0` alongside a role id slipped past the list-only guard (0 is the int zero value), and `--per-page=-1` was accepted but silently dropped because the apply sites guarded on `> 0`. Both are the same value-vs-presence confusion the `--has-subroles` tri-state already solved with a pointer: carry a `perPageSet bool` from `Changed("per-page")`, validate on it, and **apply on it** — passing the provided value (even 0/negative) through to the API so an out-of-range value surfaces the API's rejection rather than being silently ignored (matches `paging`'s documented no-client-clamp contract). General rule for any flag whose default value is also a legal-looking user input (`0`, `""`, `false`): key both validation and application on `Changed`, not on `value != default`; passing the raw value through beats a client-side clamp when an authoritative downstream (the API) already validates the range. Companion to sub-point (3)'s "the API can't distinguish requested-empty from unrequested — the request set must": both are *presence is data the value alone doesn't carry*. **Recurred on a `bool` flag — PR #73 (033 `domain.go`), caught by Copilot:** `--first-page`'s rejection was wired off the parsed bool (`firstPage: firstPage`) while its *sibling* list-only flags `--query`/`--per-page` on the same command were already correctly keyed on `Changed(...)`, so `domain <id> --first-page=false` (explicitly provided) slipped the tripwire — the bool default `false` is itself a legal-looking value. Two added cues: (i) the rule applies to `bool` flags too, not just `int`/`string` — `false` is the bool's `0`-equivalent legal input; (ii) the reliable detection is **grep the sibling-flag group on the command for the odd one out** — when N−1 flags in a presence-rejected group use `Changed(...)` and one uses its raw value, that one is the bug (the symmetric-site discipline applied to flag wiring). Pin the fix with an **end-to-end cobra-level** test (`--first-page=false` → `UsageError`, no request) — a field-level unit test that sets the `domainConfig` bit directly bypasses the constructor wiring that is the actual bug site.
- **Round 3 (PR #61 triage, Copilot — 2 more + 1 doc-drift):** (7) **URL-escape a user-supplied id concatenated into a request path, even when the design says "pass the id through unvalidated".** `runRoleGet` built `Path: "/roles/" + id`, and `apiclient.buildURL` appends `req.Path` onto the base's path component with **no escaping** (`u.Path = base + "/" + path`, then `u.String()`), so a `/` in the id added path segments (`roles "role_x/subroles"` → `GET /roles/role_x/subroles` = the *subroles* endpoint) and `..` traversed. Escaping ≠ validating: ADR-4 ("don't regex-gate the id, let the API 404") does **not** preclude `url.PathEscape(id)`, which keeps the id one opaque segment — a no-op for a valid `role_[0-9a-f]{32}` id, and a malformed/adversarial id stays a single segment the API 404s (ADR-4 honoured). Nuance specific to this repo's `buildURL`: it assigns the *decoded* `u.Path` and re-escapes on `String()`, so a `PathEscape`d segment containing `%2F` becomes `%252F` on the wire (double-encoded) — harmless here because it only affects adversarial input that 404s anyway and valid ids never contain reserved chars, but it means the right place for a *general* fix would be `buildURL` honouring `RawPath` (out of scope for a single read). Rule: any time a free-form identifier is interpolated into a path segment, `url.PathEscape` it at the call site regardless of whether the value is otherwise validated — endpoint-injection/traversal is a separate concern from "is this a well-formed id". (8) **An optional embedded object needs a render fallback.** `?include=assignments` on a role may omit the embedded `actor` object (present by default only on the assignment *endpoints*), so the template's `{{.Actor.Name}} ({{.ActorID}})` rendered a stray `-  (per_x)` (blank name, double space). Fall back to the bare id when the name is empty (`{{$n := trimSpace .Actor.Name}}{{if eq $n ""}}{{.ActorID}}{{else}}{{$n}} ({{.ActorID}}){{end}}`). When a schema field is documented optional, the renderer that reads it needs an explicit empty-branch, not just the happy-path interpolation. (doc-drift) The stale `runRoleGet` signature in `interface-spec.md` is the **symmetric-site miss** the existing forward-artifact entry warns about: round 2 changed `runRolesList`'s row to the new `(cfg, exec, format) (Outcome, error)` shape but didn't grep the sibling `runRoleGet` row, which still read `(cfg, id) (RoleDetail, Outcome)`. Reinforces: when a refactor changes a function's signature, grep **every** accord/table row that names it (`grep -n 'runRole' specs/<spec>/*.md`), not just the one the reviewer flagged.

### 2026-06-09 — interface `[ASSUMED]` toolchain pins must be re-validated against the project's actual Go version at implement time
(Found during [T001](../../specs/024-pr-validation/tasks.md) implementation)

- **Type**: design-issue
- **Location**: `.github/workflows/ci.yml` (golangci-lint step) + `.golangci.yml`; assumption originated in `specs/024-pr-validation/interface-spec.md`.
- **Severity**: medium — shipping the interface's verbatim `[ASSUMED]` pin would have installed a golangci-lint that does not support the repo's Go version, reddening the gate on its own introducing PR.
- **Description**: 024's interface-spec marked `golangci/golangci-lint-action@v6` and a v1-style `.golangci.yml` (flat `linters:` list, `gofmt` as a linter) as `[ASSUMED]`, "set to a release compatible with the go.mod Go version at implementation time." `@v6` installs golangci-lint **v1.x**, which is EOL and does not support **Go 1.26** (the repo's `go.mod`). Verified against upstream at implement time: latest golangci-lint is **v2.12.2**, latest `golangci-lint-action` major is **v9**; the locally-installed toolchain is golangci-lint **v2.11.4** built with go1.26.1. Adapted to the v2 family — action `@v9`, config schema `version: "2"` (formatters like `gofmt` moved to a separate `formatters:` section), pinned `version: v2.11.4` (matching the local toolchain so `golangci-lint run` reproduces CI exactly). This is the user-level "verify the toolchain capability against the project's current pinned version before accepting/rejecting" learning applied to a *plan/interface assumption*, not a reviewer suggestion.
- **Suggested action**: when an interface/plan flags a toolchain pin `[ASSUMED]`, treat the concrete version as **unverified** and resolve it empirically at implement time: check the tool's latest release AND the major-version compatibility against `go.mod` (a major-version bump — v1→v2 — usually means a config-schema change too). Pin to a concrete version (never `latest`), prefer the version already on the developer's host for local-CI parity, and note it as bumpable. The behavioral contract (the lint job exists, runs the three categories) is fixed; the version and config schema are implementation detail.

### 2026-06-09 — a `[ASSUMED]` linter set must be run against the actual codebase before shipping; tune policy, don't modify other specs' code
(Found during [T001](../../specs/024-pr-validation/tasks.md) implementation)

- **Type**: design-issue
- **Location**: `.golangci.yml` (`linters.exclusions`); findings spanned test files owned by specs 002/007/008/023.
- **Severity**: medium — an un-validated linter set fails the gate on its own introducing PR (24 findings), and "just fix them" would drag 024's PR into editing 5 other specs' test files.
- **Description**: 024's interface `[ASSUMED]` linter set (`errcheck, govet, ineffassign, staticcheck, unused, gofmt, misspell`) was never run against the repo. Running golangci-lint v2.11.4 with it surfaced **24 findings** — 18 `errcheck` (idiomatic unchecked `fmt.Fprint`/`fmt.Fprintf`/`*.Close`/`WriteString`, both production and test) and 6 `staticcheck` style nits (`S1025`, `ST1008`, `ST1013`, `QF1001`) **all in `_test.go` files belonging to other specs**. T001's acceptance requires the lint job to *pass on clean code*, and the plan's verification path (a) is "the workflow runs green on its own introducing PR" — so a red-on-arrival gate is a hard contradiction. The plan also states 024 "adds and modifies no tests," so fixing the test-file nits was out of scope (and would broaden the review surface across 5 specs). Resolved by tuning the *delegated, `[ASSUMED]`-flagged linter policy* (not the codebase): enabled golangci-lint's built-in `std-error-handling` exclusion preset (drops the idiomatic errcheck reports) and relaxed `staticcheck` on `_test.go` via an exclusion rule (full linting, incl. SA correctness checks, stays on production code). After tuning: `golangci-lint run ./...` → 0 issues.
- **Suggested action**: before committing any new linter config, **run it against the whole repo** and confirm it passes — a linter set is policy that is only real once validated against the code it will gate. When pre-existing findings live in code owned by *other* specs, prefer relaxing the linter policy (exclusion presets, test-file scoping) over editing those files, unless the current spec's scope explicitly includes a cleanup. Surface the chosen exclusions in the final summary so the policy is reviewable (the interface marks the set tunable in a follow-up). Companion to the toolchain-pin finding above (same PR).

### 2026-06-09 — a pure-CI-infra feature may ship with an unbound .feature verified by the workflow on its own PR (024 diverges from 022's in-process proxy)
(Found during [T001–T003](../../specs/024-pr-validation/tasks.md) implementation)

- **Type**: convention
- **Location**: `features/no-automated-pipeline/pr-validation.feature` (bound to NO godog suite); contrast `internal/build/release_bdd_test.go` (022).
- **Severity**: low — no behavior impact, but a future maintainer may expect every `.feature` to be wired to a suite and be surprised this one is not.
- **Description**: Sibling 022 (also pure CI infra) built an in-process godog proxy (`CheckReleaseWorkflow`/`CheckConfigGuard`) that parses the declarative artifacts and asserts properties on them, so its scenarios run under `go test ./...` with a `~@wip` filter. 024's plan deliberately chose a *different* path — stated three times (System Architecture, §Testing strategy, tasks.md header): "no new Go package, no new tests; it orchestrates the existing suite." Consequently `pr-validation.feature` is bound to no suite (`grep -rn pr-validation --include=*.go` is empty), and the plan's verification story is (a) the workflow running green on its **own introducing PR** (the gate validates itself before merge), (b) the existing `go test ./...`, and (c) local `gofmt`/`go vet`/`golangci-lint` reproduction. Following the implement skill's "remove @wip from passing scenarios" and 022's tag precedent, `@wip` was removed from the 7 behavioral scenarios (now realized by the shipped artifacts) and **kept** on the 3 `@validation @wip` scenarios (reserved for `/score:validate`). Because the feature is unbound, the tag change is documentary — it does not change what `go test ./...` runs.
- **Suggested action**: for a pure-CI-infra spec, decide explicitly (in the plan) whether to build an in-process artifact-parsing proxy (022's pattern — gives `go test` coverage of the YAML contract) or to rely on the workflow self-validating on its own PR (024's pattern — lighter, but the `.feature` is unbound and only enforced at PR time). If `/score:validate` later needs to execute 024's `@validation` scenarios, it must supply the binding (a suite or an equivalent harness) — there is none today by design. When un-@wip-ping scenarios on an unbound feature, remember the signal is "implemented," not "executed by a suite."

### 2026-06-09 — under SilenceErrors, a forbidden flag needs an explicit declare+reject to surface a usage message; cobra's unknown-flag is silent
(Found during [T003–T004](../../specs/026-organization-tree/tasks.md) implementation)

- **Type**: convention
- **Location**: `internal/cli/tree.go` (`validateTreeFlags` + the `--first-page`/`--per-page` declarations), `internal/cli/subroles.go` (`validateSubrolesFlags` + the `--depth` declaration); contrast the org `roles` "declare-all-then-cross-reject" shape in `roles.go` (025).
- **Severity**: medium — getting this wrong ships a forbidden-flag rejection that exits 2 with EMPTY stderr, silently failing a scenario like "A depth flag on subroles is a usage error" (which asserts a non-empty usage message).
- **Description**: `tree` and `subroles` set `SilenceErrors`/`SilenceUsage` (the standing read-command convention). With `SilenceErrors`, cobra does NOT print its own "unknown flag: --depth" message, and `Run` (dispatch.go) maps a flag-parse failure to `UsageError(2)` without printing either. So a flag that is simply *not declared* on a command yields exit 2 + no request (tripwire holds) but a BLANK stderr — which fails any scenario/test that asserts the misuse is named. The fix is to **declare the forbidden cross-flag on the command anyway** and reject it in the pure `validate*Flags` function with an explicit `fmt.Fprintln(stderr, …)` message + `UsageError`. This mirrors 025's `roles.go`, which declares both list and single-read flags on one command and cross-rejects via `validateRolesFlags`. Applied here: `tree` declares `--first-page`/`--per-page` (rejects them — the tree is unpaginated) and `subroles` declares `--depth` (rejects it — subroles is one level), each with a tripwire test asserting no request was sent. The forbidden flags carry an honest "Not valid on …" help string so `--help` explains the rejection rather than hiding it.
- **Suggested action**: when a `SilenceErrors` command must reject a flag with an operator-facing message (not just a bare exit code), declare the flag and reject it in the pre-assembly validate step — do NOT rely on cobra's unknown-flag handling, which is silent under `SilenceErrors`. Reserve undeclared-flag rejection for cases where a blank-stderr exit-2 is acceptable. Pin every fail-fast rejection with a transport tripwire (`transport.calls == 0`).

### 2026-06-09 — 026's T002 schema was reuse-only because 025 landed first; @validation count in tasks.md was stale (3 vs 4)
(Found during [T002](../../specs/026-organization-tree/tasks.md) and [T005](../../specs/026-organization-tree/tasks.md) implementation)

- **Type**: convention
- **Location**: `internal/glassfrog/roles.go` (RoleDetail + leaf models, created by 025), `internal/glassfrog/roles_test.go` (added `Page[RoleDetail]` decode test), `features/governance-reads/organization-tree.feature`.
- **Severity**: low — both resolved cleanly; recorded so the first-to-land coordination and the count drift are traceable.
- **Description**: (1) T002 was specified create-if-absent-else-reuse for the shared `RoleDetail`/leaf-model/`Role`-growth schema (the same schema 025 ADR-2 designs, under the 005/006/016 first-to-land-creates rule). At implement time, **025 had already landed** (commit 2ba7f75), so `RoleDetail`, `Assignment`/`Policy`/`Note`/`SkillSummary`, the grown `Role`, and `Page[T]` all existed. T002 therefore added **no** new types — only a focused `Page[RoleDetail]` decode test (`TestSubrolesPageDecodesRoleDetail`) confirming the shared schema serves the paginated subroles list (the shape `subroles` decodes), since 025's own tests only covered the single-object `RoleDocument`. (2) tasks.md T005 acceptance says "The 3 `@validation` scenarios keep `@wip`", but the feature file actually carries **4** `@validation @wip` scenarios (whole-org raw-envelope, typo'd-include-caught-locally, subroles-incompleteness-never-silent, depth-capped-distinguishable-from-leaf). Implemented to the file: removed `@wip` from the 16 behavioral scenarios, kept all **4** `@validation` scenarios `@wip` (held for validate). The suite reports 16/16 passing, 4 held.
- **Suggested action**: when a task is written create-if-absent-else-reuse against a sibling spec's schema, check the actual codebase/git state first (the sibling may have landed) and reduce to reuse + a coverage test for any shape the sibling's tests didn't pin. When a task states a count of scenarios/items, treat it as advisory and reconcile against the actual artifact — implement to the file, note the drift.

### 2026-06-09 — `go vet` + `gofmt` is not the lint gate; run golangci-lint locally before pushing, and make "absence" test assertions unsatisfiable-by-present-content
(Found during PR #63 triage — implement of [026](../../specs/026-organization-tree/tasks.md))

- **Type**: process / convention
- **Location**: `internal/cli/organization_tree_bdd_test.go` (unused `lastQuery` helper), `internal/cli/tree_test.go` (leaf-render assertion); CI `lint` job (`.github/workflows/ci.yml`, golangci-lint v2.11.4).
- **Severity**: medium — both shipped green locally yet reddened the PR's CI/review: the dead helper failed the `lint` gate (and cascaded to the `ci-success` aggregate), and the no-op assertion gave false coverage confidence.
- **Description**: Two gaps from the same PR. (1) **Lint gate**: implement verified with `go build` + `go vet` + `gofmt` — none of which flag a dead method. golangci-lint's `unused` linter (enabled in this repo's `.golangci.yml`) **does**, and it covers `_test.go` files: `(*orgTreeWorld).lastQuery` was added anticipating use but the request-carry step read the transport directly, leaving the method dead → CI `lint` failed → `ci-success` aggregate failed. The fix was a one-line deletion, but the *process* lesson is that `go vet`+`gofmt` is not a substitute for the lint gate. The repo pins golangci-lint **v2.11.4** (matches CI exactly — see the 024 linter-pin learning above), so it reproduces locally with zero drift. (2) **Test quality**: the leaf-tree "no nested children" check was `Contains("\n  ") && !Contains("\n  Purpose:")` — always false, because the leaf's own indented `Purpose:` line makes `\n  ` present, so it could never catch an accidental child. An *absence* assertion whose predicate is also satisfied by legitimately-present output is a no-op. Replaced with an exact-output equality (`"Solo Role (role_0123)\n  Purpose: p\n"`), which genuinely fails if any nested line renders.
- **Suggested action**: (1) Before pushing any Go change, run `golangci-lint run ./...` (v2.11.4) in addition to `go build`/`go vet`/`go test`/`gofmt` — `go vet` does NOT catch `unused` (dead funcs/methods/vars), including in test files, and CI does. Treat `golangci-lint` as part of the standing local pre-push gate, not an optional extra. Don't add speculative test helpers "for later" — the `unused` linter rejects them. (2) When asserting a behavior is *absent* ("no children", "no marker", "section omitted"), make the predicate unsatisfiable by content the success case legitimately produces — for "renders exactly X", prefer exact-string equality over substring negation, so a regression actually trips the assertion.

### 2026-06-10 — interface render examples must show the explicit-absence marker for every nullable field, not just list/include sections
(Found during PR #70 triage — Copilot review of [033](../../specs/033-role-domains/interface-cli.md))

- **Type**: convention
- **Location**: `specs/033-role-domains/interface-cli.md` (`domain` full + compact render examples); the established marker convention lives in `specs/025-role-reads/interface-cli.md` / `specs/026-organization-tree/interface-cli.md` and `internal/render/templates/*.tmpl` (`<purpose | (no purpose set)>`, `(none)`).
- **Severity**: low — a spec-artifact wording gap, caught at review; no code impact, but it cost a review round and had already been flagged as a non-blocking observation by `/score:guard` (checklist + risk H-4) before the reviewer raised it.
- **Description**: 033's `domain` render examples showed a concrete `Role: role_0456…` even though `Domain.role_id` is **nullable** per the spec. The repo's render convention already pins explicit-absence markers — `<value | (no X set)>` for nullable scalars (e.g. `(no purpose set)`) and `| (none)` for empty lists — but the examples only applied it to the `Policies:` *list* section, not the nullable *scalar* `role_id`. A concrete-value-only example for a nullable field reads as "this is always present," contradicting CONSTITUTION VIII (no fabricated data) and the established markers. Copilot flagged both the `full` and `compact` examples (two threads, same root). Fix: `Role: <role_id | (no controlling role)>` in both renders + a note. Notably **`/score:guard` had already surfaced this exact gap** as a non-blocking checklist observation and risk H-4/RC-4 ("control recommended, not yet pinned") — the review confirmed it was worth closing in the same PR rather than deferring.
- **Suggested action**: (1) When writing an interface-cli render example, apply the absence-marker convention to **every** field the schema marks nullable/optional — scalars get the inline-pipe `<value | (no X set)>` form, lists get `| (none)` — not just the obviously-optional `?include` sections. Grep the schema (`spec/glassfrog-api-v5.yaml`) for `nullable: true` on the resource being rendered and confirm each such field's example shows its absence form. (2) When `/score:guard` records a non-blocking observation or a risk control marked "recommended, not yet pinned" about a contract-example gap, prefer closing it in the same PR — an AI reviewer (Copilot) flags these in round 1 anyway, so deferring just trades a guard note for a review round. (3) When pinning such a fix, propagate the now-settled claim to every sibling artifact that referenced the gap (risk RC-N, the checklist observation, the analyze correlation, the relevant task's acceptance) in the same commit — the prose-reference-grep discipline.

### 2026-06-09 — `/score:docs` grounds per-spec; verify the user-facing surface against shipped code + STATUS, not one spec's interface artifact
(Found during PR #67 triage — `/score:docs` output for the command / output / connection / distribution surfaces)

- **Type**: process / convention
- **Location**: `docs/reference/{identity-read,my-roles,my-actions,my-projects,structured-serialization,install-script}.md`, `docs/guides/{self-service-reads,connection,installation}/…`; ground truth in `internal/cli/root.go` (persistent `-o/--output`), `internal/output/error.go` (envelope shape, unused by `internal/cli`), `internal/cli/me_actions_test.go` (`No actions.`), `specs/STATUS.md`.
- **Severity**: medium — produced 24 valid doc-vs-reality comments in round 1, 8 more in round 2, 6 in round 3, and 5 in round 4 (43 total, all valid, all the same root class). Each pass surfaced a finer nuance as the prior fix sharpened the prose.
- **Description**: The document skill generated each capability's docs strictly from its *own* spec's interface artifact, producing three drift classes. (1) **Superseded by a later spec.** The `me` family references inherited spec 011's "structured `--output json` is deferred" framing, but spec 020 later shipped a *persistent root* `-o/--output` flag (`full|compact|json|yaml`, `root.go`) inherited by every read; the per-command docs omitted it from synopsis + flags and still called it deferred. (2) **Building-block specced but not wired.** Structured Serialization (018) defines an error-envelope shape + encoder (`output/error.go`), but no command calls `output.RenderError` — failures still print cause-plus-next-step text on stderr, so documenting the envelope as *current* failure output was wrong (the sibling 020 doc already had the correct "not yet wrapped" framing). (3) **Pre-implementation stage.** Install Script (027) is at STATUS "Analyzed"; no `install.sh` exists and no release pipeline ships archives, so the documented `curl …/main/install.sh | sh` one-liner 404s — vaporware presented as working. Plus a literal-output miss: docs wrote `no actions` but the CLI prints `No actions.` (test-asserted), while siblings deliberately differ (`No roles.`, `no projects`). **Round 2 surfaced three more of the same family**: (4) **error-message text didn't match the code** — docs showed `unsupported --output value "xml" — supported: …` but `output.FormatError.Error()` emits `unsupported output value "xml" from --output — supported: …` (the `from <source>` segment was dropped); (5) **wrong output stream** — the `me actions`/`me projects` docs put the incompleteness signal as a line appended to the *stdout* projection, but the CLI writes it as a `note:` to **stderr** (`incompleteActionsNote`/`incompleteProjectsNote`), leaving stdout a clean pipe-able list (the `me roles` doc already had this right); (6) **intra-document scope inconsistency** — templated-human-rendering's intro claimed "every read command" while its body scoped to the four `me` reads (templates actually render for `roles`/`tree`/`subroles` too).
- **Suggested action**: When running `/score:docs`, before trusting a per-spec interface artifact: (a) **check STATUS** — a spec below `Implemented`/`Complete` describes intended, not shipped, behavior; gate its docs behind a "🚧 Not yet available — planned" banner (and offer the real interim path, e.g. `go install`) or skip it. (b) **Reconcile against cross-cutting surfaces from *later* specs** — persistent root flags (`-o/--output`, `--base-url`) won't appear in an earlier command's artifact; grep `root.go` + the command file and document what the binary actually exposes. (c) **Confirm literal output strings against the code** — empty-result lines, labels, AND error-message text must match the actual `Error()`/format strings (including source segments like `from --output`); pin against `*_test.go` / `*_bdd_test.go` (sibling commands intentionally differ). (d) **Verify building-block specs are actually invoked** by commands (`grep` the encoder/helper in `internal/cli`) before documenting them as runtime behavior. (e) **Cross-check sibling capability docs for the canonical statement** — the umbrella doc (here 020's output-format-selection) often already states the truth a narrower spec contradicts. (f) **State the output stream for every emitted line** — confirm stdout vs stderr against `cmd.OutOrStdout()`/`ErrOrStderr()` in the command; diagnostics, notes, and incompleteness signals usually go to stderr so stdout stays pipe-clean. (g) **Check intra-document consistency** — the one-liner/intro scope claim must match what the body documents and what the code does; an over-broad intro over a narrowly-scoped body is its own drift. (h) **"Raw/verbatim" is not uniform across a capability** — round 3 found that structured (`-o json/yaml`) output is the raw payload only for *single* reads; the *walked* list reads (`roles`, `subroles`) synthesize a `{"data":[…]}` envelope and drop per-page `meta` (`aggregateRawRoles`), empty → `{"data":[]}`. Verify the output shape per command (does it walk + aggregate?), don't assert a capability-wide "verbatim" guarantee. (i) **The forthcoming-banner rule extends to *sibling* channels mentioned in passing** — a "Related channels" aside that lists planned-but-unbuilt routes (Homebrew tap, npm wrapper — no `brews:` in `.goreleaser.yaml`, no npm package) reads as "these exist today"; mark each as planned, same as the headline unbuilt feature. **Process note:** this entry took THREE triage rounds to converge — each round Copilot found a finer doc-vs-code nuance as the prior fix sharpened the prose. Expect doc-accuracy triage on generated docs to need multiple passes; front-load the code-grep (a–h) on the *first* generation to compress the tail. (j) **Verify rendered *shape*, not just strings** — round 4 found illustrative output blocks with the wrong structure: empty sections render as a standalone indented `(none)` line (no leading `-`), not inline `- … | (none)`; and an empty structured body is the literal `null` (single read) or `{"data":[]}` (walked list). When a doc shows an example output block, open the actual template (`internal/render/templates/*.full.tmpl`) and the renderer's empty-path constant, and mirror the block layout/indentation exactly — string content matching is necessary but not sufficient.
  

### 2026-06-10 — A spec that consolidates existing code must be reconciled to the code-grounded plan/interface; distinguish the panic safety net from the classifier fail-safe; zero-pad spec cross-references
(Found during PR #69 triage — define→shape→guard of [031](../../specs/031-diagnostic-normalization/spec.md))

- **Type**: process / convention
- **Location**: `specs/031-diagnostic-normalization/spec.md` (Normalizing accord, catch-all non-behavior, 004 boundary, edge-case + clarification), `features/opaque-failures/diagnostic-normalization.feature`; contract reference is `internal/cli/clienterror.go` (`classifyClientError`, total) and `internal/cli/entrypoint.go` (`recoverToCode`).
- **Severity**: medium — 6 of 13 Copilot threads on the PR flagged the same spec↔plan↔code contradiction; the guard's own `analyze.md` rated cross-artifact consistency 16/16 and missed it.
- **Description**: 031 consolidates the already-shipped, **total** `classifyClientError` (every error maps to an `Outcome`, unrecognized → `RuntimeError`→exit 1). During `clarify` (Tier 1, no code or `.score/memory` loaded) the spec was framed as "covers only the three known families; an unrecognized failure is **not normalized** and **falls through** to 004's safety net (internal-error code **+ trace**)". That framing was wrong on three counts the code-grounded `plan.md`/`interface-spec.md` (Tier 2) already had right: (1) the classifier handles more than the listed families (auth/base-URL/rcfile/format too); (2) `Diagnose` is **total** — an unrecognized error becomes a plain `RuntimeError` *diagnostic* (cause = its own message, no next step), not a no-diagnostic fall-through; (3) the **trace** is written only by `recoverToCode` on a genuine **panic** (`entrypoint.go`), never for an ordinary normalized error. A second drift: the spec said a general non-2xx "omits the next step", but shipped behavior (and T001 byte-equivalence) keeps a generic hint — omission applies only to the internal-error fallback. The Tier-1 spec prose was the stale layer; plan/interface (authored with code + DECISIONS loaded) were correct.
- **Suggested action**: (1) When a spec describes a capability that **consolidates or refactors existing code**, treat the code-grounded `plan.md`/`interface-spec.md` as the source of truth and reconcile the spec/feature prose to them — do not let a Tier-1 `clarify` decision (made before code/memory are loaded) define a *mechanism* the implementation contradicts. During `analyze`, add an explicit consistency check for **totality / fall-through / "does-not-handle"** claims in the spec accord against the plan/interface and the shipped function. (2) Keep the two exit-1 paths distinct in any error-handling spec: the **classifier fail-safe** (`RuntimeError` diagnostic, no trace) vs the **panic safety net** (`recoverToCode`, writes `debug.Stack()`); they share code 1 but only the latter writes a trace. (3) Cross-references to sibling specs use the **zero-padded three-digit** form — `(032)`, not `(32)` — matching the repo convention (e.g. spec 020); grep `\(3[0-9]\)`-style bare numbers before pushing a spec. **032 (Output-Aware Failure Rendering)** consumes 031's `Diagnostic`; when it lands, confirm the totality contract (every failure → a renderable diagnostic) still holds end-to-end.

### 2026-06-10 — A behavior divergence (decode→APIError) ripples into four sibling features' executable scenarios + leaves prose stale
(Found during [T002](../../specs/031-diagnostic-normalization/tasks.md) implementation)

- **Type**: design-issue
- **Location**: shared classifier `internal/cli/diagnostic.go` (`Diagnose`); ripple in `internal/cli/{me,me_roles,me_actions,me_projects,roles}_test.go`, the BDD harnesses `internal/cli/{identity_read,me_actions,me_projects,me_roles}_bdd_test.go`, and the feature files `features/self-service-reads/{identity-read,my-actions,my-projects,my-roles}.feature`.
- **Severity**: medium — the change is correct and developer-confirmed, but its blast radius crossed four other already-"Complete" specs' behavioral artifacts, and the divergence leaves several prose references stale.
- **Description**: 031 ADR-2 reclassifies an undecodable 2xx body from `RuntimeError`(1) to `APIError`(3) in the **shared** classifier. Because 011–014 each pinned the old decode→exit-1 behavior through their command (BDD scenarios + unit tests), the single shared-classifier change broke executable assertions in four sibling features. Subtlety: the "exit with the internal-error result" step was **shared** between each feature's decode scenario AND its credential-error scenario (which legitimately stays exit 1) — so the decode scenarios needed a *distinct* exit step (`exitedAPIError`/`exitAPIError`, asserting APIError/3) rather than flipping the shared `exitedInternalError`/`exitInternalError`. All executable assertions (5 unit tests renamed `…IsRuntimeError`→`…IsAPIError`, 4 BDD step sets, 4 feature scenarios) were updated together and the full suite is green. **Still stale (intentionally not rewritten here):** the decode→1 rows in `specs/011/012/014/025/026/034/interface-cli.md`, `specs/011/012/015/019/020/.../plan.md`, and the user-facing `docs/reference/{identity-read,my-roles,my-actions,my-projects}.md` + `docs/reference/structured-serialization.md` (all say "decoded body → exit 1 / internal error"). These belong to other features' artifacts; 031 records the supersession via the DEPRECATION.md `[decision]` entry rather than retroactively editing shipped specs.
- **Suggested action**: (1) When a spec diverges on a behavior owned by a **shared** helper, grep for every sibling spec's *executable* assertion (BDD step defs + unit tables) of that behavior and update them in the same PR — the plan's "update all decode→exit-1 assertions together" mitigation is necessary but underestimated that the behavior was woven into four BDD harnesses with shared steps. (2) Regenerate the affected `docs/reference/*.md` via `/score:document` and reconcile the decode→1 rows in 011/012/014/025/026/034's interface-cli.md when those features are next touched, so the published exit-code tables match the shipped 3. (3) **032 (Output-Aware Failure Rendering)** must render the decode `Diagnostic` under the `APIError` envelope/kind — confirm when it lands.

### 2026-06-10 — merging main into a long-lived spec PR: detect MERGE_HEAD the worktree-safe way, expect repeated append-file reconflicts from parallel sibling PRs, and reconcile the cross-spec envelope
(Found during PR #70 triage — resolving the `main` merge conflict while #69/#71 landed in parallel)

- **Type**: process / tooling
- **Location**: `specs/STATUS.md`, `.score/memory/DECISIONS.md`, `.score/memory/LEARNINGS.md` (the three append-style index/memory files); git worktree at `conductor/repos/cli-glassfrog/.git/worktrees/lima`.
- **Severity**: low — all resolved, but the MERGE_HEAD mis-detection nearly caused a wrong conclusion mid-merge, and the reconflict cost two merge rounds.
- **Description**: Three things. (1) **`test -f .git/MERGE_HEAD` is WRONG in a git worktree.** Conductor workspaces are git worktrees, where `.git` is a *file* (a `gitdir:` pointer), not a directory — so `.git/MERGE_HEAD` never exists even mid-merge, giving a false "no merge in progress". Use `git rev-parse -q --verify MERGE_HEAD` (or `git rev-parse --git-path MERGE_HEAD`), which resolve the real per-worktree gitdir. This refines the existing "UU entries but no MERGE_HEAD → don't fabricate a non-merge commit" learning: before trusting that conclusion, confirm MERGE_HEAD's absence with `git rev-parse`, not a filesystem path. (2) **Parallel sibling-spec PRs reconflict the same append-files.** While triaging #70, sibling spec PRs #69 (031) and #71 (034) merged to `main` in succession; each re-dirtied `STATUS.md` + `DECISIONS.md` (+ sometimes `LEARNINGS.md`) at the *same end-of-file append points*, forcing a second `git merge origin/main` round. The resolution is always mechanical — **keep BOTH sides' appended blocks in spec-number order** (031, 033, 034 …); never pick one side. Expect to re-fetch and re-merge until `git merge-base --is-ancestor origin/main HEAD` is true, and confirm with `gh pr view --json mergeable` (which lags a few seconds after push). (3) **A merged sibling's recorded DECISION silently falsifies prose about a SHARED primitive you document but don't own — with zero textual conflict in that file.** Two instances from this PR's merges: (3a, schema) #34's DECISIONS entry generalizes the single-object envelope to a generic `glassfrog.Document[T]` (refactoring 025's `RoleDocument`→`Document[RoleDetail]`) while #33 specs a bespoke `DomainDocument{Data Domain}` — benign first-to-land (whichever lands second uses `Document[Domain]`). (3b, error mapping — caught by Copilot a round later) merging #69 brought 031's recorded **divergence** "decode error reclassifies `RuntimeError(1)`→`APIError(3)`" into DECISIONS.md; 033's interface error-table still said `*DecodeError → RuntimeError/1`, and `tasks.md` T003 carried the same stale row (symmetric site). 033 explicitly "owns no Outcome/ExitCode" and routes through the shared `classifyClientError`/`Diagnose`, so its table merely *documents* that primitive — the merge made the doc contradict the authoritative decision. Neither file textually conflicted; the staleness was semantic, invisible until a reviewer cross-read the table against DECISIONS.md. (3c, reciprocal precedence) two *parallel* sibling specs that independently decide the same pattern each tend to write "Sets the precedent #N follows" — so once both DECISIONS entries are co-located by the merge, they mutually claim to precede each other. #33 said "#34 should follow this two-sibling precedent" while #34 said "#33 … follows verbatim"; both actually derive from the *real* shared origin (025 ADR-1 + 026), neither downstream. Copilot flagged the 034 side; the 033 side (and its mirror in 033's plan.md/interface-cli.md) was the symmetric site.
- **Suggested action**: (1) In any worktree (all Conductor workspaces), detect in-progress merges/rebases with `git rev-parse -q --verify MERGE_HEAD`, never `test -f .git/<file>`. (2) When merging `main` into a spec PR, resolve `STATUS.md`/`DECISIONS.md`/`LEARNINGS.md` conflicts by concatenating both sides' new entries in spec-number order; then immediately re-`git fetch` and check `git merge-base --is-ancestor origin/main HEAD` — if sibling spec PRs are landing concurrently, repeat the merge until it's an ancestor. (3) After merging `main`, **cross-read this spec's interface/tasks tables against every DECISIONS.md entry the merge added** — not just for schema coordination but for any shared primitive the spec *documents but does not own* (error→exit-code mappings, classifier behavior, envelope types). If a merged sibling **decision/divergence** changes that primitive (e.g. 031's decode→`APIError(3)`), update the documenting rows (interface error table + the symmetric `tasks.md` acceptance row) to match the recorded decision in the merge commit's follow-up, and footnote the source decision so the dependency is traceable. For unimplemented *peer* coordination (schema `Document[T]` vs `DomainDocument`), flag for implement-time first-to-land reuse rather than editing speculatively. For **reciprocal precedence** claims between parallel siblings (each "sets the precedent the other follows"), reframe BOTH as parallel siblings deriving from the real shared origin (point to 025/026, name the actual downstream e.g. #38) — and fix the mirror sites in *your* spec's plan/interface, not just the flagged DECISIONS entry. The trigger is "did the merge add a DECISIONS entry touching anything my tables describe, or that cites my spec number?" — a textual-conflict check (markers) misses all of this entirely. **Refinement (PR #79, 038 Role Projects, 2026-06-11): the same staleness arrives WITHOUT any merge — by *copying a sibling spec's interface accord as a template*.** The per-role-read specs (033/034/038) are deliberately modeled on each other, so 038's `interface-cli.md` error table was copied from 034's — but 034's next-step strings were *illustrative/paraphrased* and predated 031's `Diagnose` consolidation, so the copy inherited three wrong strings ("not authenticated — run `glassfrog auth login`" vs the real "run `glassfrog auth login` or set GLASSFROG_TOKEN"; "fix or remove the malformed `.glassfrogrc`" vs "fix or re-create the credentials file with `glassfrog auth login`"; "check network access and the base URL" — actually the base-URL row's next step, conflated — vs "check connectivity; the API may be unreachable"). Copilot flagged all three. So the trigger widens: **whenever a spec's table DOCUMENTS a shared primitive it doesn't own (Diagnose messages, exit-code mapping, validateStatus set), quote the strings VERBATIM from the current source (`grep` the actual literal in `internal/cli/diagnostic.go` etc.), never copy them from a sibling spec's accord** — the sibling may be illustrative or drifted. Applies at authoring time (template-copy), not just at merge time. **Refinement 2 (PR #79 round 2, same day): the triage fix that aligned those strings to `Diagnose` ALSO introduced a *new* over-claim — it added "rendered per `--output` by 032" / "(031/032) carry the failure surface" / listed "(031/032)" under "Reuses landed machinery", treating Output-Aware Failure Rendering (032) as live. But 032 is `Analyzed` (pre-implementation) per STATUS.md: the landed failure path is `reportClientError(stderr, err)` (031's `Diagnose`) — stderr regardless of `--output`, no `reportFailure`. Copilot flagged all three the next round.** Two compounding lessons: (a) **when you cite a sibling spec as a dependency, check its STATUS.md stage first** — `Complete`/`Ready` = landed machinery you can say a spec "reuses"; `Specified`/`Planned`/`Analyzed` = specified-but-unlanded, which a *pre-implementation* spec must frame as a "future enhancement inherited when it ships", never as current/landed. (b) **A triage edit is itself reviewable — re-scan the references YOU add while fixing prose.** Fixing one drift (paraphrased→verbatim `Diagnose` strings) introduced the next (citing 032 as landed) because the verbatim strings were pulled from the 031/032 chain and the chain got name-dropped wholesale without checking 032's stage. Before committing a prose fix that adds spec-number citations, `grep specs/STATUS.md` for each newly-cited number and confirm its stage matches the claim ("reuses landed" vs "inherits when landed"). **Refinement 3 (PR #79 round 3, same day): beyond strings and dependency-status, the *mechanism / data-flow* a spec describes must match the landed code path for the pattern it reuses.** 038's plan/tasks/interface described the role-scoped *list*'s structured (`-o json/yaml`) output as "the raw payload" / "serializes the raw response bytes (018)" / "decode `Page[Project]`" / "dispatch through `renderResult[T]`". But the landed **walked-list** pattern (`internal/cli/{roles,domains,policies}.go`) does something specific and different: for structured output it walks `paging.All[json.RawMessage]` and emits an *aggregated* `{data:[…]}` document via `aggregateRawData` (per-record raw bytes preserved, **per-page `meta` dropped**), and only the *human* path walks the typed `Page[Project]` + `renderFn`. `renderResult[T]` is the **single-page `/me*`** dispatch — NOT used by any walked list. Copilot flagged five sites; ~7 more symmetric sites in the same files repeated the same framing and were fixed in the same pass. The trap: a single-resource read (the `project` single read, the `/me*` reads) genuinely IS a raw-passthrough / `renderResult[T]`, so the wrong wording reads plausibly — but a *walked list* is mechanically different. **Operative rule — this PR took THREE triage rounds, all the same root class (spec prose drifting from landed code because the artifact was template-copied from a sibling and never checked against source): before opening a per-role-read (or any template-derived) spec PR, open the nearest landed sibling command file (`policies.go`/`domains.go`) and verify EVERY mechanism claim against it — render dispatch (walked-list `aggregateRawData`+`renderFn` vs single-read `output.RenderSuccess`/`renderResult[T]`), decode types (`Page[json.RawMessage]` for structured vs `Page[T]` for human vs `Document[T]` for single), the structured-list shape (aggregated `{data:[…]}`, per-page meta dropped), error strings (verbatim from `diagnostic.go`/`paging.go`), and cited-sibling stage (STATUS.md). All three rounds' findings would have been caught by one read of `policies.go` + `diagnostic.go` at authoring time.** **Refinement 4 (PR #79 round 5, 2026-06-11): a cited sibling's landed stage is NOT static across a PR's life — a reviewer comment that *contradicts an earlier round's correct conclusion* is the tell that a sibling merged to main mid-review.** Round 2 correctly reframed 038 to "032 is Analyzed / not landed" (true then). By round 5 Copilot said the opposite — "032 is Complete; `reportFailure` renders structured failures to stdout" — and was right: spec 032 had merged to `main` (PR #78) in between, but this long-lived branch never picked it up, so the artifacts (and the branch's code) were stale. The fix was NOT a prose tweak — it was `git merge origin/main` (landing `reportFailure`, the 032 STATUS flip, the BDD/validate files), THEN reframe the artifacts to the now-landed behavior (failure rendering is format-aware: json/yaml → 018 envelope on stdout, full/compact → diagnostic on stderr; rename the error-table "stderr message" column to a channel-neutral one; partial-walk incompleteness notes stay on stderr in every format). Two operative rules: (a) **when a review comment contradicts what you established a round or two ago about a sibling's stage/behavior, `git fetch origin main` and check `git log HEAD..origin/main` for that sibling's merge BEFORE deciding the reviewer is wrong** — the world changed, the reviewer is reviewing against current main. (b) **Merge `main` into a long-lived spec PR at the START of every triage round** (not just round 1), so the branch's STATUS/code/artifacts track reality; a branch that goes several rounds without a fresh merge will accrue exactly this contradiction. Bonus mechanics from this round: the remote branch had also been "Update branch"-merged from main (a second, independent merge commit), so the local push was rejected non-fast-forward — resolve with `git pull --no-rebase` (NOT force-push; both main-merges reconcile cleanly since the spec files live only on the PR branch).**

### 2026-06-10 — Name a helper resource-neutral the moment a *second* spec reuses it verbatim — its birth-file name misleads at the new call sites
- **Type**: convention / maintainability
- **Location**: `internal/cli/roles.go` (`aggregateRawData` ← `aggregateRawRoles`, `cloneQuery` ← `cloneRolesQuery`), reused in `internal/cli/subroles.go` (026) and `internal/cli/domains.go` (033). The codebase's per-spec growth pattern (each governance read adds a command file reusing prior helpers) is the structural driver.
- **Severity**: low — naming only, no behavior; caught at review (PR #73 Copilot), cost a round.
- **Description**: A walk/list helper introduced in spec 025's `roles.go` was named for *its* resource (`aggregateRawRoles`, `cloneRolesQuery`). 026 (`subroles`) and 033 (`domains`) then reused both **verbatim** — correct reuse (the helpers are genuinely resource-neutral: `aggregateRawData` just synthesizes `{"data":[…]}` from raw page bytes; `cloneQuery` copies a `url.Values`), but the roles- name reads as a layering smell at the `subroles`/`domains` call sites ("why is the domains command calling an aggregate-*roles* function?"). The reuse is documented (the call sites even say "reusing the resource-neutral aggregator"), which is exactly the tell that the *name* lies. This is the symmetric companion to the existing "reuse landed machinery verbatim" learning: reuse is right, but a reused helper must shed its origin-resource name.
- **Suggested action**: when you reuse a helper from another spec's command file for the **second** time (the first cross-resource consumer), rename it resource-neutral in the same commit and generalize its doc comment to name all consumers — don't wait for the third consumer or a reviewer. Cue: if you write a call-site comment like "reusing the resource-neutral X" where `X` is named for a *specific* resource, the name and the comment disagree — rename `X`. Unexported helpers make this a zero-risk mechanical rename (grep the identifier across `internal/cli/*.go`, confirm no test references the name, replace_all). Prefer neutral names at *introduction* when a helper is obviously generic (operates on `json.RawMessage`/`url.Values`, not a typed resource).

### 2026-06-10 — a SilenceErrors leaf that rejects a flag purely at cobra level wrote NOTHING to stderr until dispatch's flagFailed branch was taught to print
(Found during [T005](../../specs/034-role-policies/tasks.md) implementation)

- **Type**: design-issue / cross-spec gap
- **Location**: `internal/cli/dispatch.go` (`Run`, the `flagFailed` branch); surfaced by the `policy <pol-id> --query` rejection (034 interface-cli "List-only flag on `policy`" row + the "search flag is rejected on the single read" scenario).
- **Severity**: low — one-line symmetric fix, no regression; but it was invisible until 034 because no earlier read rejected a flag this way.
- **Description**: 025 enforced list-only-ness with a `validateRolesFlags` check *inside* `runRoles`, which writes its own message to stderr. 034's plan ADR-1 instead makes list-only-ness **structural**: `--query`/`--first-page`/`--per-page` simply don't exist on the `policy` command, so a list-only flag is rejected by cobra's own unknown-flag handling *before* `runPolicyGet` runs. But `policy` (like every read leaf) sets `SilenceErrors: true`, and `Run`'s `flagFailed` branch returned `UsageError` **without printing anything** — so cobra (silenced) printed nothing and dispatch printed nothing, leaving the operator a bare exit 2 with empty stderr. The interface error table and the driving scenario both require the cobra message to surface. The fix is the exact symmetric twin of the Args-validator branch already in `Run` (which prints `Error: …\nRun '<path> --help' for usage.` when `executed.SilenceErrors`): the `flagFailed` branch now does the same, guarded by `executed != nil && executed.SilenceErrors` so non-silenced commands never double-print.
- **Suggested action**: Future specs that enforce flag-applicability **structurally** (a flag exists on one sibling command but not another — the per-role-read pattern #38 Role Projects will reuse: `project <prj-id>` rejecting list flags) get the stderr surface for free now. The general rule: when a `SilenceErrors` command can fail at a cobra phase *before* its action runs (flag-parse OR Args-validation), dispatch must print the cause itself — cobra won't, and the action never ran to do it. If a third such phase is ever added, give it the same guarded print.

### 2026-06-10 — the chokepoint's enumerated call-site list went stale between planning and implement; the signature-change backstop absorbed it
(Found during [T002](../../specs/032-output-aware-failure-rendering/tasks.md) implementation)

- **Type**: drift / plan-vs-codebase
- **Location**: `internal/cli` — `reportFailure` (was `reportClientError`) call sites. Task T002 enumerated `render.go ×2, roles.go, subroles.go, tree.go, me.go, me_roles.go, me_projects.go, me_actions.go` (~19). The actual set at implement time was 38 across those files **plus** `domain.go`, `domains.go`, `policies.go`.
- **Severity**: low — fully recovered, no behavior risk; the compiler enforced completeness.
- **Description**: 032 was planned/tasked before specs 033 (role domains → `domain.go`/`domains.go`) and 034 (role policies → `policies.go`) merged to `main`. Those siblings each added new clean-failure call sites that funnel through the same chokepoint. The task's hand-enumerated file list (and "~19" count) was therefore stale by implement time — a smaller list than reality. This is exactly the case ADR-1 designed for: evolving `reportClientError(stderr, err)` into `reportFailure(stdout, stderr, format, err)` makes every un-threaded site a **compile error**, so the stale enumeration could not cause a silent plain-text leak — `go build` named all 38 sites, and `format`+`cfg.stdout` were already in scope at each (every dispatcher resolves `format` before `newClient`; every `cfg` carries `stdout`). The threading was a single mechanical sweep (`reportClientError(cfg.stderr,` → `reportFailure(cfg.stdout, cfg.stderr, format,`).
- **Suggested action**: When a task enumerates call sites for a chokepoint/helper that **sibling specs keep extending** (the per-governance-read growth pattern — each new read adds a command file that reuses the shared failure/walk machinery), treat the enumerated list as a lower bound, not a checklist. Prefer a signature change as the completeness backstop over a grep-the-list approach: it converts "did I find every site?" into a build error the compiler answers exhaustively. After the sweep, `grep` for the old symbol name in **non-test** code to confirm zero survivors, then in test code to update direct callers. This is the inverse of the stale-enumeration risk the plan's Risks section already named ("a missed call site leaks plain text") — the type system, not the task's file list, is the authority on coverage.

### 2026-06-10 — a shared godog "When" step that hardcodes one variant doesn't exercise the scenario's stated variant
(Found during PR #78 triage — 032 review)

- **Type**: test-fidelity / convention
- **Location**: `internal/cli/output_aware_failure_rendering_bdd_test.go` — `renderTheFailure` (the `the failure is rendered` step) hardcoded `output.FormatJSON` for every scenario, including those whose Given said `--output yaml`/`full`.
- **Severity**: low — caught at review (Copilot, two passes), no shipped defect; but it masked format-specific regressions.
- **Description**: When several scenarios share one `When` step but differ in a parameter their `Given` establishes (here: the resolved `--output` format), a `When` implementation that hardcodes one value silently ignores the others. The yaml/full scenarios "passed" only because their `Then` steps did an *extra* render in the right format — so the `When` was decorative and a format-specific regression in the `When`-exercised path would not have been caught. The fix: carry the Given-established parameter in the world state (a `format` field), default it to the common case in the `Before` reset (so the majority scenarios need no boilerplate), let the differing Givens override it, and have the single `When` act on it. Then the `Then` steps read the render the `When` already produced instead of re-rendering.
- **Suggested action**: when writing a godog suite where one `When` serves scenarios that differ by a Given-set parameter, thread that parameter through the world and make the `When` consume it — never hardcode the "common" value in the `When`. Tell at review time: if a `Then` step re-runs the action in a different mode than the `When`, the `When` isn't exercising the scenario. Companion to the docstring-accuracy discipline (a comment/claim must match what the code does): a `When` step's behaviour must match what the scenario text says it does.

### 2026-06-10 — only JSON preserves struct field order; sigs.k8s.io/yaml.JSONToYAML alphabetizes keys
(Found during PR #78 triage — 032 review, round 3)

- **Type**: doc-accuracy / library behaviour
- **Location**: `internal/output/error.go` — `RenderError`'s YAML path and the `ErrorDetail` field-order doc; mirrored in 032 `interface-spec.md` / `interface-cli.md`.
- **Severity**: low — doc-only over-promise; caught at review (Copilot), no behavioural defect.
- **Description**: `RenderError` builds the JSON document with `encoding/json` (which emits struct fields in declaration order) and the YAML document via `sigs.k8s.io/yaml.JSONToYAML`. JSONToYAML round-trips the JSON through a `map[string]interface{}` and marshals with gopkg.in/yaml, which **sorts keys alphabetically** — at every nesting level. So a doc that says the rendered envelope "reads message → next_step → kind → status → body" is true for JSON but false for YAML (which emits `body, kind, message, message, next_step, status`). Verified empirically before rewording. Any prose, example, or test that asserts YAML key order is wrong.
- **Suggested action**: when documenting or testing structured output that renders via JSONToYAML (the 018/RenderSuccess + RenderError path), scope field-order claims to JSON only; tell YAML consumers to rely on keys, not order. Never write a YAML example in declaration order — alphabetize it to match real output, or it contradicts the contract and a reviewer will flag it. For order-sensitive assertions, test the JSON render; for YAML, unmarshal and assert keys/values, never positions. Companion to the docstring-accuracy discipline: an example is a claim too — it must match what the encoder actually emits.

### 2026-06-11 — follow the sibling read's pre-assembly check order, not the interface doc's incidental numbering
(Found during PR #81 triage — 038 Role Projects review)

- **Type**: convention / consistency
- **Location**: `internal/cli/projects.go` — `runProjectsList`; sibling references `internal/cli/me_projects.go` and `internal/cli/policies.go`.
- **Severity**: low — both orderings produce `UsageError`/no-request, so no behavioural defect; caught by Copilot, fixed in one swap.
- **Description**: a new read command has two pure, pre-assembly fail-fast checks — `--output` resolution (020) and `--status` validation (013/014). The landed sibling reads (`me_projects.go:59-72`, `policies.go:78-83`) all resolve `--output` **first**, then validate. 038's `interface-cli.md` § Interactions happened to number them the other way (`(2) --status … (3) --output`), and the implementation followed the doc's numbering — making `projects` the only command that validates `--status` before resolving `--output`. Functionally identical for the single-bad-flag cases (and the no-request tripwire holds either way, since both checks precede assembly), but with *both* flags invalid the order decides which error surfaces — so the odd-one-out breaks error-precedence consistency across the command family. The interface doc's ordering was incidental (its real intent is "all three are fail-fast before any request"), not a deliberate divergence from convention.
- **Suggested action**: when adding a command to an established family, mirror the siblings' ordering of pure pre-assembly checks (here: resolve `--output` first, then validate closed-enum inputs) rather than transcribing an interface doc's numbered list — a numbered list in prose is usually describing *which* checks are fail-fast, not pinning their relative order. When the spec/interface ordering and the codebase convention disagree on something incidental, prefer the convention and reconcile the doc. After fixing the code, grep the spec artifacts (`interface-*.md`, `validate.md`) and the function docstring for prose that pinned the old order and update them in the same change — this fix touched four prose sites beyond the code (docstring, step comments, interface dispatch line, two validate.md traces).

### 2026-06-11 — three authoring-time checks for a new walking read spec: endpoint per_page cap, godog quote-handling, and (recurrence) --output-first ordering
(Found during PR #83 triage — 041 Cross-Model Search review, all by Copilot)

- **Type**: spec/code fidelity (authoring-time) — three findings, one recurrence
- **Location**: `specs/041-cross-model-search/{plan,interface-cli,tasks}.md`, the feature file; sources `internal/paging/paging.go`, the existing `internal/cli/*_bdd_test.go` suites, `internal/cli/roles.go:86-107`.
- **Severity**: low–medium — caught pre-implementation; a wrong per_page default would 400 the default walk at runtime if transcribed verbatim.
- **Description**: (1) **`paging.All`'s default `per_page` is 500 (`defaultPageSize`), but each endpoint sets its own `per_page` maximum** — `GET /search` caps at 100 (`spec/glassfrog-api-v5.yaml:4182-4188`). A spec that reuses the shared walker and says "page size defaults to the API max" is wrong whenever the endpoint's max < 500; the *default* walk would send `per_page=500` and the API would `400`. The command must explicitly pass `WithPageSize(<endpoint max>)`. (2) **The existing CLI godog suites invoke via `Run(root, strings.Fields(invocation))`** (whitespace split) and assert stdout with case-sensitive `strings.Contains` — so a feature requiring a *quoted multi-word argument* (here `search "<query>"`, `ExactArgs(1)`) cannot reuse that invocation step as-is (the quotes are literal chars, the phrase splits into multiple args), and a literal-output assertion must match the exact case/punctuation of the real output (`No results.`, not `no results`). Scenario examples also showed a bare `-archived` token *outside* the quoted query, which `ExactArgs(1)` would reject as a second positional — the whole query incl. operators must sit inside one quoted arg. (3) **Recurrence**: the `--output`-resolved-**first** ordering (see the 2026-06-11 / PR #81 entry directly above) was re-violated — plan/interface again described `--types` validation before `--output` resolution. The learning existed; it still didn't transfer to the next spec.
- **Suggested action**: when authoring (not just triaging) a new *walking read* spec, before opening the PR: (a) **look up the target endpoint's `per_page` maximum in `spec/glassfrog-api-v5.yaml` and compare it to `paging.defaultPageSize` (500)** — if the cap is lower, the spec/plan/tasks must say the default walk passes `WithPageSize(<cap>)` and pin it with a first-page-request fixture; never write "defaults to the API max" without checking the number. (b) **If any scenario needs a quoted multi-word/operator argument, say so in the tasks scope** — the godog suite needs a quote-aware splitter (or an arg-list step), and literal-output steps must assert exact case/punctuation; every CLI example for an `ExactArgs(1)` free-text command must wrap the entire value (operators included) in one quoted arg. (c) **Mirror the landed sibling's `--output`-first pre-assembly order** in the dispatch prose — open `roles.go`/`policies.go` and match it; this is now the *second* spec (after 038) to get it backwards, so treat "resolve `--output` first" as the fixed convention and grep new specs' Interactions/data-flow for the order before committing.

### 2026-06-11 — tasks.md `@validation`-count was stale at implement time (4 stated, 5 actual)
(Found during /implement 041 — T003, by hand-count against the feature file)

- **Type**: spec-artifact staleness (counts of a codebase/artifact-derived set)
- **Location**: `specs/041-cross-model-search/tasks.md` T003 acceptance criteria ("The 4 `@validation` scenarios keep `@wip`") vs `features/undiscoverable-governance/cross-model-search.feature` (5 `@validation @wip` scenarios: byte-for-byte, no-raw-envelope, rejected-type-no-request, rendered-order, partial-not-complete).
- **Severity**: trivial — intent recoverable (hold ALL `@validation` scenarios regardless of the stated number); no code impact. The behavioral count (13 = "11 spec-derived + 2 architecture-informed") was correct, so 13 un-`@wip`'d + 5 held = 18 total.
- **Description**: tasks.md hardcoded "4 @validation scenarios" but the feature file carries 5. The implement step's contract is *keep every `@validation`-tagged scenario `@wip` and un-`@wip` every behavioral one* — driven by the tag, never by the count — so the stale number doesn't mislead execution as long as you act on the tag. But a downstream reader (validate, a reviewer) who trusts the "4" will mis-tally held-out scenarios. Same class as the landed [spec-enumerations-go-stale-on-merge] memory.
- **Suggested action**: when an implement task names a count of an artifact-derived set ("the N @validation scenarios", "the six reads"), grep the live artifact for the actual count before trusting it; act on the structural marker (the `@validation` tag), not the prose number. When the count is wrong, prefer fixing the tasks prose to an open-ended phrasing ("all `@validation` scenarios") over re-hardcoding a new number that will rot again.

### 2026-06-11 — validate.md conformance summary: disambiguate the dimensions count from the separate validation-scenarios row
(Found during PR #86 triage — Copilot review of 041 validate.md)

- **Type**: artifact-clarity (Guardian output authoring)
- **Location**: `specs/<NNN>/validate.md` § Conformance Summary; the Score validate template's own example exhibits the same shape.
- **Severity**: trivial — no conformance impact; a skim-readability nit on the verdict artifact.
- **Description**: the validate template puts `**Validation scenarios**` as a 6th row of the conformance summary table but the total line says "N dimensions checked" (validation scenarios are deliberately *not* one of the 5 conformance dimensions). A reader skimming sees 6 rows next to "5 dimensions" and reads it as internally inconsistent. The count is technically correct; the presentation isn't self-explaining.
- **Suggested action**: when composing a validate.md conformance summary, make the separation explicit in BOTH places — label the validation row (e.g. `**Validation scenarios** (held-out — tracked separately, not a 6th dimension)`) and word the total as "5 conformance dimensions checked … Validation scenarios are tracked separately (not a dimension): M of M satisfied." A table that mixes a counted set with a separately-counted row must state the boundary, or the count reads as a typo.

### 2026-06-11 — widening a flag's value vocabulary silently re-routes a prior spec's "invalid value" scenarios (035 User-Defined Template Output)
(Found during 035 implementation — wiring the user-template arm into the reads)

- **Type**: cross-spec behaviour shift (intended, but easy to miss)
- **Location**: `internal/output/selection.go` (`ResolveSelection` flag rung); affects every `--output`-capable read in `internal/cli`; the prior-spec scenario is `features/unconsumable-output/output-format-selection.feature` "An unknown selector value fails before any request" (`--output xml`).
- **Severity**: low — the observable outcome (UsageError(2), no API request, error names the value) is preserved across the shift, so the 020 suite stays green; but the *code path* and the *error message* changed.
- **Description**: 035 widens the `-o`/`--output` flag rung so a non-reserved value is no longer an "unsupported format" error — it is now a **template file path**. So `-o xml` (020's canonical "unknown selector" example) stops resolving to a `*output.FormatError` and instead becomes a `TemplateRef{File, "xml"}` that fails fast as a *missing-template-file* usage error. Same category (UsageError 2), same no-request fail-fast, and the message still contains "xml" — so 020's scenario (which only asserts those three things) keeps passing, but it now exercises a completely different branch (`readTemplateSource` → not-found) than the one it was written for (`ParseFormat` → FormatError). The env/config rungs are unchanged (still four tokens or `*FormatError`), so only the FLAG rung shifts.
- **Suggested action**: when a spec widens the accepted *value set* of an existing flag/env/arg, grep the prior owning spec's feature file for scenarios that feed that flag an "invalid"/"unknown" value and re-read each: confirm the new interpretation still yields the same observable outcome (category, request-made, message substring), and note in the new spec which prior scenarios now traverse a new path. Here it was safe because both the old (bad-format) and new (missing-file) cases are pre-request UsageErrors that name the value — but a widening where the new interpretation made a request, or exited a different code, would have silently broken the prior scenario's intent while keeping it green. The tell: a prior "fails fast on a bad value" scenario whose value is no longer "bad" under the wider vocabulary.

### 2026-06-11 — merging main into a seam-removing branch compiles textually but breaks the build when a concurrent PR added a new implementer (035 ⊕ 041)
(Found during PR #87 triage — after main was merged into the 035 branch)

- **Type**: cross-PR integration / seam churn
- **Location**: `internal/cli/search.go` (feat(041), merged from main) vs the 035 seam change across the read commands; surfaced at `internal/cli/app.go` wiring.
- **Severity**: medium — the merged branch did not compile (`productionSeam does not implement searchSeam (missing method resolveFormat)`); a textual `git merge main` reported no conflict and looked clean.
- **Description**: 035 *removed* `resolveFormat` from `productionSeam` and widened every read's seam to `resolveSelection` + `readTemplateSource`. Concurrently, 041 (`glassfrog search`) landed on `main` declaring `searchSeam` with the OLD `resolveFormat` method and calling it. Each PR compiled in isolation. Merging `main` into the 035 branch produced a clean textual merge (the two changes touch different files) but a **non-compiling tree**: `search.go` still required `resolveFormat`, which `productionSeam` no longer provides. Go's structural-interface satisfaction means the break shows up only at the *wiring* site (`newSearchCommand(productionSeam{})`), not at `search.go` itself — so the error message points away from the real cause.
- **Suggested action**: when a branch *changes or removes a shared interface method* (a seam, an exported signature), treat "merge main" as a build-affecting event, not a no-op: immediately run `go build ./...` after the merge, and `grep` the merged tree for the OLD method name (`grep -rn 'resolveFormat' internal/`) to find any implementer/caller a concurrent PR added on the old shape. The fix is to bring the new implementer onto the new seam (here: wire `search.go` exactly like the sibling reads — `resolveSelection`/`readTemplateSource`/`resolveRenderTarget`/`writeHuman`). Generalises: a clean textual merge between an interface-narrowing PR and an interface-consuming PR is the classic "compiles on each side, fails together" trap — the compiler is the sweep, but only after you build the merged tree. Companion to the within-PR "seam signature churn" learning: that one is "grep every consumer in *this* PR"; this one is "after merging, grep every consumer the *other* PR added."
