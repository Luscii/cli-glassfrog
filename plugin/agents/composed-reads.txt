# Composed-read registry — the reads the Governance Navigation Path traverses.
#
# THE single source of "which glassfrog read leaves the navigator composes",
# consumed by:
#   - the governance-navigator agent  (plugin/agents/governance-navigator.md)
#     — its "Composed reads" section names exactly these leaves and no others
# so the artifact prose and this registry can never disagree about the composed
# surface (mirroring the write-safety gate's gated-commands.txt,
# plugin/hooks/gated-commands.txt).
#
# Each non-comment, non-blank line is ONE top-level `glassfrog` read command the
# navigator may invoke. These are reads only — no write leaf (proposal/tension
# writes) ever appears here; the path never writes.
#
# Every leaf here must resolve to a command the shipped CLI exposes, and the
# agent artifact must name each one — a navigation artifact must never name a
# read the CLI no longer exposes, so a rename or drop in the CLI means editing
# this registry and the agent's "Composed reads" section together.
search
roles
tree
fillers
subrole-actors
domains
policies
